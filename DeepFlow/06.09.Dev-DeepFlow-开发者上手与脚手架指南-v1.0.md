# Dev-UniFlow 开发者上手与脚手架指�?

> **版本**: 1.0  
> **更新时间**: 2025-12-04  
> **适用人群**: 参与 UniFlow/DeepBase 相关开发的 Delphi 工程师、Python 工程师、测试与运维人员�?

---

## 1. 文档目的

本指南帮助新开发者在 **30�?0 分钟** 内完成：

- 搭建 DeepBase + UniFlow + Python Skill 的本地运行环境；
- 编写并注�?**第一�?Skill**（Delphi �?Python）；
- 配置并运�?**第一�?Workflow**�?
- 了解基础调试与测试方式；
- 理解与版�?实验、安全策略的最小必要知识�?

如需了解整体产品与架构，请先阅读�?

- `06.01.Dev-UniFlow开发总览-v1.0.md`
- `02.01.Product-UniFlow-名词与角色表-v1.0.md`

---

## 2. 环境准备�?0 分钟�?

### 2.1 必备组件

- 基础�?
  - 支持 Delphi 的开发环境（参�?DeepBase 主项目要求）
  - Python 3.10+（用�?Skill 服务与工具脚本）
- 依赖�?
  - Python 侧建议安装：`fastapi`、`uvicorn`、`pydantic`、`httpx` �?
  - 具体版本要求�?DeepBase 根目�?`ARCH-QUICKSTART.md` �?`requirements.txt` 为准
- 数据存储�?
  - 本地开发默认使�?SQLite（已�?DeepBase 工程集成�?
  - 如需联动 PostgreSQL 请参�?`03.01.Arch-UniFlow架构设计-v1.0.md` 与数据库指南

### 2.2 获取代码与基础配置

1. 拉取或更�?DeepBase 仓库代码�?
2. 确认以下目录存在�?
   - `Core/`、`uniFlow/`、`docs/uniFlow/` �?
3. 准备最小配置：
   - 复制示例配置文件（如存在 `*.sample`），填入本地地址、端口与基础 Key 引用�?
   - 敏感信息（LLM Key、MCP 凭证）按照《UniFlow-安全与数据权限策�?v1.0.md》配置在安全存储中，而不是直接写入源码�?

> 提示：如�?`DeepBaseRun` 或等效启动程序，可优先使用其作为“一键启动器”，简化本地调试�?

---

## 3. 启动本地服务�?0�?0 分钟�?

### 3.1 启动 DeepBase + UniFlow

- 使用 Delphi 打开 DeepBase 解决方案，编译并运行主服务：
  - 启动后应包含：配置、日志、数据库、事件总线、调度器等基础组件�?
  - UniFlow 核心组件（Engine / Dispatcher / Executor 等）作为模块加载�?

### 3.2 启动 Python Skill 服务

- 进入 Python Skill 项目目录（示例：`uniFlow_py/`）：
- 安装依赖�?

```bash
pip install -r requirements.txt
```

- 启动开发服务器（示例）�?

```bash
uvicorn main:app --reload --host 127.0.0.1 --port 8001
```

- 确认以下端点可访问（以实�?API 为准）：
  - `GET /health` 用于健康检�?
  - `POST /skills/<skill_name>/execute` 用于执行 Skill

> 具体路径与请�?响应结构，以 `05.01.API-UniFlow接口规范-v1.0.md` 中的 Python Skill HTTP API 定义为准�?

---

## 4. 编写第一�?Skill（Delphi & Python�?

### 4.1 Delphi 侧：注册一个最�?Skill

�?`uniFlow/Core/` �?`uniFlow/Skills/` 下新增一个简�?Skill 类（示例伪代码）�?

```pascal path=null start=null
type
  TDemoSkill = class(TInterfacedObject, ISkill)
  public
    function Execute(const Input: TSkillInput): TSkillOutput;
  end;

function TDemoSkill.Execute(const Input: TSkillInput): TSkillOutput;
begin
  // �?Input 读取参数，执行业务逻辑
  Result.Success := True;
  Result.Output := 'Hello UniFlow';
end;
```

�?Skill 注册表中注册�?Skill（通常在启动阶段）�?

```pascal path=null start=null
SkillRegistry.Register(
  Name := 'demo_hello',
  Version := '0.1.0',
  Impl := TDemoSkill.Create
);
```

> 具体接口和注册方式以 `UniFlow.Skill.*.pas` 中的实际定义为准，本节为示意�?

### 4.2 Python 侧：实现一�?HTTP Skill

�?`uniFlow_py/skills/` 下新�?`demo_hello.py`�?

```python path=null start=null
from fastapi import APIRouter
from pydantic import BaseModel

router = APIRouter()

class SkillRequest(BaseModel):
    text: str

class SkillResponse(BaseModel):
    success: bool
    output: str

@router.post("/skills/demo_hello/execute", response_model=SkillResponse)
async def demo_hello(req: SkillRequest):
    return SkillResponse(success=True, output=f"Hello, {req.text}")
```

�?`main.py` 中挂载该 Router�?

```python path=null start=null
from fastapi import FastAPI
from skills import demo_hello

app = FastAPI()
app.include_router(demo_hello.router)
```

�?UniFlow 端，通过配置或注册表，声明一�?HTTP Skill，指向上�?URL�?

- 名称：`demo_hello`
- 类型：`http`
- Endpoint：`http://127.0.0.1:8001/skills/demo_hello/execute`

---

## 5. 配置第一�?Workflow

### 5.1 选择一个简单用�?

�?`07.01.Case-UniFlow用户旅程案例-v1.0.md` 中选择一个最简单的场景（例如：单步内容生成或标题生成），抽象为�?

1. 接收一个输入（如一段文本或需求描述）�?
2. 调用一�?Skill（例如上文的 `demo_hello` 或已有的 `gen_titles`）；
3. 将结果返回给调用方�?

### 5.2 Workflow 定义示例（伪 YAML�?

```yaml path=null start=null
id: wf_demo_hello
version: 0.1.0
name: 示例 Workflow：Hello
run_as: user   # �?project_runtime，具体策略见安全策略 4.4 �?

steps:
  - id: step_input
    type: SystemTask
    action: ReceiveInput

  - id: step_call_skill
    type: SkillTask
    skill: demo_hello
    input_mapping:
      text: "{{ steps.step_input.output.text }}"

  - id: step_output
    type: SystemTask
    action: Reply
    input_mapping:
      result: "{{ steps.step_call_skill.output.output }}"

transitions:
  - from: step_input
    to: step_call_skill
  - from: step_call_skill
    to: step_output
```

> 实际存储格式可以�?JSON、表结构�?DSL，此处仅为概念示例�?

---

## 6. 本地调试与测�?

### 6.1 日志�?EventBus 调试

- 确保在本地开发环境中打开足够的日志级别（INFO/DEBUG）；
- 使用 `DeepBase.EventBus` 监听关键事件�?
  - Workflow 启动/结束
  - Step 进入/完成
  - 错误与重�?

### 6.2 单元测试与集成测�?

- Delphi 侧：
  - 使用 DUnitX 为核心组件和 Skill 编写单元测试�?
  - 对角色（总指�?调度�?守卫等）可以使用 Mock + EventBus 的方式做角色级测试�?
- Python 侧：
  - 使用 `pytest` �?Skill 进行单元测试�?API 级测试；
  - �?HTTP Skill，建议使�?`httpx` �?`fastapi.testclient` 做接口测试�?

### 6.3 常见问题排查

- 端口占用：检�?DeepBase、UniFlow、Python Skill 各自监听的端口未冲突�?
- 鉴权失败：确认调用方携带�?Token/上下文与安全策略配置一致�?
- Skill 未找到：检�?Skill 注册表是否注册成功，名称与版本是否匹配�?
- Workflow 不运行：检查定义是否启用、入口条件是否满足、`run_as` 是否有效�?

---

## 7. 发布、版本与实验基础

### 7.1 Skill �?Workflow 的版�?

- 建议 Skill �?Workflow 均采用语义化版本（`MAJOR.MINOR.PATCH`）�?
- 变更策略�?
  - 向后兼容的小改动：`MINOR` �?`PATCH` 增加�?
  - 破坏性变更：`MAJOR` 增加，并在旧版本上保留一段兼容窗口�?
- 版本与实验的完整策略参见 `03.02.Arch-UniFlow-版本与实验策�?v1.0.md`�?

### 7.2 功能开关与灰度

- 可通过 Feature Flag 控制�?
  - 是否启用某个新的 Workflow 版本�?
  - 是否启用某个�?Skill 或新参数�?
- 在配置中为实验性功能添加清晰的标记，便于在出现问题时快速回滚�?

---

## 8. 安全与运行时主体的最小要�?

- 开发任何新 Workflow �?Skill 时，必须考虑�?
  - 该能力涉及的最高数据等级（L0–L3）；
  - 典型情况下由哪类运行主体执行（`user` / `project_runtime` / `platform_runtime`）�?
- 具体规则参见�?
  - 《UniFlow-安全与数据权限策�?v1.0.md》中的“运行时主体与兜底规则”小节；
  - �?4.01.Data-UniFlow数据模型-v1.0.md》中对数据等级的标注�?
  - �?5.01.API-UniFlow接口规范-v1.0.md》中对接口等级与 `run_as` 约定�?

---

## 9. 下一步阅读建�?

- 若你是后�?平台工程师：
  - 深入阅读 `03.01.Arch-UniFlow架构设计-v1.0.md`、`03.02.Arch-UniFlow-版本与实验策�?v1.0.md`�?
- 若你主要开�?Skill �?Workflow�?
  - 关注 `05.01.API-UniFlow接口规范-v1.0.md`、`05.02.API-UniFlow-MCP支持规划-v1.0.md`、`06.03/06.05/06.07`�?
- 若你关注安全与合规：
  - 重点阅读《UniFlow-安全与数据权限策�?v1.0.md》与数据模型文档�?

> 对应的“开发者上手路径”可视化图见：`docs/uniFlow/ui/UniFlow-开发者上手路�?svg`�?