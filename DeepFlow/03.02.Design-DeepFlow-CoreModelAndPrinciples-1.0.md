# uniFlow 核心模型与设计原�?

*创建日期: 2025-12-04*
*最近更�? 2025-12-04*

---

## 1. 定位

uniFlow �?DevDirector 生态中�?*统一流程内核**，负责以一致的方式管理整个生命周期内的�?

- 构建循环（Loop A）：从「用户提出需求」到「生成第一版结构化结果/程序」�?
- 维护循环（Loop B）：从「用户提出修改」到「生成并应用修正」�?

uniFlow 不直接关心「界面长什么样」「具体生成哪种代码」，而是提供一套通用的：

- Flow / Event / Snapshot / Node / Version �?*核心数据模型**�?
- 严格�?*状态机与不变量约束**�?
- 统一�?*历史记录与回放能�?*�?

SoIs / SayDone / DevDirector 等上层系统，未来都应作为 uniFlow 的「参与方/适配器」，在这套模型之上实现各自的功能，而不再各自维护一套独立的流程与历史�?

---

## 2. 核心概念与数据模�?

### 2.1 Flow �?Flow 实例

- **FlowDefinition**：流程定义，描述一类典型流程的步骤与约束，例如�?
  - Loop A：构建循环（�?0 �?1）�?
  - Loop B：维护循环（�?N �?N+1）�?
- **FlowInstance**：流程实例，某个具体用户、在某个时间发起并执行的一次流程，例如�?
  - 用户 A �?2025-01-01 创建了一个登录页�?�?产生一�?FlowInstance�?
  - 同一个页面在 2025-01-05 被修改两�?�?对应两个新的维护 FlowInstance，且挂在原构�?Flow 上�?

在实现上�?

- FlowDefinition 主要体现在代�?配置中（例如枚举定义、状态机描述）�?
- FlowInstance 完全由事件（UniFlowEvent）和快照（UniFlowSnapshot）在存储中体现�?

### 2.2 基本类型

以下�?uniFlow 推荐的数据结构示意（实现时可按语言调整）：

```ts
export type UniFlowId = string;

export type UniFlowType =
  | 'Build'       // 构建流（Loop A�?
  | 'Maintain'    // 维护流（Loop B�?
  | 'NlConvert'   // 自然语言→结构化转换子流
  | 'SceneChange' // 场景�?节点级结构变�?
  | 'CodeChange'; // 代码级变�?

export type UniFlowStatus =
  | 'Created'
  | 'Running'
  | 'WaitingUser'
  | 'Succeeded'
  | 'Failed'
  | 'Cancelled';
```

### 2.3 UniFlowEvent（事件）

```ts
export interface UniFlowEvent {
  id: string;               // 事件 ID（全局唯一�?
  flowId: UniFlowId;        // 所�?FlowInstance
  type: UniFlowType;        // 流程类型
  step: string;             // 业务步骤名，�?'ClarifyRequirement'
  source: string;           // 事件来源模块，如 'SoIs' / 'SayDone' / 'DevDirector'
  status: 'Started' | 'Succeeded' | 'Failed';
  timestamp: string;        // ISO8601 时间�?
  payload: any;             // 业务数据（JSON），由各 type/step 约定
  errorCode?: string;       // �?Failed，则必须存在
}
```

约定�?

- **所有状态变化都通过 UniFlowEvent 体现，禁止静默变�?*�?
- 上层系统可以自由扩展 `payload` 结构，但 **不得改变 event 的通用字段语义**�?

### 2.4 UniFlowSnapshot（快照）

```ts
export interface UniFlowSnapshot {
  flowId: UniFlowId;
  version: number;      // �?0 开始自�?
  stateJson: any;       // 聚合后的当前状态（如结构化表述/场景树片段等�?
}
```

约定�?

- Snapshot 可以由事件「重放」得到，也可以在关键节点直接持久化（性能优化）�?
- 任意一个成功结束（Succeeded）的 FlowInstance�?*必须**有一个对应的最�?Snapshot�?

### 2.5 UniFlowNode �?NodePath

为了统一在不同层次（项目/场景/步骤/功能�?代码块）上的定位，uniFlow 推荐使用通用 Node/Path 模型�?

```ts
export interface UniFlowNode {
  nodeId: string;        // 节点 ID（全局或局部唯一�?
  type: string;          // 节点类型，如 'Project' / 'Scene' / 'Step' / 'Feature' / 'CodeBlock'
  parentId?: string;     // 父节�?ID
  attributes: any;       // 与节点相关的元数据，如名称、描述、语言�?
}

export type UniFlowNodePath = string; // 例如 'project:1/scene:Login/step:InputForm'
```

约定�?

- Node 的具体持久化可以�?DevDirector 主导，但 **NodeId/NodePath 的命名规则需�?uniFlow 层统一**�?
- 所有和「定�?修改/回滚」有关的事件，都应该携带 NodePath �?NodeId�?

---

## 3. 设计原则与不变量

本节定义的是 **uniFlow 层面的“宪法�?*，上层系统在设计各自流程时不得违反�?

### 3.1 单一事实源（Single Source of Truth�?

- 对于任意 FlowInstance�?
  - 所有状态变化必须体现在 UniFlowEvent 序列中；
  - 允许存在快照（Snapshot）作为缓存，�?**不能存在只在内存/侧路中可见的“幽灵状态�?*�?

### 3.2 全程可追�?

- 对于每个 FlowInstance，必须能�?
  - 从第一个事件追溯到最后一个事件；
  - 在任意阶段说明「是谁触发了什么操作」以及「系统为何做出某个决定（可通过 payload 解释）」�?

### 3.3 可回放与可分�?

- 任何已持久化�?FlowInstance�?
  - 可以从任意一个历史事件重放，重构出对�?Snapshot�?
  - 可以从任一历史版本分叉出新�?FlowInstance（例如新的维护流）�?

### 3.4 AI �?AI，程序归程序

- Flow 的：
  - ID 生成、状态迁移、并发控制、回滚逻辑 �?必须是确定性的纯程序实现；
  - 某些事件�?payload 生成（例如自然语言解释、补丁建议） �?可以委托 AI�?
  - 某些事件（如硬约束校验失败） �?必须由纯程序执行与裁决�?

### 3.5 错误语义统一

- 所�?Failed 状态的事件�?
  - 必须包含 `errorCode`�?
  - `errorCode` �?uniFlow 定义基础枚举，上层系统可在此基础上扩展（�?`SoIs/ClarifyTimeout`）�?

### 3.6 向后兼容

- 新增 FlowType、Step �?Payload 字段时：
  - 不得破坏既有事件的可解析性；
  - 旧事件在新版本中仍能被回放与展示（即使某些字段为空）�?

### 3.7 CQRS 读写分离策略

uniFlow 基于 Event Sourcing 模式，自然支�?CQRS（Command Query Responsibility Segregation）架构�?

#### 3.7.1 读写分离原则

- **写路径（Command�?*�?
  - 所有状态变更必须通过写入 UniFlowEvent 实现
  - Event 写入是唯一的状态变更入口，保证完整审计轨迹
  - 写路径采用乐观锁 + 版本号校验，避免写写冲突

- **读路径（Query�?*�?
  - 查询可以�?Snapshot �?Event 重放得到当前状�?
  - Snapshot 作为读优化缓存，减少重放开销
  - 读路径不改变状态，可安全并�?

#### 3.7.2 分离实现策略

```
┌───────────────────────────────────────────────────────────�?
�?                  uniFlow CQRS 架构                          �?
├─────────────────────────────┬─────────────────────────────�?
�?     写路�?(Command)       �?      读路�?(Query)         �?
├─────────────────────────────┼─────────────────────────────�?
�?Command �?Validate �?    �?Query �?Snapshot/Replay �?   �?
�?WriteEvent �?Persist     �?Return State                  �?
�?                            �?                              �?
�?严格顺序，单线程写入       �?无锁读取，并发友�?         �?
├─────────────────────────────┴─────────────────────────────�?
�?             �?                             �?              �?
�?    ┌─────────────────────────────────────────�?         �?
�?    �?           Event Store              �?         �?
�?    �? UniFlowEvents (append-only log)    �?         �?
�?    └─────────────────┬───────────────────────�?         �?
�?                      �?                                   �?
�?                      �?                                   �?
�?    ┌─────────────────────────────────────────�?         �?
�?    �?         Snapshot Store              �?         �?
�?    �? UniFlowSnapshots (read cache)       �?         �?
�?    └─────────────────────────────────────────�?         �?
└───────────────────────────────────────────────────────────�?
```

#### 3.7.3 Snapshot 生成策略

为平衡写入性能和读取效率，采用以下 Snapshot 策略�?

- **事件阀值触�?*：每 N 个事件后自动生成 Snapshot（默�?N=10�?
- **关键节点触发**：状态达�?Succeeded/Failed/Cancelled 时必须生�?
- **显式请求**：上层系统可主动请求生成 Snapshot

```pascal
// Delphi 实现示例
type
  TSnapshotPolicy = class
  private
    FEventThreshold: Integer;  // 默认 10
    FLastSnapshotEventCount: Integer;
  public
    function ShouldCreateSnapshot(const AEvent: TUniFlowEvent;
      ACurrentEventCount: Integer): Boolean;
  end;

function TSnapshotPolicy.ShouldCreateSnapshot(
  const AEvent: TUniFlowEvent; ACurrentEventCount: Integer): Boolean;
begin
  // 终态必须生�?
  if AEvent.Status in [fsSucceeded, fsFailed, fsCancelled] then
    Exit(True);
  
  // 事件阀值触�?
  if (ACurrentEventCount - FLastSnapshotEventCount) >= FEventThreshold then
    Exit(True);
  
  Result := False;
end;
```

#### 3.7.4 读写一致性保�?

- **强一致性场�?*：通过事件重放获取最新状态（实时但较慢）
- **最终一致性场�?*：从 Snapshot 读取（快速但可能略迟延）
- **选择建议**�?
  - UI 展示：优先使�?Snapshot，接受短暂延�?
  - 决策判断：必须使用事件重放，确保数据最�?

---

## 4. Flow 状态与生命周期

### 4.1 Flow 状态机

推荐 FlowInstance 状态机如下�?

```txt
Created �?Running �?(WaitingUser)* �?(Running)* �?(Succeeded | Failed | Cancelled)
```

- **Created**：FlowInstance 被创建，但尚未产生业务事件�?
- **Running**：系统正在处理，期间可以产生多条 UniFlowEvent�?
- **WaitingUser**：等待用户操作（输入/确认/点选），期间不得自动推进状态�?
- **Succeeded**：Flow 正常结束，必须有最�?Snapshot�?
- **Failed**：Flow 异常结束，必须有至少一�?Failed 事件，且�?errorCode�?
- **Cancelled**：用户或系统主动中止，允许无错误码�?

### 4.2 事件状�?

单个事件�?`status` 字段只反映该事件本身的执行状态：

- `Started` �?某个步骤开始执行；
- `Succeeded` �?某个步骤成功结束�?
- `Failed` �?某个步骤失败，必须有 `errorCode` 与错误描述�?

FlowInstance 的状态由最后一个事件与内部规则推导，而不是简单等于最后一个事件的 status�?

---

## 5. Loop A / Loop B �?uniFlow 中的映射

本节给出一个示意性映射，用于约束未来 SoIs / SayDone / DevDirector �?uniFlow 上的行为。具�?Step 名称可在实现时细化�?

### 5.1 Loop A：构建循环（Build Flow�?

示例步骤�?

1. 用户提出需�?
2. 系统澄清（多轮点�?补充�?
3. 生成草图（界�?状态流�?
4. 用户确认草图
5. 生成结构化表示（模板实例、场景树片段�?
6. 导入导演台，生成第一版程�?

�?uniFlow 中，可能对应如下事件序列（简化示意）�?

```txt
FlowType: Build

1. Created
2. Event: step='ClarifyRequirement',   source='SoIs',   status=Succeeded
3. Event: step='GenerateSketch',       source='SoIs',   status=Succeeded
4. Event: step='ConfirmSketch',        source='User',   status=Succeeded
5. Event: step='NlConvertToStructure', source='SayDone',status=Succeeded
6. Event: step='ImportToSceneTree',    source='DevDirector', status=Succeeded
7. Flow 状�?= Succeeded，产生最�?Snapshot
```

### 5.2 Loop B：维护循环（Maintain Flow�?

示例步骤�?

1. 用户运行程序并发现问�?
2. 在导演台上选择/定位问题节点
3. 对该节点发起修改需�?
4. 澄清具体修改意图
5. 生成结构化补丁（Patch�?
6. 应用补丁并更新代�?

�?uniFlow 中，可能对应�?

```txt
FlowType: Maintain

1. Created（关联到原构�?FlowId 与目�?NodePath�?
2. Event: step='LocateNode',      source='DevDirector', status=Succeeded
3. Event: step='ClarifyChange',   source='SoIs',        status=Succeeded
4. Event: step='ComputePatch',    source='SayDone',     status=Succeeded
5. Event: step='ApplyPatch',      source='DevDirector', status=Succeeded
6. Flow 状�?= Succeeded，产生最�?Snapshot（新的结构化状�?+ 代码引用�?
```

上述示例说明�?

- uniFlow 并不规定具体如何澄清、如何生�?Patch�?
- 但规定了�?*各个步骤必须产生活动事件，且可在历史中被清晰识别与回�?*�?

---

## 6. uniFlow 引擎职责

uniFlow 引擎应保持「小而硬」，只做少数但关键的事情�?

### 6.1 必须做的

1. **Flow 管理**
   - 生成 `UniFlowId`�?
   - 维护 FlowInstance 状态（Created/Running/...）�?

2. **事件写入与查�?*
   - �?UniFlowEvent 可靠写入存储（例�?SQLite �?`UniFlowEvents`）�?
   - 提供�?`flowId`、时间范围、类型、来源等条件的查询接口�?

3. **快照重建与存�?*
   - 支持基于事件序列「重放」出当前状态；
   - 在关键事件后可以持久�?Snapshot�?

4. **基础校验**
   - 校验事件字段完整性（�?Failed 必须�?errorCode）；
   - 校验状态迁移合法性（例如 Succeeded 后不得再�?Running 事件）�?

### 6.2 明确不做�?

1. 不直接调�?LLM / AI 能力�?
2. 不负责界面渲染或草图生成�?
3. 不负责具体代码生成或 AST 修改�?
4. 不嵌入与某一上层系统强耦合的业务逻辑（比�?SoIs 的具体问句模板）�?

uniFlow 应保持「对上层业务无偏好」，只提供统一的流程底座�?

---

## 7. 集成约定（面向上层系统）

本节�?uniFlow 视角约束未来 SoIs / SayDone / DevDirector 等系统的集成方式�?

### 7.1 何时创建 FlowInstance

- 当出现「一次独立的从问题到结果的过程」时，应创建新的 FlowInstance�?
  - 新建界面/功能 �?创建 Build 流；
  - 对既有界�?功能的修�?�?创建 Maintain 流；
  - 也可以为单次复杂�?NL 转换创建 NlConvert 子流（可选）�?

### 7.2 事件写入原则

- 任意可能改变结构化状态或代码状态的操作�?*必须写事�?*�?
  - 例如：澄清完成、草图更新、用户确认、模板选择、补丁生成、补丁应用等�?
- 事件 `source` 字段用于区分来自哪个系统，方便后续分析与调试�?

### 7.3 错误与中�?

- AI 调用失败、网络超时、用户取消等情况，应写入 Failed/Cancelled 事件�?
  - Failed：系统错误、业务错误（需�?errorCode）�?
  - Cancelled：用户主动中止或超时不再继续�?

### 7.4 历史查询与回�?

- 上层系统应通过 uniFlow 提供的查询接口：
  - 获取某个 FlowInstance 的历史事件和快照�?
  - �?UI 中可视化展示给高级用户（例如「本次修改经历了哪几步」）�?

---

## 8. 后续扩展方向

1. **统计与分�?*�?
   - 基于 UniFlowEvent 统计构建/维护耗时、失败率、常见错误码分布等�?

2. **可视�?Flow 调试�?*�?
   - 提供开发者界面，可浏览任�?FlowInstance 的事件时间线和状态快照�?

3. **Flow 模板/编辑�?*�?
   - �?FlowDefinition 本身抽象出来，用可视化方式定义与配置流程�?

4. **多租户与权限**�?
   - �?UniFlowEvent/FlowInstance 层面加入租户/项目/用户维度，支持团队协作�?

5. **分布式与服务�?*�?
   - �?uniFlow 从本�?SQLite 推进到独立服务，支持多客户端共享流程历史�?

---

（本文件�?uniFlow 的基础性设计文档，SoIs / SayDone / DevDirector 等模块在重构时，应优先对齐此处的模型与原则。）
