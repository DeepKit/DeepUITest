# UniFlow Workflow 定义规范

> 定义 UniFlow �?Workflow DSL 语法、步骤类型、控制流与错误处理机制�?

创建日期: 2025-12-04  版本: 1.0

---

## 1. 概述

Workflow �?UniFlow 中任务编排的核心抽象，由调度员（Dispatcher）负责解析和执行�?

### 1.1 设计原则

- **声明式优�?*：描�?做什�?而非"怎么�?
- **类型安全**：所有输入输出有明确�?Schema
- **可恢�?*：支持检查点和断点续�?
- **可观�?*：每步执行都可追踪和审计

### 1.2 核心概念

```
Workflow（工作流�?
├── Trigger（触发器�?    - 启动条件
├── Variables（变量）     - 全局/步骤变量
├── Steps（步骤）         - 执行单元
�?  ├── SkillTask        - 调用 Skill
�?  ├── AgentTask        - 调用智囊
�?  ├── HumanTask        - 等待人工输入
�?  ├── SubWorkflow      - 嵌套 Workflow
�?  ├── Parallel         - 并行执行
�?  ├── Condition        - 条件分支
�?  └── Loop             - 循环
├── ErrorHandlers（错误处理）
└── Hooks（生命周期钩子）
```

---

## 2. Workflow 定义结构

### 2.1 顶层结构

```yaml
# workflow.yaml
id: "content-generation-v1"
name: "内容生成工作�?
version: "1.0.0"
description: "根据用户输入生成和审核内�?

# 触发�?
trigger:
  type: "api"                    # api | schedule | event
  config:
    endpoint: "/api/generate"

# 输入 Schema
input:
  type: object
  required: [text]
  properties:
    text:
      type: string
      description: "用户输入文本"
    style:
      type: string
      enum: [formal, casual, creative]
      default: casual

# 输出 Schema
output:
  type: object
  properties:
    content:
      type: string
    metadata:
      type: object

# 全局变量
variables:
  maxRetries: 3
  timeout: 30000

# 步骤定义
steps:
  - id: understand
    type: AgentTask
    # ...

# 错误处理
errorHandlers:
  - match: "AI_*"
    action: retry
    # ...

# 生命周期钩子
hooks:
  onStart: "log_start"
  onComplete: "notify_user"
  onError: "alert_admin"
```

### 2.2 TypeScript 类型定义

```typescript
interface WorkflowDefinition {
  id: string
  name: string
  version: string
  description?: string
  
  trigger: TriggerConfig
  input: JSONSchema
  output: JSONSchema
  
  variables?: Record<string, unknown>
  steps: Step[]
  errorHandlers?: ErrorHandler[]
  hooks?: WorkflowHooks
  
  // 元信�?
  metadata?: {
    owner?: string
    tags?: string[]
    timeout?: number
    retryPolicy?: RetryPolicy
  }
}
```

---

## 3. 步骤类型

### 3.1 SkillTask - 技能任�?

调用已注册的 Skill，由执行者执行�?

```yaml
- id: fetch_weather
  type: SkillTask
  name: "获取天气"
  
  skill: "weather-query"          # Skill ID
  params:                         # 参数（支持表达式�?
    city: "{{ input.city }}"
    date: "{{ input.date | default('today') }}"
  
  timeout: 10000                  # 超时（ms�?
  retryPolicy:
    maxRetries: 2
    backoff: exponential
  
  output:
    variable: weather_data        # 输出存入变量
```

**TypeScript 定义**�?

```typescript
interface SkillTaskStep {
  id: string
  type: 'SkillTask'
  name?: string
  
  skill: string                   // Skill ID
  params: Record<string, Expression>
  
  timeout?: number
  retryPolicy?: RetryPolicy
  
  output?: {
    variable?: string             // 存入变量
    transform?: string            // 转换表达�?
  }
  
  condition?: Expression          // 执行条件
}
```

### 3.2 AgentTask - 智能任务

调用智囊�?AI 能力，包括理解、生成、诊断�?

```yaml
- id: understand_intent
  type: AgentTask
  name: "理解意图"
  
  action: understand              # understand | generate | diagnose
  params:
    text: "{{ input.text }}"
    schema:                       # 期望输出结构
      type: object
      properties:
        intent: { type: string }
        entities: { type: array }
  
  # AI 特有配置
  aiConfig:
    model: "gpt-4"                # 可选，覆盖默认
    temperature: 0.3
    maxTokens: 1000
  
  # 幻觉检�?
  validation:
    detectHallucination: true
    maxHallucinationScore: 0.3
  
  # 置信度配�?
  confidenceConfig:
    enabled: true                 # 启用置信度评�?
    extractMethod: auto           # auto | explicit | calibrated
    threshold:
      high: 0.9                   # >= 0.9 高置信度
      medium: 0.7                 # >= 0.7 中置信度
      low: 0.0                    # < 0.7 低置信度
  
  output:
    variable: intent_result       # 输出包含 confidence 字段
```

**TypeScript 定义**�?

```typescript
interface AgentTaskStep {
  id: string
  type: 'AgentTask'
  name?: string
  
  action: 'understand' | 'generate' | 'diagnose'
  params: {
    text?: Expression
    schema?: JSONSchema
    context?: Record<string, Expression>
    examples?: FewShotExample[]
  }
  
  aiConfig?: {
    model?: string
    temperature?: number
    maxTokens?: number
    stopSequences?: string[]
  }
  
  validation?: {
    detectHallucination?: boolean
    maxHallucinationScore?: number
    customRules?: ValidationRule[]
  }
  
  // 置信度配�?
  confidenceConfig?: {
    enabled?: boolean             // 是否启用置信度评�?
    extractMethod?: 'auto' | 'explicit' | 'calibrated'
    threshold?: {
      high?: number               // 高置信度阈�?
      medium?: number             // 中置信度阈�?
      low?: number                // 低置信度阈�?
    }
  }
  
  output?: {
    variable?: string
    transform?: string
  }
}

// AgentTask 输出结构
interface AgentTaskOutput {
  result: unknown                 // 实际输出内容
  confidence: number              // 置信度分�?0-1
  confidenceLevel: 'high' | 'medium' | 'low'
  reasoning?: string              // 置信度判断理由（可选）
  alternatives?: {                // 备选答案（可选）
    result: unknown
    confidence: number
  }[]
}
```

### 3.3 HumanTask - 人工任务

暂停 Workflow，等待人工输入�?

```yaml
- id: confirm_booking
  type: HumanTask
  name: "确认预订"
  
  prompt: "请选择要预订的会议�?
  options:                        # 提供选项
    - label: "A会议室（10人）"
      value: "room-a"
    - label: "B会议室（6人）"
      value: "room-b"
  
  # 或自由输�?
  # inputSchema:
  #   type: string
  #   minLength: 1
  
  timeout: 3600000                # 1小时超时
  timeoutAction: cancel           # cancel | skip | default
  defaultValue: "room-a"
  
  # 通知配置
  notification:
    channels: [email, app]
    recipients: ["{{ ctx.user.id }}"]
  
  output:
    variable: selected_room
```

**TypeScript 定义**�?

```typescript
interface HumanTaskStep {
  id: string
  type: 'HumanTask'
  name?: string
  
  prompt: string
  
  // 选项模式
  options?: {
    label: string
    value: unknown
    description?: string
  }[]
  
  // 自由输入模式
  inputSchema?: JSONSchema
  
  timeout?: number
  timeoutAction?: 'cancel' | 'skip' | 'default'
  defaultValue?: unknown
  
  notification?: {
    channels: ('email' | 'sms' | 'app' | 'webhook')[]
    recipients: Expression[]
    template?: string
  }
  
  output?: {
    variable?: string
  }
}
```

### 3.4 SubWorkflow - 子工作流

嵌套调用另一�?Workflow�?

```yaml
- id: run_approval
  type: SubWorkflow
  name: "执行审批流程"
  
  workflow: "approval-workflow-v1"  # 被调用的 Workflow ID
  input:
    item: "{{ vars.generated_content }}"
    approver: "{{ ctx.user.manager }}"
  
  # 是否等待完成
  async: false
  
  # 传递上下文
  inheritContext: true
  
  output:
    variable: approval_result
```

### 3.5 Parallel - 并行执行

同时执行多个步骤�?

```yaml
- id: parallel_fetch
  type: Parallel
  name: "并行获取数据"
  
  branches:
    - id: fetch_user
      steps:
        - id: get_user
          type: SkillTask
          skill: "user-service"
          params: { userId: "{{ input.userId }}" }
          output: { variable: user_info }
    
    - id: fetch_orders
      steps:
        - id: get_orders
          type: SkillTask
          skill: "order-service"
          params: { userId: "{{ input.userId }}" }
          output: { variable: order_list }
  
  # 等待策略
  waitStrategy: all               # all | any | n
  # waitCount: 2                  # �?waitStrategy �?n �?
  
  # 失败策略
  failureStrategy: failFast       # failFast | continue | rollback
```

**TypeScript 定义**�?

```typescript
interface ParallelStep {
  id: string
  type: 'Parallel'
  name?: string
  
  branches: {
    id: string
    steps: Step[]
    condition?: Expression
  }[]
  
  waitStrategy?: 'all' | 'any' | 'n'
  waitCount?: number
  failureStrategy?: 'failFast' | 'continue' | 'rollback'
  
  output?: {
    merge?: 'object' | 'array'    // 结果合并方式
    variable?: string
  }
}
```

### 3.6 Condition - 条件分支

根据条件执行不同分支�?

```yaml
- id: route_by_intent
  type: Condition
  name: "按意图路�?
  
  expression: "{{ vars.intent_result.intent }}"
  
  branches:
    - when: "query"
      steps:
        - id: handle_query
          type: SkillTask
          skill: "search-service"
          # ...
    
    - when: "command"
      steps:
        - id: handle_command
          type: AgentTask
          action: generate
          # ...
    
    - default: true               # 默认分支
      steps:
        - id: handle_unknown
          type: SkillTask
          skill: "fallback-handler"
```

**TypeScript 定义**�?

```typescript
interface ConditionStep {
  id: string
  type: 'Condition'
  name?: string
  
  expression: Expression
  
  branches: {
    when?: unknown                // 匹配�?
    match?: Expression            // 匹配表达�?
    default?: boolean             // 是否为默认分�?
    steps: Step[]
  }[]
}
```

### 3.6.1 置信度路由（Confidence-Based Routing�?

专门用于根据 AgentTask 输出的置信度进行分支路由，是处理 AI 不确定性的核心机制�?

```yaml
- id: confidence_route
  type: Condition
  name: "置信度路�?
  
  # 使用 AgentTask 输出的置信度
  expression: "{{ vars.ai_result.confidenceLevel }}"
  
  branches:
    # 高置信度：直接使用结�?
    - when: "high"
      steps:
        - id: auto_process
          type: SkillTask
          skill: "auto-processor"
          params:
            data: "{{ vars.ai_result.result }}"
    
    # 中置信度：快速人工确�?
    - when: "medium"
      steps:
        - id: quick_confirm
          type: HumanTask
          prompt: |
            AI 生成结果（置信度：{{ vars.ai_result.confidence | percent }}）：
            {{ vars.ai_result.result | json }}
            
            备选方案：
            {{ vars.ai_result.alternatives | json }}
          options:
            - label: "确认使用"
              value: { action: "confirm", useAlternative: false }
            - label: "使用备�?"
              value: { action: "confirm", useAlternative: 0 }
            - label: "拒绝，重新生�?
              value: { action: "retry" }
          timeout: 300000         # 5分钟超时
    
    # 低置信度：详细人工审�?
    - when: "low"
      steps:
        - id: detailed_review
          type: HumanTask
          prompt: |
            ⚠️ AI 置信度较低（{{ vars.ai_result.confidence | percent }}�?
            
            判断理由：{{ vars.ai_result.reasoning }}
            
            请详细审核以下结果：
            {{ vars.ai_result.result | json }}
          inputSchema:
            type: object
            required: [decision, correctedResult]
            properties:
              decision:
                type: string
                enum: [approve, correct, reject]
              correctedResult:
                type: object
          timeout: 3600000        # 1小时超时
```

**置信度提取方法说�?*�?

| extractMethod | 说明 | 适用场景 |
|---------------|------|----------|
| `auto` | �?LLM 响应自动推断（基�?logprobs、token 概率等） | 大多数场�?|
| `explicit` | 要求 LLM 在输出中显式给出置信�?| 需要解释性的场景 |
| `calibrated` | 使用校准模型对置信度进行修正 | 对准确性要求高的场�?|

**完整置信度路由示�?*�?

```yaml
id: "confidence-aware-workflow"
name: "置信度感知工作流"
version: "1.0.0"

input:
  type: object
  required: [query]
  properties:
    query: { type: string }

steps:
  # 1. AI 处理（启用置信度�?
  - id: ai_analyze
    type: AgentTask
    name: "AI 分析"
    action: understand
    params:
      text: "{{ input.query }}"
      schema:
        type: object
        properties:
          category: { type: string }
          sentiment: { type: string }
          keyPoints: { type: array }
    confidenceConfig:
      enabled: true
      extractMethod: explicit     # 要求 AI 显式给出置信�?
      threshold:
        high: 0.9
        medium: 0.7
        low: 0.0
    output:
      variable: analysis_result
  
  # 2. 基于置信度路�?
  - id: route_by_confidence
    type: Condition
    name: "置信度决�?
    expression: "{{ vars.analysis_result.confidence }}"
    
    branches:
      # 高置信度 >= 0.9
      - match: ">= 0.9"
        steps:
          - id: auto_action
            type: SkillTask
            skill: "auto-responder"
            params:
              analysis: "{{ vars.analysis_result.result }}"
      
      # 中置信度 >= 0.7
      - match: ">= 0.7"
        steps:
          - id: quick_review
            type: HumanTask
            prompt: "请确�?AI 分析结果（置信度 {{ vars.analysis_result.confidence | percent }}�?
            options:
              - { label: "确认", value: true }
              - { label: "拒绝", value: false }
            timeout: 300000
            output:
              variable: confirmed
          
          - id: check_confirm
            type: Condition
            expression: "{{ vars.confirmed }}"
            branches:
              - when: true
                steps:
                  - id: proceed
                    type: SkillTask
                    skill: "auto-responder"
                    params:
                      analysis: "{{ vars.analysis_result.result }}"
              - when: false
                steps:
                  - id: escalate
                    type: SkillTask
                    skill: "escalate-to-expert"
      
      # 低置信度 < 0.7
      - default: true
        steps:
          - id: human_analysis
            type: HumanTask
            prompt: |
              AI 置信度过低，需要人工分析：
              原始查询：{{ input.query }}
              AI 尝试结果：{{ vars.analysis_result.result | json }}
              AI 判断理由：{{ vars.analysis_result.reasoning }}
            inputSchema:
              type: object
              required: [analysis]
              properties:
                analysis:
                  type: object
            timeout: 7200000
            output:
              variable: human_result

variables:
  # 统计置信度分布（用于后续优化�?
  confidenceStats:
    highCount: 0
    mediumCount: 0
    lowCount: 0

hooks:
  onStepComplete:
    - type: metric
      condition: "{{ step.type == 'AgentTask' }}"
      config:
        name: "ai.confidence"
        value: "{{ step.output.confidence }}"
        tags:
          level: "{{ step.output.confidenceLevel }}"
```

### 3.7 Loop - 循环

遍历集合或条件循环�?

```yaml
# For-Each 循环
- id: process_items
  type: Loop
  name: "处理每个项目"
  
  mode: forEach
  collection: "{{ vars.items }}"
  itemVariable: item
  indexVariable: idx
  
  steps:
    - id: process_item
      type: SkillTask
      skill: "item-processor"
      params:
        data: "{{ vars.item }}"
  
  # 并行�?
  parallelism: 5                  # 最多并�?�?
  
  output:
    variable: processed_items
    collect: true                 # 收集每次循环的输�?

# While 循环
- id: wait_completion
  type: Loop
  name: "等待完成"
  
  mode: while
  condition: "{{ vars.status != 'completed' }}"
  maxIterations: 100
  
  steps:
    - id: check_status
      type: SkillTask
      skill: "status-checker"
      output: { variable: status }
    
    - id: wait
      type: SkillTask
      skill: "delay"
      params: { ms: 5000 }
```

---

## 4. 表达式语�?

### 4.1 变量引用

```yaml
# 输入变量
"{{ input.fieldName }}"

# 上下文变�?
"{{ ctx.user.id }}"
"{{ ctx.correlationId }}"

# 自定义变�?
"{{ vars.myVariable }}"

# 步骤输出
"{{ steps.step_id.output }}"

# 环境变量
"{{ env.API_KEY }}"
```

### 4.2 内置函数

```yaml
# 字符�?
"{{ input.text | upper }}"
"{{ input.text | lower }}"
"{{ input.text | trim }}"
"{{ input.text | truncate(100) }}"

# 默认�?
"{{ input.style | default('casual') }}"

# 类型转换
"{{ input.count | int }}"
"{{ input.price | float }}"
"{{ input.data | json }}"

# 集合
"{{ items | length }}"
"{{ items | first }}"
"{{ items | last }}"
"{{ items | join(',') }}"

# 条件
"{{ input.vip ? 'premium' : 'standard' }}"

# 日期
"{{ now() }}"
"{{ now() | format('YYYY-MM-DD') }}"
```

### 4.3 条件表达�?

```yaml
# 比较
"{{ input.count > 10 }}"
"{{ input.type == 'premium' }}"
"{{ input.name != null }}"

# 逻辑
"{{ input.a && input.b }}"
"{{ input.a || input.b }}"
"{{ !input.flag }}"

# 包含
"{{ input.type in ['a', 'b', 'c'] }}"
"{{ 'keyword' in input.text }}"

# 正则
"{{ input.email matches '^[\\w]+@[\\w]+\\.[a-z]+$' }}"
```

---

## 5. 错误处理

### 5.1 步骤级错误处�?

```yaml
- id: risky_operation
  type: SkillTask
  skill: "external-api"
  
  # 重试配置
  retryPolicy:
    maxRetries: 3
    backoff: exponential
    baseMs: 1000
    maxBackoffMs: 30000
    retryableErrors:
      - "EXTERNAL_TIMEOUT"
      - "EXTERNAL_SERVICE_UNAVAILABLE"
  
  # 失败处理
  onError:
    - match: "EXTERNAL_*"
      action: fallback
      fallbackStep: use_cache
    
    - match: "*"
      action: fail
```

### 5.2 Workflow 级错误处�?

```yaml
errorHandlers:
  # AI 错误降级
  - match: "AI_TIMEOUT"
    action: retry
    config:
      maxRetries: 2
      fallbackStep: rule_based_fallback
  
  # 外部服务熔断
  - match: "EXTERNAL_SERVICE_UNAVAILABLE"
    action: circuitBreak
    config:
      fallbackStep: cached_response
      cooldownMs: 30000
  
  # 权限错误直接失败
  - match: "GUARD_PERMISSION_DENIED"
    action: fail
    config:
      message: "权限不足，无法执行此操作"
  
  # 其他错误
  - match: "*"
    action: fail
```

### 5.3 补偿（Compensation�?

```yaml
- id: book_resources
  type: Parallel
  
  branches:
    - id: book_room
      steps:
        - id: reserve_room
          type: SkillTask
          skill: "room-booking"
          compensation:            # 补偿步骤
            skill: "room-cancel"
            params:
              bookingId: "{{ steps.reserve_room.output.bookingId }}"
    
    - id: book_equipment
      steps:
        - id: reserve_equipment
          type: SkillTask
          skill: "equipment-booking"
          compensation:
            skill: "equipment-cancel"
  
  # 任一失败时触发所有补�?
  failureStrategy: rollback
```

---

## 6. 生命周期钩子

```yaml
hooks:
  # Workflow 开�?
  onStart:
    - type: log
      config: { level: info, message: "Workflow started" }
    - type: metric
      config: { name: "workflow.started", tags: { id: "{{ workflow.id }}" } }
  
  # Workflow 完成
  onComplete:
    - type: notify
      config:
        channel: webhook
        url: "{{ env.CALLBACK_URL }}"
        payload:
          status: "completed"
          result: "{{ output }}"
  
  # Workflow 错误
  onError:
    - type: alert
      config:
        channel: slack
        message: "Workflow {{ workflow.id }} failed: {{ error.message }}"
  
  # 步骤开�?完成
  onStepStart:
    - type: log
      config: { message: "Step {{ step.id }} started" }
  
  onStepComplete:
    - type: metric
      config: { name: "step.duration", value: "{{ step.duration }}" }
```

---

## 7. 完整示例

### 7.1 内容生成审核流程

```yaml
id: "content-review-workflow"
name: "内容生成与审�?
version: "1.0.0"

trigger:
  type: api
  config:
    endpoint: "/api/content/generate"

input:
  type: object
  required: [topic, style]
  properties:
    topic: { type: string }
    style: { type: string, enum: [formal, casual] }
    maxLength: { type: integer, default: 500 }

output:
  type: object
  properties:
    content: { type: string }
    reviewStatus: { type: string }
    reviewComments: { type: array }

variables:
  maxRetries: 3

steps:
  # 1. 理解需�?
  - id: understand
    type: AgentTask
    name: "理解生成需�?
    action: understand
    params:
      text: "生成关于 {{ input.topic }} �?{{ input.style }} 风格内容"
      schema:
        type: object
        properties:
          keywords: { type: array }
          tone: { type: string }
          targetAudience: { type: string }
    output:
      variable: requirements

  # 2. 生成内容
  - id: generate
    type: AgentTask
    name: "生成内容"
    action: generate
    params:
      text: |
        根据以下要求生成内容�?
        关键词：{{ vars.requirements.keywords | join(', ') }}
        语调：{{ vars.requirements.tone }}
        目标受众：{{ vars.requirements.targetAudience }}
        最大长度：{{ input.maxLength }}
    aiConfig:
      temperature: 0.7
      maxTokens: "{{ input.maxLength * 2 }}"
    validation:
      detectHallucination: true
      maxHallucinationScore: 0.2
    output:
      variable: generated_content

  # 3. 自动审核
  - id: auto_review
    type: SkillTask
    name: "自动内容审核"
    skill: "content-review"
    params:
      content: "{{ vars.generated_content.content }}"
      rules:
        - checkSensitiveWords
        - checkLength
        - checkFormat
    output:
      variable: auto_review_result

  # 4. 根据审核结果分支
  - id: review_route
    type: Condition
    name: "审核路由"
    expression: "{{ vars.auto_review_result.passed }}"
    
    branches:
      # 审核通过
      - when: true
        steps:
          - id: finalize
            type: SkillTask
            skill: "content-finalizer"
            params:
              content: "{{ vars.generated_content.content }}"
            output:
              variable: final_content
      
      # 审核不通过 - 人工审核
      - when: false
        steps:
          - id: human_review
            type: HumanTask
            name: "人工审核"
            prompt: |
              自动审核未通过，请人工审核以下内容�?
              ---
              {{ vars.generated_content.content }}
              ---
              问题：{{ vars.auto_review_result.issues | join(', ') }}
            options:
              - label: "通过"
                value: { action: "approve" }
              - label: "修改后通过"
                value: { action: "edit" }
              - label: "拒绝"
                value: { action: "reject" }
            timeout: 86400000
            notification:
              channels: [email, app]
              recipients: ["{{ ctx.user.manager }}"]
            output:
              variable: human_decision

          # 根据人工决定处理
          - id: handle_decision
            type: Condition
            expression: "{{ vars.human_decision.action }}"
            branches:
              - when: "approve"
                steps:
                  - id: approve_content
                    type: SkillTask
                    skill: "content-finalizer"
                    params:
                      content: "{{ vars.generated_content.content }}"
                    output:
                      variable: final_content
              
              - when: "edit"
                steps:
                  - id: regenerate
                    type: AgentTask
                    action: generate
                    params:
                      text: |
                        请修改以下内容，解决这些问题：{{ vars.auto_review_result.issues | join(', ') }}
                        原内容：{{ vars.generated_content.content }}
                    output:
                      variable: final_content
              
              - when: "reject"
                steps:
                  - id: reject_content
                    type: SkillTask
                    skill: "workflow-fail"
                    params:
                      reason: "内容被人工审核拒�?

  # 5. 输出结果
  - id: output_result
    type: SkillTask
    skill: "workflow-output"
    params:
      content: "{{ vars.final_content.content }}"
      reviewStatus: "{{ vars.auto_review_result.passed ? 'auto_approved' : 'human_reviewed' }}"

errorHandlers:
  - match: "AI_*"
    action: retry
    config:
      maxRetries: 2
      fallbackStep: fallback_generate

  - match: "GUARD_HALLUCINATION_DETECTED"
    action: goto
    config:
      step: generate
      maxTimes: 2

hooks:
  onComplete:
    - type: metric
      config:
        name: "content.generated"
        tags:
          style: "{{ input.style }}"
          reviewed: "{{ !vars.auto_review_result.passed }}"
```

---

## 8. 验证与调�?

### 8.1 Workflow 验证

```typescript
interface WorkflowValidator {
  // 验证 Workflow 定义
  validate(definition: WorkflowDefinition): ValidationResult
  
  // 检查项
  // - Schema 有效�?
  // - 步骤 ID 唯一�?
  // - 变量引用有效�?
  // - 循环依赖检�?
  // - 表达式语�?
}
```

### 8.2 Dry Run

```typescript
// 模拟执行，不实际调用 Skill/Agent
const result = await dispatcher.dryRun(workflowId, {
  input: { topic: "AI技�?, style: "casual" },
  mocks: {
    "understand": { keywords: ["AI", "技�?], tone: "轻松" },
    "generate": { content: "模拟生成的内�?.." }
  }
})
```

---

## 9. 与其他文档的关系

- 步骤类型的执行由《UniFlow-接口契约定义.md》中的角色实�?
- 错误处理参考《UniFlow-错误处理策略.md》的错误�?
- 变量与记忆交互参考《UniFlow-产物与驱动机�?md�?
- 消息格式参考《UniFlow-消息协议规范.md�?
