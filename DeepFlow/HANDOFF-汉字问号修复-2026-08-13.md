# HANDOFF — DeepFlow 文档 汉字+? 编码损坏修复

> 交接时间：2026-08-13 · 分支：`deepframes-docs-review-fixes`
> 原会话中断原因：用户重开会话。当前进行到一半，未提交。

---

## 一、任务背景

DeepFlow 文档库存在 UTF-8 尾字节丢失编码损坏。两类损坏：

1. **U+FFFD(`�`)** — 已完成修复并提交。
2. **`汉字+?` 模式** — U+FFFD 修复时遗留的 `?` 占位（原全角标点/丢失汉字被工具补成半角 `?`）。这是**当前任务**。

`?` 性质多样（已确认）：
- 表格分隔 `|` 前 / 加粗 `*` 前 → 纯残留，删 `?`（确定性，已完成）
- `智能? (Level 3)` → 丢字，应补 `体` → `智能体`
- `分配计算?存储/AI` → 丢标点 `、`
- 疑问句句尾 → 应改全角 `？`
- 框线图/代码块内 → 尽量保结构，删多余 `?`

---

## 二、已完成（已提交）

- commit `469366d6`：修复 4 篇文档 U+FFFD 71 行（01.03 / 02.05 / 06.05 / 06.07），verify 全绿。
- 4 文件 diff：1439 insertions / 1434 deletions，行尾统一 LF+BOM 匹配 HEAD。

## 三、已完成（未提交！）

> **以下改动已写入工作区，但尚未 commit。重开会话后先 review 再提交。**

1. **02.05** 全部 43 处 `汉字+?` 已处理（25 行修改）→ 当前 02.05 该模式清零。
   - 表格/节点内删 `?`（L49-56 角色名、L129/L151 框线图、L416-422 流程节点等）
   - 疑问句改全角 `？`（L42 标题、L359 `天气？`、L393 `成功？`、L439 `输入？`）
2. **确定性规则批量落地 24 文件**：`汉字+?` 后跟 `|` 或 `*` 的直接删 `?`，共 116 处。
   - 涉及：00.01/01.01/01.07/02.03/03.02/03.03/03.04/03.06/03.07/05.02/05.05/05.07/05.09/06.11/06.14/06.15/07.01/07.02/08.10/08.11/08.19/09.01/bugfix.md/history.md
3. **新脚本 `tools/fix_qmark.py`**（未提交）：检测 `汉字+?` 模式，LLM 语义恢复。
   - 复用 `tools/fix_doc_encoding.py` 的 `build_prompt`/`chat`/`parse_json_resp` 思路
   - MODEL 当前为 `claude-duojie-gpt-5-6-luna`
   - 用法：`python -X utf8 tools/fix_qmark.py [--dry-run] [--limit N] [file.md ...]`

## 四、剩余工作（卡点）

**剩余 354 处 `汉字+?` / 38 文件** 需 LLM 语义修复。大文件分布：
- bugfix.md 46、06.14 32、history.md 32、03.04 30、02.03 29、01.01(三层演进) 17、01.01(Quick) 17、03.13 16、06.11 16、04.01 11 ...

### ⚠️ 卡点：网关 502

- Kiro 网关 `http://127.0.0.1:8000/v1/chat/completions`，key 在 `D:\_Progs\01Center\kiro-gateway\.env`（`PROXY_API_KEY=`）
- **短 prompt OK**（已实测 `claude-agnes-2-5-flash`、`claude-duojie-gpt-5-6-luna` 均正常响应）
- **长 prompt（fix_qmark.py 构造的块，约几百~上千字符）→ 502 empty_shell**
  - 报错：`Provider 'agnes' stream aggregated but response had no usable content ... type empty_shell`
  - luna 模型同样 502
- **下一步尝试**（按优先级）：
  1. 换其他模型（网关有 63 个，`/v1/models` 可查，如 `claude-haiku-4-5`、`claude-sonnet-4-5` 系列）
  2. 缩小 `MAX_PER_BLOCK`（16→4）或减小 prompt（减少 CTX_BEFORE/AFTER 12→4）
  3. 降低 `max_tokens` 8000→2000
  4. 检查网关是否对超长输出流式聚合有问题，尝试 `stream: false` 或换 `/v1/chat/completions` 变体

## 五、环境与工具

- verify 脚本：`python -X utf8 tools/verify_doc_encoding.py`（查 U+FFFD，**EXIT=1 只因 02.01/03.04/08.17/history.md 的原有表格列数不一致，非本次引入**）
- 全目录 `汉字+?` 扫描：
  ```python
  python -X utf8 -c "import pathlib,re; root=pathlib.Path(r'D:/_Progs/02Business/DeepFlow'); print(sum(len(re.findall(r'[一-鿿]\?', f.read_text(encoding='utf-8',errors='replace'))) for f in root.glob('*.md')))"
  ```
- 分支：`deepframes-docs-review-fixes`；**未 push**（不可 push，需老板授权）

## 六、历史提交参考

- `469366d6` docs(fix): 修复 4 篇文档 U+FFFD 编码损坏 (71 行) ← 上一提交
- 前序：`8c78dc6c`/`a8328049` 封版审计归档、`b75d8a35` 封版 v1.0.0

---

## 给新会话的提示词

> 读 `D:\_Progs\02Business\DeepFlow\HANDOFF-汉字问号修复-2026-08-13.md`，这是罗辑文档编码修复的交接文档。
> 当前任务是清掉 DeepFlow 全目录剩余 354 处 `汉字+?` 编码损坏。确定性规则已落地 116 处（未提交），02.05 已清零（未提交），`tools/fix_qmark.py` 已写好但 LLM 网关对长 prompt 返回 502。
> 先 review 未提交改动并提交，再解决 502（换模型/缩块/降 max_tokens），跑完 fix_qmark.py 剩余 38 文件，verify 确认 `汉字+?` 清零，最后 commit + 报备。
