# UniFlow �?uniFlow 关系说明

> 厘清 11 角色协作架构（UniFlow）与事件溯源核心模型（uniFlow）的关系、定位和适用场景�?

创建日期: 2025-12-04  版本: 1.0

---

## 1. 为何有两套设计？

DeepBase 文档集中存在两套看似独立的架构设计，这并非冗余，而是服务于不同层次的需求：

| 设计 | 文档 | 关注�?| 服务对象 |
|------|------|--------|----------|
| **UniFlow** | `03.01.Arch-UniFlow架构设计` | 角色协作、AI 能力编排 | 洞见、AI Agent 应用 |
| **uniFlow** | `03.02.Design-uniFlow-CoreModel` | 流程追溯、事件记�?| SayDone、SoIs、DevDirector |

---

## 2. 架构定位

### 2.1 分层关系

```
┌─────────────────────────────────────────────────────────────────�?
�?                     业务应用�?                                 �?
�?        洞见(DeepDeepDeepDeepDeepInsight) �?SayDone �?SoIs �?DevDirector            �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                 �?
�? ┌────────────────────────�? ┌────────────────────────────�?    �?
�? �?     UniFlow           �? �?       uniFlow             �?    �?
�? �?  (角色协作引擎)        �? �?   (流程追溯引擎)          �?    �?
�? �?                       �? �?                           �?    �?
�? �? �?11 角色军事隐喻      �? �? �?Flow/Event/Snapshot    �?    �?
�? �? �?Workflow 编排       �? �? �?事件溯源                �?    �?
�? �? �?AI 能力复用         �? �? �?历史回放                �?    �?
�? �? �?安全守卫            �? �? �?版本分叉                �?    �?
�? └───────────┬────────────�? └─────────────┬──────────────�?    �?
�?             �?                            �?                    �?
�?             └──────────┬──────────────────�?                    �?
�?                        �?                                       �?
�?                        �?                                       �?
�? ┌─────────────────────────────────────────────────────────�?   �?
�? �?                   DeepBase 基础�?                       �?   �?
�? �?          Config · Logging · DB · Security              �?   �?
�? └─────────────────────────────────────────────────────────�?   �?
└─────────────────────────────────────────────────────────────────�?
```

### 2.2 核心区别

| 维度 | UniFlow (角色协作) | uniFlow (事件溯源) |
|------|-------------------|-------------------|
| **核心抽象** | Role（角色）、Message（消息） | Flow（流程）、Event（事件） |
| **设计模式** | �?Agent 协作模式 | 事件溯源 (Event Sourcing) |
| **主要目标** | AI 能力的安全、可控编�?| 流程的可追溯、可回放 |
| **状态管�?* | 角色内部状�?+ 消息传�?| 事件序列 + 快照重建 |
| **适用场景** | AI 推理、Skill 执行、安全审�?| 构建循环、维护循环、版本管�?|

---

## 3. 典型使用场景

### 3.1 洞见（DeepInsight）使�?UniFlow

洞见�?AI 决策辅助产品，侧重于 **AI 能力的安全编�?*�?

```
用户请求 "分析这个决策"
    �?
[UniFlow 角色协作]
├── Commander: 接收请求，权限校�?
├── Guard: 检�?Prompt 注入
├── Advisor: 调用 LLM 分析
├── Executor: 执行 decision_coach Skill
├── Guard: 审核输出，检测幻�?
└── Commander: 返回结果

核心价�? 安全、可控、可审计�?AI 调用
```

### 3.2 SayDone 使用 uniFlow

SayDone 是自然语言转结构化引擎，侧重于 **流程的完整记�?*�?

```
用户输入 "创建一个登录页�?
    �?
[uniFlow 事件记录]
├── Event: ClarifyRequirement (Started �?Succeeded)
├── Event: GenerateSketch (Started �?Succeeded)
├── Event: ConfirmSketch (Started �?WaitingUser �?Succeeded)
├── Event: NlConvertToStructure (Started �?Succeeded)
└── Snapshot: 最终结构化数据

核心价�? 可追溯、可回放、可分叉的流程历�?
```

### 3.3 混合使用场景

某些复杂场景可能同时使用两套机制�?

```
SoIs 意图澄清流程
    �?
    ├── [uniFlow] 记录流程事件
    �?  └── Event: ClarifyIntent (Succeeded)
    �?
    └── [UniFlow] 执行 AI 能力
        ├── Commander: 分发任务
        ├── Advisor: 理解用户意图
        └── Guard: 校验澄清结果
```

---

## 4. 设计原则对比

### 4.1 共同原则

两套设计共享以下核心原则�?

| 原则 | UniFlow 体现 | uniFlow 体现 |
|------|-------------|-------------|
| **AI �?AI，程序归程序** | Guard 是纯程序，Advisor �?AI | 状态机是纯程序，payload 可含 AI |
| **单一事实�?* | 消息是角色间唯一通信方式 | Event 是状态变化的唯一来源 |
| **可审�?* | Chronicler 记录所有操�?| Event 序列完整可查 |
| **向后兼容** | 消息协议版本�?| Event 结构可扩�?|

### 4.2 侧重点差�?

| UniFlow 强调 | uniFlow 强调 |
|-------------|-------------|
| 角色职责清晰、边界明�?| 事件完整、无幽灵状�?|
| 消息路由、交互约�?| 时间线追溯、版本分�?|
| 安全守卫、信任层�?| 快照重建、状态恢�?|
| 实时响应、流式输�?| 历史回放、可视化调试 |

---

## 5. 何时使用哪套�?

### 5.1 选择 UniFlow 的场�?

- �?需要调�?AI/LLM 能力
- �?需要多步骤 Skill 编排
- �?需要严格的安全审核（输�?输出校验�?
- �?需要实时响应或流式输出
- �?面向终端用户的智能交�?

### 5.2 选择 uniFlow 的场�?

- �?需要完整的操作历史
- �?需要从任意历史点回�?分叉
- �?需要向用户展示"经历了哪些步�?
- �?需要支持撤销/重做
- �?构建/维护循环的流程管�?

### 5.3 同时使用的场�?

- �?AI 辅助的构�?维护循环（如 SoIs�?
- �?需要既安全又可追溯的复杂流�?
- �?需要将 AI 能力嵌入事件驱动流程

---

## 6. 集成指南

### 6.1 UniFlow 调用 uniFlow

�?UniFlow �?Workflow 需要记录到 uniFlow 时：

```pascal
// �?Dispatcher 中创�?uniFlow Event
procedure TDispatcher.OnWorkflowStepCompleted(AStep: TWorkflowStep);
begin
  // 记录�?UniFlow �?Chronicler
  Chronicler.Log('STEP_COMPLETED', AStep.ToJSON);
  
  // 同时记录�?uniFlow �?Event 系统（如果启用）
  if FUniFlowEnabled then
    UniFlowEngine.WriteEvent(TUniFlowEvent.Create(
      FCurrentFlowId,
      'WorkflowStep',
      AStep.Name,
      'Dispatcher',
      'Succeeded',
      AStep.OutputPayload
    ));
end;
```

### 6.2 uniFlow 触发 UniFlow

�?uniFlow 的某个步骤需�?AI 能力时：

```pascal
// �?uniFlow 步骤中调�?UniFlow
function TUniFlowStep.ExecuteAITask(APayload: TJSONObject): TJSONObject;
var
  LMessage: TUniFlowMessage;
  LResponse: TUniFlowMessage;
begin
  // 通过 UniFlow �?Commander 发起 AI 请求
  LMessage := TMessageBuilder.Create
    .WithAction('EXECUTE_AI_TASK')
    .From(rtDispatcher)
    .To([rtCommander])
    .WithPayload(APayload)
    .Build;
  
  LResponse := FUniFlowEngine.SendAndWait(LMessage);
  Result := LResponse.Body.Payload;
end;
```

---

## 7. 命名约定

为避免混淆，建议遵循以下命名约定�?

| 场景 | 使用 | 示例 |
|------|------|------|
| 角色协作相关 | **UniFlow**（大�?U�?| UniFlow.Role.Commander |
| 事件溯源相关 | **uniFlow**（小�?u�?| uniFlow.Event.Write |
| 文档标题 | 根据内容选择 | `03.01.Arch-UniFlow...` / `03.02.Design-uniFlow...` |
| 代码命名空间 | 区分使用 | `UniFlow.Roles.*` / `uniFlow.Core.*` |

---

## 8. 常见问题

### Q1: 能否只用一套？

**可以**，但会牺牲部分能力：
- 只用 UniFlow：缺少完整的历史追溯和回放能�?
- 只用 uniFlow：缺�?AI 能力的安全编�?

### Q2: 两套会不会增加复杂度�?

**会增加学习成�?*，但�?
- 职责分离使单个系统更简�?
- 可以根据产品需求选择性使�?
- 文档明确划分了适用场景

### Q3: 两套的数据如何关联？

- UniFlow �?`traceId` 可以关联�?uniFlow �?`flowId`
- Chronicler 的日志可以作为两者的桥接审计
- 建议�?uniFlow Event �?payload 中记录相关的 UniFlow traceId

---

## 9. 相关文档

| 文档 | 说明 |
|------|------|
| `03.01.Arch-UniFlow架构设计` | UniFlow 11 角色架构详细设计 |
| `03.02.Design-uniFlow-CoreModel` | uniFlow 事件溯源模型详细设计 |
| `09.01.ADR-UniFlow-架构决策记录` | UniFlow 关键架构决策 |
| `02.07.Spec-UniFlow-术语表` | 统一术语定义 |

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作�?|
|------|------|----------|------|
| 1.0 | 2025-12-04 | 初始版本 | - |
