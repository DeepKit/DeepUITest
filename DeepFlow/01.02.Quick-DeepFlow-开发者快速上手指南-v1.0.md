# UniFlow 开发者快速上手指�?

| 版本 | 日期 | 作�?| 变更说明 |
|------|------|------|----------|
| v1.0 | 2024-12-04 | 产品�?| 初稿，定�?5/15/30 分钟上手路径 |

## 前言

本指南帮助外部开发者快速集�?UniFlow。按照以下三步路径，您可以：
- **5 分钟**：调通第一�?API
- **15 分钟**：执行一个完�?Workflow
- **30 分钟**：接收异步回调并处理结果

---

## 1. 5 分钟 - Hello World

### 1.1 前置条件

- UniFlow 服务已启动（默认 http://localhost:8080�?
- 获取 API Key（联系管理员或在控制台生成）
- 准备 HTTP 客户端（curl、Postman 或代码）

### 1.2 测试连接

```bash
# 检查服务状�?
curl http://localhost:8080/health

# 期望响应
{
  "status": "healthy",
  "version": "1.0.0",
  "timestamp": 1733323172
}
```

### 1.3 执行第一�?Skill

```bash
# 调用 echo Skill（内置，用于测试�?
curl -X POST http://localhost:8080/api/v1/skill/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{
    "skill_name": "builtin.echo",
    "input": {
      "message": "Hello UniFlow!"
    }
  }'

# 期望响应
{
  "success": true,
  "data": {
    "output": "Hello UniFlow!",
    "timestamp": "2024-12-04T10:30:45Z"
  },
  "request_id": "req-xxx-xxx"
}
```

### 1.4 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 401 Unauthorized | API Key 无效或缺�?| 检�?X-API-Key header |
| 404 Not Found | Skill 不存�?| 使用 `builtin.echo` 测试 |
| 503 Service Unavailable | 服务未就�?| 等待几秒后重�?|

�?**恭喜�?* 您已成功调通第一�?UniFlow API�?

---

## 2. 15 分钟 - 执行 Workflow

### 2.1 查看可用 Workflow

```bash
# 列出所�?Workflow
curl -X GET http://localhost:8080/api/v1/workflow/list \
  -H "X-API-Key: your-api-key"

# 响应示例
{
  "workflows": [
    {
      "id": "simple-qa",
      "name": "简单问�?,
      "description": "接收问题，返�?LLM 回答",
      "version": "1.0"
    },
    {
      "id": "DeepDeepDeepDeepDeepInsight-flow",
      "name": "洞见流程",
      "description": "多角色协作的深度分析流程",
      "version": "1.0"
    }
  ]
}
```

### 2.2 执行简单问�?Workflow

```bash
# 执行同步 Workflow（等待完成）
curl -X POST http://localhost:8080/api/v1/workflow/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{
    "workflow_id": "simple-qa",
    "input": {
      "question": "什么是 UniFlow�?
    },
    "options": {
      "sync": true,
      "timeout": 60
    }
  }'

# 响应（同步模式，直接返回结果�?
{
  "success": true,
  "execution_id": "exec-xxx-xxx",
  "status": "completed",
  "result": {
    "answer": "UniFlow 是一个轻量级�?AI 工作流引�?..",
    "confidence": 0.95
  },
  "metadata": {
    "duration_ms": 2340,
    "tokens_used": 856,
    "model": "gpt-4"
  }
}
```

### 2.3 异步执行（推荐用于长时间任务�?

```bash
# 步骤 1：提交异步任�?
curl -X POST http://localhost:8080/api/v1/workflow/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{
    "workflow_id": "DeepDeepDeepDeepDeepInsight-flow",
    "input": {
      "topic": "如何提高团队协作效率"
    },
    "options": {
      "sync": false
    }
  }'

# 响应（立即返回，任务在后台执行）
{
  "success": true,
  "execution_id": "exec-abc-123",
  "status": "running",
  "message": "Workflow submitted, use execution_id to query status"
}

# 步骤 2：查询执行状�?
curl -X GET http://localhost:8080/api/v1/workflow/status/exec-abc-123 \
  -H "X-API-Key: your-api-key"

# 响应
{
  "execution_id": "exec-abc-123",
  "workflow_id": "DeepDeepDeepDeepDeepInsight-flow",
  "status": "running",
  "progress": {
    "current_step": "分析阶段",
    "completed_steps": 2,
    "total_steps": 5,
    "percentage": 40
  },
  "started_at": "2024-12-04T10:30:00Z"
}

# 步骤 3：获取最终结果（status=completed 后）
curl -X GET http://localhost:8080/api/v1/workflow/result/exec-abc-123 \
  -H "X-API-Key: your-api-key"
```

### 2.4 传递上下文

```json
{
  "workflow_id": "simple-qa",
  "input": {
    "question": "继续分析"
  },
  "context": {
    "session_id": "sess-user-123",       // 会话 ID，用于上下文关联
    "user_id": "user-456",               // 用户标识
    "metadata": {                        // 自定义元数据
      "source": "web-app",
      "language": "zh-CN"
    }
  }
}
```

�?**恭喜�?* 您已成功执行 UniFlow Workflow�?

---

## 3. 30 分钟 - Webhook 回调

### 3.1 配置 Webhook

在执�?Workflow 时指定回调地址�?

```bash
curl -X POST http://localhost:8080/api/v1/workflow/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{
    "workflow_id": "DeepDeepDeepDeepDeepInsight-flow",
    "input": {
      "topic": "数字化转型策�?
    },
    "webhook": {
      "url": "https://your-server.com/webhook/uniflow",
      "events": ["completed", "failed", "progress"],
      "headers": {
        "Authorization": "Bearer your-webhook-secret"
      }
    }
  }'
```

### 3.2 Webhook 事件类型

| 事件 | 触发时机 | 说明 |
|------|----------|------|
| `workflow.started` | Workflow 开始执�?| 可用于更�?UI 状�?|
| `workflow.progress` | 步骤完成/状态变�?| 实时进度通知 |
| `workflow.completed` | Workflow 成功完成 | 包含完整结果 |
| `workflow.failed` | Workflow 执行失败 | 包含错误详情 |
| `workflow.cancelled` | Workflow 被取�?| 用户或系统取�?|

### 3.3 Webhook 请求格式

UniFlow 将向您的 Webhook URL 发�?POST 请求�?

```json
{
  "event": "workflow.completed",
  "timestamp": "2024-12-04T10:35:00Z",
  "data": {
    "execution_id": "exec-abc-123",
    "workflow_id": "DeepDeepDeepDeepDeepInsight-flow",
    "status": "completed",
    "result": {
      "DeepDeepDeepDeepDeepInsights": [
        {
          "title": "洞见1",
          "content": "..."
        }
      ],
      "summary": "..."
    },
    "metadata": {
      "duration_ms": 45000,
      "tokens_used": 12500
    }
  },
  "signature": "sha256=xxxx"  // 用于验证请求来源
}
```

### 3.4 验证 Webhook 签名

为确保安全，您应验证 Webhook 请求的签名：

```python
import hmac
import hashlib

def verify_webhook(payload: bytes, signature: str, secret: str) -> bool:
    """验证 Webhook 签名"""
    expected = hmac.new(
        secret.encode(),
        payload,
        hashlib.sha256
    ).hexdigest()
    return hmac.compare_digest(f"sha256={expected}", signature)

# Flask 示例
@app.route('/webhook/uniflow', methods=['POST'])
def handle_webhook():
    signature = request.headers.get('X-UniFlow-Signature')
    if not verify_webhook(request.data, signature, WEBHOOK_SECRET):
        return jsonify({'error': 'Invalid signature'}), 401
    
    event = request.json
    
    if event['event'] == 'workflow.completed':
        process_result(event['data'])
    elif event['event'] == 'workflow.failed':
        handle_error(event['data'])
    
    return jsonify({'received': True})
```

### 3.5 Webhook 重试机制

UniFlow 会自动重试失败的 Webhook 请求�?

| 重试次数 | 等待时间 | 说明 |
|----------|----------|------|
| 1 | 立即 | 首次请求 |
| 2 | 10 �?| 第一次重�?|
| 3 | 60 �?| 第二次重�?|
| 4 | 5 分钟 | 第三次重�?|
| 5 | 30 分钟 | 最后一次重�?|

**重试条件**�?
- HTTP 状态码 5xx（服务器错误�?
- 连接超时（默�?10 秒）
- 网络错误

**不重�?*�?
- HTTP 状态码 2xx（成功）
- HTTP 状态码 4xx（客户端错误，除 429�?

�?**恭喜�?* 您已完成 UniFlow 基础集成�?

---

## 代码示例

### Python SDK 使用

```python
from uniflow import UniFlowClient

# 初始化客户端
client = UniFlowClient(
    base_url="http://localhost:8080",
    api_key="your-api-key"
)

# 同步执行 Workflow
result = client.workflow.execute(
    workflow_id="simple-qa",
    input={"question": "什么是 UniFlow�?},
    sync=True
)
print(result.answer)

# 异步执行 + 轮询
execution = client.workflow.execute_async(
    workflow_id="DeepDeepDeepDeepDeepInsight-flow",
    input={"topic": "AI 发展趋势"}
)

# 等待完成
result = execution.wait(timeout=300)
print(result.DeepDeepDeepDeepDeepInsights)

# 或者使用回�?
execution.on_complete(lambda r: print(r.DeepDeepDeepDeepDeepInsights))
execution.on_progress(lambda p: print(f"Progress: {p.percentage}%"))
```

### JavaScript/TypeScript 示例

```typescript
import { UniFlowClient } from '@uniflow/sdk';

const client = new UniFlowClient({
  baseUrl: 'http://localhost:8080',
  apiKey: 'your-api-key'
});

// 执行 Workflow
async function runWorkflow() {
  const result = await client.workflow.execute({
    workflowId: 'simple-qa',
    input: { question: '什么是 UniFlow�? },
    sync: true
  });
  
  console.log(result.answer);
}

// 使用 SSE 接收实时进度
const stream = client.workflow.executeStream({
  workflowId: 'DeepDeepDeepDeepDeepInsight-flow',
  input: { topic: 'AI 发展趋势' }
});

stream.on('progress', (data) => {
  console.log(`Step ${data.currentStep}: ${data.percentage}%`);
});

stream.on('complete', (result) => {
  console.log('Done:', result);
});
```

### Delphi 示例

```pascal
uses
  UniFlowClient;

var
  Client: TUniFlowClient;
  Result: TWorkflowResult;
begin
  Client := TUniFlowClient.Create('http://localhost:8080', 'your-api-key');
  try
    // 同步执行
    Result := Client.ExecuteWorkflow('simple-qa', 
      TJSONObject.Create.AddPair('question', '什么是 UniFlow�?));
    
    ShowMessage(Result.GetString('answer'));
  finally
    Client.Free;
  end;
end;
```

---

## 错误处理

### 错误响应格式

```json
{
  "success": false,
  "error": {
    "code": "WORKFLOW_NOT_FOUND",
    "message": "Workflow 'unknown-flow' not found",
    "details": {
      "workflow_id": "unknown-flow",
      "available_workflows": ["simple-qa", "DeepDeepDeepDeepDeepInsight-flow"]
    }
  },
  "request_id": "req-xxx-xxx"
}
```

### 常见错误�?

| 错误�?| HTTP 状�?| 说明 | 处理建议 |
|--------|-----------|------|----------|
| `AUTH_FAILED` | 401 | 认证失败 | 检�?API Key |
| `PERMISSION_DENIED` | 403 | 无权�?| 联系管理�?|
| `WORKFLOW_NOT_FOUND` | 404 | Workflow 不存�?| 检�?workflow_id |
| `INVALID_INPUT` | 400 | 输入参数无效 | 检�?input 格式 |
| `RATE_LIMITED` | 429 | 请求过于频繁 | 降低请求频率 |
| `EXECUTION_TIMEOUT` | 504 | 执行超时 | 使用异步模式 |
| `SERVICE_DEGRADED` | 503 | 服务降级 | 稍后重试 |

### 错误处理最佳实�?

```python
from uniflow import UniFlowClient, UniFlowError, RateLimitError

client = UniFlowClient(...)

try:
    result = client.workflow.execute(...)
except RateLimitError as e:
    # 等待后重�?
    time.sleep(e.retry_after)
    result = client.workflow.execute(...)
except UniFlowError as e:
    # 记录错误
    logger.error(f"UniFlow error: {e.code} - {e.message}")
    # 根据错误码处�?
    if e.code == "SERVICE_DEGRADED":
        # 使用备用方案
        pass
```

---

## 下一�?

完成基础集成后，您可以：

1. **深入学习**
   - [API 完整文档](05.01.API-UniFlow-API规范.md)
   - [Workflow 定义指南](06.06.Dev-UniFlow-Workflow定义与执�?md)
   - [Skill 开发指南](06.07.Dev-UniFlow-Skill定义与开�?md)

2. **高级功能**
   - [MCP 集成](06.08.Dev-UniFlow-外部MCP与Skill集成指南.md)
   - [自定�?Skill 开发](06.07.Dev-UniFlow-Skill定义与开�?md)
   - [安全配置](05.09.API-UniFlow-安全规范.md)

3. **获取帮助**
   - 查看 [FAQ](07.01.FAQ-UniFlow常见问题.md)
   - 提交 Issue 到项目仓�?
   - 联系技术支�?

---

## 附录：测试用 curl 命令�?

```bash
# 1. 健康检�?
curl http://localhost:8080/health

# 2. Echo 测试
curl -X POST http://localhost:8080/api/v1/skill/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: test-key" \
  -d '{"skill_name": "builtin.echo", "input": {"message": "test"}}'

# 3. 列出 Workflow
curl -H "X-API-Key: test-key" http://localhost:8080/api/v1/workflow/list

# 4. 同步执行 Workflow
curl -X POST http://localhost:8080/api/v1/workflow/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: test-key" \
  -d '{"workflow_id": "simple-qa", "input": {"question": "test"}, "options": {"sync": true}}'

# 5. 异步执行 Workflow
curl -X POST http://localhost:8080/api/v1/workflow/execute \
  -H "Content-Type: application/json" \
  -H "X-API-Key: test-key" \
  -d '{"workflow_id": "simple-qa", "input": {"question": "test"}, "options": {"sync": false}}'

# 6. 查询状�?
curl -H "X-API-Key: test-key" http://localhost:8080/api/v1/workflow/status/{execution_id}

# 7. 获取结果
curl -H "X-API-Key: test-key" http://localhost:8080/api/v1/workflow/result/{execution_id}
```
