# UniFlow 架构设计

> **版本**: 1.0 | **更新日期**: 2025-12-03
> 本文档定�?UniFlow 的技术架构，开发时应结合《Agent系统软件设计完整指导手册》�?

---

## 1. 概述

### 1.1 架构定位

UniFlow �?*智能应用的操作系�?*，提供：
- 11 个标准化角色的运行时
- Workflow 流程引擎
- Skill 扩展机制
- 安全与信任边�?
- 多层记忆体系

### 1.2 开发方法论

UniFlow 开发应遵循 `00.ai智能体架构规�?025-12-03.md`（Agent系统软件设计完整指导手册）中的：
- **设计模式**：Reflection、Tool Use、Planning、Multi-Agent、Evaluator-Optimizer
- **Prompt工程**：XML 标签隔离、思维链、Few-Shot 示例
- **评估体系**：单元测试、模型评估�?*黄金数据�?(Golden Dataset)** 回归测试
- **降级策略**：自动重试、输出修复、传统方法兜�?

---

## 2. 系统架构

### 2.1 整体分层

```
┌─────────────────────────────────────────────────────────────────�?
�?                       业务应用�?                              �?
�?            DevDirector / 链影工厂 / 其他应用                    �?
├─────────────────────────────────────────────────────────────────�?
�?                  UniFlow 前端组件                              �?
�?┌─────────────────────────�? ┌─────────────────────────�?       �?
�?�?     SoIs 如是          �? �?    SayDone 述成        �?       �?
�?�?   意图澄清组件         �? �?  NL→结构化组件         �?       �?
�?└─────────────────────────�? └─────────────────────────�?       �?
├─────────────────────────────────────────────────────────────────�?
�?                  UniFlow.Core                                  �?
�?┌─────────────────────────────────────────────────────────�?    �?
�?�?                   角色运行�?                          �?    �?
�?�?L0: 引擎(Engine) + 督察(Inspector)                      �?    �?
�?�?L4: 总指�?Commander) + 调度�?Dispatcher)              �?    �?
�?�?L3: 智囊(Advisor)                                       �?    �?
�?�?L2: 执行�?Executor) + 守卫(Guard) + 军需�?Quartermaster)    �?
�?�?L1: 后勤(Logistics) + 记录�?Chronicler) + 通信�?Signal)     �?
�?└─────────────────────────────────────────────────────────�?    �?
�?┌──────────────�?┌──────────────�?┌──────────────�?             �?
�?│Workflow 引擎 �?�?Skill 框架   �?�? 安全机制    �?             �?
�?└──────────────�?└──────────────�?└──────────────�?             �?
├─────────────────────────────────────────────────────────────────�?
�?                       DeepBase                                  �?
�?         Config · Logging · DB · i18n · Security                �?
└─────────────────────────────────────────────────────────────────�?
```

### 2.2 目录结构

```
DeepBase/
├── uniFlow/                    # UniFlow 源码
�?  ├── Core/                   # 核心引擎
�?  �?  ├── Roles/              # 11 角色实现
�?  �?  ├── Workflow/           # 流程引擎
�?  �?  ├── Skill/              # Skill 框架
�?  �?  ├── Security/           # 安全机制
�?  �?  └── Memory/             # 记忆系统
�?  ├── SoIs/                   # 如是组件
�?  �?  ├── Clarifier/          # 澄清�?
�?  �?  ├── Sketcher/           # 草图�?
�?  �?  └── ...
�?  └── SayDone/                # 述成组件
�?      ├── Parser/             # NL 解析
�?      ├── Validator/          # 抗幻觉验�?
�?      └── ...
├── docs/uniFlow/               # UniFlow 文档
�?  ├── 01.*.Product-*.md       # 产品文档
�?  ├── 03.*.Arch-*.md          # 架构文档
�?  ├── 06.*.Dev-*.md           # 开发文�?
�?  ├── SoIs/                   # 如是文档
�?  └── SayDone/                # 述成文档
└── Core/                       # DeepBase 核心
```

---

## 3. 11 角色架构

### 3.1 角色层级

| 层级 | 角色 | 职责 | 信任级别 |
|------|------|------|----------|
| L0 元层 | 引擎(Engine) | 系统生命周期管理 | 完全信任 |
| L0 元层 | 督察(Inspector) | 调试与可观测�?| 完全信任 |
| L4 决策�?| 总指�?Commander) | 任务分解与分�?| 有限信任 |
| L4 决策�?| 调度�?Dispatcher) | 流程编排与调�?| 有限信任 |
| L3 智能�?| 智囊(Advisor) | AI 推理与建�?| 不信�?|
| L2 能力�?| 执行�?Executor) | 执行具体任务 | 不信�?|
| L2 能力�?| 守卫(Guard) | 安全校验 | 有限信任 |
| L2 能力�?| 军需�?Quartermaster) | 资源与配额管�?| 有限信任 |
| L1 基础�?| 后勤(Logistics) | 资源管理 | 完全信任 |
| L1 基础�?| 记录�?Chronicler) | 日志与审�?| 完全信任 |
| L1 基础�?| 通信�?SignalOfficer) | 外部通信 | 有限信任 |

### 3.2 角色交互原则

1. **单向依赖**：高层可调用低层，低层不能调用高�?
2. **接口隔离**：角色间通过接口通信，不直接访问内部状�?
3. **输出校验**：不信任区角色的输出必须经过守卫校验
4. **消息驱动**：角色间通过标准消息协议通信

### 3.3 角色接口定义

详见 `05.01.API-UniFlow接口规范-v1.0.md`

---

## 4. Workflow 引擎

### 4.1 任务类型

| 类型 | 说明 | 示例 |
|------|------|------|
| SystemTask | 系统内置任务 | 变量赋值、条件判�?|
| SkillTask | 调用 Skill | HTTP 请求、数据库查询 |
| AgentTask | AI 推理任务 | 内容生成、意图理�?|
| HumanTask | 人工介入任务 | 审批、确�?|

### 4.2 流程结构

支持的流程结构：
- **顺序执行**：A �?B �?C
- **条件分支**：if/else、switch
- **并行执行**：fork/join
- **循环**：while、for-each
- **子流�?*：嵌�?Workflow

### 4.3 状态机

```
┌─────�?  创建     ┌─────────�?
�?NEW �?─────────> �?PENDING �?
└─────�?           └────┬────�?
                        �?开始执�?
                        �?
                   ┌─────────�?
            ┌───── �?RUNNING �?─────�?
            �?     └────┬────�?     �?
            �?暂停      �?完成      �?失败
            �?          �?          �?
       ┌────────┐┌──────────┐┌────────�?
       �?PAUSED ││ COMPLETED││ FAILED �?
       └────────┘└──────────┘└────────�?
```

### 4.4 快速通道 (Fast Path)

针对低风险、无状态的简�?Skill 调用，架构支�?*快速通道**模式�?

**触发条件（必须同时满足）**�?
| 条件 | 阈�?规则 | 说明 |
|------|----------|------|
| Skill 信任级别 | `Core` �?`Official` | 排除 Community/User 级别 |
| Skill 副作用类�?| 在白名单�?| 见下�?|
| 预估执行时长 | < 100ms | 基于历史统计 |
| 输入大小 | < 4KB | 避免大数据校验开销 |
| 当前系统负载 | < 80% | 高负载时禁用快速通道 |

**副作用白名单**�?
| 允许 | 禁止 |
|------|------|
| 纯计算（无IO�?| 数据库写�?|
| 内存缓存读取 | 文件写入 |
| 本地配置读取 | 外部 API 调用 |
| 日志写入（低级别�?| 发送通知/消息 |

**跳过的步�?*�?
- Commander 任务分解（直接使用原始请求）
- Guard 入口深度校验（仅做格式校验）
- 完整审计日志（仅记录摘要�?

**保留的步�?*�?
- Guard 出口校验（必须）
- 基础权限检查（必须�?
- 错误处理与降级（必须�?

---

### 4.5 Saga 补偿机制

对于包含多个有副作用步骤�?Workflow，采�?Saga 模式保障最终一致性：

```
┌─────────────────────────────────────────────────────────────────�?
�?                   Saga 执行流程                                �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�? 正向执行：Step1 ─�?Step2 ─�?Step3 ─�?Step4 ─�?完成            �?
�?              �?       �?       �?       �?                     �?
�?              �?       �?       �?       �?                     �?
�?           [记录]   [记录]   [记录]   [记录]                    �?
�?                                                                �?
�? 补偿执行（Step3 失败时）�?                                     �?
�?              Step2.Compensate ◄─ Step1.Compensate              �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

**Skill 补偿接口**�?
```pascal
ICompensableSkill = interface(ISkill)
  // 检查是否支持补�?
  function CanCompensate: Boolean;
  // 执行补偿操作
  function Compensate(const AOriginalInput: TSkillInput;
    const AOriginalOutput: TSkillOutput;
    const AReason: string): TSkillOutput;
  // 获取补偿超时时间
  function GetCompensateTimeout: Integer;
end;
```

**补偿策略配置**�?
```json
{
  "sagaConfig": {
    "enabled": true,
    "compensateOrder": "reverse",
    "maxCompensateRetries": 3,
    "compensateTimeout": 30000,
    "onCompensateFail": "alert_and_manual"
  }
}
```

**补偿失败处理**�?
| 策略 | 说明 |
|------|------|
| `retry` | 自动重试（最�?N 次） |
| `alert_and_manual` | 告警并等待人工介�?|
| `force_complete` | 标记为补偿失败但继续（需审计�?|
| `dead_letter` | 进入死信队列等待处理 |

---

## 5. Skill 框架

### 5.1 Skill 定义

```pascal
type
  ISkill = interface
    function GetMetadata: TSkillMetadata;
    function Execute(const AInput: TSkillInput): TSkillOutput;
    function Validate(const AInput: TSkillInput): TValidationResult;
    // 架构优化：增加回滚机�?
    function Rollback(const AInput: TSkillInput; const AReason: string): TSkillOutput;
  end;

  TSkillMetadata = record
    Name: string;
    Version: string;
    Description: string;
    InputSchema: TJSONSchema;
    OutputSchema: TJSONSchema;
    TrustLevel: TTrustLevel;  // Core/Official/Community/User
    Permissions: TPermissions;
  end;
```

### 5.2 Skill 信任层级

| 层级 | 来源 | 信任�?| 执行环境 |
|------|------|--------|----------|
| Core | 内置 | 完全信任 | 直接执行 |
| Official | 官方认证 | 有限信任 | 权限约束 |
| Community | 社区贡献 | 低信�?| 沙箱隔离 |
| User | 用户自建 | 不信�?| 严格沙箱 |

### 5.3 Core Skills（MVP�?

1. **SkillHTTP** - HTTP 调用
2. **SkillDatabase** - 数据库读写（基于 DeepBase�?
3. **SkillFile** - 文件操作
4. **SkillLLM** - LLM 调用封装
5. **SkillTimer** - 定时触发
6. **SkillHuman** - 人工任务

---

## 6. 服务治理

### 6.1 熔断降级机制

**熔断器状态机**�?
```
                    连续失败 N �?
        ┌────────────────────────────�?
        �?                           �?
    ┌───────�?                  ┌────────�?
    �?CLOSED �?                  �? OPEN  �?
    �?正常   �?                  �?熔断�?�?
    └───┬───�?                  └────┬───�?
        �?                           �?
        �?   探测成功                �?超时�?
        �? ┌──────────�?             �?
        └──┤HALF_OPEN ├◄─────────────�?
           �?半开探测  �?
           └──────────�?
```

**熔断器配�?*�?
```pascal
TCircuitBreakerConfig = record
  FailureThreshold: Integer;     // 触发熔断的连续失败次数，默认 5
  SuccessThreshold: Integer;     // 恢复所需连续成功次数，默�?3
  OpenTimeout: Integer;          // 熔断持续时间(ms)，默�?30000
  HalfOpenMaxCalls: Integer;     // 半开状态最大探测调用数，默�?3
  MonitorWindow: Integer;        // 监控窗口时长(ms)，默�?60000
end;
```

**熔断适用角色**�?
| 角色 | 熔断对象 | 降级策略 |
|------|----------|----------|
| 通信�?| 外部 HTTP/MCP 服务 | 返回缓存结果或默认�?|
| 智囊 | LLM API | 返回低置信度默认响应 |
| 执行�?| Python Skill 服务 | 使用 Delphi 备用实现 |

### 6.2 负载控制

**背压机制**�?
```pascal
TBackpressureConfig = record
  MaxPendingRequests: Integer;   // 最大排队请求数，默�?1000
  MaxConcurrentTasks: Integer;   // 最大并发任务数，默�?100
  RejectPolicy: TRejectPolicy;   // 拒绝策略：drop/queue/error
  QueueTimeout: Integer;         // 队列等待超时(ms)，默�?30000
end;
```

**限流规则**�?
| 维度 | 限制 | 说明 |
|------|------|------|
| 全局 QPS | 1000/s | 系统整体吞吐上限 |
| 单用�?QPS | 100/s | 防止单用户占用过多资�?|
| LLM 调用 | 10/s | API 成本控制 |
| �?Workflow 并发步骤 | 10 | 防止 fork 爆炸 |

---

## 7. 安全机制

### 7.1 三重守卫

```
请求 ──> [门卫] ──> 处理 ──> [监工] ──> 输出 ──> [审核官] ──> 响应
         入口校验      过程监控           出口校验
```

### 7.2 信任边界

|| 区域 | 角色 | 约束 |
||------|------|------|
|| 完全信任�?| 引擎/督察/后勤/记录�?| 无额外校�?|
|| 有限信任�?| 总指�?调度�?守卫/军需�?通信�?| 权限检�?|
|| 不信任区 | 智囊/执行�?| 输出必须校验 |

### 7.3 安全原则

1. **硬执行优于软执行** - 安全规则由架构强制，不依�?Prompt
2. **最小权�?* - 每个角色只拥有完成任务所需的最小权�?
3. **纵深防御** - 多层校验，单点失败不导致安全漏洞
4. **可审�?* - 所有操作记录在案，可追�?

---

## 8. 记忆系统

### 8.1 记忆层级

| 层级 | 生命周期 | 存储 | 用�?|
|------|----------|------|------|
| 工作记忆 | 单次请求 | 内存 | 当前任务上下�?|
| 会话记忆 | 单次会话 | 内存/Redis | 对话历史 |
| 持久记忆 | 长期 | 数据�?| 用户偏好、历史决�?|
| 知识�?| 永久 | 向量�?| 领域知识、最佳实�?|

### 8.2 记忆管理与上下文优化

- **记录�?*负责写入记忆�?
- **后勤**负责读取记忆，并执行**上下文压缩策�?*�?
  - 针对不同角色（如 Guard vs Advisor）提供不同切片的 Context�?
  - 对长历史会话进行摘要（Summarization）处理，防止 Token 爆炸�?
- **智囊**可查询知识库�?

---

## 9. 前端组件

### 9.1 SoIs（如是）

**定位**：意图澄清组�?

**核心能力**�?
- 多轮对话澄清用户意图
- 生成可视化草图供用户确认
- 定位并修改已有结�?

**专属角色**�?
- 澄清�?Clarifier) - 意图澄清
- 草图�?Sketcher) - 可视化预�?
- 定位�?Locator) - 节点定位
- 方案�?Proposer) - 方案推荐
- 默认�?Defaulter) - 智能默认�?
- 进度�?Tracker) - 进度追踪

**详细设计**：见 `SoIs/README.md`

### 9.2 SayDone（述成）

**定位**：自然语言转结构化数据引擎

**核心能力**�?
- 多场景支持（表单/列表/详情/导航/布局�?
- 四级语义缓存
- 抗幻觉验证链
- 智能模型路由

**详细设计**：见 `SayDone/README.md`

---

## 10. 技术选型

### 10.1 技术栈总览

| 层级 | 组件 | 技�?| 说明 |
|------|------|------|------|
| 前端 | UI | Delphi + FMX | 原生桌面应用 |
| 核心 | UniFlow Engine | Delphi | 11 角色运行�?|
| 基础 | DeepBase | Delphi | 70+ 单元复用 |
| AI | Python Skill | FastAPI | HTTP API 调用 |
| AI | LLM 调用 | LiteLLM | 多模型支�?|
| 存储 | 本地 | SQLite | 隐私优先 |
| 存储 | 云端 | PostgreSQL | 1.0+ 可选同�?|

### 10.2 Python Skill 服务架构

```
┌─────────────────────────────────────────────────────────�?
�?                Delphi 应用                             �?
�?┌─────────────────────────────────────────────────�?    �?
�?�?            UniFlow Core                        �?    �?
�?�?�?角色运行�?                                   �?    �?
�?�?�?Workflow 引擎                                 �?    �?
�?�?�?Skill 调度                                    �?    �?
�?└────────────────────┬────────────────────────────�?    �?
�?                     �?HTTP POST (Keep-Alive)           �?
�?└───────────────────────┼─────────────────────────────────�?
�?                       �?
�?                       �?
�?┌─────────────────────────────────────────────────────────�?
�?�?            Python Skill Service                        �?
�?�?┌─────────────────────────────────────────────────�?    �?
�?�?�?            FastAPI Server (Uvicorn)            �?    �?
�?�?�?�?/skills/{name}/execute                        �?    �?
�?�?�?�?/skills/{name}/execute/stream (SSE)           �?    �?
�?�?�?�?热重载支�?                                   �?    �?
�?�?└────────────────────┬────────────────────────────�?    �?
�?�?                     �?                                 �?
�?�?┌────────────────────▼────────────────────────────�?    �?
�?�?�?            LiteLLM                             �?    �?
�?�?�?�?OpenRouter 多模�?                            �?    �?
�?�?�?�?流式输出                                      �?    �?
�?�?�?�?统一 API                                      �?    �?
�?�?└─────────────────────────────────────────────────�?    �?
�?└─────────────────────────────────────────────────────────�?
```

### 10.3 性能与通信优化 (IPC)

鉴于 Delphi �?Python 之间�?HTTP 通讯可能产生延迟，架构采用以下策略优化：

1.  **长连�?(Keep-Alive)**: 复用 HTTP 连接，避免每次请求握手开销�?
2.  **Payload 压缩**: 对于超过 4KB 的报文，启用 GZIP 压缩�?
3.  **本地回环优化**: 明确绑定 `127.0.0.1`，避�?DNS 解析�?
4.  **Fast Path (未来)**:
    - 对于高频、低计算量的 Skill (如正则匹�?，在 Delphi 侧原生实现�?
    - 考虑集成 `Python4Delphi` (P4D) 直接调用 DLL，或使用 ZeroMQ/共享内存替代 HTTP�?

### 10.4 Python 服务生命周期管理

为确保桌面环境下的稳定性，UniFlow Core (Delphi) 负责 Python 服务的全生命周期�?
- **内嵌运行�?*：集�?Embeddable Python，无需用户手动安装环境�?
- **自动启动/守护**：Engine 启动时自动拉�?Python 进程，SignalOfficer 负责心跳检测�?
- **自动重启**：检测到服务崩溃时自动重启，并恢复上下文�?

### 9.4 数据存储策略

**本地优先原则**�?
- 每个应用独立 SQLite 数据�?
- 离线可用，保护用户隐�?
- 决策数据默认不上�?

**可选云同步�?.0+�?*�?
- 用户主动选择同步内容
- PostgreSQL 存储同步数据
- 用户账号、Skill 市场元数�?

### 9.5 DeepBase 复用

| DeepBase 组件 | UniFlow 用�?|
|--------------|-------------|
| DeepBase.LLM + LLM.Manager | Advisor 角色 LLM 调用 |
| DeepBase.StateMachine | Workflow 任务状�?|
| DeepBase.EventBus | 角色间消息通信 |
| DeepBase.WorkerQueue | 异步任务执行 |
| DeepBase.Scheduler | Timer Skill |
| DeepBase.Config | 配置管理 |
| DeepBase.Logging | 日志系统 |

### 9.6 遵循的开发约�?

参�?`D:\_Progs\05docs\delphi程序开发约定指�?md`�?
- 文件命名：前缀 + 功能描述（如 `CtrlWorkflow.pas`、`ModelSkill.pas`�?
- 零目录结构：代码文件放项目根目录，通过前缀区分层级
- 四层隔离：View / Ctrl / Model / uDM
- UTF-8 + BOM 编码

---

## 10. 相关文档

### 10.1 产品与规�?

| 文档 | 说明 |
|------|------|
| `01.01.Product-UniFlow三层演进规划-v1.0.md` | 产品演进规划 |
| `00.ai智能体架构规�?025-12-03.md` | Agent 系统设计指导手册 |

### 10.2 开发规�?

| 文档 | 说明 |
|------|------|
| `06.01.Dev-UniFlow开发总览-v1.0.md` | 开发总览 |
| `06.03.Dev-UniFlow-MVP开发规�?v1.0.md` | MVP 开发规�?|
| `06.05.Dev-UniFlow-1.0开发规�?v1.0.md` | 1.0 开发规�?|
| `06.07.Dev-UniFlow-理想版本规范-v1.0.md` | 理想版本规范 |

### 10.3 数据与接�?

| 文档 | 说明 |
|------|------|
| `04.01.Data-UniFlow数据模型-v1.0.md` | 数据库设�?|
| `05.01.API-UniFlow接口规范-v1.0.md` | 接口定义 |

### 10.4 组件设计

| 文档 | 说明 |
|------|------|
| `SoIs/README.md` | 如是组件设计 |
| `SayDone/README.md` | 述成组件设计 |

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作�?|
|------|------|----------|------|
| 1.0 | 2025-12-03 | 初始版本�?1 角色架构、Workflow 引擎、Skill 框架 | - |
| 1.1 | 2025-12-04 | 修复章节编号；确�?Fast Path 条件�?Saga 机制已存�?| - |
