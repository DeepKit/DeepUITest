# UniFlow 部署架构

> 定义 UniFlow 的部署模式、角色实例化策略、消息总线选型与配置示例�?

创建日期: 2025-12-04  版本: 1.0

---

## 1. 部署模式

### 1.1 模式概览

| 模式 | 适用场景 | 角色部署 | 消息传�?|
|------|----------|----------|----------|
| **单机模式** | 开�?测试/小型应用 | 单进程内 | 内存队列 |
| **进程分离模式** | 中型应用 | 多进�?| 本地 IPC |
| **分布式模�?* | 大型/高可用应�?| 多节�?| 消息中间�?|

### 1.2 单机模式

```
┌─────────────────────────────────────────────────────────────────�?
�?                       单进�?                                   �?
�? ┌─────────────────────────────────────────────────────────────┐│
�? �?                    UniFlow Engine                          ││
�? �? ┌─────────�?┌─────────�?┌─────────�?┌─────────�?         ││
�? �? �?Commander�?│Dispatcher�?�?Advisor �?�?Executor�?         ││
�? �? └────┬────�?└────┬────�?└────┬────�?└────┬────�?         ││
�? �?      �?          �?          �?          �?                ││
�? �?      └───────────┴───────────┴───────────�?                ││
�? �?                         �?                                  ││
�? �?                 ┌───────┴───────�?                         ││
�? �?                 �? MemoryBus    �?                         ││
�? �?                 └───────────────�?                         ││
�? └─────────────────────────────────────────────────────────────┘│
�?                                                                 �?
�? 数据存储: SQLite / 内存                                         �?
└─────────────────────────────────────────────────────────────────�?
```

**特点**�?
- 所有角色在同一进程内运�?
- 消息通过内存队列传递（零网络开销�?
- 适合开发调试和小型部署
- 无高可用，单点故�?

**配置示例**�?
```yaml
# uniflow-standalone.yaml
deployment:
  mode: standalone
  
engine:
  messageQueue:
    type: memory
    maxSize: 10000
    
storage:
  type: sqlite
  path: ./data/uniflow.db
  
roles:
  all:
    instances: 1
```

### 1.3 进程分离模式

```
┌─────────────────────────────────────────────────────────────────�?
�?                       单机多进�?                               �?
�?                                                                 �?
�? ┌──────────────────�? ┌──────────────────�?                   �?
�? �? 主进�?          �? �? AI 进程          �?                   �?
�? �? ┌────────────�? �? �? ┌────────────�? �?                   �?
�? �? �?  Engine   �? �? �? �? Advisor   �? �?                   �?
�? �? �?Commander  �? �? �? �?(多实�?   �? �?                   �?
�? �? �?Dispatcher �? �? �? └────────────�? �?                   �?
�? �? �?  Guard    �? �? └────────┬─────────�?                   �?
�? �? └─────┬──────�? �?          �?                             �?
�? └────────┼─────────�?          �?                             �?
�?          �?                    �?                             �?
�?          └──────────┬──────────�?                             �?
�?                     �?                                         �?
�?             ┌───────┴───────�?                                 �?
�?             �?  Redis/IPC   �?                                 �?
�?             └───────────────�?                                 �?
�?                                                                 �?
�? 数据存储: PostgreSQL / Redis                                    �?
└─────────────────────────────────────────────────────────────────�?
```

**特点**�?
- 计算密集型角色（Advisor）独立进�?
- 进程间通过 Redis �?Unix Socket 通信
- 可独立扩�?AI 进程
- 主进程故障仍会导致服务不可用

**配置示例**�?
```yaml
# uniflow-multiprocess.yaml
deployment:
  mode: multiprocess
  
engine:
  messageQueue:
    type: redis
    host: localhost
    port: 6379
    
processes:
  main:
    roles: [engine, commander, dispatcher, guard, chronicler, logistics]
    
  ai:
    roles: [advisor]
    instances: 4
    scaling:
      min: 2
      max: 8
      metric: queue_depth
      threshold: 100
      
  executor:
    roles: [executor, signalOfficer]
    instances: 2
```

### 1.4 分布式模�?

```
┌─────────────────────────────────────────────────────────────────�?
�?                       分布式集�?                               �?
�?                                                                 �?
�? ┌──────────────────────────────────────────────────────────�? �?
�? �?                   控制平面节点                            �? �?
�? �? ┌─────────�? ┌─────────�? ┌─────────�?                  �? �?
�? �? │Engine-1 �? │Engine-2 �? │Engine-3 �? (主备)           �? �?
�? �? │Commander�? │Commander�? │Commander�?                  �? �?
�? �? │Dispatch �? │Dispatch �? │Dispatch �?                  �? �?
�? �? └─────────�? └─────────�? └─────────�?                  �? �?
�? └──────────────────────────────────────────────────────────�? �?
�?                             �?                                 �?
�?                     ┌───────┴───────�?                        �?
�?                     �?   Kafka      �?                        �?
�?                     �? Message Bus  �?                        �?
�?                     └───────┬───────�?                        �?
�?                             �?                                 �?
�? ┌──────────────────────────────────────────────────────────�? �?
�? �?                   数据平面节点                            �? �?
�? �? ┌─────────�? ┌─────────�? ┌─────────�? ┌─────────�?    �? �?
�? �? │Advisor-1�? │Advisor-2�? │Executor �? │Executor �?    �? �?
�? �? �?        �? �?        �? �?  -1    �? �?  -2    �?    �? �?
�? �? └─────────�? └─────────�? └─────────�? └─────────�?    �? �?
�? └──────────────────────────────────────────────────────────�? �?
�?                                                                 �?
�? 存储: PostgreSQL 集群 + Redis 集群 + 向量数据�?                 �?
└─────────────────────────────────────────────────────────────────�?
```

**特点**�?
- 角色按功能分组部署到不同节点
- 消息通过 Kafka/RabbitMQ 传�?
- 支持水平扩展和高可用
- 需要服务发现和负载均衡

---

## 2. 角色实例化策�?

### 2.1 实例化规�?

| 角色 | 单机 | 分布�?| 扩展策略 |
|------|------|--------|----------|
| **引擎** | 1 | 3 (主备) | 无状态，选举主节�?|
| **督察** | 1 | 1-3 | 无状态，可多实例 |
| **总指�?* | 1 | 3 (主备) | 有状态，需选举 |
| **调度�?* | 1 | N | �?Workflow 分片 |
| **智囊** | 1-N | N | 按负载扩�?|
| **执行�?* | 1-N | N | �?Skill 类型分组 |
| **守卫** | 1 | N | 无状态，可多实例 |
| **军械�?* | 1 | 3 (主备) | 有状态，需同步 |
| **后勤** | 1 | N | 无状态，可多实例 |
| **记录�?* | 1 | N | 按分区写�?|
| **通信�?* | 1-N | N | 按外部服务分�?|

### 2.2 智囊扩展配置

```yaml
# 智囊扩展策略
advisor:
  scaling:
    type: horizontal
    
    # 基于队列深度
    rules:
      - metric: message_queue_depth
        threshold: 50
        action: scale_up
        cooldown: 60s
        
      - metric: message_queue_depth
        threshold: 10
        action: scale_down
        cooldown: 300s
    
    # 实例限制
    limits:
      min: 2
      max: 20
      
    # 资源配置
    resources:
      cpu: "2"
      memory: "4Gi"
      gpu: "1"  # 可选，用于本地模型
```

### 2.3 执行者分�?

```yaml
# �?Skill 类型分组执行�?
executor:
  groups:
    # 数据处理�?
    data:
      skills: ["db-query", "data-transform", "file-parser"]
      instances: 3
      resources:
        cpu: "1"
        memory: "2Gi"
        
    # 媒体处理�?
    media:
      skills: ["image-gen", "video-process", "audio-transcribe"]
      instances: 2
      resources:
        cpu: "4"
        memory: "8Gi"
        gpu: "1"
        
    # 外部调用�?
    external:
      skills: ["http-call", "api-*"]
      instances: 5
      resources:
        cpu: "0.5"
        memory: "512Mi"
```

---

## 3. 消息总线选型

### 3.1 选型对比

| 方案 | 吞吐�?| 延迟 | 持久�?| 顺序保证 | 适用场景 |
|------|--------|------|--------|----------|----------|
| **内存队列** | 极高 | <1ms | �?| �?| 单机开�?|
| **Redis Streams** | �?| 1-5ms | 可�?| 分区内是 | 中小规模 |
| **RabbitMQ** | 中高 | 1-10ms | �?| 队列内是 | 中型应用 |
| **Kafka** | 极高 | 5-50ms | �?| 分区内是 | 大规�?审计 |

### 3.2 消息路由配置

```yaml
# 消息路由规则
messageRouting:
  # 直连路由（单实例角色�?
  direct:
    - target: commander
      queue: uniflow.commander
    - target: quartermaster
      queue: uniflow.quartermaster
      
  # 负载均衡路由（多实例角色�?
  loadBalance:
    - target: advisor
      queue: uniflow.advisor
      strategy: round_robin
    - target: executor
      queue: uniflow.executor.{group}
      strategy: least_connections
      
  # 广播路由（事件）
  broadcast:
    - pattern: "*.event.*"
      exchange: uniflow.events
      type: fanout
```

### 3.3 消息分区策略

```yaml
# Kafka 分区配置
kafka:
  topics:
    # 命令消息 - �?correlationId 分区保证顺序
    uniflow.commands:
      partitions: 16
      replication: 3
      partitionKey: correlationId
      
    # 事件消息 - 按角色类型分�?
    uniflow.events:
      partitions: 8
      replication: 3
      partitionKey: sourceRole
      
    # Workflow 状�?- 按实�?ID 分区
    uniflow.workflow.state:
      partitions: 32
      replication: 3
      partitionKey: instanceId
      retention: 7d
```

---

## 4. 存储配置

### 4.1 存储层次

```
┌─────────────────────────────────────────────────────────────────�?
�?                       存储架构                                  �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                 �?
�? 热数据层 (< 1 小时)                                            �?
�? ┌─────────────────────────────────────────────────────────────┐│
�? �? Redis Cluster                                              ││
�? �? �?会话状�?�?工作记忆 �?配置缓存 �?限流计数                 ││
�? └─────────────────────────────────────────────────────────────┘│
�?                             �?                                  �?
�? 温数据层 (1 小时 - 7 �?                                       �?
�? ┌─────────────────────────────────────────────────────────────┐│
�? �? PostgreSQL                                                 ││
�? �? �?Workflow 状�?�?审计日志 �?角色配置                       ││
�? └─────────────────────────────────────────────────────────────┘│
�?                             �?                                  �?
�? 冷数据层 (> 7 �?                                              �?
�? ┌─────────────────────────────────────────────────────────────┐│
�? �? 对象存储 (S3/MinIO)                                        ││
�? �? �?历史日志 �?备份 �?大文�?                                 ││
�? └─────────────────────────────────────────────────────────────┘│
�?                             �?                                  �?
�? 向量数据�?(长期记忆)                                          �?
�? ┌─────────────────────────────────────────────────────────────┐│
�? �? Milvus / Qdrant                                            ││
�? �? �?Few-shot 示例 �?用户偏好 �?知识�?                        ││
�? └─────────────────────────────────────────────────────────────┘│
�?                                                                 �?
└─────────────────────────────────────────────────────────────────�?
```

### 4.2 存储配置示例

```yaml
storage:
  # 热数�?
  redis:
    mode: cluster
    nodes:
      - redis-1:6379
      - redis-2:6379
      - redis-3:6379
    ttl:
      session: 3600        # 1 小时
      working_memory: 1800 # 30 分钟
      config_cache: 300    # 5 分钟
      
  # 温数�?
  postgres:
    host: postgres-primary
    replicas:
      - postgres-replica-1
      - postgres-replica-2
    database: uniflow
    pool:
      min: 10
      max: 100
      
  # 冷数�?
  objectStore:
    type: s3
    bucket: uniflow-archive
    region: us-east-1
    lifecycle:
      transitionDays: 7
      expirationDays: 365
      
  # 向量数据
  vector:
    type: milvus
    host: milvus-proxy
    port: 19530
    collections:
      few_shot: { dimension: 1536, index: IVF_FLAT }
      user_pref: { dimension: 768, index: HNSW }
```

---

## 5. 高可用配�?

### 5.1 故障转移

```yaml
highAvailability:
  # 引擎选举
  engine:
    election:
      type: raft
      heartbeatInterval: 100ms
      electionTimeout: 1s
      
  # 角色健康检�?
  healthCheck:
    interval: 10s
    timeout: 5s
    unhealthyThreshold: 3
    healthyThreshold: 2
    
  # 故障转移
  failover:
    automatic: true
    cooldown: 60s
    maxFailoversPerHour: 3
```

### 5.2 负载均衡

```yaml
loadBalancer:
  # 外部入口
  ingress:
    type: nginx
    replicas: 2
    healthCheck: /health
    
  # 内部服务发现
  serviceDiscovery:
    type: consul
    healthCheck:
      interval: 5s
      deregisterAfter: 30s
      
  # 负载均衡策略
  strategy:
    advisor: round_robin
    executor: least_connections
    signalOfficer: ip_hash  # 会话亲和
```

---

## 6. 部署配置示例

### 6.1 开发环�?

```yaml
# uniflow-dev.yaml
environment: development

deployment:
  mode: standalone
  
engine:
  messageQueue:
    type: memory
    
storage:
  type: sqlite
  path: ./dev.db
  
logging:
  level: debug
  
inspector:
  enabled: true
  allowDebug: true
  allowMock: true
```

### 6.2 生产环境

```yaml
# uniflow-prod.yaml
environment: production

deployment:
  mode: distributed
  
engine:
  replicas: 3
  messageQueue:
    type: kafka
    brokers:
      - kafka-1:9092
      - kafka-2:9092
      - kafka-3:9092
      
storage:
  redis:
    mode: cluster
    nodes: [redis-1:6379, redis-2:6379, redis-3:6379]
  postgres:
    host: postgres-primary
    replicas: [postgres-replica-1, postgres-replica-2]
    
roles:
  advisor:
    instances: 4
    scaling:
      enabled: true
      min: 2
      max: 10
  executor:
    groups:
      default:
        instances: 3
      media:
        instances: 2
        gpu: true
        
monitoring:
  prometheus:
    enabled: true
    port: 9090
  tracing:
    type: jaeger
    endpoint: jaeger-collector:14268
    
security:
  tls:
    enabled: true
    certPath: /certs/server.crt
    keyPath: /certs/server.key
  authentication:
    type: jwt
    issuer: https://auth.example.com
```

---

## 7. 运维命令

### 7.1 健康检�?

```bash
# 检查整体健康状�?
uniflow health

# 检查特定角�?
uniflow health --role advisor

# 检查消息队�?
uniflow health --queue
```

### 7.2 扩缩�?

```bash
# 扩展智囊实例
uniflow scale advisor --replicas 6

# 自动扩展
uniflow autoscale advisor --min 2 --max 10 --cpu 70
```

### 7.3 故障处理

```bash
# 隔离故障角色
uniflow isolate advisor-3

# 强制重启
uniflow restart advisor-3 --force

# 查看故障日志
uniflow logs advisor-3 --since 1h
```

---

## 8. 相关文档

- [UniFlow-接口契约定义.md](./UniFlow-接口契约定义.md) - 角色接口
- [UniFlow-消息协议规范.md](./UniFlow-消息协议规范.md) - 消息格式
- [UniFlow-指标体系.md](./UniFlow-指标体系.md) - 监控指标
- [UniFlow-安全机制.md](./UniFlow-安全机制.md) - 安全配置
