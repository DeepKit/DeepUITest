# 自然语言交互设计原则

> 四仙论道：盘古、灵子、鲁班、仙�?共同探讨

## 1. 核心理念

**用户眼中的便捷高�?= 最少的动作 + 最短的路径 + 最低的心智负担 + 最及时的反�?*

用户不是懒，�?*珍惜注意�?*。每多一次点选、每多一个输入框，都是在消耗用户的耐心资本�?

---

## 2. 交互设计四原�?

### 1. 减少启动成本（灵子）
- **智能默认�?* �?90%用户的选择，直接帮他选好
- **渐进式披�?* �?高级功能藏起来，新手看不到不焦虑
- **记住上次** �?用户的习惯偏好自动延�?

### 2. 减少物理动作（鲁班）
- **手势替代点击** �?滑动删除、长按预览、双击收�?
- **批量操作** �?选中多个一起处�?
- **快捷入口** �?最常用功能放在拇指热区
- **键盘快捷�?* �?专业用户直接按键完成

### 3. 减少认知成本（仙儿）
- **用动词而非名词** �?"保存草稿" 而非 "草稿�?
- **即时预览** �?改了什么立刻能看到效果
- **上下文菜�?* �?在当前位置弹出选项，不跳页�?
- **视觉层级** �?主操作大而亮，次要操作小而淡

### 4. 减少时间感知（盘古）
- **乐观更新** �?点了就当成功，后台静默同�?
- **骨架�?* �?先出框架，内容慢慢填�?
- **预加�?* �?用户还没点，内容已经在路�?
- **进度反馈** �?告诉用户"还要多久"

---

## 3. 自然语言到显影的设计要点

### 3.1 输入�?�?让用户敢说、会说、愿意说

| 设计�?| 说明 |
|--------|------|
| 提示词示�?| "试试说：帮我做一个蓝色的登录页面" |
| 历史记录 | "您上次说的是..." |
| 场景引导 | "您想�?[页面/组件/表单] �? |
| 容错解析 | "弄个按钮" = "加一个button" |
| 上下文记�?| "把它改成红色"，系统要知道"�?是什�?|
| 多模态输�?| 支持贴图、语音、拖拽示�?|

### 3.2 等待�?�?流式显影

```
�?秒：[    空画�?   ]
�?秒：[  �?布局骨架  ]  �?用户看到"有东西了"
�?秒：[ 🟦 色块填充  ]  �?用户看到"方向对了"
�?秒：[ 📝 内容渲染  ]  �?用户看到"成了�?
```

关键�?*复述确认** �?**进度显影** �?**渐进渲染**

### 3.3 结果呈现 �?看得懂、改得动

- **对照�?* �?左边用户的话，右边生成的结果
- **可调�?* �?不满意直接点击修�?
- **追问�?* �?"还想调整哪里�?

### 3.4 迭代循环 �?越用越准

```
用户�?�?系统�?�?用户�?�?系统�?
    ↑__________________________|
```

- **偏好学习** �?记住用户喜好，下次默认使�?
- **纠错记忆** �?改过的错误不再犯
- **快捷复用** �?"和上次类�? 能调用历�?

---

## 4. 从模糊愿景到需求数�?

当用户表述宏观模糊时（如"我要做一个同步盘软件"），如何快速转化为需求数据？

### 核心策略：先猜后确认

**用户看图�?对不�?，比从头描述�?0倍�?*

```
�?差体验：
用户�?我要做一个同步盘软件"
系统�?请问您需要哪些功能？"  �?用户懵了

�?好体验：
用户�?我要做一个同步盘软件"
系统：[立刻展示功能框架草图]
      "这是常见功能，您看哪些需要，哪些不要�?
```

### 实现四步�?

**第一步：领域识别（秒级）**
- 关键词匹配领域模板："同步�? �?文件同步工具
- 预置常见产品类型的功能框�?

**第二步：功能推荐（做减法�?*
- 展示该领域的标准功能清单
- 核心功能默认勾选，可选功能让用户决定
- 用户删比用户填更自然

**第三步：快速显影（边选边看）**
- 左侧功能选择，右侧实时预�?
- 每个选择立刻反映到预览中

**第四步：关键追问（只问必要的�?*
- 一次只问一个问�?
- 给选项而非开放式
- 解释为什么问（让用户知道影响什么）

### 需求数据的渐进式填�?

```
第一层：领域识别（秒级完成）�?命中模板
第二层：功能选择（用户勾选）�?确定范围
第三层：用户画像（追问得来）�?明确约束
第四层：详细需求（后续补充）→ 细化设计
```

---

## 5. 针对氛围编程的质量保�?

### 氛围编程的弱�?

"氛围编程"（Vibe Coding）指用户用自然语言描述需求，AI直接生成代码的模式。其核心弱点�?

| 弱点 | 表现 | 后果 |
|------|------|------|
| **需求模�?* | 用户说得不完整、有歧义 | 生成的代码偏离意�?|
| **上下文丢�?* | 多轮对话后AI忘记前文 | 代码前后矛盾 |
| **架构缺失** | 只关注当前功能，不考虑整体 | 技术债堆�?|
| **质量不可�?* | 生成即采用，缺少审查 | 隐藏bug、安全漏�?|
| **不可追溯** | 不知道代码为什么这样写 | 维护困难 |

### 我们的设计目�?

**在保�?自然表达"体验的同时，引入质量保障机制，但不让用户感到繁琐�?*

### 质量保障策略

#### 1. 需求澄清层 �?在生成前确保理解正确

- **意图复述**：生成前先说"我理解您想要..."，让用户确认
- **歧义检�?*：发现模糊表述时，给出选项让用户明�?
- **边界确认**：明�?会做什�?�?不会做什�?

#### 2. 架构守护�?�?保证整体一致�?

- **设计规范约束**：生成的代码必须符合项目既有规范
- **依赖关系检�?*：新功能与现有模块的关系要明�?
- **架构决策记录**：重要决策要记录原因，便于追�?

#### 3. 生成审查�?�?不直接采用，先过�?

- **分步显影**：先展示设计方案，确认后再生成代�?
- **差异对比**：修改现有代码时，清晰展示改动点
- **风险提示**：涉及安全、性能、兼容性问题时主动提醒

#### 4. 可追溯层 �?每行代码有来�?

- **需求关�?*：代码与原始需求描述绑�?
- **决策记录**：为什么选择这个方案而非其他
- **变更历史**：谁在什么时候因为什么改了这段代�?

#### 5. 验证闭环�?�?生成后验证正确�?

- **自动测试**：关键功能自动生成测试用�?
- **运行验证**：生成后立即运行，确保无语法/运行错误
- **用户确认**：展示运行结果，让用户判断是否符合预�?

### 体验与质量的平衡

| 场景 | 体验优先 | 质量优先 | 我们的选择 |
|------|----------|----------|------------|
| 简单修�?| 直接�?| 先确认再�?| 直接改，但可撤销 |
| 新增功能 | 直接生成 | 先设计再生成 | 展示设计草图，快速确�?|
| 架构调整 | 一步到�?| 分步验证 | 必须分步，但每步要快 |
| 涉及安全 | 跳过检�?| 强制审查 | 强制审查，但简化流�?|

### 核心原则

**灵子**�?
> 质量保障�?*无感嵌入**，不是额外的步骤，而是自然流程的一部分

**鲁班**�?
> 宁可**多生成一层中间产�?*（设计稿），也不要直接生成最终代码后返工

**仙儿**�?
> 用户不需要懂工程规范，但系统�?*替他守住底线**

**盘古**�?
> 氛围编程的问题不�?太快"，而是"快得没有章法"。我们要做的�?*有章法的�?*�?

---

## 6. 实现架构：三层转换引�?

### 核心思路

**用户说人话，系统在后台悄悄翻译成结构化数据�?*

```
用户看到�?                    系统在做�?
─────────────────────────────────────────────────
"做个同步�?                   �?识别领域、加载模板、推断功�?
[取消勾选团队协作]             �?记录变更、更新架构约�?
"要能在手机上�?                �?识别平台约束、影响技术选型
```

### 第一层：语义捕获�?

每一句话、每一次点击，都提取结构化信息�?

- **输入类型**：文�?语音/点击/手势
- **解析结果**：动�?create/modify/delete) + 目标 + 属�?
- **置信�?*：系统对理解的确定程�?
- **歧义检�?*：标记不确定的点

### 第二层：需求累积层

不是每次从头来，而是**增量累积**�?

- 维护当前需求状�?
- 每次输入**补充和修�?*现有数据
- 检查完整性：哪些就绪、哪些缺失、哪些可�?
- 决定是否需要追�?

### 第三层：显影触发�?

什么时候该展示结果给用户：

- 累积到足够信息时
- 用户明确想看结果�?
- 检测到用户可能困惑�?
- 用户静默超时�?

### 追问策略

| 信息类型 | 影响程度 | 处理方式 |
|----------|----------|----------|
| 核心功能 | �?| 必须追问 |
| 技术选型 | �?| 给默认�?提示 |
| 样式偏好 | �?| 用智能默�?|
| 边缘功能 | �?| 先不管，后面再说 |

追问形式�?
- �?选择式："同步方式您倾向哪种？[实时] [定时] [手动]"
- �?确认式："我准备按「实时同�?双端」来设计，可以吗�?
- �?开放式�?请问您对文件同步的具体需求是什么？"

### 质量检查无感嵌�?

质量检查融入显影过程，用户不感知：

```
用户说："加个登录功能"
     �?
系统内部检查（用户无感）：
  �?是否已有用户系统�?
  �?认证方式�?
  �?安全要求�?
  �?与现有架构冲突？
     �?
展示给用户："好的，这需要先建立用户系统，这样设计可以吗�?
```

### 层次总览

| 层次 | 用户感知 | 系统实际在做 |
|------|----------|----------------|
| 输入�?| 随便�?| 结构化解�?|
| 累积�?| 越聊越懂�?| 增量构建需求数�?|
| 显影�?| 想法变成�?| 按需触发渲染 |
| 质量�?| 没感�?| 静默检�?智能兆底 |

---

## 7. 氛围编程六大核心问题与根治方�?

### 问题一：需求在聊天中蒸�?

**现象**：用户说�?0句话，AI只记住最后一句�?

**根因**：自然语言对话 �?需求文档。聊天是发散的，需求是收敛的�?

**根治方案：需求状态机**
- 不存对话历史，存**结构化的需求状�?*
- 项目级：整体在做什�?
- 功能级：功能清单是什么（树状结构�?
- 焦点级：当前在讨�?修改什�?
- 每次输入更新状态，而非追加对话

### 问题二：没有设计直接编码

**现象**：需�?�?？？�?�?代码出来了（设计在哪里？�?

**根因**：跳过设计阶段，不知道架构、模块关系、数据流动�?

**根治方案：强制设计层**
- 在生成代码前，必须经过设计层
- 自动生成设计稿：功能边界、数据结构、接口定义、依赖关�?
- 用户只需确认，不需要自己画
- 确认后再生成代码

### 问题三：代码生成后无法追�?

**现象**：一个月后看代码，不知道为什么这样写、能不能改、改了影响什么�?

**根因**：代码与需求脱钩�?

**根治方案：代�?需求双向绑�?*
- 每段代码带溯源元信息
- 记录：来自哪个需求、哪个设计决策、为什么这样写
- 记录：依赖哪些代码、被哪些代码依赖

### 问题四：改了A坏了B

**现象**：用户改个按钮颜色，结果登录功能挂了�?

**根因**：没有影响范围分析�?

**根治方案：变更影响分析系�?*
- 执行变更前，先分析影�?
- 展示：直接影响的文件/元素、间接影响的功能、风险评�?
- 提供回滚方案

### 问题五：技术债无形累�?

**现象**：每次都"能跑就行"，代码越来越乱�?

**根因**：只管当前功能，不管代码质量�?

**根治方案：技术债可视化 + 主动重构建议**
- 持续监控：重复代码、复杂度、架构违规、过时模�?
- 在合适时机提醒用户重�?
- 不每次都提醒，找空档时机

### 问题六：用户不知道自己不知道什�?

**现象**：用户说"做个登录"，不知道要考虑密码强度、失败锁定、多因素认证...

**根因**：领域知识缺失，系统不引导就全漏了�?

**根治方案：领域知识主动引�?*
- 建立领域知识�?
- 用户提到某领域概念时，主动展示相关考量�?
- 渐进式引导：先展示关键的，确认后再展示可选的

### 总览�?

| 核心问题 | 根本原因 | 根治方案 |
|----------|----------|----------|
| 需求蒸�?| 对话≠需�?| 需求状态机 |
| 跳过设计 | 直接生成代码 | 强制设计�?|
| 无法追溯 | 代码与需求脱�?| 双向绑定 |
| 改A�?B | 无影响分�?| 影响分析系统 |
| 技术�?| 只管能跑 | 债务监控+重构建议 |
| 盲区漏需 | 用户不知�?| 领域知识引导 |

### 本质

**在自然语言交互的外表下，偷偷建立工程化的内核�?*

用户感受到的�?随便�?，系统在做的�?严格工程"�?

---

## 8. 以人为本的交互设�?

### 核心认知

人类与系统交互的本质�?

```
👁�?眼睛�?�?获取信息（输入）
🖐�?手操�?�?发出指令（输出）—�?未来用嘴
�?眼确�?�?验证结果（反馈）
```

系统设计围绕这三点：让人**看得到、看得明白、操作简单、还喜欢**�?

### 维度一：看得到

一切操作都要有可视反馈，不能有"空白时刻"�?

| 用户动作 | 系统反馈 |
|----------|----------|
| 说了一句话 | 显示气泡，表�?我听到了" |
| 系统在理�?| 显示"正在理解..."状�?|
| 理解完成 | 高亮显示识别出的关键�?|
| 开始生�?| 骨架屏渐进显�?|
| 生成完成 | 新内容有入场动画 |
| 发生变化 | 变化部分闪烁/高亮 |

**始终可见的状态栏**：用户任何时候看屏幕，都知道现在是什么状态�?

### 维度二：看得明白

**用用户的语言，不用系统的语言**

```
�?系统语言�?创建 UserModel 实例，配�?bcrypt 加密算法"
�?用户语言�?建立用户数据存储，密码会安全加密保存"
                [展开详情] �?想看技术细节再�?
```

**复杂信息分层展示**
- 第一眼（0.5秒）：知道大概是什�?
- 第二眼（2秒）：知道包含什�?
- 第三眼（需要时）：知道细节

**用对比让变化显而易�?*：修改前/修改后并排展示，明确标注改了什么�?

### 维度三：操作简�?

**最常用的操作，最少的动作**

| 操作频率 | 操作方式 |
|----------|----------|
| 高频（每分钟�?| 单击/单词语音 |
| 中频（每小时�?| 右键菜单/短句语音 |
| 低频（每天） | 顶部菜单/完整对话 |
| 罕见（偶尔） | 设置面板 |

**设计原则**�?
- 智能预判，减少操作（推荐下一步）
- 就近操作，不要跳转（原地弹出小面板）
- 语音和点击协同（点击选中 + 语音指令�?

### 维度四：确认结果

**即时反馈，所见即所�?*

用户说话的同时，预览区实时变化。说完即完成，不需要等待�?

**给用户后悔的权利**

始终可撤销，用户才敢大胆尝试�?

**正向确认，给用户信心**

完成后展示：做了什么、包含哪些、下一步可以做什么�?

### 维度五：喜欢（情感设计）

用户喜欢用，来自情感体验�?

| 情感 | 来源 |
|------|------|
| 成就�?| 说了一句话，出来这么好的东�?|
| 掌控�?| 我能控制它，它听我的 |
| 安全�?| 不怕出错，随时可以回退 |
| 惊喜�?| 它居然想到了我没想到�?|
| 流畅�?| 整个过程没有卡顿、没有打�?|

**设计原则**�?
- 让用户感�?系统懂我"
- 适度的小惊喜（完成时的仪式感�?
- 温柔的错误处理（不是冷冰冰的 Error�?

### 人本设计检查表

| 维度 | 核心问题 | 设计目标 |
|------|----------|----------|
| 看得�?| 系统在干什么？ | 一切操作有反馈，没有空白时�?|
| 看得明白 | 这是什么意思？ | 用人话，分层展示，对比变�?|
| 操作简�?| 怎么才能完成�?| 最少动作，智能预判，就近操�?|
| 确认结果 | 对不对？ | 即时反馈，可撤销，正向确�?|
| 喜欢 | 想不想用�?| 懂我、小惊喜、有温度 |

### 本质

> **眼睛是输入，�?嘴是输出，眼睛再确认�?*
>
> 我们的系统要做的就是�?
> - **喂给眼睛正确的信�?*（看得到、看得明白）
> - **让手/嘴最省力**（操作简单）
> - **让眼睛确认时开�?*（结果对、还喜欢�?

---

## 9. 界面布局设计

### 核心布局：三区域模型

根据"�?�?�?交互模型，界面分三个核心区域�?

```
┌─────────────────────────────────────────────────────────�?
�? 状态栏：始终显示当前状态（眼睛随时能看到在干什么）    �?
├─────────────────────────────┴───────────────────────────�?
�?   输入区（�?嘴操作）     �?        显影区（眼睛看结果）      �?
�?                             �?                               �?
�? �?对话式输�?              �?  �?实时预览（边说边变）        �?
�? �?智能推荐                  �?  �?需�?设计面板              �?
�? �?历史记录                  �?  �?多模式切换（预览/设计/代码�?�?
├─────────────────────────────────────────────────────────�?
�? 操作栏：[↗撤销] [预览] [代码] [发布] （常用操作触手可及）  �?
└─────────────────────────────────────────────────────────�?
```

### 输入区设�?

- 输入框够大，不压�?
- 提示词引导，降低门槛�?试试这样�?.."�?
- 历史记录，快速复�?
- 语音按钮醒目

### 显影区设�?

四种显示模式可切换：
- **预览模式**：所见即所得，点击元素可直接编�?
- **设计模式**：架构树状图，看清模块关�?
- **代码模式**：查看生成的代码
- **对比模式**：修改前/修改后并�?

### 反馈设计

| 阶段 | 反馈内容 |
|------|----------|
| 输入�?| 关键词实时高�?|
| 理解完成 | 复述确认�?我理解您想要..."�?|
| 生成�?| 进度�?+ 分步状�?|
| 完成�?| 摘要 + 下一步操�?|

### 错误处理

温柔而非冷冰冰：
```
�?"错误：Error: Cannot parse input"
�?"我没太理�?..你是�?[选项A] 还是 [选项B]�?
```

### 界面检验标�?

| 标准 | 达标表现 |
|------|----------|
| 可见�?| 任何时候看屏幕都有状态显�?|
| 清晰�?| 不需要技术背景也能理解|
| 简洁�?| 主流程不超过3�?|
| 反馈�?| 0.1秒内有视觉反�?|
| 愉悦�?| 有成就感，想继续�?|

---

## 10. 进阶优化方案

### 优化一：多模态融合输�?

不只是文�?语音，而是多种输入方式无缝结合�?

- **贴图生成**：用户贴一张截图，�?做个类似�?
- **指点+语音**：点击元�?+ "把这个变�?
- **拖拽示意**：拖动元素表示位置调�?
- **涂鸦标注**：在预览图上画圈表示"这里要改"

### 优化二：预测性界�?

系统提前预判用户下一步，提前准备�?

- **输入预测**：用户打"做个�?.."，自动补�?登录页面"
- **意图预测**：刚做完登录，推�?要不要做注册�?
- **预加�?*：用户还没点，相关资源已在加�?

### 优化三：上下文感�?

系统理解用户当前情境，智能调整：

- **时间感知**：深夜用暗色主题，提�?要不要保存明天继�?
- **进度感知**：知道用户在做什么阶段，提供相关建议
- **历史感知**�?您上次做过类似的，要参考吗�?

### 优化四：协作模式

不只是单人用，支持团队协作：

- **实时同步**：多人同时编辑，实时看到彼此操作
- **评论标注**：在设计稿上留言讨论
- **版本分支**�?试试另一个方�?，不影响主线

### 优化五：学习型系�?

系统越用越懂用户�?

- **偏好学习**：记住用户喜欢的颜色、风格、布局
- **词汇学习**：用户说"大一�?通常�?.2倍，记住
- **流程学习**：用户常用的操作顺序，下次主动推�?

### 优化六：渐进式披露专业功�?

新手看到简单界面，高手解锁更多�?

- **新手模式**：只显示核心功能，引导式操作
- **标准模式**：完整功能，智能推荐
- **专家模式**：显示技术细节，支持直接编辑代码

### 优化七：离线能力

不依赖网络也能用�?

- **本地缓存**：常用模板、组件本地存�?
- **离线编辑**：断网时可继续工作，联网后同�?
- **轻量模型**：简单操作本地处理，复杂操作云端

### 优化�?

| 优化方向 | 核心价�?| 实现难度 |
|----------|----------|----------|
| 多模态输�?| 表达更自�?| �?|
| 预测性界�?| 操作更快 | �?|
| 上下文感�?| 更懂用户 | �?|
| 协作模式 | 团队可用 | �?|
| 学习型系�?| 越用越顺 | �?|
| 渐进披露 | 适合不同用户 | �?|
| 离线能力 | 不依赖网�?| �?|

---

### 优化八：情景式模板启�?

从用户场景出发，而非从功能出发：

```
"您今天想解决什么问题？"
[给客户展示方案]  [卖东西]  [收集信息]  [做个小游戏]
        �?
直接展示对应模板，用户只需填内�?
```

### 优化九：智能分步引导

复杂任务不一次问完，像聊天一样一步步来：

```
Step 1: "您想做什么类型的产品�? [电商] [工具] [社交]
Step 2: "这个工具解决什么问题？" ______
Step 3: "主要在哪里用�? [手机] [电脑] [都要]
...每一步都简单，累积起来信息完整
```

### 优化十：副驾驶模式（AI伴随�?

AI全程伴随，主动观察、适时建议�?

- 用户编辑时，AI观察并给出优化建�?
- 不频繁打扰，建议要真的有价�?
- 用户可以关掉

### 优化十一：样例驱动设�?

用户不知道要什么，但看到就知道�?

```
用户�?做个好看的登录页�?
系统�?这些风格您喜欢哪个？"
[简约] [商务] [活泼] [科技]
�?[上传参考图]
```

让用户用眼睛选择，比用嘴描述容易100倍�?

### 优化十二：渐进式交付

不是做完整个东西才给用户看，边做边交付：

```
10�? "先看看整体框架对不对�?
确认�? "首页布局这样如何�?
再确�? "商品卡片用这种？"
...每一步都确认，最终结果一定对
```

### 优化十三：可逆性保�?

可视化历史时间线，随时可回退�?

- 点击任意历史节点可回到那个状�?
- 可以从历史点分叉出新版本
- 自动保存，不怕丢�?

### 优化十四：智能复�?

用户做过的东西能智能复用�?

- 复用整个页面/组件
- 只复用风�?配色
- 只复用布局结构

### 优化十五：目标导向反�?

告诉用户离目标还有多远：

```
您的电商网站完成�?80%
├─ 商品展示 �?
├─ 购物�?�?
├─ 结算流程 �?进行�?
└─ 支付接入 �?待开�?
```

### 进阶优化汇�?

| 优化方向 | 核心价�?| 关键�?|
|----------|----------|--------|
| 情景式启�?| 降低启动门槛 | 从场景出�?|
| 分步引导 | 降低认知负担 | 一次只问一�?|
| 副驾驶模�?| AI主动辅助 | 观察+建议 |
| 样例驱动 | 降低表达难度 | 让用户选而非�?|
| 渐进交付 | 减少返工 | 边做边确�?|
| 可逆性保�?| 鼓励尝试 | 随时回退 |
| 智能复用 | 提高效率 | 复用之前的工�?|
| 目标导向 | 明确进度 | 知道离目标多�?|

---

## 11. AI能力应用方案

### 当前可用的AI能力

| AI能力 | 成熟�?| 应用场景 |
|---------|--------|----------|
| 大语言模型 (LLM) | �?| 自然语言理解、代码生成、对�?|
| 视觉理解 (Vision) | �?| 图片识别、UI截图分析、设计稿解析 |
| 语音识别 (ASR) | �?| 语音输入、语音指�?|
| 语音合成 (TTS) | �?| 语音反馈、语音引�?|
| 嵌入向量 (Embedding) | �?| 语义搜索、相似度匹配 |
| 多模态模�?| 中高 | 图文结合理解、跨模态生�?|
| 代码生成 | �?| 组件代码、逻辑代码、样式代�?|

### AI应用方案一：多层意图理�?

用LLM实现从模糊到精确的多层解析：

```
用户输入�?做个同步�?

AI Layer 1 - 领域识别�?
  �?分类：工具软�?文件同步
  �?置信度：0.95

AI Layer 2 - 功能推断�?
  �?核心功能：文件上传、同步、多端访�?
  �?可选功能：版本管理、分享、协�?

AI Layer 3 - 细节补充�?
  �?推荐技术栈、数据结构、架构模�?
```

### AI应用方案二：视觉理解生成

用Vision模型实现"看图生成"�?

```
用户贴一张截�?+ "做个类似�?
        �?
Vision模型分析�?
  - 布局结构：左右分�?
  - 配色方案：蓝白主�?
  - 组件类型：导航栏、卡片、按�?
  - 风格特征：简约、圆角、阴�?
        �?
生成相似设计
```

### AI应用方案三：上下文记�?

用Embedding+向量数据库实现长期记忆：

```
每次对话/操作 �?嵌入向量�?�?存入向量数据�?
        �?
用户新输�?�?检索相关历�?�?注入上下�?�?LLM理解
        �?
解决"需求蒸�?问题
```

### AI应用方案四：流式生成与显�?

用Streaming API实现边生成边显示�?

```
LLM流式输出 �?解析器实时解�?�?渲染器实时更新UI
        �?
用户看到内容逐渐浮现，而不是等待后突然出现
```

### AI应用方案五：指代消解

用LLM理解"这个""那个"指代什么：

```
上下文：用户刚点击了登录按钮
用户说："把这个变�?
        �?
LLM推理�?
  - "这个" = 最近操作的元素 = 登录按钮
  - "变大" = 调整尺寸
  - 置信度：0.92
```

### AI应用方案六：领域知识�?

用RAG构建领域知识检索系统：

```
预置知识（最佳实践、设计模式） �?向量�?�?知识�?
        �?
用户�?做个登录" �?检索相关知�?
        �?
返回：登录功能应考虑密码强度、失败锁定、多FA�?
        �?
解决"用户不知道自己不知道什�?问题
```

### AI应用方案七：代码生成与审�?

用专门的代码LLM生成可靠代码�?

```
设计稿（结构化） �?Prompt构建 �?代码LLM
        �?
生成代码 �?语法检�?�?规范检�?�?运行测试
        �?
通过后才展示给用�?
```

### AI应用方案八：智能建议

用LLM分析用户操作，给出优化建议：

```
用户当前设计 + 最佳实践知�?�?LLM分析
        �?
"您的按钮可以再大一点，方便用户点击"
"建议增加加载状态，提升用户体验"
```

### AI能力与问题映射表

| 问题 | AI解决方案 | 核心技�?|
|------|------------|----------|
| 需求蒸�?| 上下文记�?| Embedding + 向量数据�?|
| 跳过设计 | 自动生成设计�?| LLM + 领域知识 |
| 无法追溯 | 代码注释生成 | LLM生成说明 |
| 改A坏B | 影响分析 | 代码理解LLM |
| 技术�?| 质量检�?| 代码分析LLM |
| 盲区漏需 | 领域引导 | RAG + 知识�?|
| 模糊表达 | 意图解析 | 多层LLM推理 |
| 图片参�?| 视觉理解 | Vision模型 |

### AI编排架构

```
用户输入（文�?语音/图片�?
        �?
════════════════════════════════
�?          AI编排�?                �?
�? ┌──────────────────────────�?�?
�? �?意图理解 �?上下文融�?�?任务规划 �?�?
�? └──────────────────────────�?�?
════════════════════════════════
        �?
┌────────────────────────────────────�?
�?Vision �?LLM �?代码LLM �?Embedding �?
└────────────────────────────────────�?
        �?
════════════════════════════════
�?        质量审查�?                �?
════════════════════════════════
        �?
结构化设计数�?+ 可运行代�?
```

---

## 12. 落地保障机制

### 核心目标

确保设计在程序中能够落实，在运行中不会跑偏：

- **AI按计划输�?*：输出格式可控、可验证
- **用户切实可用**：输出内容能直接使用
- **流程简�?*：最少步骤完成任�?
- **可见即得**：实时看到结�?
- **事后可改**：随时可修改、可回退

### 保障一：AI输出约束机制

确保AI不会“跑偏”，输出符合预期�?

#### 1. 结构化输出约�?

```typescript
// AI输出必须符合预定义的Schema
interface AIOutputSchema {
  // 设计稿输出格�?
  design: {
    components: ComponentDef[];  // 组件定义
    layout: LayoutDef;           // 布局定义
    styles: StyleDef;            // 样式定义
    dataModels: DataModel[];     // 数据模型
  };
  
  // 代码输出格式
  code: {
    files: CodeFile[];           // 代码文件
    dependencies: string[];      // 依赖
    runnable: boolean;           // 是否可运�?
  };
}

// 强制验证
async function validateAIOutput(output: any, schema: Schema): ValidationResult {
  const errors = validate(output, schema);
  if (errors.length > 0) {
    // 拒绝不符合格式的输出，要求重新生�?
    return { valid: false, errors, needsRegeneration: true };
  }
  return { valid: true };
}
```

#### 2. Prompt工程规范

```typescript
// Prompt模板，确保输出可�?
const PROMPT_TEMPLATE = `
你是一个UI设计生成器�?

## 输入
用户需求：{{userInput}}
上下文：{{context}}

## 输出要求
你必须输出符合以�?JSON Schema 的内容：
{{jsonSchema}}

## 约束
1. 只输�?JSON，不要其他内�?
2. 所有字段必须填�?
3. 代码必须可运�?
4. 组件必须使用预定义的组件�?
`;
```

#### 3. 输出验证流水�?

```
AI原始输出
    �?
[格式验证] �?不通过 �?重新生成（最�?次）
    �?通过
[内容验证] �?不通过 �?修正或重新生�?
    �?通过
[代码验证] �?语法错误 �?自动修复
    �?通过
[运行测试] �?运行失败 �?调试修复
    �?通过
展示给用�?
```

### 保障二：用户可用性保�?

确保AI输出对用户切实可用�?

#### 1. 可用性检查清�?

```typescript
interface UsabilityCheck {
  // 生成的设计是否可预览
  previewable: boolean;
  
  // 生成的代码是否可运行
  runnable: boolean;
  
  // 是否有明确的下一步操�?
  hasNextAction: boolean;
  
  // 是否可修�?
  editable: boolean;
  
  // 是否可回退
  reversible: boolean;
}

// 每次输出前必须通过
function ensureUsability(output: AIOutput): UsabilityCheck {
  return {
    previewable: canPreview(output),
    runnable: canRun(output.code),
    hasNextAction: output.nextActions.length > 0,
    editable: output.editPoints.length > 0,
    reversible: snapshotCreated()
  };
}
```

#### 2. 输出转换�?

```typescript
// AI输出 �?用户可用格式
class OutputTransformer {
  // 转换为可预览的组�?
  toPreviewable(design: DesignOutput): PreviewComponent {
    return {
      component: renderDesign(design),
      interactivePoints: extractEditPoints(design), // 可点击编辑的�?
      tooltip: generateTooltips(design)             // 元素说明
    };
  }
  
  // 转换为可运行的代�?
  toRunnable(code: CodeOutput): RunnableCode {
    return {
      files: code.files,
      entryPoint: code.entry,
      sandbox: createSandbox(code),  // 创建运行沙箱
      hotReload: true                // 支持热重�?
    };
  }
}
```

### 保障三：流程简单保�?

确保用户操作流程最简化�?

#### 1. 流程简化规�?

| 规则 | 说明 | 实现方式 |
|------|------|----------|
| 一句话启动 | 用户说一句话就能开�?| 默认�?+ 智能推断 |
| 最�?�?| 任何任务不超�?�?| 流程合并 + 智能跳过 |
| 选择代替输入 | 能选就不让用户打字 | 选项卡片 + 智能推荐 |
| 默认可用 | 生成即可用，不需配置 | 智能默认�?|

#### 2. 流程状态机

```typescript
// 严格控制流程状�?
enum FlowState {
  IDLE = 'idle',           // 等待输入
  UNDERSTANDING = 'understanding',  // 理解�?
  CONFIRMING = 'confirming',        // 确认�?
  GENERATING = 'generating',        // 生成�?
  PREVIEWING = 'previewing',        // 预览�?
  EDITING = 'editing',              // 编辑�?
  COMPLETED = 'completed'           // 完成
}

// 状态转换规�?
const FLOW_TRANSITIONS = {
  [FlowState.IDLE]: [FlowState.UNDERSTANDING],
  [FlowState.UNDERSTANDING]: [FlowState.CONFIRMING, FlowState.GENERATING],
  [FlowState.CONFIRMING]: [FlowState.GENERATING, FlowState.UNDERSTANDING],
  [FlowState.GENERATING]: [FlowState.PREVIEWING],
  [FlowState.PREVIEWING]: [FlowState.EDITING, FlowState.COMPLETED],
  [FlowState.EDITING]: [FlowState.GENERATING, FlowState.COMPLETED],
  [FlowState.COMPLETED]: [FlowState.IDLE, FlowState.EDITING]
};
```

### 保障四：可见即得保证

确保用户实时看到结果�?

#### 1. 实时反馈规范

| 操作阶段 | 反馈时间 | 反馈内容 |
|----------|----------|----------|
| 用户输入 | <100ms | 关键词高�?|
| 意图理解 | <500ms | "我理解您想要..." |
| 开始生�?| 即时 | 骨架屏出�?|
| 生成过程 | 实时 | 流式填充内容 |
| 生成完成 | <100ms | 完成动画 + 操作指引 |

#### 2. 流式显影实现

```typescript
// 流式输出处理�?
class StreamingRenderer {
  private buffer: string = '';
  private renderer: UIRenderer;
  
  // 处理流式输出
  async processStream(stream: AsyncIterable<string>) {
    for await (const chunk of stream) {
      this.buffer += chunk;
      
      // 尝试解析并渲�?
      const parsed = this.tryParse(this.buffer);
      if (parsed.renderable) {
        await this.renderer.update(parsed.content);
      }
    }
  }
  
  // 增量解析，尽早显�?
  private tryParse(buffer: string): ParseResult {
    // 即使不完整也尝试渲染部分内容
    return incrementalParse(buffer);
  }
}
```

### 保障五：事后可改保证

确保用户可以随时修改和回退�?

#### 1. 状态快照系�?

```typescript
// 每次操作前自动保存快�?
class SnapshotManager {
  private snapshots: Snapshot[] = [];
  private maxSnapshots = 100;
  
  // 操作前自动调�?
  beforeOperation(operation: Operation): string {
    const snapshot = {
      id: generateId(),
      timestamp: Date.now(),
      state: deepClone(currentState),
      operation: operation.description
    };
    this.snapshots.push(snapshot);
    this.cleanup();
    return snapshot.id;
  }
  
  // 回退到指定快�?
  rollbackTo(snapshotId: string): State {
    const snapshot = this.snapshots.find(s => s.id === snapshotId);
    if (snapshot) {
      return snapshot.state;
    }
    throw new Error('Snapshot not found');
  }
  
  // 获取历史时间�?
  getTimeline(): TimelineItem[] {
    return this.snapshots.map(s => ({
      id: s.id,
      time: s.timestamp,
      description: s.operation,
      preview: generatePreview(s.state)
    }));
  }
}
```

#### 2. 可编辑点标记

```typescript
// 标记所有可编辑的点
interface EditableOutput {
  content: any;
  
  // 可编辑点列表
  editPoints: {
    id: string;
    path: string;           // 数据路径
    type: 'text' | 'color' | 'size' | 'position' | 'component';
    currentValue: any;
    suggestions?: any[];    // 可选建议�?
  }[];
  
  // 编辑方法
  edit(pointId: string, newValue: any): EditableOutput;
}
```

### 保障六：异常处理机制

确保系统在任何情况下都能给用户可用的反馈�?

#### 1. 分级异常处理

| 异常类型 | 处理方式 | 用户看到 |
|----------|----------|----------|
| AI输出格式错误 | 自动重试 | 不可见，静默处理 |
| AI理解错误 | 追问确认 | "我理解的对吗�? |
| 代码生成失败 | 降级处理 | "暂时生成了简化版" |
| 网络错误 | 重试+缓存 | "正在重新连接..." |
| 未知错误 | 安全降级 | "遇到问题，请尝试重新描述" |

#### 2. 降级策略

```typescript
// 分级降级，确保始终有输出
async function generateWithFallback(input: UserInput): Output {
  try {
    // 级别1：完整生�?
    return await fullGeneration(input);
  } catch (e1) {
    try {
      // 级别2：简化生�?
      return await simplifiedGeneration(input);
    } catch (e2) {
      try {
        // 级别3：模板生�?
        return await templateGeneration(input);
      } catch (e3) {
        // 级别4：安全响�?
        return safeResponse(input);
      }
    }
  }
}
```

---

## 13. 开发规�?

### 代码结构规范

```
src/
├── core/                    # 核心逻辑
�?  ├── ai/                  # AI编排�?
�?  �?  ├── orchestrator.ts  # AI调度�?
�?  �?  ├── intent.ts        # 意图理解
�?  �?  ├── context.ts       # 上下文管�?
�?  �?  └── validator.ts     # 输出验证
�?  ├── state/               # 状态管�?
�?  �?  ├── requirement.ts   # 需求状态机
�?  �?  ├── snapshot.ts      # 快照管理
�?  �?  └── flow.ts          # 流程状态机
�?  └── quality/             # 质量保障
�?      ├── checker.ts       # 质量检查器
�?      └── fallback.ts      # 降级处理
├── ui/                      # 界面�?
�?  ├── input/               # 输入�?
�?  ├── preview/             # 显影�?
�?  ├── feedback/            # 反馈组件
�?  └── timeline/            # 历史时间�?
└── services/                # 服务�?
    ├── llm/                 # LLM服务
    ├── vision/              # 视觉服务
    ├── embedding/           # 嵌入服务
    └── code/                # 代码生成服务
```

### 开发检查清�?

每个功能开发完成后必须检查：

- [ ] AI输出是否有Schema约束�?
- [ ] 输出是否经过验证流水线？
- [ ] 用户操作是否不超�?步？
- [ ] 是否�?.1秒内的即时反馈？
- [ ] 是否支持流式显影�?
- [ ] 是否有操作前快照�?
- [ ] 是否标记了可编辑点？
- [ ] 异常时是否有降级方案�?
- [ ] 用户语言是否人性化�?

### API设计规范

```typescript
// 每个AI接口必须遵循的模�?
interface AIService {
  // 输入必须包含上下�?
  process(input: UserInput, context: Context): Promise<AIOutput>;
  
  // 输出必须有Schema
  outputSchema: JSONSchema;
  
  // 必须有降级方�?
  fallback(input: UserInput): Promise<SafeOutput>;
  
  // 必须支持流式输出
  processStream(input: UserInput, context: Context): AsyncIterable<Chunk>;
}
```

---

## 14. 后台角色架构设计

### 设计时与运行�?

| 阶段 | 中文角色 | 英文角色 | 职责 |
|------|----------|----------|------|
| **设计�?* | 总设计师 + 参谋团队 | Chief Architect + Advisory Team | 定规则、定流程、定标准 |
| **运行�?* | 总导�?| Director (调度�?Orchestrator) | 按规则执行、协调各方、保证秩�?|

### 角色总览

系统后台角色分为**三大�?*�?

```
┌─────────────────────────────────────────────────────────────────�?
�?                     后台角色架构                                 �?
�?                 Runtime Role Architecture                       �?
├─────────────────────────────────────────────────────────────────�?
�? 【流程控制类 / Flow Control�?                                   �?
�?   · 总导�?调度�?(Director/Orchestrator) - 运行时总指�?        �?
�?   · 监工 (Supervisor) - 状态监控与异常处理                       �?
�?                                                                �?
�? 【能力执行类 / Capability Execution�?                           �?
�?   · AI代理 (AI Agent) - 智能任务执行                            �?
�?   · 记忆�?(Memory Keeper) - 上下文与历史管理                    �?
�?   · 共情�?(Empathy Agent) - 情感感知与体验优�?                 �?
�?                                                                �?
�? 【质量守护类 / Quality Assurance�?                              �?
�?   · 守卫�?(Guardian) - 格式/安全验证（硬性关卡）                 �?
�?   · 自检�?(Self-Checker) - AI输出自审                          �?
�?   · 比对�?(Diff Analyzer) - 变更影响分析                        �?
�?   · 回滚�?(Rollback Agent) - 故障恢复                          �?
└─────────────────────────────────────────────────────────────────�?
```

### 角色详细定义

#### 1. 总导�?调度�?(Director/Orchestrator)

**角色定位**：运行时的总指挥、总导�?

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 按设计规则协调各方执行，保证流程正确 | Coordinate execution according to design rules |
| **权威来源** | 掌握流程、控制入口、协调输�?| Controls flow, entry point, and output assembly |
| **边界** | 只负�?谁来做、何时做"，不负责"怎么�? | Decides WHO and WHEN, not HOW |
| **AI配合** | 无需AI，纯程序逻辑（确定性、可靠） | No AI, pure deterministic logic |

**总导演的四大职能**�?

| 职能 | 中文说明 | English | 具体行为 |
|------|----------|---------|----------|
| **现场指挥** | 接收请求，分配任�?| Command | 判断走哪条流程，谁先做谁后做 |
| **进度掌控** | 知道当前在哪，下一步做什�?| Progress Control | 维护流程状态机，跟踪执行进�?|
| **异常决策** | 失败时决定重试还是降�?| Exception Handling | 指示监工触发降级，协调安全停�?|
| **最终呈�?* | 汇总各方输出，组装结果 | Final Presentation | 控制何时展示给用�?|

```pascal
// 总导演接�?(Director/Orchestrator Interface)
type
  IDirector = interface
    // 接收请求，制定执行计�?(Receive request, create execution plan)
    function 制定计划(const 用户请求: T用户请求): T执行计划;
    
    // 按计划分发任�?(Dispatch tasks according to plan)
    procedure 分发任务(const 计划: T执行计划);
    
    // 协调多任务执�?(Coordinate multiple tasks)
    function 协调执行(const 任务列表: TArray<T任务>): T执行结果;
    
    // 汇总结果呈现给用户 (Assemble and present results)
    procedure 呈现结果(const 结果: T执行结果);
  end;
```

**总导演的指挥关系**�?

```
                         用户 (User)
                           �?
                           �?
              ┌────────────────────────�?
              �?  总导�?调度�?         �?
              �?  Director/Orchestrator �?
              �? �?接收请求             �?
              �? �?制定执行计划         �?
              �? �?协调全局             �?
              �? �?最终呈�?            �?
              └───────────┬────────────�?
                          �?发号施令
         ┌────────────────┼────────────────�?
         �?               �?               �?
         �?               �?               �?
    ┌─────────�?    ┌─────────�?    ┌─────────�?
    �?能力�?  �?    �?质量�?  �?    �?保障�?  �?
    │Capability�?    �?Quality �?    �?Support �?
    ├─────────�?    ├─────────�?    ├─────────�?
    �?AI代理  �?    �?守卫�?  �?    �?监工    �?
    �?记忆�? �?    �?自检�?  �?    �?回滚�? �?
    �?        �?    �?比对�?  �?    �?        �?
    └─────────�?    └─────────�?    └─────────�?
         �?               �?               �?
         └────────────────┴────────────────�?
                          �?
                    向总导演汇�?
```

### 总导演的约束机制

**核心问题**：谁来监督监督者？如何确保总导演优秀履行职责�?

#### 约束原则：制度约�?+ 透明执行 + 事后审计

```
┌─────────────────────────────────────────────────────────────────�?
�?                  总导演约束体�?                                 �?
�?             Director Constraint System                          �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�? 【制度约�?/ Institutional Constraints�?                       �?
�?   · 流程剧本不可篡改 —�?总导演只能按剧本执行，不能改剧本          �?
�?   · 必经关卡不可跳过 —�?守卫员检查是硬编码的，总导演无权绕�?      �?
�?   · 权限边界硬隔�?—�?总导演只能协调，不能直接操作数据            �?
�?                                                                �?
�? 【透明执行 / Transparent Execution�?                           �?
�?   · 每个决策都记�?—�?为什么走这条路、为什么调用这个角�?         �?
�?   · 执行轨迹可追�?—�?完整的执行日志，任何人可审查               �?
�?   · 状态实时可�?—�?用户和系统都能看到当前在哪个节点             �?
�?                                                                �?
�? 【事后审�?/ Post-Audit�?                                      �?
�?   · 监工独立监控 —�?监工不受总导演指挥，直接观察执行状�?         �?
�?   · 异常自动上报 —�?流程异常时自动记录，供事后分�?              �?
�?   · 用户最终裁�?—�?用户可以否决任何结果，要求重�?              �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

#### 约束一：流程剧本不可篡�?(Immutable Playbook)

总导演只是剧本的执行者，不是编写者：

```pascal
// 流程剧本 —�?设计时固化，运行时只�?
type
  T流程剧本 = class
  private
    F节点列表: TArray<T流程节点>;  // 设计时定义，运行时不可修�?
    F转换规则: TArray<T状态转�?;  // 什么条件下从A到B
  public
    // 总导演只能查询，不能修改
    function 获取当前节点(const 状�? T流程状�?: T流程节点;
    function 获取下一节点(const 状�? T流程状�? const 事件: T事件): T流程节点;
    // 没有任何Set方法�?
  end;
```

**关键**：流程定义是**编译时常�?*，不是运行时变量�?

#### 约束二：必经关卡不可跳过 (Mandatory Checkpoints)

守卫员检查是硬编码在流程中的，总导演无权决定是否执行：

```pascal
// 总导演的执行流程 —�?守卫检查是硬编码的
procedure T总导�?执行生成流程(const 请求: T请求);
begin
  // 1. AI生成
  内容 := AI代理.生成(请求);
  
  // 2. 自检（总导演可以看结果，但必须执行�?
  自检结果 := 自检�?检�?内容);
  if not 自检结果.通过 then
    内容 := AI代理.重新生成(请求, 自检结果.问题);
  
  // ══════════════════════════════════════════════════�?
  // 3. 守卫检�?—�?【硬编码，总导演无法跳过或注释掉�?
  //    即使总导演想跳过，代码结构也不允�?
  // ══════════════════════════════════════════════════�?
  守卫结果 := 守卫�?验证(内容);  // 必须调用
  if not 守卫结果.通过 then
    raise E质量不达�?Create(守卫结果.原因);  // 必须处理
  
  // 4. 只有通过守卫才能继续
  呈现给用�?内容);
end;
```

**关键**：守卫检查不是if判断，而是**必经的函数调�?*�?

#### 约束三：权限边界硬隔�?(Hard Permission Boundary)

总导演只能通过接口协调，不能直接操作底层：

```
┌─────────────────────────────────────────────────────────────────�?
�?总导演能做的（协调层�?                                           �?
├─────────────────────────────────────────────────────────────────�?
�?�?调用 AI代理.生成(...)                                         �?
�?�?调用 记忆�?获取上下�?...)                                    �?
�?�?调用 守卫�?验证(...)                                         �?
�?�?调用 回滚�?创建快照(...)                                      �?
└─────────────────────────────────────────────────────────────────�?

┌─────────────────────────────────────────────────────────────────�?
�?总导演不能做的（隔离层）                                          �?
├─────────────────────────────────────────────────────────────────�?
�?�?直接访问数据�?                                                �?
�?�?直接调用LLM API                                               �?
�?�?直接修改用户数据                                               �?
�?�?跳过任何验证步骤                                               �?
�?�?修改其他角色的行�?                                            �?
└─────────────────────────────────────────────────────────────────�?
```

#### 约束四：执行轨迹全记�?(Complete Execution Trail)

总导演的每个决策都被记录，可追溯、可审计�?

```pascal
// 执行轨迹记录
type
  T执行轨迹 = record
    时间�? TDateTime;
    会话ID: string;
    节点: string;           // 当前在哪个流程节�?
    决策: string;           // 做了什么决�?
    原因: string;           // 为什么这样决�?
    调用角色: string;       // 调用了哪个角�?
    调用参数摘要: string;   // 传了什么参数（脱敏�?
    返回结果摘要: string;   // 得到什么结果（脱敏�?
    耗时毫秒: Integer;
  end;

// 总导演每个动作都自动记录
procedure T总导�?记录轨迹(const 轨迹: T执行轨迹);
begin
  // 写入执行日志表，供事后审�?
  F轨迹存储.追加(轨迹);
end;
```

#### 约束五：监工独立监控 (Independent Supervisor)

监工不受总导演指挥，而是**独立观察�?*�?

```
┌─────────────────────────────────────────────────────────────────�?
�?                   监控关系（非指挥关系�?                         �?
�?                Observation (Not Command)                        �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�?   总导�?──────────────────────────────�?各角�?                �?
�?     �?        发号施令 (Command)           �?                   �?
�?     �?                                     �?                   �?
�?     �?                                     �?                   �?
�?     �?                                     �?                   �?
�?   监工 ◀════════════════════════════════════                   �?
�?          独立观察，不受总导演指�?                                �?
�?          (Independent observation)                              �?
�?                                                                �?
�?   监工职责�?                                                   �?
�?   · 观察总导演的执行是否符合预期                                 �?
�?   · 观察各角色的响应是否超时                                     �?
�?   · 发现异常时触发告警（不需要总导演同意）                        �?
�?   · 记录异常供事后分�?                                         �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

```pascal
// 监工接口 —�?独立于总导�?
type
  I监工 = interface
    // 观察任务执行状态（被动观察，不是主动查询）
    procedure 观察任务(const 任务ID: string; const 状�? T任务状�?;
    
    // 检测异常（独立判断，不受总导演影响）
    function 检测异�?const 任务ID: string): T异常报告;
    
    // 触发告警（直接上报，不经过总导演）
    procedure 触发告警(const 告警: T告警信息);
    
    // 强制降级（紧急情况下可以绕过总导演）
    procedure 强制降级(const 原因: string);
  end;
```

#### 约束六：用户最终裁决权 (User Final Authority)

无论总导演做了什么决策，用户都有最终否决权�?

```
总导演的所有输�?──────�?用户确认界面 ──────�?用户决策
                                              �?
                         ┌────────────────────┼────────────────────�?
                         �?                   �?                   �?
                         �?                   �?                   �?
                     [�?确认]            [�?调整]            [�?重来]
                         �?                   �?                   �?
                         �?                   �?                   �?
                    用户满意              用户修改             用户否决
                    流程结束              返回调整             重新开�?
```

**用户权力**�?
- 可以否决任何AI生成的内�?
- 可以要求完全重来
- 可以随时回滚到之前的状�?
- 可以查看完整的执行轨�?

#### 约束总结�?

| 约束机制 | 中文 | English | 如何实现 |
|----------|------|---------|----------|
| **流程不可篡改** | 剧本是只读的 | Immutable Playbook | 流程定义为编译时常量 |
| **关卡不可跳过** | 守卫是硬编码�?| Mandatory Checkpoints | 验证调用嵌入代码结构 |
| **权限硬隔�?* | 只能协调不能操作 | Hard Permission Boundary | 接口隔离，无直接访问�?|
| **轨迹全记�?* | 每个决策都记�?| Complete Trail | 自动写入审计日志 |
| **监工独立** | 观察者不受指�?| Independent Supervisor | 监工与总导演平�?|
| **用户裁决** | 用户有最终否决权 | User Final Authority | 所有输出需用户确认 |

---

#### 2. 监工 (Supervisor)

**角色定位**：独立观察者，总导演的平级监督

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 监控系统运行状态、处理超时、触发降级、健康检�?| Monitor runtime status, handle timeout, trigger fallback |
| **独立�?* | 不受总导演指挥，独立观察执行情况 | Independent from Director, observes autonomously |
| **边界** | 只负�?是否正常"，不负责"正不正确" | Only cares about "normal or not", not "correct or not" |
| **AI配合** | 无需AI，纯程序逻辑 | No AI, pure deterministic logic |

```pascal
// 监工接口 (Supervisor Interface)
type
  I监工 = interface
    // 观察任务执行状�?(Observe task execution status)
    procedure 观察任务(const 任务ID: string; const 状�? T任务状�?;
    
    // 超时处理 (Handle timeout)
    function 处理超时(const 任务ID: string): T降级结果;
    
    // 触发降级 (Trigger fallback)
    procedure 触发降级(const 原因: string);
    
    // 健康检�?(Health check)
    function 健康检�? T系统健康状�?
    
    // 强制干预（紧急情况）(Emergency intervention)
    procedure 强制干预(const 原因: string);
  end;
```

**监工与总导演的关系**�?

| 关系 | 说明 |
|------|------|
| **不是上下�?* | 监工不向总导演汇报，而是独立观察 |
| **平级制衡** | 总导演指挥执行，监工监督执行 |
| **紧急权�?* | 监工在紧急情况下可以绕过总导演直接干�?|

#### 3. AI代理 (AI Agent)

**角色定位**：智能任务执行者，系统�?能力担当"

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 调用AI能力完成具体任务 | Execute intelligent tasks using AI capabilities |
| **能力范围** | 意图理解、内容生成、代码编�?| Intent understanding, content generation, code writing |
| **边界** | 只负�?生成内容"，不负责"验证质量" | Only generates content, not validates quality |
| **AI配合** | **核心AI角色**，调用LLM/Vision/代码LLM�?| Core AI role, calls LLM/Vision/Code LLM |

```pascal
// AI代理接口 (AI Agent Interface)
type
  IAI代理 = interface
    // 意图理解 (Understand intent)
    function 理解意图(const 输入: T用户输入; const 上下�? T上下�?: T意图;
    
    // 内容生成 (Generate content)
    function 生成内容(const 意图: T意图; const 输出Schema: TSchema): TAI输出;
    
    // 流式生成 (Stream generation)
    function 流式生成(const 意图: T意图): I流式输出;
  end;
```

**AI配合方式 / AI Integration**�?
- 调用LLM进行自然语言理解和生�?(LLM for NLU and generation)
- 调用Vision模型解析用户上传的图�?(Vision model for image analysis)
- 调用代码LLM生成可运行代�?(Code LLM for runnable code)
- 使用Embedding进行语义相似度匹�?(Embedding for semantic matching)

#### 4. 共情�?(Empathy Agent)

**角色定位**：情感感知与体验优化者，系统�?情感担当"

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 感知用户情绪、调整交互语气、实现情绪闭�?| Sense user emotion, adjust tone, achieve emotional closure |
| **能力** | 情绪识别、偏好学习、共鸣响�?| Emotion recognition, preference learning, empathetic response |
| **边界** | 只负�?让用户舒�?，不负责"完成任务" | Only ensures comfort, not task completion |
| **AI配合** | **需要AI**，使用LLM进行情感分析 | Requires AI for emotion analysis |

```pascal
// 共情官接�?(Empathy Agent Interface)
type
  I共情�?= interface
    // 感知用户情绪 (Sense user emotion)
    function 感知情绪(const 输入: T用户输入; const 上下�? T上下�?: T情绪状�?
    
    // 生成共鸣回应 (Generate empathetic response)
    function 生成共鸣回应(const 情绪: T情绪状�? const 原始回应: string): string;
    
    // 调整交互策略 (Adjust interaction strategy)
    function 调整策略(const 情绪: T情绪状�?: T交互策略;
    
    // 添加情绪闭环 (Add emotional closure)
    function 添加情绪闭环(const 任务结果: T任务结果): string;
  end;

type
  T情绪状�?= record
    当前情绪: string;      // 着�?困惑/正常/开�?
    信心程度: Double;     // 0-1
    疑虑�? TStringList;  // 用户可能担心什�?
  end;

  T交互策略 = record
    语气: string;          // 简�?详细/鼓励
    流程: string;          // 简�?正常/放慢
    确认频率: string;      // �?�?�?
  end;
```

**AI配合方式 / AI Integration**�?
- 使用LLM分析用户输入的情感色�?(Emotion analysis)
- 根据情绪调整AI回应的语�?(Tone adjustment)
- 在任务完成时生成庆祝/鼓励�?(Celebration/encouragement)

---

#### 5. 记忆�?(Memory Keeper)

**角色定位**：上下文与历史管理者，系统�?记忆担当"

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 管理会话上下文、维护需求历�?| Manage session context and requirement hiDeepDeepDeepDeepDeepStory |
| **能力** | 状态存取、语义检索、历史回�?| State storage, semantic retrieval, hiDeepDeepDeepDeepDeepStory traceback |
| **边界** | 只负�?存取数据"，不负责"数据含义" | Only stores/retrieves data, not interprets meaning |
| **AI配合** | **需要AI**，使用Embedding向量化存�?| Requires AI for Embedding vectorization |

```pascal
// 记忆官接�?(Memory Keeper Interface)
type
  I记忆�?= interface
    // 保存上下�?(Save context)
    procedure 保存上下�?const 会话ID: string; const 上下�? T上下�?;
    
    // 检索相关历�?(Retrieve relevant hiDeepDeepDeepDeepDeepStory)
    function 检索相关历�?const 查询: string; const 数量: Integer): TArray<T历史�?;
    
    // 获取完整上下�?(Get full context)
    function 获取上下�?const 会话ID: string): T上下�?
    
    // 清理过期记忆 (Cleanup expired memory)
    procedure 清理过期(早于: TDateTime);
  end;
```

**AI配合方式 / AI Integration**�?
- 使用Embedding模型将对�?操作向量�?(Embedding model for vectorization)
- 使用向量数据库进行语义检�?(Vector DB for semantic search)
- 智能判断哪些上下文与当前任务相关 (Intelligent context relevance)

#### 5. 守卫�?(Guardian)

**角色定位**：硬性质量关卡，系统�?底线守护�?

| 属�?| 中文 | English |
|------|------|------|
| **职责** | Schema验证、语法检查、安全检�?| Schema validation, syntax check, security check |
| **特�?* | 硬性关卡，不可绕过 | Hard checkpoint, cannot be bypassed |
| **边界** | 只负�?格式对不�?，不负责"内容好不�? | Only validates format, not content quality |
| **AI配合** | **无需AI**，纯程序化验�?| No AI, pure programmatic validation |

```pascal
// 守卫员接�?(Guardian Interface)
type
  I守卫�?= interface
    // Schema验证 (Validate schema)
    function 验证Schema(const 输出: TAI输出; const Schema: TSchema): T验证结果;
    
    // 代码语法检�?(Check syntax)
    function 检查语�?const 代码: string; const 语言: string): T语法检查结�?
    
    // 安全检�?(Security check)
    function 安全检�?const 内容: TAI输出): T安全检查结�?
    
    // 综合校验 (Comprehensive validation)
    function 验证(const 输出: TAI输出): T守卫结果;
  end;
```

**关键特�?/ Key Characteristics**�?
- **不可绕过 (Cannot bypass)**：所有AI输出必须经过守卫�?
- **硬性规�?(Hard rules)**：程序化检查，没有弹�?
- **快速失�?(Fail fast)**：不通过立即拦截，不传递给用户

#### 6. 自检�?(Self-Checker)

**角色定位**：AI输出自审员，系统�?内容质量�?

| 属�?| 中文 | English |
|------|------|------|
| **职责** | AI输出后立即自我审�?| Self-review immediately after AI output |
| **检查内�?* | 完整性、正确性、一致�?| Completeness, correctness, consistency |
| **边界** | 只负�?内容质量"，不负责"格式规范" | Only checks content quality, not format |
| **AI配合** | **需要AI**，使用LLM进行自我审查 | Requires AI for LLM self-review |

```pascal
// 自检员接�?(Self-Checker Interface)
type
  I自检�?= interface
    // 自检 (Self check)
    function 检�?const 输出: TAI输出; const 原始意图: T意图): T自检结果;
    
    // 发现问题后自我修�?(Self correct when issues found)
    function 修正(const 输出: TAI输出; const 问题列表: TArray<T问题>): TAI输出;
  end;
```

**AI配合方式 / AI Integration**�?
- 使用专门的自检Prompt让AI审查自己的输�?(Self-review prompt)
- 检查：是否完整回应了用户需求？逻辑错误？上下文一致？
- 发现问题则自动修正，修正后再次自检 (Auto-correct and re-check)

#### 7. 比对�?(Diff Analyzer)

**角色定位**：变更影响分析师，系统的"风险评估�?

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 分析变更影响范围，确�?改A不坏B" | Analyze change impact, ensure "changing A won't break B" |
| **能力** | 依赖分析、破坏检测、风险评�?| Dependency analysis, breakage detection, risk assessment |
| **边界** | 只负�?影响分析"，不负责"执行变更" | Only analyzes impact, not executes changes |
| **AI配合** | **需要AI**，使用代码理解LLM | Requires AI for code understanding LLM |

```pascal
// 比对员接�?(Diff Analyzer Interface)
type
  I比对�?= interface
    // 分析变更影响 (Analyze change impact)
    function 分析影响(const 变更�? T状�? const 变更�? T状�?: T影响报告;
    
    // 检测潜在破�?(Detect potential breakage)
    function 检测破�?const 变更列表: TArray<T变更>): TArray<T破坏风险>;
    
    // 生成差异报告 (Generate diff report)
    function 生成差异报告(const �? T状�? const �? T状�?: T差异报告;
  end;
```

**AI配合方式 / AI Integration**�?
- 使用代码理解LLM分析代码依赖关系 (Code LLM for dependency analysis)
- 智能判断变更可能影响的模�?(Intelligent impact prediction)
- 生成人类可读的影响报�?(Human-readable impact report)

#### 8. 回滚�?(Rollback Agent)

**角色定位**：故障恢复专家，系统�?安全兜底�?

| 属�?| 中文 | English |
|------|------|------|
| **职责** | 故障恢复、状态回滚、版本切�?| Fault recovery, state rollback, version switch |
| **能力** | 快照管理、一键回滚、分支创�?| Snapshot management, one-click rollback, branching |
| **边界** | 只负�?恢复状�?，不负责"判断是否需要恢�? | Only restores state, not decides whether to restore |
| **AI配合** | **无需AI**，纯程序化操�?| No AI, pure programmatic operation |

```pascal
// 回滚员接�?(Rollback Agent Interface)
type
  I回滚�?= interface
    // 回滚到指定快�?(Rollback to specific snapshot)
    function 回滚�?const 快照ID: string): T状�?
    
    // 撤销最近操�?(Undo recent operation)
    function 撤销: T状�?
    
    // 重做 (Redo)
    function 重做: T状�?
    
    // 创建分支 (Create branch)
    function 创建分支(const 来源快照ID: string): string;
    
    // 创建快照 (Create snapshot)
    function 创建快照(const 描述: string): string;
  end;
```

### 角色与节点映�?

#### 完整流程节点�?

```
用户输入
    �?
    �?
┌─────────────────────────────────────────────────────────────�?
�?节点1: 请求接收                                              �?
�?参与角色: 调度�?                                            �?
�?负责�? 调度�?                                              �?
�?AI配合: �?                                                  �?
�?职责: 解析请求，创建执行计�?                                 �?
└────────────────────────────┬────────────────────────────────�?
                             �?
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点2: 上下文加�?                                           �?
�?参与角色: 记忆官、调度员                                      �?
�?负责�? 记忆�?                                              �?
�?AI配合: Embedding检索相关历�?                                �?
�?职责: 获取会话上下文，检索相关历史信�?                        �?
└────────────────────────────┬────────────────────────────────�?
                             �?
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点3: 意图理解                                              �?
�?参与角色: AI代理、记忆官                                      �?
�?负责�? AI代理                                               �?
�?AI配合: LLM意图解析，指代消�?                                �?
�?职责: 理解用户真正想要什�?                                   �?
└────────────────────────────┬────────────────────────────────�?
                             �?
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点4: 内容生成                                              �?
�?参与角色: AI代理、监�?                                       �?
�?负责�? AI代理                                               �?
�?AI配合: LLM/代码LLM/Vision生成内容                           �?
�?职责: 生成设计稿、代码或其他内容                              �?
�?监工同时监控执行状态，超时则触发降�?                         �?
└────────────────────────────┬────────────────────────────────�?
                             �?
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点5: AI自检                                                �?
�?参与角色: 自检�?                                            �?
�?负责�? 自检�?                                              �?
�?AI配合: LLM自我审查Prompt                                    �?
�?职责: 检查生成内容的完整性、正确性、一致�?                    �?
�?边界: 发现问题 �?返回节点4重新生成                            �?
└────────────────────────────┬────────────────────────────────�?
                             �?自检通过
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点6: 系统守卫检�?                                         �?
�?参与角色: 守卫�?                                            �?
�?负责�? 守卫�?                                              �?
�?AI配合: 无（纯程序化验证�?                                   �?
�?职责: Schema验证、语法检查、安全检�?                         �?
�?边界: 不通过 �?返回节点4重新生成（最�?次）                    �?
└────────────────────────────┬────────────────────────────────�?
                             �?验证通过
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点7: 影响分析（如涉及变更�?                                �?
�?参与角色: 比对�?                                            �?
�?负责�? 比对�?                                              �?
�?AI配合: 代码理解LLM分析依赖                                   �?
�?职责: 分析变更影响范围，检测潜在破�?                         �?
�?边界: 高风险变�?�?提示用户确认                               �?
└────────────────────────────┬────────────────────────────────�?
                             �?
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点8: 创建快照                                              �?
�?参与角色: 回滚员、记忆官                                      �?
�?负责�? 回滚�?                                              �?
�?AI配合: �?                                                  �?
�?职责: 保存当前状态快照，支持后续回滚                          �?
└────────────────────────────┬────────────────────────────────�?
                             �?
                             �?
┌─────────────────────────────────────────────────────────────�?
�?节点9: 用户确认                                              �?
�?参与角色: 调度员（展示）、用户（决策�?                        �?
�?负责�? 用户                                                 �?
�?AI配合: �?                                                  �?
�?职责: 展示结果，等待用户确�?调整/重来                         �?
└────────────────────────────┬────────────────────────────────�?
                             �?
            ┌────────────────┼────────────────�?
            �?               �?               �?
            �?               �?               �?
        [确认]           [调整]           [重来]
            �?               �?               �?
            �?               �?               �?
┌───────────────────�?      �?               �?
�?节点10: 应用变更   �?      �?               �?
�?记忆官保存状�?    �?      �?               �?
└───────────────────�?      �?               �?
                            �?               �?
                    返回节点3           返回节点1
```

### 角色职责边界总表

| 角色 | 负责 | 不负�?| AI配合 | 可绕�?|
|------|------|--------|--------|--------|
| 调度�?| 任务分发、流程编�?| 具体执行、内容生�?| �?| �?|
| 监工 | 状态监控、超时处理、降级触�?| 内容正确�?| �?| �?|
| AI代理 | 意图理解、内容生�?| 质量验证 | LLM/Vision/代码LLM | �?|
| 记忆�?| 上下文存取、历史检�?| 数据含义解释 | Embedding | �?|
| 守卫�?| 格式验证、安全检�?| 内容质量判断 | �?| **绝对不可** |
| 自检�?| 内容质量自审 | 格式规范 | LLM | �?|
| 比对�?| 影响分析、破坏检�?| 执行变更 | 代码理解LLM | �?|
| 回滚�?| 状态恢复、版本切�?| 判断是否需要恢�?| �?| �?|

### AI配合详细说明

#### 需要AI配合的角�?

| 角色 | AI能力 | 配合方式 | 调用时机 |
|------|--------|----------|----------|
| **AI代理** | LLM | 意图理解、内容生�?| 每次用户输入 |
|  | Vision | 图片解析、UI分析 | 用户上传图片�?|
|  | 代码LLM | 代码生成、代码补�?| 生成代码�?|
|  | ASR/TTS | 语音识别、语音合�?| 语音交互�?|
| **记忆�?* | Embedding | 对话/操作向量�?| 每次对话保存 |
|  | 向量检�?| 语义相似度匹�?| 每次需要上下文�?|
| **自检�?* | LLM | 自我审查Prompt | 每次AI生成�?|
| **比对�?* | 代码理解LLM | 依赖分析、影响评�?| 涉及代码变更�?|

#### 不需要AI的角色（纯程序逻辑�?

| 角色 | 原因 | 优势 |
|------|------|------|
| 调度�?| 流程编排是确定性逻辑 | 快速、可靠、可预测 |
| 监工 | 状态监控是程序化操�?| 实时、不依赖外部服务 |
| 守卫�?| 格式验证是硬性规�?| 100%可靠、不可绕�?|
| 回滚�?| 状态恢复是数据操作 | 快速、确定�?|

### 设计原则

1. **职责单一**：每个角色只做一件事，便于测试和替换
2. **松耦合**：角色间通过消息/事件通信，不直接调用
3. **守护不可绕过**：守卫员是硬性关卡，AI输出必须经过
4. **AI归AI、程序归程序**：需要智能的用AI，需要可靠的用程�?
5. **监工全程在线**：任何环节超时或异常，监工都能感知并处理

### 数据存储与通信协议

本章设计的数据模型和通信协议已在 `Schema_MVP_v0.md` 中定义：

| 内容 | 位置 | 说明 |
|------|------|------|
| SQLite表结�?| Schema_MVP_v0.md �?�?| 9张表（nl_session/nl_message/nl_task等） |
| 枚举定义 | Schema_MVP_v0.md �?.1�?| 会话状�?流程状�?任务类型�?|
| 通信协议 | Schema_MVP_v0.md �?0�?| Delphi记录类型定义 |

**角色与表的映�?*�?

| 角色 | 主要操作的表 |
|------|----------------|
| 调度�?| nl_session, nl_task |
| 监工 | nl_task (状态监�? |
| AI代理 | nl_message, nl_generated_output |
| 记忆�?| nl_context_memory, nl_requirement_state |
| 守卫�?| nl_validation |
| 自检�?| nl_validation (self_check类型) |
| 比对�?| nl_impact_analysis |
| 回滚�?| nl_snapshot |

---

## 15. 人性化设计原则

### 核心认知：用户要的不是结果，是被�?

```
┌─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�?   人类更需要的是“你懂我”，而不是“拿到结果�?                      �?
�?   What humans need most is "you understand me",                �?
�?   not "getting results"                                        �?
�?                                                                �?
�?   让人心情好，就更易完成工�?                                      �?
�?   When people feel good, work gets done easier                 �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

**传统工具思维** vs **“懂我”思维**�?

| 传统工具思维 | “懂我”思维 |
|----------------|------------|
| 用户来是为了完成任务 | 用户来是为了被理�?|
| 成功 = 任务完成 | 成功 = 用户舒服 |
| 效率第一 | 共鸣第一 |
| 流程要闭�?| 情绪要闭�?|
| 用户中断 = 失败 | 用户中断 = 需要休�?|

### 原则一：容忍中断，保证可恢�?(Tolerate Interruption)

人类会半途而废，这不是缺点，是人性。系统要适应人，不是人适应系统�?

```
传统思维�?                         人性化思维�?
────────────────────────────────────────────────────────
开�?�?必须完成 �?结束            开�?�?可以暂停 �?随时回来 �?继续
     �?                                  �?
     用户走了 = 失败                    用户走了 = 保存草稿，下次见
```

**具体策略**�?

| 用户行为 | 传统处理 | 人性化处理 |
|----------|----------|------------|
| 生成到一半不想要�?| 强制完成或放�?| “我先保存着，您随时回来�?|
| 确认等待超时 | 错误/失败 | “已保存为草稿，下次打开继续�?|
| 用户忘了这件�?| 资源浪费 | “您上次做到了这�?..�?|
| 反复修改 | 禁止或限�?| “慢慢来，我都记着�?|

**超时处理示例**�?

```pascal
procedure T总导�?等待用户确认(const 内容: T内容);
begin
  case 等待响应(超时30�? of
    用户确认: 
      应用变更(内容);
    用户调整: 
      返回调整流程;
    超时:  // 用户走了，没关系
      begin
        保存为草�?内容);
        显示提示('已保存，您可以随时回来继�?😊');
        会话状�?:= 暂停;
      end;
  end;
end;
```

### 原则二：懂我优先，结果其�?(Understanding First, Results Second)

用户要的不是“你帮我做了什么”，而是“你懂了我要什么”�?

```
                    用户的真实需求层�?

                         被理�?
                           �?
                          ┏┻�?
                          �? �? �?最渴望的：“你懂我�?
                          ┗┳�?
                          ┌┼�?
                          �? �? �?其次：“你在乎我�?
                          └┬�?
                        ┌──┼──�?
                        �?   �? �?然后：“你能帮我�?
                        └──┬──�?
                      ┌────┼────�?
                      �?     �? �?最后：“拿到结果�?
                      └─────────�?
```

**“懂我”的三个层次**�?

| 层次 | 表现 | 系统实现 |
|------|------|----------|
| **听懂�?* | 准确复述用户意图 | “我理解您想�?..�?|
| **记住�?* | 用户不用重复�?| “您之前说过...�?|
| **理解�?* | 知道用户没说的部�?| “您可能还需�?..�?|

**具体策略**�?

#### 1. 先共鸣，再做�?

```
�?差体验：
用户�?我想做个简单的网站"
系统：[直接开始生成代�?..]

�?好体验：
用户�?我想做个简单的网站"
系统�?简单的网站，让人感觉清爽不累对吧？您是想展示什么呢�?
        [个人作品集] [公司介绍] [卖东西] [先随便看看]
```

#### 2. 允许发泪，不急着解决

```
用户�?这个功能太复杂了，我不知道怎么�?

�?差体验：
系统�?我来帮您做，第一�?.." �?急着解决问题

�?好体验：
系统�?复杂的功能确实让人头疼。您先告诉我哪里卡住了？
        或者我们慢慢来，一点一点拆开看？" �?先共情，再解�?
```

#### 3. 记住用户的偏好，不要每次都问

```pascal
// 记忆官不只记事实，也记偏�?
type
  T用户偏好 = record
    喜欢的风�? string;       // "简�?
    常用的配�? string;       // "蓝白"
    说话的习�? string;       // "干脆利落"
    工作时段: string;         // "晚上"
    情绪特征: string;         // "着急时简短句�?
  end;

// 用户第五次说"简单点"，系统应该记住了
记忆�?学习偏好('简单点', +1);
// 下次默认就用简约风�?
```

#### 4. 感知用户情绪，调整交互方�?

```
情绪信号                          系统调整
────────────────────────────────────────────────────
输入很短、有错别�?      �?    用户可能着急，别追问太�?
夜里还在�?               �?    用户可能很累，简化流�?
连续删除重来              �?    用户可能不满意，主动问问�?
长时间没操作              �?    用户可能在想，别�?
```

```pascal
// 情绪感知
function T记忆�?感知用户情绪: T情绪状�?
begin
  if 输入平均长度 < 5 then
    Result.标记('可能着�?);
  if 当前时间 > 23�?then
    Result.标记('可能累了');
  if 连续撤销次数 > 3 then
    Result.标记('可能不满�?);
end;
```

### 原则三：软硬关卡分层 (Soft/Hard Checkpoint Layers)

不是所有关卡都要强制，区分“底线”和“锦上添花”�?

| 关卡类型 | 能否跳过 | 跳过后果 | 示例 |
|----------|----------|----------|------|
| **硬关�?* | 绝对不可 | - | 安全检查、Schema验证 |
| **软关�?* | 可跳�?| 记录风险、后续提�?| 预览确认、影响分�?|

```pascal
// 软关卡：允许跳过，但温柔提醒
procedure T总导�?预览确认(const 内容: T内容);
begin
  显示预览(内容);
  
  case 等待用户响应 of
    确认: 
      继续流程;
    跳过: 
      begin
        记录日志('用户跳过预览');
        添加后续提醒('您跳过了预览，如果有问题随时�?);
        继续流程;  // 允许跳过
      end;
  end;
end;

// 硬关卡：绝对不能跳过
procedure T总导�?守卫检�?const 内容: T内容);
begin
  // 没有跳过选项，代码结构强制执�?
  守卫结果 := 守卫�?验证(内容);
  if not 守卫结果.通过 then
    raise E质量不达�?Create(守卫结果.原因);
end;
```

### 原则四：温度表达，不要冰�?(Warm Expression)

同样的信息，不同的表达，感受完全不同�?

| 场景 | 冰冷表达 �?| 温暖表达 �?|
|------|-------------|-------------|
| 等待 | "请稍�?.." | "正在为您准备..." |
| 错误 | "Error: Invalid input" | "我没太理解，您是�?..�? |
| 完成 | "操作完成" | "搞定�?�? |
| 中断 | "Session timeout" | "我先保存着，您随时回来 😊" |
| 失败 | "操作失败" | "没成功，要不试试别的方式�? |
| 重复 | "您已说过该内�? | "您说的跟之前那个有点像，是想调整还是新做一个？" |

### 原则五：情绪闭环，不只是任务闭环 (Emotional Closure)

任务完成不等于用户满意。要让用户的情绪也“闭环”�?

```
任务闭环�?                         情绪闭环�?
───────────────────────────────────────────────────────
开�?�?执行 �?完成              困惑 �?被理�?�?有进�?�?成就�?
                                     �?        �?       �?
                                   认同情绪   小步前进   庆祝时刻
```

**具体实现**�?

```
1. 认同困惑
   用户�?这个好难�?
   系统�?确实有点绕，我们一起拆开看看"

2. 小步前进
   系统�?这部分完成了 ✔，接下来简单了..."

3. 庆祝时刻
   系统�?太棒了！整个功能都做完了 🎉"
```

### 原则总结

```
┌─────────────────────────────────────────────────────────────────�?
�?                  人性化设计五原�?                               �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�? 1. 容忍中断   —�?用户随时可走，系统自动保�?                     �?
�?    Tolerate Interruption                                       �?
�?                                                                �?
�? 2. 懂我优先   —�?先共鸣再做事，记住偏�?                         �?
�?    Understanding First                                         �?
�?                                                                �?
�? 3. 软硬分层   —�?底线不退，锦上添花可�?                         �?
�?    Soft/Hard Layers                                            �?
�?                                                                �?
�? 4. 温度表达   —�?同样的信息，温暖的语言                          �?
�?    Warm Expression                                             �?
�?                                                                �?
�? 5. 情绪闭环   —�?不只完成任务，还要让人舒�?                     �?
�?    Emotional Closure                                           �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

**不变的底�?*�?
- 安全 —�?绝对不妥�?
- 可恢�?—�?绝对保证
- 可追�?—�?绝对记录

**可灵活的部分**�?
- 流程 —�?可中�?
- 确认 —�?可超�?
- 预览 —�?可跳�?
- 节奏 —�?可调�?

---

## 16. 所说即所见的核心设计

> 四仙论道：盘古、灵子、鲁班、仙�?

### 核心问题

```
用户觉得简�?+ 所说即所�?+ 系统能抽取结构化数据
```

这三者看似矛盾，实则是系统成败的关键�?

### 灵子（用户体验）的观�?

用户要的"简�?，是**心理上的简�?*，不是功能上的简单�?

```
用户说："我要做个简单的登录页面"

�?复杂感：
  系统：请选择认证方式、数据库类型、密码加密算�?..
  
�?简单感�?
  系统：好的，登录页面。这样可以吗�?
  [直接显示一个登录页面的样子]
  用户：嗯，但是要蓝色�?
  系统：[页面变成蓝色]
```

**核心洞察**�?
> **用户不是来做选择题的，是来看结果的�?*
> 
> 让用�?挑毛�?比让用户"出主�?容易100倍�?

### 鲁班（技术实现）的观�?

要实�?所说即所�?，需要在后台做大量结构化工作。关键是**什么时候做、怎么做，用户不知�?*�?

```
用户看到�?                     系统在做�?
─────────────────────────────────────────────────────────────
"做个登录页面"                �?识别意图：创建页�?
                              �?识别类型：登�?
                              �?调用模板：login_page
                              �?填充默认值：标准配色、标准布局
                              
[看到一个登录页面]            �?渲染预览
                              
"蓝色�?                      �?解析：修改配�?
                              �?定位：主题色
                              �?值：blue
                              �?更新状�?
                              
[页面变蓝]                    �?重新渲染
```

**核心洞察**�?
> **用户只管说，系统偷偷翻译成结构化数据�?*
> 
> 这个"翻译"过程要快到用户感觉不到�?

### 仙儿（界面表现）的观�?

"所说即所�?的关键是**即时反馈**。用户说完话�?.5秒内必须有响应�?

```
时间线：
───────────────────────────────────────────────────────
0ms     用户说完"蓝色�?
100ms   系统显示"正在变蓝..."（哪怕还没真的变�?
300ms   页面开始变色（渐变动画�?
500ms   变色完成
1000ms  系统�?蓝色的登录页，还要调整什么？"
```

**核心洞察**�?
> **宁可先展�?假的"即时反馈，也不能让用户等"真的"结果�?*
> 
> 人对时间的感知是主观的，有反馈就不觉得慢�?

**未来优化（待AI API速度提升后）**�?

仙儿提出了一个更激进的想法—�?边听边做"，即用户还在说话时就开始渲染。但由于当前AI API速度限制，暂不实现，作为未来优化方向记录�?

```
未来激进模式（待实现）�?
用户正在输入�?做个登录..."
0.1秒：系统已经猜到可能�?登录页面"
       [立刻展示一个模糊的页面轮廓]
       
用户继续输入�?...页面，蓝色的"
0.3秒：[轮廓变成登录页面的样子]
0.5秒：[页面开始变蓝]

1秒：用户说完
     [完整的蓝色登录页面已经显示]
```

### 盘古（架构整合）的总结

#### 核心架构

```
┌─────────────────────────────────────────────────────────────────�?
�?                   所说即所见的核心架构                            �?
�?                 "Say & See" Architecture                        �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�? 用户层（只管说、只管看�?                                         �?
�? ┌───────────────────────────────────────────────────────�?    �?
�? �? "做个登录页面"  �? [看到登录页面]  �? "蓝色�?  �? [变蓝]  �?    �?
�? └───────────────────────────────────────────────────────�?    �?
�?                             ↑↓                                  �?
�? ══════════════════════════════════════════════════════════  �?
�?                        魔法发生的地�?                           �?
�? ══════════════════════════════════════════════════════════  �?
�?                             ↑↓                                  �?
�? 转换层（自然语言 �?结构化数据）                                   �?
�? ┌───────────────────────────────────────────────────────�?    �?
�? �? 意图解析 �?实体提取 �?状态更�?�?差异计算 �?渲染指令      �?    �?
�? └───────────────────────────────────────────────────────�?    �?
�?                             ↑↓                                  �?
�? 数据层（纯结构化，用户不可见�?                                   �?
�? ┌───────────────────────────────────────────────────────�?    �?
�? �? { type: "page", subtype: "login", theme: "blue", ... }     �?    �?
�? └───────────────────────────────────────────────────────�?    �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

#### "所说即所�?的四个关�?

```
┌─────────────────────────────────────────────────────────────────�?
�?              "所说即所�?的四个关�?                              �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�? 1. 【先猜后确认�?                                               �?
�?    不问用户要什么，猜一个给他看，让他说对不�?                    �?
�?    Guess first, confirm later                                  �?
�?                                                                �?
�? 2. 【即时反馈�?                                                 �?
�?    用户说完0.5秒内必须有响应，哪怕是"假的"反馈                    �?
�?    Immediate feedback within 0.5s                              �?
�?                                                                �?
�? 3. 【渐进显影�?                                                 �?
�?    先出轮廓，再填细节，让用户看�?正在�?                         �?
�?    Progressive rendering: skeleton �?details                   �?
�?                                                                �?
�? 4. 【温柔纠错�?                                                 �?
�?    理解错了不要紧，提供选项让用户轻松纠�?                        �?
�?    Gentle correction with options                              �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

#### 温柔纠错机制

当AI理解错误时，不要让用户觉得是系统的错，而是给选项让用户轻松纠正：

```
场景：AI理解错了

�?差体验：
用户�?蓝色�?
系统：[把文字变成蓝色]  �?理解错了
用户：不是这个意思！😠

�?好体验：
用户�?蓝色�?
系统：[把整个页面变成蓝色主题]
      "把主题色改成蓝色了，是这个意思吗�?
      [是] [不是，我要改文字颜色] [不是，我�?..]
```

```pascal
// 每次理解都带置信�?
type
  T理解结果 = record
    意图: string;
    置信�? Double;  // 0-1
    备选理�? TArray<string>;  // 其他可能的理�?
  end;

// 置信度低时，主动确认
procedure T总导�?处理用户输入(const 输入: string);
var
  理解: T理解结果;
begin
  理解 := AI代理.理解意图(输入);
  
  if 理解.置信�?< 0.8 then
    // 不确定时，先做最可能的，同时展示备�?
    执行并展示备�?理解)
  else
    // 确定时，直接�?
    执行(理解);
end;
```

#### 核心本质

> **用户只管"说人�?，系统在背后"做翻�?�?*
> 
> 用户看到的是魔法，系统做的是工程�?
> 
> 翻译的结果就是结构化数据——用户不需要知道它存在�?

### 草稿箱与上下文推�?

> 四仙论道�?

#### 问题：用户改变主意时怎么办？

```
用户�?做个登录页面"
系统：[展示登录页面]
用户�?蓝色�?
系统：[变蓝]
用户�?加个微信登录"
系统：[加了微信登录按钮]
用户�?算了，不做登录了，做个商品列表页"  �?推翻�?
```

之前的结构化数据怎么办？

#### 解决方案：草稿箱机制

**1. 草稿保留策略**

| 策略 | 说明 |
|--------|------|
| **默认保留** | 推翻时自动存为草稿，不丢�?|
| **用户主动删除** | 用户�?把那个删�?才删�?|
| **超时自动清理** | 默认1个月无交互则清理（可配置�?|

**2. "算了"的意图处理——不猜，给选项**

```
用户�?算了，不做登录了，做个商品列表页"

系统�?好的，您想换方向。登录页怎么处理�?
      
      [保存草稿，以后继续]  �?登录页存着，先做商品列�?
      [不要了，直接删除]    �?登录页彻底不要了
      [替换成商品列表]      �?这个项目只做商品列表
```

**3. "还是做登录吧"的处理——问用户要继续还是重�?*

```
用户�?还是做登录吧"

系统�?您之前有个登录页草稿（蓝�?微信登录），要继续那个还是重新做�?
      
      [继续之前的]  �?恢复草稿
      [重新做一个]  �?从头开�?
```

**4. 草稿箱对用户可见**

用户需要知道草稿箱的存在，界面上要有入口：

```
┌─────────────────────────────────────────────────────────────────�?
�? 📁 我的草稿 (2)                                    [展开/收起]   �?
├─────────────────────────────────────────────────────────────────�?
�? · 登录页（蓝色+微信登录�?          10分钟�?   [继续] [删除]    �?
�? · 用户中心�?                       昨天        [继续] [删除]    �?
└─────────────────────────────────────────────────────────────────�?
```

**5. 用户偏好跨草稿保�?*

即使推翻了项目，用户的偏好应该记住：

```pascal
type
  T用户偏好 = record
    喜欢的颜�? string;     // 用户多次选蓝�?�?记住
    风格偏好: string;       // 用户喜欢简�?�?记住
    常用组件: TStringList; // 用户常加微信登录 �?记住
  end;

// 新建项目时，偏好自动应用为默认�?
procedure T记忆�?应用用户偏好(var 需求状�? T需求状�?;
begin
  if 需求状�?主题�?未指�?then
    需求状�?主题�?:= 用户偏好.喜欢的颜�?  // 默认用蓝�?
end;
```

#### 草稿箱数据结�?

```pascal
type
  T草稿 = record
    ID: string;
    标题: string;           // "登录�?
    摘要: string;           // "蓝色+微信登录"
    创建时间: TDateTime;
    最后修�? TDateTime;
    需求状�? T需求状�?   // 完整的结构化数据
    对话历史: TArray<T消息>;  // 可选，保留对话上下�?
  end;

  T草稿�?= class
    草稿列表: TArray<T草稿>;
    超时天数: Integer = 30;  // 默认1个月
    
    procedure 保存草稿(const 草稿: T草稿);
    procedure 删除草稿(const ID: string);
    function 恢复草稿(const ID: string): T需求状�?
    procedure 清理过期草稿;  // 定期执行
  end;
```

#### 设计原则

> **不丢弃用户的任何努力，哪怕他自己�?算了"�?*
> 
> 用户�?算了"可能只是一时的想法，系统要帮他留后路�?

---

## 17. 核心目标与质量监�?

### 核心目标

```
核心目标三角�?

            自然表达
              �?
             / \
            /   \
           / �?  \    �?核心：可靠转�?
          /       \
         ▼─────────�?
      可用结果    用户掌控
```

- **自然表达**：随便说、随便指、随便画
- **可用结果**：能看、能用、能�?
- **用户掌控**：知道系统在干什么、可确认、可控制
- **可靠转换**：在“自然”和“精确”之间建立可靠的桥梁

### 质量监督三方模型

交互过程中的质量由三方协同监督：

| 监督�?| 负责什�?| 怎么负责 | 特点 |
|--------|----------|----------|------|
| **AI监督�?* | 内容质量 | 自检Prompt，发现问题自我修�?| 柔性、智能、可能漏检 |
| **系统守卫** | 格式/安全 | 程序化验证，不通过则拒�?| 硬性、可靠、不可绕�?|
| **用户裁判** | 最终满意度 | 确认界面，决定是否接�?| 最终权威、不过度打扰 |

### 质量监督工作流程

```
用户输入
    �?
    �?
┌─────────────────────────�?
�?AI生成 + AI自检         �?
�?�?生成内容               �?
�?�?自我检查完整性、正确�?  �?
�?�?发现问题则自我修�?    �?
└────────────┬────────────�?
             �?
             �?
┌─────────────────────────�?
�?系统守卫检�?            �?
�?�?Schema验证 �?不通过 �?重生�?
�?�?代码验证 �?不通过 �?自动修复
�?�?安全检�?�?不通过 �?拒绝
└────────────┬────────────�?
             �?全部通过
             �?
┌─────────────────────────�?
�?用户确认                 �?
�?�?展示AI理解（复述）       �?
�?�?展示生成结果（预览）     �?
�?�?用户决定：确�?调整/重来 �?
└─────────────────────────�?
```

### 质量监督关键原则

1. **AI不信任自�?*：AI生成后必须自检，不能盲目自�?
2. **系统不信任AI**：即使AI说“我检查过了”，系统也要再验�?
3. **用户不需要懂技�?*：质量问题在到达用户之前就应该被拦截
4. **宁可降级不可出错**：检查不通过时降级处理，而非展示错误结果

---

## 18. 设计检查清�?

### 用户体验检�?

- [ ] 用户第一次用�?秒内能开始核心操作吗�?
- [ ] 完成主任务需要几步？能砍掉一步吗�?
- [ ] 能用选择代替输入吗？能智能填充吗�?
- [ ] 每个操作后用户知道发生了什么吗�?
- [ ] 误操作了能回退吗？
- [ ] 下次来还要重新设置吗�?

### AI质量检�?

- [ ] AI输出是否有Schema约束�?
- [ ] 输出是否经过验证流水线？
- [ ] 是否支持流式输出�?
- [ ] 异常时是否有降级方案�?
- [ ] 是否有上下文记忆�?

### 可用性检�?

- [ ] 生成的设计是否可预览�?
- [ ] 生成的代码是否可运行�?
- [ ] 是否有明确的下一步操作？
- [ ] 是否可修改？
- [ ] 是否可回退�?

### 质量监督检�?

- [ ] AI自检机制是否实现�?
- [ ] 系统守卫是否覆盖所有输出？
- [ ] 用户确认流程是否完善�?
- [ ] 三方协同是否顺畅�?

---

## 19. 开发任务清�?

### 第一阶段：基础架构

- [ ] **核心状态管�?*
  - [ ] 需求状态机实现
  - [ ] 流程状态机实现
  - [ ] 快照系统实现

- [ ] **AI编排�?*
  - [ ] 意图理解模块
  - [ ] 上下文管理模�?
  - [ ] 任务路由模块

- [ ] **质量保障�?*
  - [ ] AI自检模块
  - [ ] 系统守卫模块（Schema验证、代码验证）
  - [ ] 降级处理模块

### 第二阶段：用户界�?

- [ ] **输入�?*
  - [ ] 对话式输入组�?
  - [ ] 语音输入支持
  - [ ] 智能推荐组件

- [ ] **显影�?*
  - [ ] 流式渲染�?
  - [ ] 预览模式
  - [ ] 设计模式
  - [ ] 代码模式

- [ ] **反馈系统**
  - [ ] 实时状态显�?
  - [ ] 进度指示�?
  - [ ] 确认对话�?

- [ ] **历史时间�?*
  - [ ] 快照可视�?
  - [ ] 一键回退
  - [ ] 分支功能

### 第三阶段：AI服务

- [ ] **LLM服务**
  - [ ] 意图解析接口
  - [ ] 设计生成接口
  - [ ] 流式输出支持

- [ ] **代码生成服务**
  - [ ] 组件代码生成
  - [ ] 语法检�?
  - [ ] 运行测试

- [ ] **上下文记忆服�?*
  - [ ] Embedding服务
  - [ ] 向量数据库集�?
  - [ ] 上下文检�?

### 第四阶段：优化与扩展

- [ ] **多模态支�?*
  - [ ] 图片输入解析
  - [ ] 指点+语音协同

- [ ] **学习系统**
  - [ ] 用户偏好学习
  - [ ] 词汇学习

- [ ] **协作模式**
  - [ ] 实时同步
  - [ ] 版本分支

---

## 20. 一句话总结

> 最好的设计是：用户**随口一�?*，系�?*心领神会**，结�?*一点就�?*，体�?*越用越懂�?*�?

---

*文档创建�?2025-11-29*
