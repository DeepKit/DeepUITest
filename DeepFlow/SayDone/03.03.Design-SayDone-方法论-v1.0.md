# 方法论：需求到结构化数�?

> �?DevDirector 自然语言交互系统设计中提炼的通用方法�?
> 
> 核心目标：让用户"说人�?，系�?懂意�?，结�?看得�?

---

## 1. 核心公式

```
结构化抽�?= 领域识别 + 模板激�?+ 增量修改 + 超模板AI补充
```

### 公式解读

| 步骤 | 做什�?| 用户感知 |
|------|--------|---------|
| **领域识别** | 从用户输入识�?这是什么类型的东西" | 无感 |
| **模板激�?* | 加载该类型的默认结构和默认�?| "系统好像懂我" |
| **增量修改** | 用户每句话映射到字段变更 | "说什么变什�? |
| **超模板补�?* | 模板没有的，AI推断+用户确认 | "它还想到了我没想到的" |

---

## 2. 三层架构

```
┌─────────────────────────────────────────────────────────────────�?
�? 用户�?User Layer                                               �?
�? 用户只管"说人�?�?看结�?�?挑毛�?                              �?
└─────────────────────────────────────────────────────────────────�?
                              �?翻译
┌─────────────────────────────────────────────────────────────────�?
�? 转换�?Translation Layer                                        �?
�? 意图解析 �?领域识别 �?模板匹配 �?字段映射 �?差异计算 �?渲染指令    �?
└─────────────────────────────────────────────────────────────────�?
                              �?存取
┌─────────────────────────────────────────────────────────────────�?
�? 数据�?Data Layer                                               �?
�? 结构化数据（用户不可见）                                          �?
�? { "type": "...", "fields": {...}, "确定�?: {...}, "默认�?: {...} } �?
└─────────────────────────────────────────────────────────────────�?
```

---

## 3. 领域模板设计（通用结构�?

### 3.1 模板定义结构

```typescript
interface DomainTemplate {
  // 模板标识
  id: string;                    // "page.login", "api.crud", "table.user"
  domain: string;                // 所属领�? "ui", "api", "database", "workflow"
  type: string;                  // 具体类型: "login", "crud", "user"
  
  // 识别规则
  keywords: string[];            // 触发关键�? ["登录", "login", "sign in"]
  patterns: RegExp[];            // 正则模式
  
  // 结构定义
  schema: FieldDefinition[];     // 字段定义列表
  
  // 默认�?
  defaults: Record<string, any>; // 90%用户会选的�?
  
  // 约束规则
  constraints: Constraint[];     // 字段间约束关�?
  
  // 关联模板
  related: string[];             // 可能一起用的模�?
}

interface FieldDefinition {
  name: string;                  // 字段�?
  type: FieldType;               // 类型: string, number, enum, object, array
  required: boolean;             // 是否必填
  default?: any;                 // 默认�?
  options?: any[];               // 枚举选项
  description: string;           // 用于AI理解的描�?
  mappingKeywords: string[];     // 什么词会触发修改这个字�?
}
```

### 3.2 模板示例：登录页

```json
{
  "id": "page.login",
  "domain": "ui",
  "type": "login",
  "keywords": ["登录", "login", "登入", "sign in"],
  "schema": [
    {
      "name": "theme",
      "type": "enum",
      "options": ["blue", "green", "red", "dark"],
      "default": "blue",
      "mappingKeywords": ["颜色", "主题", "�?, "�?, "�?]
    },
    {
      "name": "authMethods",
      "type": "array",
      "default": ["password"],
      "options": ["password", "wechat", "phone", "email"],
      "mappingKeywords": ["微信", "手机", "邮箱", "密码"]
    },
    {
      "name": "rememberMe",
      "type": "boolean",
      "default": true,
      "mappingKeywords": ["记住", "自动登录"]
    },
    {
      "name": "forgotPassword",
      "type": "boolean",
      "default": true,
      "mappingKeywords": ["忘记密码", "找回"]
    }
  ],
  "defaults": {
    "theme": "blue",
    "authMethods": ["password"],
    "rememberMe": true,
    "forgotPassword": true
  },
  "related": ["page.register", "page.forgotPassword"]
}
```

### 3.3 模板示例：CRUD API

```json
{
  "id": "api.crud",
  "domain": "api",
  "type": "crud",
  "keywords": ["接口", "API", "增删改查", "CRUD"],
  "schema": [
    {
      "name": "resource",
      "type": "string",
      "required": true,
      "mappingKeywords": ["用户", "订单", "商品", "文章"]
    },
    {
      "name": "operations",
      "type": "array",
      "default": ["create", "read", "update", "delete", "list"],
      "mappingKeywords": ["创建", "查询", "修改", "删除", "列表"]
    },
    {
      "name": "pagination",
      "type": "boolean",
      "default": true,
      "mappingKeywords": ["分页", "翻页"]
    },
    {
      "name": "filter",
      "type": "boolean",
      "default": true,
      "mappingKeywords": ["筛�?, "过滤", "搜索"]
    },
    {
      "name": "softDelete",
      "type": "boolean",
      "default": false,
      "mappingKeywords": ["软删�?, "假删�?, "逻辑删除"]
    }
  ]
}
```

---

## 4. 字段映射机制

### 4.1 映射规则

用户说一句话，系统需要判断：

1. **操作类型**：添�?add) / 修改(modify) / 删除(remove) / 查询(query)
2. **目标字段**：改的是哪个字段
3. **目标�?*：改成什么�?

```typescript
interface MappingResult {
  operation: 'add' | 'modify' | 'remove' | 'query';
  field: string;
  value: any;
  confidence: number;  // 0-1
}

// 示例
用户�?蓝色�?
�?{ operation: 'modify', field: 'theme', value: 'blue', confidence: 0.95 }

用户�?加个微信登录"
�?{ operation: 'add', field: 'authMethods', value: 'wechat', confidence: 0.92 }

用户�?不要忘记密码"
�?{ operation: 'remove', field: 'forgotPassword', value: false, confidence: 0.88 }
```

### 4.2 映射策略

| 策略 | 说明 | 示例 |
|------|------|------|
| **关键词直接匹�?* | 字段有明确关键词 | "蓝色"→theme=blue |
| **同义词扩�?* | 预置同义词表 | "简�?="简�?="清爽"→style=minimal |
| **上下文推�?* | 结合前文理解 | "把它变大"→根据上文确�?�?是什�?|
| **AI补充** | 超出模板的，问AI | "要高大上"→AI推断具体配置 |

### 4.3 置信度处�?

```
置信�?�?0.9  �?直接执行，无需确认
置信�?0.7-0.9 �?执行但展示备选："我理解为...，对吗？[是] [不是，我�?..]"
置信�?< 0.7  �?展示选项让用户选："您是�?..？[选项A] [选项B] [其他]"
```

---

## 5. 需求状态管�?

### 5.1 状态结�?

```typescript
interface RequirementState {
  // 基本信息
  id: string;
  templateId: string;           // 使用的模�?
  
  // 三类字段�?
  确定�? Record<string, any>;  // 用户明确说过�?
  默认�? Record<string, any>;  // 模板默认值（用户未改�?
  删除�? string[];             // 用户明确�?不要"�?
  
  // 元信�?
  completeness: number;         // 完整�?0-1
  missingRequired: string[];    // 缺少的必填项
}
```

### 5.2 状态更新流�?

```
用户输入
    �?
领域识别 �?是否切换模板�?
    �?�?         �?�?
字段映射      新建状�?保存旧状态为草稿
    �?
更新状�?
    �?
检查完整度
    �?完整           �?不完�?
可以生成/渲染    追问缺失�?
```

---

## 6. 交互设计原则

### 6.1 核心原则

| 原则 | 说明 | 实现 |
|------|------|------|
| **先猜后确�?* | 给用户看结果，而非让用户描�?| 立刻展示默认模板 |
| **即时反馈** | 0.5秒内必须有响�?| 流式渲染、骨架屏 |
| **渐进显影** | 先出轮廓，再填细�?| 分阶段渲�?|
| **温柔纠错** | 理解错了提供选项，不让用户尴�?| 备选理解列�?|
| **不丢弃努�?* | 用户�?算了"时自动保存草�?| 草稿箱机�?|

### 6.2 反馈时机

| 时机 | 反馈内容 | 时限 |
|------|---------|------|
| 用户输入�?| 关键词高�?| <100ms |
| 意图理解完成 | "我理解您想要..." | <500ms |
| 开始生�?| 骨架屏出�?| 即时 |
| 生成过程 | 流式填充内容 | 实时 |
| 生成完成 | 完成动画 + 下一步引�?| <100ms |

---

## 7. 适用场景判断

### 7.1 适用的场景特�?

- �?**领域可枚�?* �?能识别出"这是什么类型的东西"
- �?**有模板可�?* �?该类型有标准化的结构/套路
- �?**增量可映�?* �?用户的每句话能对应到具体字段变更
- �?**默认值合�?* �?90%用户的选择是相似的

### 7.2 适用场景列表

| 场景 | 领域识别 | 模板化程�?| 增量映射 |
|------|----------|-----------|---------|
| UI/页面设计 | �?页面类型有限 | �?�?| �?容易 |
| API设计 | �?CRUD模式 | �?�?| �?容易 |
| 数据库设�?| �?实体类型 | �?中高 | �?容易 |
| 工作流设�?| �?流程类型 | �?�?| �?中等 |
| 报表设计 | �?报表类型 | �?�?| �?容易 |
| 测试用例 | �?测试类型 | �?�?| �?容易 |
| 配置管理 | �?配置类型 | �?�?| �?容易 |
| 文档生成 | �?文档类型 | �?�?| ⚠️ 中等 |
| 智能表单 | �?字段类型 | �?�?| �?容易 |

### 7.3 不太适用的场�?

| 场景 | 原因 |
|------|------|
| 创意写作 | �?模板"，内容完全开�?|
| 自由绘画 | 结构化程度低 |
| 策略决策 | 需要推理而非模板匹配 |
| 复杂算法 | 逻辑难以模板�?|

---

## 8. 实现路径

### 8.1 最小可行版�?(MVP)

1. **选一个垂直领�?*（如：UI页面设计�?
2. **�?-10个核心模�?*（登�?注册/列表/详情/表单�?
3. **实现基础映射**（颜�?布局/组件增删�?
4. **简单的即时渲染**（静态预览）

### 8.2 逐步扩展

```
MVP (1个领�?
    �?
多领�?(UI + API + DB)
    �?
领域联动 (设计API自动影响DB)
    �?
AI增强 (超模板能�?
    �?
学习系统 (用户偏好)
```

---

## 9. 质量保障

### 9.1 三方监督模型

| 监督�?| 负责 | 特点 |
|--------|------|------|
| AI自检 | 内容质量 | 柔性、智能、可能漏检 |
| 系统守卫 | 格式/安全 | 硬性、可靠、不可绕�?|
| 用户裁判 | 最终满意度 | 最终权�?|

### 9.2 关键检查点

- [ ] 模板匹配是否正确�?
- [ ] 字段映射是否准确�?
- [ ] 必填项是否完整？
- [ ] 约束规则是否满足�?
- [ ] 用户是否确认�?

---

## 10. 连接器层（效用最大化�?

> **结构化数据本身不产生价值，连接到行动才产生价值�?*

### 10.1 数据的五种效用出�?

| 出口类型 | 说明 | 示例 |
|----------|------|------|
| **生成** | 数据→代�?文档/配置 | 登录页结构→React代码 |
| **调用** | 数据→API/服务 | 请假结构→OA系统API |
| **存储** | 数据→数据库/知识�?| 客户信息→CRM |
| **展示** | 数据→可视化/报表 | 销售数据→Dashboard |
| **流转** | 数据→下一个系�?�?| 需求结构→Jira工单 |

### 10.2 连接器架�?

```
用户说话 �?结构化数�?�?连接器层 �?实际效用
                         �?
        ┌───────────────┼────────────────�?
        �?              �?               �?
        �?              �?               �?
   [代码生成器]    [API调用器]    [存储适配器]
   [可视化器]      [工作流引擎]   [导出器]
        �?              �?               �?
        �?              �?               �?
     React           Jira           MySQL
      代码             工单           数据
```

### 10.3 连接器接口定�?

```typescript
interface StructuredOutput {
  // 元信�?
  domain: string;        // "meeting", "leave", "page.login"
  templateId: string;    // 用了哪个模板
  confidence: number;    // 置信�?
  
  // 数据本体
  data: Record<string, any>;
  
  // 声明支持的出�?
  supportedOutputs: OutputType[];
}

type OutputType = 
  | 'code'        // 可生成代�?
  | 'api'         // 可调用API  
  | 'storage'     // 可存�?
  | 'visual'      // 可可视化
  | 'export'      // 可导�?JSON/CSV/PDF)
  | 'workflow';   // 可触发流�?

interface Connector {
  id: string;
  name: string;
  supportedDomains: string[];   // 支持哪些领域
  outputType: OutputType;
  
  // 执行连接
  execute(data: StructuredOutput): Promise<ConnectorResult>;
}

interface ConnectorResult {
  success: boolean;
  output: any;           // 生成的代�?返回的结�?
  error?: string;
  nextActions?: string[];  // 建议的下一步操�?
}
```

### 10.4 三层效用策略

| 层级 | 策略 | 成本 | 覆盖�?|
|------|--------|------|--------|
| **Layer 1** | 即时可用 �?导出JSON/Markdown/复制到剪贴板 | 0 | 100% |
| **Layer 2** | 领域适配 �?每个领域2-3个推荐出�?| �?| 80% |
| **Layer 3** | 深度集成 �?定制连接器对接用户系�?| �?| 20% |

### 10.5 不同领域的最佳出�?

| 领域 | 首选出�?| 次选出�?|
|------|---------|----------|
| **UI页面设计** | 代码生成(React/Vue) | 设计�?Figma) |
| **API设计** | 代码生成 + OpenAPI | Postman集合 |
| **数据库设�?* | DDL语句 | ER�?|
| **会议安排** | 日历事件 + 邮件邀�?| 会议纪要模板 |
| **请假申请** | OA系统API | 审批邮件 |
| **需求描�?* | Jira/禅道工单 | Markdown文档 |
| **菜谱** | 格式化文�?| 购物清单 |
| **旅行计划** | 日程�?+ 地图 | 预算�?|
| **简�?* | PDF导出 | 在线页面 |

### 10.6 用户选择出口界面示例

```
╔═════════════════════════════════════════════════════�?
�?                                                    �?
�? 用户�?下周三下�?点开产品评审会，邀请张三李�?       �?
�?                                                    �?
�? 系统识别�?                                         �?
�? ┌───────────────────────────────────────�?     �?
�? �?📅 会议安排                            �?     �?
�? �?时间：下周三 14:00                     �?     �?
�? �?主题：产品评审会                         �?     �?
�? �?参会人：张三、李�?                      �?     �?
�? └───────────────────────────────────────�?     �?
�?                                                    �?
�? 您想要？                                          �?
�? ┌────────�?┌────────�?┌────────�?┌────────�?�?
�? │�?发邮件│ │�?加日历│ │�?生成 �?│�?复制 �?�?
�? �? 邀�? �?�? 事件  �?�?议程模板�?�? JSON �?�?
�? └────────�?└────────�?└────────�?└────────�?�?
�?                                                    �?
╚═════════════════════════════════════════════════════�?
```

---

## 11. 通用化扩�?

### 11.1 适用领域（超越软件工程）

| 领域 | 有套路吗�?| 模板化程�?| 可用�?|
|------|-----------|----------|--------|
| **商业领域** | | | |
| 商业计划�?| �?市场/产品/财务/团队 | �?| �?很适合 |
| 营销方案 | �?目标/渠道/预算/时间�?| �?| �?很适合 |
| 招聘JD | �?职责/要求/待遇/福利 | �?| �?很适合 |
| **教育领域** | | | |
| 教案设计 | �?目标/重点/步骤/作业 | �?| �?很适合 |
| 考试出题 | �?题型/难度/知识�?分�?| �?| �?很适合 |
| 学习计划 | �?目标/阶段/资源/检�?| �?| �?适合 |
| **生活领域** | | | |
| 菜谱/烹饪 | �?食材/步骤/火�?时间 | �?| �?很适合 |
| 旅行规划 | �?日期/地点/交�?住宿 | �?| �?很适合 |
| 装修方案 | �?风格/预算/材料/工期 | �?| �?适合 |
| **专业领域** | | | |
| 法律文书 | �?有固定格式和条款 | 中高 | �?适合 |
| 科研proposal | �?背景/问题/方法/预期 | �?| �?适合 |
| 项目申报 | �?有严格的模板要求 | �?| �?很适合 |

> **判断标准：凡�?填表"能解决的事，就能用这套方法论。我们做的，就是�?填表"变成"聊天"�?*

### 11.2 GUI模板编辑器（元方法论�?

用这套方法论来配置方法论本身�?

```
用户�?帮我建个模板，用来做菜谱"
系统�?好的，菜谱通常包括：[食材] [步骤] [时间] [难度]，还要加什么？"
用户�?加个口味，比如麻辣、清�?
系统：[自动识别为枚举类型，填充选项]
系统�?模板创建完成！触发词�?做菜''食谱''菜谱'可以吗？"
```

### 11.3 AI渲染器（分级策略�?

| Level | 策略 | 速度 | 可靠�?| 适用场景 |
|-------|--------|------|--------|----------|
| **1** | 模板渲染 | 最�?| 最�?| 标准化場�?|
| **2** | 规则渲染 | �?| �?| 结构固定内容变化 |
| **3** | AI渲染 | �?| �?| 特殊需�?|
| **4** | AI+人工 | 最�?| 最灵活 | 高价值低频场�?|

> **底线原则：能用模板就用模板，模板不够才用AI。AI生成的必须经过验证�?*

---

## 12. 核心洞察

> **凡是"有套�?的工作，都可以用这套方法论�?*

> **让用�?挑毛�?比让用户"出主�?容易100倍�?*

> **系统的智能不在于"猜得�?，而在�?猜错了能优雅地让用户纠正"�?*

---

*文档提炼�?DevDirector 自然语言交互系统设计*
*创建日期: 2025-11-30*
