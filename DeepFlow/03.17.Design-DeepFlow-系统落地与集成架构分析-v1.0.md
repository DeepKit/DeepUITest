# UniFlow 系统落地与集成架构深度分�?

> **版本**: 1.0 | **更新日期**: 2025-12-04
> **定位**: 本文档是�?UniFlow 系统落地、MCP 深度利用、外部系统集成的综合分析�?
>
> **关联文档**�?
> - 落地策略：`06.12.Dev-UniFlow-落地实施策略与避坑指�?v1.0.md`
> - MCP 规划：`05.02.API-UniFlow-MCP支持规划-v1.0.md`
> - 外部集成：`06.08.Dev-UniFlow-外部MCP与Skill集成指南-v1.0.md`
> - MVP 指南：`06.11.Dev-UniFlow-MVP定义与实现指�?v1.0.md`

---

## 1. 核心命题

本文档回答三个关键问题：

1. **系统落地**：如何让 UniFlow 从设计走向可运行的系统？
2. **易于集成**：如何让外部系统能方便地使用 UniFlow 能力�?
3. **MCP 利用**：如何充分利�?MCP 协议实现能力互通？

---

## 2. 系统落地策略

### 2.1 最小可运行系统（MRS - Minimum Runnable System�?

**核心理念**：在 MVP 之前，先有一�?能跑起来"的系统�?

```
MRS 阶段目标�?
┌─────────────────────────────────────────────────────────────────�?
�?                   最小可运行系统                                 �?
├─────────────────────────────────────────────────────────────────�?
�? 1. Delphi 主程序能启动                                          �?
�? 2. 能发送消息到 Python 服务                                     �?
�? 3. Python 服务能调�?LLM 返回结果                               �?
�? 4. 结果能显示在 UI �?                                          �?
�? 5. 整个过程可追溯（有日志）                                     �?
└─────────────────────────────────────────────────────────────────�?
```

### 2.2 分层落地路线

```
阶段 0: 基础设施�?(Infrastructure)
├── Python 嵌入式环境打�?
├── HTTP 通信验证（Delphi <-> FastAPI�?
├── SQLite 数据库初始化
└── 配置管理（JSON/YAML�?

阶段 1: 核心运行时层 (Core Runtime)
├── 消息总线实现
├── 6 角色基础框架
├── Skill 注册与调�?
└── 基础日志审计

阶段 2: 流程执行�?(Workflow Engine)
├── 状态机实现
├── 顺序/条件执行
├── 状态持久化
└── 中断与恢�?

阶段 3: 业务应用�?(Application)
├── 洞见·决策流程
├── 多角色并行推�?
├── 记忆系统集成
└── UI 交互完善
```

### 2.3 落地风险与缓�?

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Python 环境问题 | �?| 使用 Embedded Python + 预打包依�?|
| LLM API 不稳�?| �?| 多后端容�?+ Mock 模式 |
| 状态恢复复�?| �?| 简化状态模型，只持久化关键节点 |
| 性能瓶颈 | �?| 异步执行 + 连接�?+ 缓存 |
| 调试困难 | �?| 完善日志 + Inspector 角色 |

### 2.4 落地检验标�?

**MRS 验收标准**�?
- [ ] 冷启动到可接受用户输�?< 5�?
- [ ] Delphi -> Python 往返延�?< 100ms（排�?LLM�?
- [ ] 异常情况有明确错误码和日�?
- [ ] 主进程崩溃不会丢失执行状�?

**MVP 验收标准**�?
- [ ] 完整的决策流程可执行
- [ ] 至少 3 个角色可并行推演
- [ ] HumanTask 可正确等待和恢复
- [ ] 重启�?Workflow 可从断点继续

---

## 3. 外部系统集成架构

### 3.1 集成模式分类

UniFlow 支持三种集成模式�?

```
┌─────────────────────────────────────────────────────────────────�?
�?                     集成模式分类                                �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�? 模式 A: UniFlow 作为被调用方（Provider�?                      �?
�? ┌─────────────────────────────────────────────────────────�?  �?
�? �? 外部系统 ──HTTP/MCP──> UniFlow API ──> 内部处理        �?  �?
�? �? - 外部系统发起请求                                     �?  �?
�? �? - UniFlow 提供 REST API / MCP Server                   �?  �?
�? �? - 适用：AI 能力输出、Workflow 执行服务                  �?  �?
�? └─────────────────────────────────────────────────────────�?  �?
�?                                                                �?
�? 模式 B: UniFlow 作为调用方（Consumer�?                        �?
�? ┌─────────────────────────────────────────────────────────�?  �?
�? �? UniFlow ──HTTP/MCP──> 外部 API/MCP Server              �?  �?
�? �? - UniFlow 主动调用外部能力                             �?  �?
�? �? - 外部能力封装�?Skill                                 �?  �?
�? �? - 适用：外部数据检索、第三方服务调用                    �?  �?
�? └─────────────────────────────────────────────────────────�?  �?
�?                                                                �?
�? 模式 C: 双向集成（Hub�?                                       �?
�? ┌─────────────────────────────────────────────────────────�?  �?
�? �? 外部系统 <──> UniFlow <──> 其他外部系统                �?  �?
�? �? - UniFlow 作为能力中枢                                 �?  �?
�? �? - 协调多个外部系统                                     �?  �?
�? �? - 适用：复杂业务编排、跨系统工作�?                    �?  �?
�? └─────────────────────────────────────────────────────────�?  �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

### 3.2 Provider 模式实现（UniFlow 被调用）

#### 3.2.1 REST API 暴露

```
UniFlow REST API 层次�?

/api/v1/
├── /workflow/                      # Workflow 相关
�?  ├── POST   /execute            # 执行 Workflow
�?  ├── GET    /{id}/status        # 查询状�?
�?  ├── POST   /{id}/resume        # 恢复执行
�?  └── DELETE /{id}               # 取消执行
�?
├── /skill/                         # Skill 相关
�?  ├── GET    /                   # 列出可用 Skill
�?  ├── GET    /{name}/schema      # 获取 Skill Schema
�?  └── POST   /{name}/execute     # 执行单个 Skill
�?
├── /session/                       # 会话相关
�?  ├── POST   /                   # 创建会话
�?  ├── POST   /{id}/message       # 发送消�?
�?  └── GET    /{id}/hiDeepDeepDeepDeepDeepStory       # 获取历史
�?
└── /health                         # 健康检�?
```

#### 3.2.2 认证与授�?

```
外部调用认证流程�?

1. API Key 认证（简单场景）
   Header: X-API-Key: <key>
   
2. OAuth 2.0 / JWT（企业场景）
   Header: Authorization: Bearer <token>

3. 授权检�?
   - 每个 API 绑定所需权限
   - 检查调用方权限是否满足
   - 记录审计日志
```

#### 3.2.3 限流与配�?

```pascal
// 限流配置示例
type
  TRateLimitConfig = record
    Endpoint: string;
    MaxRequestsPerMinute: Integer;
    MaxConcurrent: Integer;
    TokenBudgetPerDay: Integer;
  end;

const
  DEFAULT_LIMITS: array[0..2] of TRateLimitConfig = (
    (Endpoint: '/workflow/execute'; MaxRequestsPerMinute: 60; MaxConcurrent: 10; TokenBudgetPerDay: 100000),
    (Endpoint: '/skill/*/execute';  MaxRequestsPerMinute: 120; MaxConcurrent: 20; TokenBudgetPerDay: 50000),
    (Endpoint: '/session/*/message'; MaxRequestsPerMinute: 30; MaxConcurrent: 5; TokenBudgetPerDay: 200000)
  );
```

### 3.3 Consumer 模式实现（UniFlow 调用外部�?

#### 3.3.1 外部服务注册

```json
// external_services.json
{
  "services": [
    {
      "id": "notion-api",
      "name": "Notion API",
      "type": "rest",
      "base_url": "https://api.notion.com/v1",
      "auth": {
        "type": "bearer",
        "token_ref": "secrets://notion_api_token"
      },
      "rate_limit": {
        "requests_per_second": 3
      },
      "timeout_ms": 30000,
      "retry": {
        "max_attempts": 3,
        "backoff": "exponential"
      }
    },
    {
      "id": "github-mcp",
      "name": "GitHub MCP Server",
      "type": "mcp",
      "endpoint": "http://localhost:3001",
      "capabilities": ["tools", "resources"],
      "trust_level": "official"
    }
  ]
}
```

#### 3.3.2 Skill 封装模式

```python
# 外部服务封装�?Skill 的标准模�?
class ExternalServiceSkill(BaseSkill):
    def __init__(self, service_config: dict):
        self.service = ExternalServiceClient(service_config)
        
    async def execute(self, input: SkillInput) -> SkillOutput:
        try:
            # 1. 输入转换（内部格�?-> 外部格式�?
            external_request = self._transform_input(input)
            
            # 2. 调用外部服务
            external_response = await self.service.call(external_request)
            
            # 3. 输出转换（外部格�?-> 内部格式�?
            result = self._transform_output(external_response)
            
            return SkillOutput(success=True, data=result)
            
        except TimeoutError:
            return SkillOutput(success=False, error="EXTERNAL_TIMEOUT")
        except AuthError:
            return SkillOutput(success=False, error="EXTERNAL_AUTH_FAILED")
        except Exception as e:
            return SkillOutput(success=False, error=f"EXTERNAL_ERROR: {str(e)}")
```

### 3.4 集成点汇�?

| 集成方向 | 协议 | 场景 | 实现位置 |
|----------|------|------|----------|
| 外部 �?UniFlow | REST | 执行 Workflow | Commander |
| 外部 �?UniFlow | MCP | 调用 Tool | Python Service |
| UniFlow �?LLM | REST | AI 推理 | Advisor |
| UniFlow �?MCP Server | MCP | 外部工具 | SignalOfficer |
| UniFlow �?数据�?| SQLite | 状态持久化 | Chronicler |
| UniFlow �?文件 | File I/O | 日志/配置 | Logistics |

---

## 4. MCP 深度利用策略

### 4.1 MCP �?UniFlow 中的角色

```
MCP 定位：作�?能力桥梁"，而非"核心运行�?

┌─────────────────────────────────────────────────────────────────�?
�?                   UniFlow 能力架构                              �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�?  ┌─────────────────────────────────────────────────────────�? �?
�?  �?                对外接口�?                              �? �?
�?  �?  ┌───────────�? ┌───────────�? ┌───────────�?        �? �?
�?  �?  �?REST API  �? │MCP Server �? �? WebSocket �?        �? �?
�?  �?  └─────┬─────�? └─────┬─────�? └─────┬─────�?        �? �?
�?  └─────────┼──────────────┼──────────────┼────────────────�? �?
�?            �?             �?             �?                   �?
�?  ┌─────────┼──────────────┼──────────────┼────────────────�? �?
�?  �?        �?             �?             �?               �? �?
�?  �?  ┌─────────────────────────────────────────────────�?�? �?
�?  �?  �?         UniFlow Core (11 角色运行�?            �?�? �?
�?  �?  �?  Engine | Commander | Dispatcher | Guard | ...   �?�? �?
�?  �?  └───────────────────────┬─────────────────────────�?�? �?
�?  �?                          �?                           �? �?
�?  �?  ┌───────────────────────┼───────────────────────�?  �? �?
�?  �?  �?                Skill 执行�?                  �?  �? �?
�?  �?  �?  ┌─────────�? ┌─────────�? ┌─────────�?     �?  �? �?
�?  �?  �?  �?Delphi  �? �?Python  �? �?  MCP   �?     �?  �? �?
�?  �?  �?  �?Skills  �? �?Skills  �? �?Skills  �?     �?  �? �?
�?  �?  �?  └────┬────�? └────┬────�? └────┬────�?     �?  �? �?
�?  �?  └────────┼────────────┼────────────┼───────────�?  �? �?
�?  └────────────┼────────────┼────────────┼───────────────�? �?
�?               �?           �?           �?                   �?
�?  ┌────────────┼────────────┼────────────┼───────────────�? �?
�?  �?           �?           �?           �?              �? �?
�?  �?  ┌─────────────�?┌──────────�?┌─────────────────�? �? �?
�?  �?  �?DeepBase.LLM �?�?LiteLLM  �?�?External MCP    �? �? �?
�?  �?  �?DeepBase.DB  �?�?FastAPI  �?�?Servers         �? �? �?
�?  �?  └─────────────�?└──────────�?└─────────────────�? �? �?
�?  �?                外部能力�?                          �? �?
�?  └─────────────────────────────────────────────────────�? �?
�?                                                            �?
└─────────────────────────────────────────────────────────────────�?
```

### 4.2 MCP Server 实现�?.0 阶段�?

#### 4.2.1 暴露 UniFlow 能力�?MCP Tool

```python
# uniFlow_py/mcp_server.py
from mcp import MCPServer, Tool, Resource

class UniFlowMCPServer(MCPServer):
    def __init__(self, skill_registry):
        super().__init__()
        self.skill_registry = skill_registry
        self._register_tools()
    
    def _register_tools(self):
        """�?UniFlow Skills 自动转换�?MCP Tools"""
        for skill in self.skill_registry.list_all():
            meta = skill.get_meta()
            tool = Tool(
                name=f"uniflow_{meta.name}",
                description=meta.description,
                input_schema=meta.input_schema,
                # MCP 回调 -> Skill 执行
                handler=lambda input: self._execute_skill(meta.name, input)
            )
            self.register_tool(tool)
    
    async def _execute_skill(self, skill_name: str, input: dict) -> dict:
        """MCP Tool 调用转发�?Skill 执行"""
        skill = self.skill_registry.get(skill_name)
        result = await skill.execute(SkillInput(data=input))
        return result.data
```

#### 4.2.2 MCP 能力暴露策略

| 能力类型 | MCP 暴露 | 说明 |
|----------|----------|------|
| 只读查询 | �?优先 | 决策记录、会话历史、Workflow 状�?|
| 纯计�?| �?优先 | 文本分析、摘要、结构化抽取 |
| LLM 包装 | ⚠️ 受控 | 需要配额和安全审计 |
| Workflow 执行 | ⚠️ 受控 | 仅暴露预定义模板 |
| 写入操作 | �?禁止 | 不通过 MCP 暴露写入能力 |

### 4.3 MCP Client 实现（理想版本）

#### 4.3.1 接入外部 MCP Server

```python
# uniFlow_py/mcp_client.py
from mcp.client import MCPClient

class MCPSkillBridge:
    """将外�?MCP Tool 桥接�?UniFlow Skill"""
    
    def __init__(self, server_config: dict):
        self.client = MCPClient(server_config['endpoint'])
        self.server_id = server_config['id']
        
    async def discover_tools(self) -> list[SkillMeta]:
        """发现并转�?MCP Tools �?Skill 元数�?""
        tools = await self.client.list_tools()
        return [self._tool_to_skill_meta(t) for t in tools]
    
    async def execute_tool(self, tool_name: str, input: dict) -> dict:
        """执行 MCP Tool"""
        return await self.client.call_tool(tool_name, input)
    
    def _tool_to_skill_meta(self, tool: MCPTool) -> SkillMeta:
        """MCP Tool -> SkillMeta 转换"""
        return SkillMeta(
            name=f"mcp.{self.server_id}.{tool.name}",
            version="1.0",
            description=tool.description,
            input_schema=tool.input_schema,
            output_schema=tool.output_schema or {},
            source_type=SkillSourceType.MCP
        )
```

#### 4.3.2 MCP 安全边界

```
MCP 调用安全检查链�?

请求 -> Guard 输入检�?-> 配额检�?-> MCP 调用 -> Guard 输出检�?-> 返回

┌─────────────────────────────────────────────────────────────────�?
�?                   MCP 安全边界                                  �?
├─────────────────────────────────────────────────────────────────�?
�? 1. 输入检�?(Guard)                                            �?
�?    - 字段白名�?                                               �?
�?    - 敏感信息过滤（不能向外部泄露 L2/L3 数据�?                 �?
�?    - 大小限制                                                  �?
�?                                                                �?
�? 2. 配额检�?(Quartermaster)                                    �?
�?    - 每日调用次数                                              �?
�?    - Token 消耗预�?                                           �?
�?    - 并发限制                                                  �?
�?                                                                �?
�? 3. 输出检�?(Guard)                                            �?
�?    - 格式校验                                                  �?
�?    - 内容安全（敏感词、幻觉检测）                              �?
�?    - 大小限制                                                  �?
�?                                                                �?
�? 4. 审计记录 (Chronicler)                                       �?
�?    - 调用来源、时间、参数摘�?                                 �?
�?    - 结果摘要、耗时、Token 使用                                �?
�?    - 错误详情                                                  �?
└─────────────────────────────────────────────────────────────────�?
```

### 4.4 MCP 使用场景矩阵

| 场景 | MCP 角色 | 实现阶段 | 价�?|
|------|----------|----------|------|
| 外部 AI Agent 调用洞见能力 | Server | 1.0 | 能力输出 |
| IDE 插件调用代码分析 | Server | 1.0 | 开发者体�?|
| 接入 Notion/Confluence 检�?| Client | 理想 | 知识扩展 |
| 接入 GitHub Issues 管理 | Client | 理想 | 项目管理 |
| �?MCP Server 协调 | Hub | 理想 | 复杂编排 |

---

## 5. 易于集成的设计原�?

### 5.1 SDK 设计（让集成更简单）

```
目标：让外部系统 5 分钟内完成基础集成

Python SDK 示例�?
```python
from uniflow import UniFlowClient

# 初始�?
client = UniFlowClient(
    base_url="http://localhost:8080",
    api_key="your-api-key"
)

# 执行 Workflow
result = client.workflow.execute(
    workflow_id="DeepDeepDeepDeepDeepInsight-decision-flow",
    input={"question": "是否应该扩张业务�?}
)

# 发送对�?
response = client.session.send_message(
    session_id="session-001",
    message="帮我分析一下市场趋�?
)

# 调用 Skill
analysis = client.skill.execute(
    skill_name="text_analysis",
    input={"text": "..."}
)
```

### 5.2 Webhook 支持（异步通知�?

```json
// Webhook 配置
{
  "webhooks": [
    {
      "event": "workflow.completed",
      "url": "https://your-server.com/webhook",
      "secret": "webhook-secret",
      "retry": {
        "max_attempts": 3,
        "backoff_seconds": [5, 30, 300]
      }
    }
  ]
}

// Webhook 事件示例
{
  "event": "workflow.completed",
  "timestamp": "2025-12-04T12:00:00Z",
  "data": {
    "workflow_id": "wf-001",
    "instance_id": "wfi-001",
    "status": "completed",
    "result_summary": "决策完成，建议保守扩�?
  },
  "signature": "sha256=..."
}
```

### 5.3 插件架构（扩展点�?

```
UniFlow 插件扩展点：

┌─────────────────────────────────────────────────────────────────�?
�?                     插件类型                                    �?
├─────────────────────────────────────────────────────────────────�?
�? 1. Skill 插件                                                  �?
�?    - Python Skill（推荐）                                      �?
�?    - MCP Tool 封装                                             �?
�?    - 自定�?HTTP Service                                       �?
�?                                                                �?
�? 2. Workflow 模板插件                                           �?
�?    - YAML 定义的流程模�?                                      �?
�?    - 可打包发布到 Skill 市场                                   �?
�?                                                                �?
�? 3. 人格插件（Persona�?                                        �?
�?    - 自定�?AI 人格配置                                        �?
�?    - Prompt 模板�?                                            �?
�?                                                                �?
�? 4. UI 插件（理想版本）                                         �?
�?    - 自定义面�?                                               �?
�?    - 可视化组�?                                               �?
└─────────────────────────────────────────────────────────────────�?
```

---

## 6. 实施路线�?

### 6.1 阶段目标

```
Phase 1: 基础落地 (2 �?
├── MRS 完成
├── 6 角色框架就绪
├── 基础 REST API
└── 验收：Hello World 流程可运�?

Phase 2: MVP 完成 (6 �?
├── Workflow 引擎
├── 状态持久化
├── Python Skill 集成
├── 洞见核心流程
└── 验收：完整决策流程可执行

Phase 3: 对外开�?(1.0) (8 �?
├── REST API 完善
├── MCP Server 实现
├── 认证授权
├── 配额管理
└── 验收：外部系统可调用 UniFlow

Phase 4: 生态扩�?(理想版本)
├── MCP Client
├── Skill 市场
├── SDK 发布
└── 插件架构
```

### 6.2 关键里程�?

| 里程�?| 时间�?| 验证标准 |
|--------|--------|----------|
| MRS 就绪 | Week 2 | Delphi-Python 通信正常 |
| Workflow 基础可用 | Week 4 | 线性流程可执行 |
| MVP 完成 | Week 8 | 洞见核心流程完整 |
| API 开�?| Week 12 | 外部 HTTP 调用成功 |
| MCP Server | Week 16 | 外部 LLM 可调�?|

---

## 7. 总结

### 7.1 核心结论

1. **落地优先**：先有能跑的系统，再追求功能完善�?
2. **分层设计**：内部保持抽象，对外提供标准协议�?
3. **安全第一**：所有外部接口必须经过认证、授权、审计�?
4. **渐进开�?*：先�?Provider（被调用），再做 Consumer（调用外部）�?

### 7.2 行动�?

- [ ] 完成 MRS 基础设施搭建
- [ ] 定义 REST API 规范文档
- [ ] 实现 MCP Server 原型
- [ ] 设计 SDK 接口
- [ ] 规划 Webhook 事件体系

### 7.3 相关详细设计文档

| 文档 | 说明 |
|------|------|
| `06.14.Dev-UniFlow-IPC通信详细设计-v1.0.md` | Delphi-Python IPC 通信规范，含连接池、超时、熔断、Python 进程管理 |
| `05.09.API-UniFlow-安全规范-v1.0.md` | API 安全规范，含签名、重放防护、HTTPS |
| `01.02.Quick-UniFlow-开发者快速上手指�?v1.0.md` | 外部开发者集成指�?|

---

*文档版本: 1.1*
*更新日期: 2024-12-04*
*变更说明: 增加相关详细设计文档引用*
