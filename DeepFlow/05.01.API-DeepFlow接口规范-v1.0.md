# UniFlow 接口规范

> **版本**: 1.0 | **更新日期**: 2025-12-03
> 本文档定�?UniFlow 所有对外接口，覆盖 Delphi 内部接口�?Python Skill 服务 HTTP API

---

## 1. 接口分类

> 全局约定：每个对外可见接口都应在设计阶段明确�?
> - 可触达的最高数据等级（L0–L3）；
> - 典型执行主体类型（`user` / `project_runtime` / `platform_runtime`）�?
> 具体安全边界与运行时主体决策规则见《UniFlow-安全与数据权限策�?v1.0.md》“运行时主体与兜底规则”章节�?

### 1.1 接口层次

```
┌─────────────────────────────────────────────────────────────�?
�?                     接口层次结构                            �?
├─────────────────────────────────────────────────────────────�?
�?                                                            �?
�? ┌───────────────────────────────────────────────────────�?�?
�? �?            产品�?API（应用调用）                      �?�?
�? �? �?IUniFlow - 统一入口                                 �?�?
�? �? �?ISessionManager - 会话管理                          �?�?
�? �? �?IWorkflowRunner - 工作流运�?                       �?�?
�? └───────────────────────────────────────────────────────�?�?
�?                         �?                                 �?
�? ┌───────────────────────▼───────────────────────────────�?�?
�? �?            角色�?API（内部协作）                      �?�?
�? �? �?IEngine, IInspector - 元层                          �?�?
�? �? �?ICommander, IDispatcher - 决策�?                   �?�?
�? �? �?IAdvisor - 智力�?                                  �?�?
�? �? �?IExecutor, IGuard, IQuartermaster - 能力�?         �?�?
�? �? �?ILogistics, IChronicler, ISignalOfficer - 基础�?   �?�?
�? └───────────────────────────────────────────────────────�?�?
�?                         �?                                 �?
�? ┌───────────────────────▼───────────────────────────────�?�?
�? �?            Skill �?API                               �?�?
�? �? �?ISkill - Skill 基础接口                             �?�?
�? �? �?ISkillRegistry - Skill 注册                         �?�?
�? �? �?HTTP API - Python Skill 服务                        �?�?
�? └───────────────────────────────────────────────────────�?�?
�?                                                            �?
└─────────────────────────────────────────────────────────────�?
```

### 1.2 接口协议

| 调用�?| 被调用方 | 协议 |
|--------|----------|------|
| 产品应用 | UniFlow Core | Delphi Interface |
| UniFlow Core | Delphi Skill | Delphi Interface |
| UniFlow Core | Python Skill | HTTP/JSON |
| 角色之间 | 角色之间 | EventBus Message |

---

## 2. 产品�?API

### 2.1 IUniFlow - 统一入口

```pascal
type
  /// UniFlow 统一入口接口
  IUniFlow = interface
    ['{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}']
    
    // 初始化与生命周期
    procedure Initialize(const AConfig: TUniFlowConfig);
    procedure Shutdown;
    function GetStatus: TUniFlowStatus;
    
    // 子系统访�?
    function GetSessionManager: ISessionManager;
    function GetWorkflowRunner: IWorkflowRunner;
    function GetSkillRegistry: ISkillRegistry;
    function GetMemoryManager: IMemoryManager;
    
    // 便捷方法
    function Chat(const ASessionId: string; const AMessage: string): string;
    function ChatAsync(const ASessionId: string; const AMessage: string;
      const ACallback: TChatCallback): IFuture;
    function ChatStream(const ASessionId: string; const AMessage: string;
      const AChunkCallback: TChunkCallback): IFuture;
      
    // 配置
    procedure SetConfig(const AKey: string; const AValue: TValue);
    function GetConfig(const AKey: string): TValue;
  end;

  /// 初始化配�?
  TUniFlowConfig = record
    DatabasePath: string;           // 数据库路�?
    PythonServiceUrl: string;       // Python 服务地址
    DefaultLLMConfig: string;       // 默认 LLM 配置�?
    LogLevel: TLogLevel;
    EnableAudit: Boolean;
  end;

  /// 状态信�?
  TUniFlowStatus = record
    IsInitialized: Boolean;
    DatabaseConnected: Boolean;
    PythonServiceConnected: Boolean;
    ActiveSessions: Integer;
    RunningWorkflows: Integer;
  end;
```

### 2.2 ISessionManager - 会话管理

```pascal
type
  /// 会话管理接口
  ISessionManager = interface
    ['{B2C3D4E5-F6A7-8901-BCDE-F12345678901}']
    
    // 会话生命周期
    function CreateSession(const AProductId: string; 
      const AProjectId: string = ''): ISession;
    function GetSession(const ASessionId: string): ISession;
    function ListSessions(const AFilter: TSessionFilter): TArray<TSessionInfo>;
    procedure CloseSession(const ASessionId: string);
    procedure ArchiveSession(const ASessionId: string);
    
    // 批量操作
    procedure CloseIdleSessions(const AIdleMinutes: Integer);
    procedure ArchiveOldSessions(const ADays: Integer);
  end;

  /// 会话接口
  ISession = interface
    ['{C3D4E5F6-A7B8-9012-CDEF-123456789012}']
    
    function GetId: string;
    function GetProductId: string;
    function GetProjectId: string;
    function GetStatus: TSessionStatus;
    
    // 消息
    function SendMessage(const AContent: string): TMessageResponse;
    function SendMessageAsync(const AContent: string;
      const ACallback: TMessageCallback): IFuture;
    function SendMessageStream(const AContent: string;
      const AChunkCallback: TChunkCallback): IFuture;
    function GetMessages(const ALimit: Integer = 100): TArray<TMessage>;
    
    // 记忆
    function GetMemory: ISessionMemory;
    
    // 上下�?
    procedure SetContext(const AKey: string; const AValue: TJSONValue);
    function GetContext(const AKey: string): TJSONValue;
    procedure ClearContext;
    
    // Persona
    procedure SetPersona(const APersonaName: string);
    function GetPersona: string;
  end;

  /// 会话过滤�?
  TSessionFilter = record
    ProductId: string;
    ProjectId: string;
    Status: TSessionStatus;
    CreatedAfter: TDateTime;
    CreatedBefore: TDateTime;
    Limit: Integer;
    Offset: Integer;
  end;
```

### 2.3 IWorkflowRunner - 工作流运�?

```pascal
type
  /// 工作流运行器接口
  IWorkflowRunner = interface
    ['{D4E5F6A7-B8C9-0123-DEFA-234567890123}']
    
    // 定义管理
    function RegisterDefinition(const ADef: TWorkflowDefinition): string;
    function GetDefinition(const ADefId: string): TWorkflowDefinition;
    function ListDefinitions(const ACategory: string = ''): TArray<TWorkflowInfo>;
    procedure UpdateDefinition(const ADefId: string; const ADef: TWorkflowDefinition);
    procedure DeleteDefinition(const ADefId: string);
    
    // 执行
    function Start(const ADefId: string; const AInput: TJSONObject): string;
    function StartAsync(const ADefId: string; const AInput: TJSONObject;
      const ACallback: TWorkflowCallback): IFuture;
    procedure Pause(const AInstanceId: string);
    procedure Resume(const AInstanceId: string);
    procedure Cancel(const AInstanceId: string);
    
    // 状态查�?
    function GetInstance(const AInstanceId: string): TWorkflowInstance;
    function GetInstanceStatus(const AInstanceId: string): TWorkflowStatus;
    function ListInstances(const AFilter: TWorkflowFilter): TArray<TWorkflowInstance>;
    
    // 事件
    procedure SendEvent(const AInstanceId: string; const AEvent: TWorkflowEvent);
  end;

  /// 工作流定�?
  TWorkflowDefinition = record
    Id: string;
    Name: string;
    Description: string;
    Version: Integer;
    Nodes: TArray<TWorkflowNode>;
    Edges: TArray<TWorkflowEdge>;
    InputSchema: TJSONObject;
    OutputSchema: TJSONObject;
    Config: TWorkflowConfig;
  end;

  /// 工作流节�?
  TWorkflowNode = record
    Id: string;
    NodeType: TWorkflowNodeType;  // skill, condition, parallel, wait, subflow
    Name: string;
    Config: TJSONObject;
    
    // Skill 节点特有
    SkillName: string;
    SkillInput: TJSONObject;
    
    // 条件节点特有
    Condition: string;
    
    // 并行节点特有
    ParallelBranches: TArray<string>;
    JoinType: TJoinType;  // all, any, n_of_m
  end;

  /// 工作流边
  TWorkflowEdge = record
    Id: string;
    FromNode: string;
    ToNode: string;
    Condition: string;  // 可选的条件表达�?
  end;
```

---

## 3. 角色�?API

### 3.1 L0 元层

#### 3.1.1 IEngine - 引擎

```pascal
type
  /// 引擎接口 - UniFlow 核心调度�?
  IEngine = interface
    ['{E5F6A7B8-C9D0-1234-EFAB-345678901234}']
    
    // 生命周期
    procedure Start;
    procedure Stop;
    function GetState: TEngineState;
    
    // 任务提交
    function Submit(const ATask: TTask): string;
    function SubmitBatch(const ATasks: TArray<TTask>): TArray<string>;
    
    // 角色管理
    procedure RegisterRole(const ARole: IRole);
    function GetRole(const ARoleName: string): IRole;
    function ListRoles: TArray<string>;
    
    // 事件总线
    function GetEventBus: IEventBus;
    
    // 监控
    function GetMetrics: TEngineMetrics;
  end;

  /// 任务定义
  TTask = record
    Id: string;
    TaskType: TTaskType;
    Priority: Integer;
    Payload: TJSONObject;
    Context: TJSONObject;
    Timeout: Integer;
    RetryPolicy: TRetryPolicy;
  end;
```

#### 3.1.2 IInspector - 督察

```pascal
type
  /// 督察接口 - 监控与审�?
  IInspector = interface
    ['{F6A7B8C9-D0E1-2345-FABC-456789012345}']
    
    // 监控
    procedure Watch(const AComponent: string; const AMetrics: TMetrics);
    function GetHealth(const AComponent: string): THealthStatus;
    function GetAllHealth: TArray<TComponentHealth>;
    
    // 审计
    procedure Audit(const AEvent: TAuditEvent);
    function QueryAudit(const AFilter: TAuditFilter): TArray<TAuditEvent>;
    
    // 告警
    procedure SetAlert(const ARule: TAlertRule);
    procedure ClearAlert(const ARuleId: string);
    function GetAlerts: TArray<TAlert>;
    
    // 限流
    function CheckRateLimit(const AKey: string; const ALimit: Integer): Boolean;
  end;
```

### 3.2 L4 决策�?

#### 3.2.1 ICommander - 指挥�?

```pascal
type
  /// 指挥官接�?- 意图理解与任务分�?
  ICommander = interface
    ['{A7B8C9D0-E1F2-3456-ABCD-567890123456}']
    
    // 意图分析
    function AnalyzeIntent(const AInput: string; 
      const AContext: TJSONObject): TIntent;
    
    // 任务分解
    function Decompose(const AIntent: TIntent): TArray<TSubTask>;
    
    // 策略选择
    function SelectStrategy(const AIntent: TIntent): TStrategy;
    
    // 结果整合
    function Integrate(const AResults: TArray<TSubTaskResult>): TFinalResult;
  end;

  /// 意图
  TIntent = record
    IntentType: string;
    Confidence: Double;
    Entities: TArray<TEntity>;
    RequiredCapabilities: TArray<string>;
    Context: TJSONObject;
  end;

  /// 子任�?
  TSubTask = record
    Id: string;
    TaskType: string;
    Dependencies: TArray<string>;
    Input: TJSONObject;
    AssignTo: string;  // 目标角色�?Skill
    Priority: Integer;
  end;
```

#### 3.2.2 IDispatcher - 调度�?

```pascal
type
  /// 调度员接�?- 任务分发
  IDispatcher = interface
    ['{B8C9D0E1-F2A3-4567-BCDE-678901234567}']
    
    // 任务分发
    procedure Dispatch(const ATask: TSubTask);
    procedure DispatchBatch(const ATasks: TArray<TSubTask>);
    
    // 队列管理
    function GetQueueLength(const AQueue: string): Integer;
    procedure PrioritizeTask(const ATaskId: string; const APriority: Integer);
    procedure CancelTask(const ATaskId: string);
    
    // 负载均衡
    function SelectExecutor(const ATaskType: string): string;
    procedure UpdateExecutorLoad(const AExecutor: string; const ALoad: Double);
  end;
```

### 3.3 L3 智力�?

#### 3.3.1 IAdvisor - 顾问

```pascal
type
  /// 顾问接口 - LLM 交互
  IAdvisor = interface
    ['{C9D0E1F2-A3B4-5678-CDEF-789012345678}']
    
    // LLM 调用
    function Consult(const APrompt: string; const AContext: TJSONObject): string;
    function ConsultAsync(const APrompt: string; const AContext: TJSONObject;
      const ACallback: TLLMCallback): IFuture;
    function ConsultStream(const APrompt: string; const AContext: TJSONObject;
      const AChunkCallback: TChunkCallback): IFuture;
    
    // 结构化输�?
    function ConsultStructured(const APrompt: string; 
      const ASchema: TJSONObject): TJSONObject;
    
    // 多轮对话
    function ConsultWithHiDeepDeepDeepDeepDeepStory(const AMessages: TArray<TMessage>): string;
    
    // 配置
    procedure SetModel(const AModelName: string);
    procedure SetTemperature(const ATemp: Double);
    procedure SetMaxTokens(const ATokens: Integer);
  end;
```

### 3.4 L2 能力�?

#### 3.4.1 IExecutor - 执行�?

```pascal
type
  /// 执行者接�?- Skill 执行
  IExecutor = interface
    ['{D0E1F2A3-B4C5-6789-DEFA-890123456789}']
    
    // 执行
    function Execute(const ASkillName: string; 
      const AInput: TJSONObject): TSkillResult;
    function ExecuteAsync(const ASkillName: string;
      const AInput: TJSONObject;
      const ACallback: TSkillCallback): IFuture;
    
    // 批量执行
    function ExecuteBatch(const ARequests: TArray<TSkillRequest>): TArray<TSkillResult>;
    function ExecuteParallel(const ARequests: TArray<TSkillRequest>): TArray<TSkillResult>;
    
    // 状态查�?
    function GetExecutionStatus(const AExecutionId: string): TExecutionStatus;
    procedure CancelExecution(const AExecutionId: string);
  end;

  /// Skill 执行结果
  TSkillResult = record
    ExecutionId: string;
    SkillName: string;
    Status: TExecutionStatus;
    Output: TJSONObject;
    Error: string;
    StartTime: TDateTime;
    EndTime: TDateTime;
    DurationMs: Integer;
  end;
```

#### 3.4.2 IGuard - 守卫

```pascal
type
  /// 守卫接口 - 安全校验
  IGuard = interface
    ['{E1F2A3B4-C5D6-7890-EFAB-901234567890}']
    
    // 输入校验
    function ValidateInput(const AInput: TJSONObject;
      const ASchema: TJSONObject): TValidationResult;
    
    // 输出校验
    function ValidateOutput(const AOutput: TJSONObject;
      const ASchema: TJSONObject): TValidationResult;
    
    // LLM 输出检�?
    function CheckLLMOutput(const AOutput: string): TSafetyCheckResult;
    
    // 权限检�?
    function CheckPermission(const AActor: string; 
      const AResource: string;
      const AAction: string): Boolean;
    
    // 敏感信息
    function SanitizeInput(const AInput: string): string;
    function DetectSensitiveData(const AData: string): TArray<TSensitiveMatch>;
  end;

  /// 校验结果
  TValidationResult = record
    IsValid: Boolean;
    Errors: TArray<TValidationError>;
    Warnings: TArray<string>;
  end;

  /// 安全检查结�?
  TSafetyCheckResult = record
    IsSafe: Boolean;
    RiskLevel: TRiskLevel;  // low, medium, high, critical
    Issues: TArray<TSafetyIssue>;
    SuggestedAction: TSuggestedAction;
  end;
```

#### 3.4.3 IQuartermaster - 军需�?

```pascal
type
  /// 军需官接�?- 资源管理
  IQuartermaster = interface
    ['{F2A3B4C5-D6E7-8901-FABC-012345678901}']
    
    // 资源分配
    function Allocate(const AResourceType: string;
      const AAmount: Integer): TResourceAllocation;
    procedure Release(const AAllocationId: string);
    
    // 配额管理
    function CheckQuota(const AResourceType: string): TQuotaStatus;
    procedure SetQuota(const AResourceType: string; const ALimit: Integer);
    
    // 成本追踪
    function GetCost(const ATimeRange: TTimeRange): TCostReport;
    procedure SetBudget(const AResourceType: string; const ABudget: Double);
    
    // 缓存
    function GetFromCache(const AKey: string): TJSONValue;
    procedure PutToCache(const AKey: string; const AValue: TJSONValue;
      const ATTLSeconds: Integer);
    procedure InvalidateCache(const APattern: string);
  end;
```

### 3.5 L1 基础�?

#### 3.5.1 ILogistics - 后勤

```pascal
type
  /// 后勤接口 - 存储服务
  ILogistics = interface
    ['{A3B4C5D6-E7F8-9012-ABCD-123456789012}']
    
    // 键值存�?
    procedure Put(const ANamespace, AKey: string; const AValue: TJSONValue);
    function Get(const ANamespace, AKey: string): TJSONValue;
    procedure Delete(const ANamespace, AKey: string);
    function Exists(const ANamespace, AKey: string): Boolean;
    
    // 批量操作
    procedure PutBatch(const ANamespace: string; 
      const AItems: TArray<TKeyValue>);
    function GetBatch(const ANamespace: string;
      const AKeys: TArray<string>): TArray<TKeyValue>;
    
    // 查询
    function Query(const ANamespace: string;
      const AFilter: TQueryFilter): TArray<TKeyValue>;
    function Count(const ANamespace: string;
      const AFilter: TQueryFilter): Integer;
    
    // 事务
    function BeginTransaction: ITransaction;
  end;
```

#### 3.5.2 IChronicler - 记录�?

```pascal
type
  /// 记录官接�?- 日志与审�?
  IChronicler = interface
    ['{B4C5D6E7-F8A9-0123-BCDE-234567890123}']
    
    // 日志
    procedure Log(const ALevel: TLogLevel; const AComponent: string;
      const AMessage: string; const ADetails: TJSONObject = nil);
    procedure Debug(const AComponent, AMessage: string);
    procedure Info(const AComponent, AMessage: string);
    procedure Warn(const AComponent, AMessage: string);
    procedure Error(const AComponent, AMessage: string; const AError: Exception = nil);
    
    // 审计
    procedure AuditLog(const AEvent: TAuditEvent);
    
    // 查询
    function QueryLogs(const AFilter: TLogFilter): TArray<TLogEntry>;
    
    // 配置
    procedure SetLogLevel(const AComponent: string; const ALevel: TLogLevel);
    procedure EnableAudit(const AEnabled: Boolean);
  end;
```

#### 3.5.3 ISignalOfficer - 通信�?

```pascal
type
  /// 通信官接�?- 外部通信
  ISignalOfficer = interface
    ['{C5D6E7F8-A9B0-1234-CDEF-345678901234}']
    
    // HTTP 请求
    function HttpGet(const AUrl: string; 
      const AHeaders: THeaders = nil): THttpResponse;
    function HttpPost(const AUrl: string; const ABody: TJSONObject;
      const AHeaders: THeaders = nil): THttpResponse;
    function HttpRequest(const ARequest: THttpRequest): THttpResponse;
    
    // 异步请求
    function HttpGetAsync(const AUrl: string;
      const ACallback: THttpCallback): IFuture;
    function HttpPostAsync(const AUrl: string; const ABody: TJSONObject;
      const ACallback: THttpCallback): IFuture;
    
    // Webhook
    procedure RegisterWebhook(const AId: string; const AUrl: string;
      const AEvents: TArray<string>);
    procedure UnregisterWebhook(const AId: string);
    procedure TriggerWebhook(const AEvent: string; const AData: TJSONObject);
    
    // SSE (Server-Sent Events)
    function ConnectSSE(const AUrl: string;
      const AEventCallback: TSSECallback): ISSEConnection;
  end;
```

---

## 4. Skill �?API

### 4.1 ISkill - Skill 基础接口

```pascal
type
  /// Skill 基础接口
  ISkill = interface
    ['{D6E7F8A9-B0C1-2345-DEFA-456789012345}']
    
    // 元信�?
    function GetMeta: TSkillMeta;
    
    // 执行
    function Execute(const AInput: TSkillInput): TSkillOutput;
    
    // 校验
    function Validate(const AInput: TSkillInput): TValidationResult;
    
    // 生命周期
    procedure Initialize;
    procedure Shutdown;
  end;

  /// Skill 元信�?
  TSkillMeta = record
    Name: string;
    Version: string;
    Description: string;
    Category: string;
    Language: TSkillLanguage;  // delphi, python
    TrustLevel: TTrustLevel;
    InputSchema: TJSONObject;
    OutputSchema: TJSONObject;
    ConfigSchema: TJSONObject;
    Dependencies: TArray<string>;
    Author: string;
    Tags: TArray<string>;
  end;

  /// Skill 输入
  TSkillInput = record
    Data: TJSONObject;
    Context: TSkillContext;
    Config: TJSONObject;
  end;

  /// Skill 上下�?
  TSkillContext = record
    SessionId: string;
    WorkflowId: string;
    NodeId: string;
    UserId: string;
    Timeout: Integer;
    TraceId: string;
  end;

  /// Skill 输出
  TSkillOutput = record
    Success: Boolean;
    Data: TJSONObject;
    Error: TSkillError;
    Metrics: TSkillMetrics;
  end;
```

### 4.2 ISkillRegistry - Skill 注册中心

```pascal
type
  /// Skill 注册中心接口
  ISkillRegistry = interface
    ['{E7F8A9B0-C1D2-3456-EFAB-567890123456}']
    
    // 注册
    procedure Register(const ASkill: ISkill);
    procedure RegisterPython(const AMeta: TSkillMeta; const AEndpoint: string);
    procedure Unregister(const ASkillName: string);
    
    // 查询
    function Get(const ASkillName: string): ISkill;
    function Exists(const ASkillName: string): Boolean;
    function List(const ACategory: string = ''): TArray<TSkillMeta>;
    function Search(const AQuery: string): TArray<TSkillMeta>;
    
    // 状�?
    function GetStatus(const ASkillName: string): TSkillStatus;
    procedure Enable(const ASkillName: string);
    procedure Disable(const ASkillName: string);
    
    // 热重�?
    procedure Reload(const ASkillName: string);
    procedure ReloadAll;
  end;
```

### 4.3 IMemoryManager - 记忆管理

```pascal
type
  /// 记忆管理接口
  IMemoryManager = interface
    ['{F8A9B0C1-D2E3-4567-FABC-678901234567}']
    
    // 工作记忆
    function GetWorkingMemory(const ASessionId: string): IWorkingMemory;
    
    // 会话记忆
    function GetSessionMemory(const ASessionId: string): ISessionMemory;
    
    // 项目记忆
    function GetProjectMemory(const AProjectId: string): IProjectMemory;
    
    // 跨层操作
    procedure Consolidate(const ASessionId: string);
    procedure Forget(const ASessionId: string; const APolicy: TForgetPolicy);
    
    // 搜索
    function Search(const AQuery: string;
      const AScope: TMemoryScope): TArray<TMemoryItem>;
  end;

  /// 会话记忆接口
  ISessionMemory = interface
    ['{A9B0C1D2-E3F4-5678-ABCD-789012345678}']
    
    procedure Store(const AKey: string; const AValue: TJSONValue;
      const ATags: TArray<string> = nil);
    function Recall(const AKey: string): TJSONValue;
    function RecallByTag(const ATag: string): TArray<TMemoryItem>;
    procedure Forget(const AKey: string);
    procedure Clear;
    
    function GetHiDeepDeepDeepDeepDeepStory(const ALimit: Integer = 10): TArray<TMessage>;
    procedure AddToHiDeepDeepDeepDeepDeepStory(const AMessage: TMessage);
  end;
```

---

## 5. Python Skill 服务 HTTP API

### 5.1 服务信息

| 项目 | �?|
|------|-----|
| Base URL | `http://localhost:8765` |
| 协议 | HTTP/1.1 |
| 编码 | UTF-8 |
| 格式 | JSON |

### 5.2 通用响应格式

```json
{
  "success": true,
  "data": { ... },
  "error": {
    "code": "ERROR_CODE",
    "message": "Error description",
    "details": { ... }
  },
  "meta": {
    "request_id": "uuid",
    "timestamp": "2025-12-03T00:00:00Z",
    "duration_ms": 100
  }
}
```

### 5.3 健康检�?

#### GET /health

检查服务健康状态�?

**响应**:
```json
{
  "success": true,
  "data": {
    "status": "healthy",
    "version": "1.0.0",
    "uptime_seconds": 3600,
    "skills_loaded": 6
  }
}
```

### 5.4 Skill 管理

#### GET /skills

获取已注册的 Skill 列表�?

**响应**:
```json
{
  "success": true,
  "data": {
    "skills": [
      {
        "name": "skill_llm",
        "version": "1.0.0",
        "description": "LLM 调用 Skill",
        "category": "core",
        "trust_level": "limited",
        "status": "active"
      }
    ]
  }
}
```

#### GET /skills/{name}

获取指定 Skill 的详细信息�?

**响应**:
```json
{
  "success": true,
  "data": {
    "name": "skill_llm",
    "version": "1.0.0",
    "description": "LLM 调用 Skill",
    "input_schema": { ... },
    "output_schema": { ... },
    "config_schema": { ... }
  }
}
```

### 5.5 Skill 执行

#### POST /skills/{name}/execute

同步执行 Skill�?

**请求**:
```json
{
  "input": {
    "prompt": "Hello, world!",
    "max_tokens": 100
  },
  "context": {
    "session_id": "sess_123",
    "workflow_id": "wf_456",
    "trace_id": "trace_789"
  },
  "config": {
    "model": "gpt-4",
    "temperature": 0.7
  }
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "execution_id": "exec_abc",
    "output": {
      "response": "Hello! How can I help you today?",
      "tokens_used": 15
    }
  },
  "meta": {
    "duration_ms": 1500
  }
}
```

#### POST /skills/{name}/execute/stream

流式执行 Skill（SSE）�?

**请求**: 同上

**响应** (SSE):
```
event: chunk
data: {"content": "Hello", "index": 0}

event: chunk
data: {"content": "! How", "index": 1}

event: chunk
data: {"content": " can I", "index": 2}

event: done
data: {"execution_id": "exec_abc", "total_tokens": 15}
```

### 5.6 批量执行

#### POST /skills/batch

批量执行多个 Skill�?

**请求**:
```json
{
  "requests": [
    {
      "skill": "skill_llm",
      "input": { "prompt": "Question 1" },
      "context": { "session_id": "sess_123" }
    },
    {
      "skill": "skill_llm",
      "input": { "prompt": "Question 2" },
      "context": { "session_id": "sess_123" }
    }
  ],
  "parallel": true
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "results": [
      {
        "skill": "skill_llm",
        "success": true,
        "output": { ... }
      },
      {
        "skill": "skill_llm",
        "success": true,
        "output": { ... }
      }
    ]
  }
}
```

### 5.7 Skill 热重�?

#### POST /skills/{name}/reload

热重载指�?Skill�?

**响应**:
```json
{
  "success": true,
  "data": {
    "skill": "skill_llm",
    "previous_version": "1.0.0",
    "current_version": "1.0.1"
  }
}
```

### 5.8 错误�?

| 错误�?| HTTP 状态码 | 说明 |
|--------|-------------|------|
| SKILL_NOT_FOUND | 404 | Skill 不存�?|
| SKILL_DISABLED | 400 | Skill 已禁�?|
| INVALID_INPUT | 400 | 输入校验失败 |
| EXECUTION_TIMEOUT | 408 | 执行超时 |
| EXECUTION_FAILED | 500 | 执行失败 |
| RATE_LIMITED | 429 | 请求过于频繁 |
| INTERNAL_ERROR | 500 | 内部错误 |

---

## 6. 事件总线消息格式

### 6.1 消息结构

```pascal
type
  /// 事件消息
  TEventMessage = record
    Id: string;               // 消息ID
    EventType: string;        // 事件类型
    Source: string;           // 来源角色
    Target: string;           // 目标角色（空表示广播�?
    Payload: TJSONObject;     // 消息负载
    CorrelationId: string;    // 关联ID（用于追踪）
    Timestamp: TDateTime;
    Priority: TMessagePriority;
  end;
```

### 6.2 事件类型

| 事件类型 | 发送�?| 接收�?| 说明 |
|----------|--------|--------|------|
| task.submit | Commander | Dispatcher | 提交任务 |
| task.dispatch | Dispatcher | Executor | 分发任务 |
| task.complete | Executor | Commander | 任务完成 |
| task.failed | Executor | Commander | 任务失败 |
| skill.execute | Executor | (Skill) | 执行 Skill |
| skill.result | (Skill) | Executor | Skill 结果 |
| memory.store | * | Chronicler | 存储记忆 |
| memory.recall | * | Chronicler | 召回记忆 |
| audit.log | * | Inspector | 审计日志 |
| alert.trigger | Inspector | * | 触发告警 |

---

## 7. 相关文档

- **MVP 开发规�?*：`06.03.Dev-UniFlow-MVP开发规�?v1.0.md`
- **数据模型**：`04.01.Data-UniFlow数据模型-v1.0.md`
- **架构设计**：`03.01.Arch-UniFlow架构设计-v1.0.md`

---

*文档版本: 1.0*
*更新日期: 2025-12-03*
