# UniFlow 快速入门指�?

> 10 分钟了解 UniFlow 核心概念，编写第一�?Skill，掌握调试技巧�?

创建日期: 2025-12-04  版本: 1.0

---

## 1. UniFlow 是什么？�? 分钟�?

UniFlow 是一�?**AI Agent 协作引擎**，采�?军事指挥"隐喻设计，将 AI 能力分解�?11 个专职角色协同工作�?

### 1.1 一句话理解

```
用户请求 �?11个角色协�?�?智能响应
```

### 1.2 核心价�?

- **AI 能力复用**：一次接�?LLM，所有业务复�?
- **安全可控**：所�?AI 输入输出都经过审�?
- **可观�?*：每个决策过程都可追�?

### 1.3 11 角色速览

```
┌─────────────────────────────────────────────────────────────�?
�?                     UniFlow 角色图谱                        �?
├─────────────────────────────────────────────────────────────�?
�? 🎯 指挥�?                                                  �?
�?    Commander(总指�? �?Dispatcher(调度�? �?Executor(执行�? �?
�?                                                             �?
�? 🧠 智能�?                                                  �?
�?    Advisor(智囊) - AI 推理    Memory(记忆) - 上下文管�?     �?
�?                                                             �?
�? 🛡�?安全�?                                                  �?
�?    Guard(守卫) - 输入输出审核                                �?
�?                                                             �?
�? 🔧 支撑�?                                                  �?
�?    Quartermaster(军械�? - Skill/Prompt 管理                �?
�?    SignalOfficer(通信�? - 外部 API 调用                    �?
�?    Logistics(后勤) - 配置和资�?                            �?
�?    Chronicler(记录�? - 日志和审�?                         �?
�?                                                             �?
�? ⚙️ 系统�?                                                  �?
�?    Engine(引擎) - 角色生命周期    Inspector(督察) - 调试     �?
└─────────────────────────────────────────────────────────────�?
```

### 1.4 典型请求流程

```
用户: "帮我分析这个决策"
     �?
[Commander] 接收请求，权限检�?
     �?
[Dispatcher] 创建 Workflow，安排任�?
     �?
[Advisor] 调用 LLM 理解意图
     �?
[Executor] 执行 decision_coach Skill
     �?
[Guard] 审核输出内容
     �?
返回: 多角度分析结�?
```

---

## 2. 环境准备�? 分钟�?

### 2.1 前置条件

```
�?Delphi 12+ �?Lazarus 3.0+
�?Python 3.10+（用�?AI Skills�?
�?SQLite（内置，无需安装�?
�?OpenAI API Key 或兼�?API
```

### 2.2 获取代码

```bash
# 克隆仓库
git clone https://github.com/xxx/uniflow.git
cd uniflow

# 安装 Python 依赖
cd skills
pip install -r requirements.txt
```

### 2.3 配置

创建 `config/uniflow.local.json`�?

```json
{
  "llm": {
    "baseUrl": "https://api.openai.com/v1",
    "apiKey": "sk-xxx",
    "model": "gpt-4o-mini",
    "timeout": 30000
  },
  "pythonService": {
    "pythonPath": "python",
    "port": 8765
  },
  "logging": {
    "level": "debug",
    "file": "logs/uniflow.log"
  }
}
```

### 2.4 启动

```bash
# 方式 1：IDE 中运�?
打开 UniFlow.dproj，按 F9 运行

# 方式 2：命令行
./UniFlow.exe --config=config/uniflow.local.json
```

---

## 3. Hello World Skill�? 分钟�?

### 3.1 创建 Delphi Skill

```pascal
// skills/SkillHello.pas
unit SkillHello;

interface

uses
  UniFlow.Skill.Base, System.JSON;

type
  TSkillHello = class(TBaseSkill)
  public
    class function GetId: string; override;
    class function GetDescription: string; override;
    function Execute(AInput: TJSONObject; AContext: TJSONObject): TSkillResult; override;
  end;

implementation

class function TSkillHello.GetId: string;
begin
  Result := 'hello';
end;

class function TSkillHello.GetDescription: string;
begin
  Result := '返回问候语';
end;

function TSkillHello.Execute(AInput: TJSONObject; AContext: TJSONObject): TSkillResult;
var
  LName: string;
begin
  // 获取输入参数
  LName := AInput.GetValue<string>('name', 'World');
  
  // 构建结果
  Result := TSkillResult.Success(
    TJSONObject.Create.AddPair('greeting', 'Hello, ' + LName + '!')
  );
end;

initialization
  // 注册 Skill
  TSkillRegistry.Register(TSkillHello);

end.
```

### 3.2 创建 Python Skill

```python
# skills/skill_hello_ai.py
from uniflow.skill import Skill, skill_result

class HelloAISkill(Skill):
    """使用 AI 生成个性化问�?""
    
    skill_id = "hello_ai"
    description = "AI 生成个性化问候语"
    
    async def execute(self, input: dict, context: dict) -> dict:
        name = input.get("name", "朋友")
        style = input.get("style", "友好")
        
        # 调用 LLM
        prompt = f"用{style}的语气，给{name}写一句问候语，不超过20字�?
        response = await self.advisor.generate(prompt)
        
        return skill_result(
            success=True,
            data={"greeting": response.content}
        )
```

### 3.3 测试 Skill

```pascal
// 单元测试
procedure TSkillHelloTest.TestBasicGreeting;
var
  LSkill: TSkillHello;
  LInput: TJSONObject;
  LResult: TSkillResult;
begin
  LSkill := TSkillHello.Create;
  try
    LInput := TJSONObject.Create;
    LInput.AddPair('name', '张三');
    
    LResult := LSkill.Execute(LInput, nil);
    
    CheckTrue(LResult.Success);
    CheckEquals('Hello, 张三!', LResult.Data.GetValue<string>('greeting'));
  finally
    LSkill.Free;
    LInput.Free;
  end;
end;
```

### 3.4 通过 Workflow 调用

```json
{
  "id": "greeting-workflow",
  "name": "问候流�?,
  "steps": [
    {
      "id": "step1",
      "type": "SkillTask",
      "skillId": "hello",
      "input": {
        "name": "${input.userName}"
      }
    }
  ]
}
```

---

## 4. 调试速查表（2 分钟�?

### 4.1 日志查看

```bash
# 实时查看日志
tail -f logs/uniflow.log | grep "TRACE:你的traceId"

# 过滤特定角色
tail -f logs/uniflow.log | grep "[Commander]"
```

### 4.2 常见问题排查

| 症状 | 可能原因 | 解决方法 |
|------|----------|----------|
| Python Skill 调用失败 | Python 服务未启�?| 检�?`http://localhost:8765/health` |
| LLM 响应超时 | API 网络问题 | 检�?`config.llm.baseUrl` 配置 |
| 权限被拒�?| Guard 校验失败 | 查看日志中的 `GUARD_*` 错误�?|
| Workflow 卡住 | HumanTask 等待�?| 调用 `completeHumanTask` API |
| 角色不响�?| 心跳超时 | Inspector 查看角色状�?|

### 4.3 Inspector 调试命令

```pascal
// 获取角色状�?
Inspector.GetRoleStatus('advisor');

// 查看消息队列
Inspector.GetPendingMessages('dispatcher');

// 注入 Mock
Inspector.InjectMock('signalOfficer', TMockSignalOfficer.Create);

// 设置断点
Inspector.SetBreakpoint('executor', 'beforeExecute');

// 追踪请求
Inspector.EnableTracing('trace-id-xxx');
```

### 4.4 错误码速查

| 错误�?| 含义 | 处理建议 |
|--------|------|----------|
| `AI_TIMEOUT` | LLM 调用超时 | 增加 timeout 或重�?|
| `AI_RATE_LIMIT` | API 限流 | 等待后重�?|
| `GUARD_INJECTION_DETECTED` | Prompt 注入检�?| 检查用户输�?|
| `GUARD_HALLUCINATION` | 幻觉内容 | 重新生成或人工审�?|
| `SKILL_NOT_FOUND` | Skill 未注�?| 检�?Skill 注册代码 |
| `WORKFLOW_STEP_FAILED` | 步骤执行失败 | 查看 stepError 详情 |
| `PERMISSION_DENIED` | 权限不足 | 检查用户角色配�?|
| `BUDGET_EXCEEDED` | 预算超限 | 等待预算重置或调整限�?|

### 4.5 性能检查点

```
�?单次 Skill 执行 < 100ms（不�?LLM�?
�?LLM 调用 P99 < 30s
�?角色间消息传�?< 5ms
�?Workflow 状态持久化 < 50ms
```

---

## 5. 下一�?

| 目标 | 推荐阅读 |
|------|----------|
| 理解架构设计 | `03.01.Arch-UniFlow架构设计` |
| 开发复�?Skill | `06.01.Dev-UniFlow-Skill开发指南` |
| 设计 Workflow | `05.03.API-UniFlow-Workflow定义规范` |
| 接入 AI 能力 | `03.03.Design-UniFlow-AI能力复用设计哲学` |
| 了解安全机制 | `08.01.Quality-UniFlow-安全与数据权限策略` |
| 部署上线 | `08.05.Quality-UniFlow-部署架构` |

---

## 附录：文件结�?

```
uniflow/
├── src/
�?  ├── Core/                    # 核心框架
�?  �?  ├── UniFlow.Engine.pas   # 引擎
�?  �?  ├── UniFlow.Message.pas  # 消息协议
�?  �?  └── UniFlow.Types.pas    # 公共类型
�?  ├── Roles/                   # 11 角色实现
�?  �?  ├── UniFlow.Role.Commander.pas
�?  �?  ├── UniFlow.Role.Dispatcher.pas
�?  �?  └── ...
�?  └── Skills/                  # Delphi Skills
�?      ├── SkillBase.pas
�?      └── SkillHello.pas
├── skills/                      # Python Skills
�?  ├── main.py                  # FastAPI 入口
�?  ├── requirements.txt
�?  └── skill_*.py               # 各个 Skill
├── config/
�?  ├── uniflow.default.json     # 默认配置
�?  └── uniflow.local.json       # 本地覆盖
├── tests/
�?  ├── unit/                    # 单元测试
�?  └── integration/             # 集成测试
└── docs/                        # 文档
```

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作�?|
|------|------|----------|------|
| 1.0 | 2025-12-04 | 初始版本 | - |
