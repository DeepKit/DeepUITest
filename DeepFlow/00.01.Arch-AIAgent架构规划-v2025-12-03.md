好的，我直接在聊天区为你输出这份完整的指导文件：

---

# Agent系统软件设计完整指导手册

**版本�?* 1.0  
**更新日期�?* 2025-12-03  
**适用对象�?* 软件架构师、高级开发者、技术负责人

---

## 目录

1. [理论基础](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#1-%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80)
2. [架构设计](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#2-%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1)
3. [设计模式库](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#3-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E5%BA%93)
4. [现有程序改造指南](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#4-%E7%8E%B0%E6%9C%89%E7%A8%8B%E5%BA%8F%E6%94%B9%E9%80%A0%E6%8C%87%E5%8D%97)
5. [实践指南](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#5-%E5%AE%9E%E8%B7%B5%E6%8C%87%E5%8D%97)
6. [技术实现](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#6-%E6%8A%80%E6%9C%AF%E5%AE%9E%E7%8E%B0)
7. [开发流程](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#7-%E5%BC%80%E5%8F%91%E6%B5%81%E7%A8%8B)
8. [实战案例](https://claude.ai/chat/37eb15bb-2622-401b-b8d1-0450a3d6902f#8-%E5%AE%9E%E6%88%98%E6%A1%88%E4%BE%8B)

---

## 1. 理论基础

### 1.1 传统软件 vs Agent系统对比

| 维度       | 传统软件设计        | Agent系统设计     |
| -------- | ------------- | ------------- |
| **执行模式** | 确定性、预定义流程     | 动态决策、自主规�?    |
| **控制方式** | 显式编程控制        | 目标驱动、AI决策     |
| **架构重点** | 模块划分、接口设�?    | 工具生态、能力边�?    |
| **流程定义** | 详细的业务逻辑       | 高层目标和约�?      |
| **错误处理** | Try-catch、状态码 | 自我修正、反思机�?    |
| **适应�?*  | 需要代码修�?       | 可通过prompt调整   |
| **文档形式** | UML图、流程图      | Prompt模板、工具定�?|

### 1.2 何时使用Agent vs 传统工作�?

**使用传统工作流的场景�?*

- 流程固定、步骤明�?
- 性能要求极高
- 需�?00%确定性结�?
- 简单的CRUD操作
- 成本敏感的高频操�?

**使用Agent系统的场景：**

- 需要动态决策和规划
- 处理非结构化输入
- 需要多步推�?
- 任务复杂度高、变化多
- 需要自然语言交互
- 需要工具组合使�?

**混合模式（推荐）�?*

- 核心业务用传统工作流保证稳定�?
- 复杂决策部分用Agent增强能力
- 用户交互层用Agent提升体验

---

## 2. 架构设计

### 2.1 Agent系统核心架构

```
┌─────────────────────────────────────────────────────�?
�?                  用户交互�?                         �?
�?           (自然语言输入/输出界面)                     �?
└───────────────────┬─────────────────────────────────�?
                    �?
┌───────────────────▼─────────────────────────────────�?
�?               Agent协调�?                          �?
�? ┌──────────�? ┌──────────�? ┌──────────�?         �?
�? �?意图识别  �? �?任务规划  �? �?决策引擎  �?         �?
�? └──────────�? └──────────�? └──────────�?         �?
└───────────────────┬─────────────────────────────────�?
                    �?
┌───────────────────▼─────────────────────────────────�?
�?                 LLM引擎�?                          �?
�? ┌─────────────────────────────────────────�?       �?
�? �? Claude Sonnet 4.5 / Opus 4             �?       �?
�? �? - System Prompt                         �?       �?
�? �? - Context Management                    �?       �?
�? �? - Tool Calling                          �?       �?
�? └─────────────────────────────────────────�?       �?
└───────────────────┬─────────────────────────────────�?
                    �?
        ┌───────────┼───────────�?
        �?          �?          �?
┌───────▼─────�?┌──▼────�?┌───▼──────�?
�? 工具�?     �?│记忆层  �?�?评估�?  �?
�?┌─────────�?�?│┌─────┐│ │┌────────┐│
�?│内部工�?�?�?││短期 ││ ││反思模块│�?
�?│API调用  �?�?││记忆 ││ ││验证�? ││
�?│数据查�?�?�?│└─────┘│ │└────────┘│
�?│代码执�?�?�?││长期 ││ �?         �?
�?└─────────�?�?││记忆 ││ �?         �?
�?            �?│└─────┘│ �?         �?
└─────────────�?└───────�?└──────────�?
        �?          �?          �?
┌───────▼───────────▼───────────▼─────────�?
�?           底层系统/数据�?               �?
�?  数据�?| 外部API | 文件系统 | 服务     �?
└─────────────────────────────────────────�?
```

### 2.2 核心组件详解

#### 2.2.1 Agent协调�?

- **意图识别**：理解用户真实需�?
- **任务规划**：分解复杂任务为可执行步�?
- **决策引擎**：选择最优执行路�?

#### 2.2.2 LLM引擎�?

- **System Prompt**：定义Agent角色和能�?
- **Context Management**：管理对话历史和状�?
- **Tool Calling**：结构化工具调用

#### 2.2.3 工具�?

- **内部工具**：业务逻辑封装
- **外部API**：第三方服务集成
- **Claude Skills**：预定义专业能力

#### 2.2.4 记忆�?

- **短期记忆**：当前会话上下文（使用React state/变量�?
- **长期记忆**：持久化知识库（使用window.storage API�?

#### 2.2.5 评估�?

- **反思模�?*：检查输出质�?
- **验证�?*：确保结果正确�?

---

## 3. 设计模式�?

### 3.1 Reflection（反思）模式

**适用场景�?* 需要高质量输出、多轮优化的任务

**流程图：**

```
用户请求 �?LLM生成初稿 �?反思模块评�?
                              �?
                        是否满足要求�?
                              �?
                  �?←────────┴────────�?�?
                  �?                     �?
              返回结果              根据反馈改进
                                        �?
                                  重新生成 ──�?
                                        �?   �?
                                        └────�?
```

**实现要点�?*

```
System Prompt结构�?
1. 主任务提示词
2. 评估标准定义
3. 反思提示词模板

工作流程�?
Step 1: 生成初始输出
Step 2: 使用评估标准检�?
Step 3: 识别问题�?
Step 4: 针对性改�?
Step 5: 重复直到满意或达到最大迭代次�?
```

### 3.2 Tool Use（工具使用）模式

**适用场景�?* 需要访问外部数据、执行特定操�?

**流程图：**

```
用户请求 �?分析所需工具 �?选择合适工�?
                              �?
                        调用工具API
                              �?
                        获取执行结果
                              �?
                    结果是否足够�?
                              �?
                  �?←────────┴────────�?�?
                  �?                     �?
            整合结果返回            继续调用其他工具
```

**工具设计规范�?*

```json
{
  "name": "search_database",
  "description": "在数据库中搜索特定信�?,
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "搜索关键�?
      },
      "filters": {
        "type": "object",
        "description": "过滤条件"
      }
    },
    "required": ["query"]
  }
}
```

### 3.3 Planning（规划执行）模式

**适用场景�?* 复杂多步骤任�?

**流程图：**

```
复杂任务 �?任务分解 �?生成执行计划
                          �?
                    [步骤1, 步骤2, ..., 步骤N]
                          �?
                    逐步执行 ←─────�?
                          �?       �?
                    记录执行结果   �?
                          �?       �?
                    检查依赖关�?  �?
                          �?       �?
                    是否完成全部�?�?
                          �?       �?
                  �?─────�?       �?
                  �?              
            汇总结果并返回
```

**实现策略�?*

```
1. 使用思维链（Chain of Thought）分解任�?
2. 识别步骤间的依赖关系
3. 并行执行无依赖的步骤
4. 维护执行状态跟�?
5. 支持中断恢复
```

### 3.4 Multi-Agent（多Agent协作）模�?

**适用场景�?* 需要不同专业能力协作的复杂任务

**架构图：**

```
                  ┌──────────────�?
                  �?协调器Agent   �?
                  �?(Orchestrator)�?
                  └───────┬───────�?
                          �?
        ┌─────────────────┼─────────────────�?
        �?                �?                �?
   ┌────▼────�?     ┌────▼────�?     ┌────▼────�?
   │研究Agent�?     │编码Agent�?     │测试Agent�?
   �?Worker) �?     �?Worker) �?     �?Worker) �?
   └─────────�?     └─────────�?     └─────────�?
```

**协作流程�?*

```
1. 协调器接收任�?
2. 协调器分配子任务给专门Agent
3. 各Worker Agent并行/串行执行
4. Worker返回结果给协调器
5. 协调器整合结�?
6. 必要时进行多轮协�?
```

### 3.5 Evaluator-Optimizer（评估优化）模式

**适用场景�?* 需要持续优化的任务

**流程图：**

```
初始方案 �?执行 �?评估器打�?�?优化器改�?
              �?                     �?
              └──────────────────────�?
                   (迭代循环)
```

**关键指标�?*

- 成功�?
- 执行效率
- 资源消�?
- 用户满意�?

---

## 4. 现有程序改造指�?

### 4.1 改造评估矩�?

**第一步：识别改造机�?*

| 模块类型   | Agent化潜�?| 改造难�?| 优先�?| 建议方案         |
| ------ | -------- | ---- | --- | ------------ |
| 用户交互界面 | ⭐⭐⭐⭐�?   | �?   | �?  | 添加自然语言接口     |
| 复杂业务逻辑 | ⭐⭐⭐⭐     | �?   | �?  | 规则引擎→Agent决策 |
| 数据分析模块 | ⭐⭐⭐⭐�?   | �?   | �?  | Agent自动分析    |
| 报表生成   | ⭐⭐⭐⭐     | �?   | �?  | 自然语言生成报表     |
| CRUD操作 | ⭐⭐       | �?   | �?  | 保持原样         |
| 核心算法   | �?       | �?   | �?  | 不建议改�?       |

### 4.2 渐进式改造策�?

#### 策略1：外层包装模式（最安全�?

```
现有系统保持不变，在外层添加Agent接口�?

┌────────────────────────────────�?
�?     Agent交互层（新增�?        �?
�? - 自然语言理解                  �?
�? - 意图识别                      �?
�? - 参数提取                      �?
└──────────┬─────────────────────�?
           �?转换为标准API调用
┌──────────▼─────────────────────�?
�?      原有系统（不变）           �?
�? - 业务逻辑                      �?
�? - 数据�?                       �?
└────────────────────────────────�?
```

**优点�?*

- 风险最�?
- 不破坏现有系�?
- 可快速回�?

**实施步骤�?*

1. 为现有API创建工具定义（Tool Schema�?
2. 编写Agent System Prompt
3. 实现参数映射�?
4. 增加结果格式化层

**示例代码结构�?*

```javascript
// 工具定义
const tools = [
  {
    name: "query_user_info",
    description: "查询用户信息",
    input_schema: {
      type: "object",
      properties: {
        user_id: { type: "string" }
      }
    }
  }
];

// Agent包装�?
async function agentQuery(userMessage) {
  const response = await callClaude({
    messages: [{ role: "user", content: userMessage }],
    tools: tools
  });

  // 提取工具调用
  const toolCall = response.content.find(c => c.type === "tool_use");

  // 调用原有系统API
  const result = await legacyAPI.queryUser(toolCall.input.user_id);

  // 返回给Agent继续处理
  return formatResponse(result);
}
```

#### 策略2：局部替换模式（平衡�?

```
保留核心业务逻辑，替换决策层为Agent

原架构：
UI �?业务规则引擎 �?数据�?

新架构：
UI �?Agent决策�?�?简化的业务逻辑 �?数据�?
```

**适合替换的模块：**

- 复杂的if-else规则�?�?Agent智能决策
- 硬编码的业务规则 �?Prompt配置
- 固定的流程编�?�?Agent动态规�?

**改造示例：**

原代码（复杂规则）：

```python
def process_order(order):
    if order.amount > 10000:
        if order.user.vip_level >= 3:
            discount = 0.15
        else:
            discount = 0.10
    elif order.amount > 5000:
        if order.user.vip_level >= 2:
            discount = 0.10
        else:
            discount = 0.05
    # ... 更多规则

    return apply_discount(order, discount)
```

Agent化改造：

```python
async def process_order_with_agent(order):
    prompt = f"""
    根据以下订单信息，决定合适的折扣率：
    - 订单金额：{order.amount}
    - 用户VIP等级：{order.user.vip_level}
    - 用户历史订单：{order.user.hiDeepDeepDeepDeepDeepStory_count}
    - 当前促销活动：{get_active_promotions()}

    返回JSON格式：{{"discount": 0.xx, "reason": "原因"}}
    """

    result = await call_claude(prompt)
    discount_info = json.loads(result)

    # 验证合理�?
    if validate_discount(discount_info):
        return apply_discount(order, discount_info['discount'])
    else:
        # 降级到原规则
        return fallback_process_order(order)
}
```

#### 策略3：智能增强模式（推荐�?

```
在关键节点注入Agent能力，增强而非替换

┌─────────────────────────────────────�?
�?         现有业务流程                �?
�?                                    �?
�? Step 1 �?Step 2 �?Step 3 �?Step 4 �?
�?            �?                      �?
�?       ┌────┴────�?                �?
�?       �?Agent   �?(智能增强)      �?
�?       �?辅助决策 �?                �?
�?       └─────────�?                �?
└─────────────────────────────────────�?
```

**增强点识别：**

1. **数据分析增强**：自动洞察数据趋�?
2. **异常处理增强**：智能诊断问�?
3. **用户体验增强**：自然语言交互
4. **自动化增�?*：减少人工操�?

**实施案例�?*

场景：现有的数据报表系统

```javascript
// 原有功能：生成固定格式报�?
function generateReport(data) {
  return {
    summary: calculateSummary(data),
    charts: generateCharts(data),
    tables: formatTables(data)
  };
}

// Agent增强：自动洞�?+ 自然语言问答
async function enhancedReport(data, userQuestion = null) {
  // 保留原有功能
  const basicReport = generateReport(data);

  // Agent增强分析
  const DeepDeepDeepDeepDeepInsights = await analyzeWithAgent(data);

  // 如果用户有问题，直接回答
  let answer = null;
  if (userQuestion) {
    answer = await answerQuestion(data, userQuestion);
  }

  return {
    ...basicReport,
    ai_DeepDeepDeepDeepDeepInsights: DeepDeepDeepDeepDeepInsights,  // 新增：AI洞察
    qa_response: answer     // 新增：问答功�?
  };
}
```

### 4.3 改造风险控�?

#### 4.3.1 降级策略

```javascript
async function safeAgentCall(input, fallbackFunction) {
  try {
    const result = await agentProcess(input);

    // 验证结果合理�?
    if (validateResult(result)) {
      return { success: true, data: result, method: 'agent' };
    } else {
      // Agent结果不合理，降级到传统方�?
      console.warn('Agent result invalid, using fallback');
      return { 
        success: true, 
        data: fallbackFunction(input), 
        method: 'fallback' 
      };
    }
  } catch (error) {
    // Agent调用失败，降级到传统方法
    console.error('Agent failed:', error);
    return { 
      success: true, 
      data: fallbackFunction(input), 
      method: 'fallback' 
    };
  }
}
```

#### 4.3.2 AB测试框架

```javascript
class AgentABTest {
  constructor(rolloutPercentage = 10) {
    this.rolloutPercentage = rolloutPercentage;
  }

  async process(input, agentFn, legacyFn) {
    const useAgent = Math.random() * 100 < this.rolloutPercentage;

    if (useAgent) {
      const startTime = Date.now();
      const result = await agentFn(input);

      // 记录性能指标
      this.logMetrics({
        method: 'agent',
        latency: Date.now() - startTime,
        success: result.success,
        input: input
      });

      return result;
    } else {
      return legacyFn(input);
    }
  }

  logMetrics(metrics) {
    // 发送到监控系统
    analytics.track('agent_performance', metrics);
  }
}
```

#### 4.3.3 灰度发布策略

```
阶段1 (1-2�?：内部测�?
- 仅团队成员使�?
- 收集基础数据

阶段2 (2-4�?：小范围灰度 (5%)
- VIP用户 / Beta测试�?
- 密切监控错误�?

阶段3 (4-6�?：逐步扩大 (5% �?25% �?50%)
- 根据指标调整
- 准备随时回滚

阶段4 (6-8�?：全量发�?(100%)
- 保留传统方法作为备份
- 持续优化
```

### 4.4 数据迁移和兼容�?

#### 4.4.1 现有数据适配

```javascript
// 为Agent提供数据访问工具
const dataAccessTools = [
  {
    name: "query_legacy_database",
    description: "查询原有数据�?,
    function: async (params) => {
      // 适配层：转换Agent参数到原有SQL
      const sql = convertToLegacySQL(params);
      const result = await legacyDB.query(sql);

      // 格式化为Agent友好的格�?
      return formatForAgent(result);
    }
  }
];
```

#### 4.4.2 API版本共存

```javascript
// 同时支持原有API和Agent接口
app.post('/api/v1/orders', legacyOrderHandler);  // 原有接口
app.post('/api/v2/orders', agentOrderHandler);   // Agent接口
app.post('/api/chat/orders', naturalLanguageOrderHandler);  // 自然语言接口

// 统一后端处理
async function unifiedOrderProcess(order, source) {
  const processedOrder = normalizeOrder(order, source);
  return await coreOrderLogic(processedOrder);
}
```

---

## 5. 实践指南

### 5.1 Prompt工程最佳实�?

#### 5.1.1 System Prompt结构模板

```xml
<role>
你是一个专业的[具体角色]，具有以下能力：
- 能力1
- 能力2
- 能力3
</role>

<capabilities>
你可以使用以下工具：
1. [工具名称]: [工具描述]
2. [工具名称]: [工具描述]
</capabilities>

<guidelines>
在工作时请遵循以下原则：
1. [原则1]
2. [原则2]
3. [原则3]
</guidelines>

<output_format>
请按照以下格式输出结果：
[具体格式说明]
</output_format>

<constraints>
约束条件�?
- 不要做：[禁止事项]
- 必须做：[必要事项]
</constraints>
```

#### 5.1.2 Few-Shot示例技�?

```
给Agent提供2-3个高质量示例�?

示例1�?
输入：[具体输入]
思考过程：[推理步骤]
输出：[期望输出]

示例2�?
输入：[具体输入]
思考过程：[推理步骤]
输出：[期望输出]

现在处理实际任务�?
输入：[用户实际输入]
```

#### 5.1.3 思维链（Chain of Thought�?

```
在prompt中明确要求推理过程：

"请按以下步骤思考：
1. 分析问题的核心需�?
2. 识别需要的信息和工�?
3. 制定解决方案
4. 执行并验证结�?
5. 总结并给出最终答�?

请在<thinking>标签中展示你的思考过程，�?answer>标签中给出最终答案�?
```

### 5.2 工具设计规范

#### 5.2.1 工具定义清单

�?**好的工具设计�?*

- 单一职责：一个工具只做一件事
- 清晰描述：让Agent明确知道工具用�?
- 完整Schema：包含所有参数说�?
- 错误处理：返回清晰的错误信息
- 幂等性：多次调用结果一致（如果适用�?

�?**避免的设计：**

- 功能过于宽泛
- 描述模糊不清
- 缺少参数说明
- 返回格式不一�?
- 没有错误处理

#### 5.2.2 工具定义示例

```json
{
  "name": "send_email",
  "description": "发送电子邮件给指定收件人。适用于需要通知、提醒或分享信息的场景�?,
  "input_schema": {
    "type": "object",
    "properties": {
      "recipient": {
        "type": "string",
        "description": "收件人邮箱地址，必须是有效的email格式"
      },
      "subject": {
        "type": "string",
        "description": "邮件主题，简洁明确地概括邮件内容"
      },
      "body": {
        "type": "string",
        "description": "邮件正文，支持纯文本或HTML格式"
      },
      "cc": {
        "type": "array",
        "items": { "type": "string" },
        "description": "抄送人列表（可选）"
      },
      "attachments": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "filename": { "type": "string" },
            "content_type": { "type": "string" },
            "data": { "type": "string", "description": "Base64编码的文件内�? }
          }
        },
        "description": "附件列表（可选）"
      }
    },
    "required": ["recipient", "subject", "body"]
  },
  "returns": {
    "type": "object",
    "properties": {
      "success": { "type": "boolean" },
      "message_id": { "type": "string" },
      "error": { "type": "string", "description": "错误信息（如果失败）" }
    }
  }
}
```

### 5.3 上下文管理策�?

#### 5.3.1 短期记忆管理（会话级别）

```javascript
class ConversationContext {
  constructor() {
    // 使用React state或JS变量，不使用localStorage
    this.messages = [];
    this.metadata = {
      userId: null,
      sessionId: generateId(),
      startTime: Date.now()
    };
  }

  addMessage(role, content) {
    this.messages.push({
      role,
      content,
      timestamp: Date.now()
    });

    // 限制上下文长度，保持最�?0�?
    if (this.messages.length > 20) {
      this.messages = this.messages.slice(-20);
    }
  }

  getContext() {
    return {
      messages: this.messages,
      metadata: this.metadata
    };
  }

  summarizeIfNeeded() {
    // 当上下文过长时，生成摘要
    if (this.messages.length >= 15) {
      const summary = this.generateSummary();
      this.messages = [
        { role: "system", content: `历史对话摘要�?{summary}` },
        ...this.messages.slice(-5)  // 保留最�?�?
      ];
    }
  }
}
```

#### 5.3.2 长期记忆管理（跨会话�?

```javascript
// 使用 window.storage API 进行持久�?
class LongTermMemory {
  async saveKnowledge(key, value) {
    try {
      await window.storage.set(
        `knowledge:${key}`, 
        JSON.stringify(value)
      );
      return true;
    } catch (error) {
      console.error('Failed to save knowledge:', error);
      return false;
    }
  }

  async retrieveKnowledge(key) {
    try {
      const result = await window.storage.get(`knowledge:${key}`);
      return result ? JSON.parse(result.value) : null;
    } catch (error) {
      console.log('Knowledge not found:', key);
      return null;
    }
  }

  async searchKnowledge(prefix) {
    try {
      const keys = await window.storage.list(`knowledge:${prefix}`);
      const results = [];

      for (const key of keys.keys) {
        const data = await this.retrieveKnowledge(
          key.replace('knowledge:', '')
        );
        if (data) results.push(data);
      }

      return results;
    } catch (error) {
      console.error('Search failed:', error);
      return [];
    }
  }
}
```

### 5.4 错误处理和容�?

#### #### 5.4.1 自动重试机制

对于LLM调用的偶发性错误（如网络超时、限流），需要实现指数退避重试策略�?

JavaScript

```
async function callLLMWithRetry(prompt, retries = 3) {
  for (let i = 0; i < retries; i++) {
    try {
      return await anthropic.messages.create({ ... });
    } catch (error) {
      if (i === retries - 1) throw error; // 最后一次尝试失败，抛出异常

      // 指数退避：1s, 2s, 4s...
      const waitTime = Math.pow(2, i) * 1000;
      console.warn(`Attempt ${i + 1} failed. Retrying in ${waitTime}ms...`);
      await new Promise(r => setTimeout(r, waitTime));
    }
  }
}
```

#### 5.4.2 输出修复（Self-Correction�?

当模型输出的JSON格式错误或不符合Schema时，与其直接报错，不如让模型“自己修”�?

Python

```
def validate_and_fix_output(output, schema):
    try:
        # 尝试解析
        data = json.loads(output)
        validate(instance=data, schema=schema)
        return data
    except Exception as e:
        # 捕获错误，构建修复Prompt
        fix_prompt = f"""        你之前的输出存在格式错误：{str(e)}
                原始输出�?       {output}
                请输出修正后的JSON，不要包含任何解释文字�?       """
        # 递归调用（限制深度）
        return call_llm(fix_prompt)
```

#### 5.5 评估体系 (Evals)

基于《Building effective agents》，评估是开发的生命线�?

1. **单元测试 (Unit Tests)**�?
   
   - 测试Prompt是否总是返回合法的JSON�?
   
   - 测试工具调用参数是否符合Type检查�?

2. **基于模型的评�?(Model-based Evals)**�?
   
   - 使用更强的模型（�?Claude 3.5 Sonnet）来评估较小模型的输出�?
   
   - 评分标准：准确性、相关性、语气安全性�?

3. **黄金数据�?(Golden Dataset)**�?
   
   - 维护一份包�?50+ 个典型“输�?理想输出”对的测试集�?
   
   - 每次修改 Prompt 后，必须跑通这个测试集�?

---

### 6. 技术实�?

本章节定义了推荐的技术栈和实现细节�?

#### 6.1 推荐技术栈

- **核心语言**：Python (数据处理/后端) �?TypeScript (全栈/前端)�?

- **LLM 接入**：直接使�?SDK (`anthropic`, `openai`)，避免过度封装的 LangChain（除非需要快速原型）�?

- **数据验证**：Pydantic (Python) �?Zod (TypeScript)。这是结构化输出的核心�?

- **向量数据�?*：Pinecone / Milvus (仅在需要大规模 RAG 时引入，简单记忆可�?JSON/Redis)�?

#### 6.2 关键代码模式：结构化输出

不要让模型随意输出文本，**强制结构�?*�?

**Python (Pydantic) 示例:**

Python

```
from pydantic import BaseModel, Field
from typing import List

# 定义输出结构
class ActionPlan(BaseModel):
    reasoning: str = Field(description="思考过�?)
    steps: List[str] = Field(description="具体执行步骤")
    tool_needed: str = Field(description="需要的工具名称")

# 在Prompt中注入Schema
system_prompt = f"""请严格按照以下JSON Schema输出�?
{ActionPlan.model_json_schema()}
"""
```

#### 6.3 状态机管理

对于复杂 Agent，使用有限状态机 (FSM) 管理状态流转�?

Code snippet

```
stateDiagram-v2
    [*] --> Idle
    Idle --> Planning: 接收任务
    Planning --> Executing: 生成计划
    Executing --> Reviewing: 步骤完成
    Reviewing --> Executing: 步骤未通过
    Reviewing --> Finishing: 全部完成
    Finishing --> Idle: 交付结果
```

---

### 7. 开发流�?(SDLC for Agents)

将《Building effective agents》的理念转化为标准开发生命周期�?

#### 阶段 1: 探索与原�?(Playground Phase)

- **工具**：Claude Workbench / Console�?

- **目标**：验证可行性�?

- **动作**�?
  
  1. 手动编写 System Prompt�?
  
  2. 粘贴几个典型 Case 进行测试�?
  
  3. **如果成功率低�?50%，放�?Agent 模式，改用传统代码�?*

#### 阶段 2: 技能封�?(Skill Definition)

- **工具**：IDE (VS Code)�?

- **目标**：代码化�?

- **动作**�?
  
  1. 编写 `SKILL_NAME.md` (SOP文档)�?
  
  2. 编写对应�?Python/TS 函数�?
  
  3. 编写单元测试，确保函数逻辑正确�?

#### 阶段 3: 提示词工�?(Prompt Engineering)

- **工具**：代码库�?

- **目标**：提升稳定性�?

- **动作**�?
  
  1. 添加 `<thinking>` 标签�?
  
  2. 添加 3-5 �?Few-shot examples (少样本示�?�?
  
  3. 使用 XML 标签隔离 `<user_input>` �?`<context>`�?

#### 阶段 4: 评估与迭�?(Evaluation Phase)

- **工具**：测试脚本�?

- **目标**：量化质量�?

- **动作**�?
  
  1. 运行 Golden Dataset�?
  
  2. 分析失败案例 (Failure Analysis)�?
  
  3. **优化路径**�?
     
     - 逻辑错误 -> 修改 Prompt (增加 CoT)�?
     
     - 工具参数�?-> 修改 Tool Definition�?
     
     - 缺少知识 -> 增加 Context/RAG�?

---

### 8. 实战案例 (Case Studies)

#### 案例一：智能代码审查助�?(Automated Code Reviewer)

- **模式**：Orchestrator-Workers (编排�?工人模式)�?

- **架构**�?
  
  1. **Orchestrator**: 分析 Pull Request 改动范围，决定重点检查哪里（安全性、性能、风格）�?
  
  2. **Worker A (Security)**: 专门加载 OWASP Top 10 规则，扫描漏洞�?
  
  3. **Worker B (Style)**: 检查命名规范和注释�?

- **关键 Prompt 策略**�?
  
  - System Prompt 中包含具体的代码风格指南 (Style Guide)�?
  
  - 要求输出必须包含 `<file_path>`, `<line_number>`, `<suggestion>` 标签�?

#### 案例二：企业级数据查�?Agent (Text-to-SQL)

- **模式**：Tool Use + Reflection�?

- **痛点**：用户不�?SQL，数据库结构复杂�?

- **流程**�?
  
  1. **Thinking**: 用户问“上个月销售额”，模型思考“需要查�?orders 表，时间范围�?2023-10-01 �?2023-10-31”�?
  
  2. **Tool Call**: 生成 SQL 语句�?
  
  3. **Reflection (关键�?**: 在执行前，Agent 自己检查生成的 SQL：“这�?SQL 会不会全表扫描？字段名是否存在？�?
  
  4. **Execute**: 执行查询�?
  
  5. **Response**: 将数据转换为自然语言回答�?

#### 案例三：客户服务路由系统 (Triage Agent)

- **模式**：Routing (路由模式) + Workflow�?

- **描述**：这是一个纯 Workflow 的优化，不是完全自主 Agent�?

- **流程**�?
  
  1. **Input**: 用户邮件�?
  
  2. **Classification**: 模型分析意图（退款、技术支持、投诉）�?
  
  3. **Extraction**: 提取关键信息（订单号、用户ID）�?
  
  4. **Action**:
     
     - 如果“退款”且“金�?< $50�?-> **自动调用退�?API**�?
     
     - 如果“技术支持�?-> **生成工单并派发给人工**�?

- **价�?*：利�?AI 的理解能力，结合传统代码的确定性执行�?

---

### 9. 附录：检查清�?(Definition of Done)

在发布任�?Agent 功能前，请核对以下清单：

- [ ] **Prompt 结构**: 是否使用�?XML 标签隔离数据�?

- [ ] **思维�?*: 是否强制开启了 `<thinking>`�?

- [ ] **工具定义**: 每个工具是否有清晰的 Description 和参数说明？

- [ ] **安全边界**: System Prompt 是否明确了“什么不能做”？

- [ ] **测试覆盖**: 是否通过了至�?20 个不同场景的测试用例�?

- [ ] **降级方案**: 如果 LLM 挂了，系统是否会崩溃（还是优雅降级）�?

- [ ] **成本估算**: 是否计算�?Token 消耗量�?

---


