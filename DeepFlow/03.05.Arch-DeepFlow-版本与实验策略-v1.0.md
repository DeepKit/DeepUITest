# UniFlow 版本与实验策�?

> **文档编号**: 03.02  
> **版本**: 1.0  
> **更新日期**: 2025-12-03  
> **关联文档**: `03.01.Arch-UniFlow架构设计-v1.0.md` / `04.01.Data-UniFlow数据模型-v1.0.md` / `05.01.API-UniFlow接口规范-v1.0.md` / `05.02.API-UniFlow-MCP支持规划-v1.0.md`

---

## 1. 文档目的

本文件从架构视角定义 UniFlow �?**版本管理与实验（灰度 / A/B）策�?*，目标是�?

- 统一不同层级的“版本”含义：平台、Schema、Skill、Workflow、配�?策略�?
- 约定版本号的语义、使用方式与兼容性承诺；
- 定义如何在不破坏稳定性的前提下进�?**灰度发布与实�?*�?
- 为日志、审计、排障与合规提供可追溯的版本信息�?

> 核心原则�?*所有重要行为在“是什么版本”这个问题上必须可追溯、可解释�?*

---

## 2. 版本维度总览

UniFlow 中与版本相关的主要维度：

1. **平台版本（Platform / Engine�?*
   - DeepBase + UniFlow Core + Python Skill 服务整体代码基线�? 
   - 对外用于描述“当前跑的是 MVP / 1.0 / 理想版哪一阶段 + 小版本号”�?

2. **数据�?Schema 版本（Schema Version�?*
   - `schema_migrations` 表中递增的版本号�? 
   - 控制 SQLite / PostgreSQL 等数据结构的演进次序�?

3. **Skill 版本（Skill Version�?*
   - 每个 Skill 的版本号，体现在 `TSkillMeta.Version` / Skill Registry 中�? 
   - 可存在多个版本并行（尤其�?Official / Community / User Skill）�?

4. **Workflow 模板版本（Workflow Definition Version�?*
   - `workflow_definitions.version` 字段�? 
   - 同一 Workflow ID 的多个版本可共存，老实例需要标明来自哪个版本�?

5. **配置 / Persona / 策略版本**
   - LLM 配置（`llm_configs`）、Persona（`ai_personas`）、合规策略（`compliance_policies`）、角�?权限（`roles`）、保留策略（`retention_policies`）�?
   - 多为“数据驱动”的版本演进，需要记录变更与回滚路径�?

6. **文档与规范版�?*
   - �?Markdown 规范文件内的 `文档版本` �?`更新日期`�? 
   - 为设计决策与实现提供“生效时点”的上下文�?

---

## 3. 版本编号规范

### 3.1 平台版本（UniFlow 平台�?

- 使用 **语义化版�?*：`MAJOR.MINOR.PATCH`。例如：`0.3.1`�?
- 对外同时关联到产品阶段：
  - MVP：`0.x.y` 对应 MVP 能力范围�?
  - 1.0：`1.x.y` 对应可对外发布版本；
  - 理想版：`2.x.y` 及后续，代表“智能应�?OS”完整能力�?

**语义�?*

- `MAJOR`：可能存在不兼容变更�?
  - API 重大调整、数�?Schema 重大变更�?
  - 不保证旧客户�?/ 旧数据无需迁移即可运行�?
- `MINOR`：向后兼容新增功能：
  - 增加�?Workflow 模板、新 Skill、新配置项；
  - 保证现有功能/接口/数据结构继续可用�?
- `PATCH`：兼容性修复：
  - Bug 修复、性能优化、文档修正；
  - 不改变对外可见的行为契约�?

### 3.2 数据 Schema 版本

- �?`schema_migrations` 中维护递增整数版本：`1, 2, 3, ...`�?
- 每个迁移脚本只做“一件事”：
  - 新增�?/ 字段 / 索引�? 
  - 或安全地变更字段属性（必要时伴随数据迁移）�?
- 平台启动时：
  - 检查当�?DB �?Schema 版本�?
  - 若低于最低支持版本，拒绝启动并提示需要迁移�?

### 3.3 Skill 版本

- 每个 Skill 有独立版本号，采�?**语义化版�?* `MAJOR.MINOR.PATCH`�?

  - `MAJOR`：不兼容变更�?
    - 输入/输出 Schema 发生破坏性变化；
    - 行为语义有明显改变（例如从“创建”变成“覆盖写入”）�?

  - `MINOR`：向后兼容增强：
    - 增加可选字段；
    - 行为增强但旧调用方式仍然安全有效�?

  - `PATCH`：Bugfix�?
    - 在不改变接口与语义前提下修复错误�?

- Registry 需要支持：
  - 每个 `SkillName` 有一个标记为 `DefaultVersion` 的版本；
  - 允许 Workflow/租户/Session 显式指定 Skill 版本（Pin）�?

### 3.4 Workflow 模板版本

- `workflow_definitions` 中：
  - `id`：逻辑标识�?
  - `version`：整数，�?1 递增�?

- 当发生以下变更时，必�?`version++`�?
  - 节点拓扑变更（新�?删除/重排节点）；
  - 输入/输出结构发生变化�?
  - 引入了新的外部副作用（如外部写操作）�?

- 工作流实例表 `workflow_instances` 中，必须记录�?
  - `definition_id` + `definition_version`（可通过新增字段或序列化�?`context_json` 中存档）�?

### 3.5 配置 / Persona / 策略版本

- 对于配置类实体：
  - �?`llm_configs`�?
    - 使用 `id` + `name` + `created_at` + `is_default` 模式�?
    - 可增加简�?`version` 字段（整数），便于人类理解�?

- 对于策略/权限类实体：
  - �?`compliance_policies`、`roles`、`retention_policies`�?
    - 每次重大调整需增加版本或新记录�?
    - 变更需要伴随“变更原�?/ 影响范围 / 回滚策略”的记录（可存在额外的变更日志表中）�?

---

## 4. 运行时行为与兼容策略

### 4.1 Skill 版本选择

- Quartermaster 提供统一接口�?

  ```pascal
  type
    ISkillRegistry = interface
      function ResolveSkill(const AName: string; const AVersion: string = ''): ISkill;
      function GetDefaultVersion(const AName: string): string;
      function ListVersions(const AName: string): TArray<string>;
    end;
  ```

- 版本选择策略（优先级）：
  1. Workflow 节点显式指定版本 �?优先使用�?
  2. 租户 / Session 层有显式 Pin �?其次�?
  3. 否则使用 Registry 中的 `DefaultVersion`�?

- 对于 Official Skills�?
  - `MAJOR` 升级应谨慎，建议先通过灰度 / 实验运行�?
  - `MINOR/PATCH` 升级可逐步提升为默认版本，同时保留旧版本一段时间�?

### 4.2 Workflow 版本与实例行�?

- 新建实例�?
  - 默认使用 WorkflowDef 当前 `active` 版本�?
  - 支持显式指定版本（用于回归测试或兼容老行为）�?

- 已有实例�?
  - 原则�?*不自动切换到新版本定�?*，除非有专门的迁移工具与策略�?
  - 在查看实例详情与日志时，需明确显示其版本来源�?

- 变更影响�?
  - 对关键业�?Workflow 的版本升级，应伴随：
    - 变更说明�?
    - 回滚方案（退回旧版本 + 停用新实例创建）�?

### 4.3 API 版本与外部兼�?

- 若对外提�?HTTP API�?
  - 优先采用 URL 前缀方式：`/api/v1/...`�?
  - 当需要破坏性变更时，新�?`/api/v2/...`，保�?`/v1` 若干时间�?

- API 文档中需�?
  - 明确各版本支持的字段与行为；
  - 标记弃用字段与弃用时间表�?

### 4.4 MCP 与版�?

- 对于 MCP Tool�?
  - 尽量通过内部 Skill 封装进行版本管理�?
    - MCP Tool 版本变化 �?先在封装层适配�?
    - 仅在语义发生重大变化时更新内�?Skill 版本�?

- 对外暴露 UniFlow �?MCP Server 时：
  - MCP Tool 描述中应包含内部 Skill 版本信息�?
  - 若需要破坏性变更，可通过�?Tool 名或 `version` 字段区分�?

---

## 5. 实验与灰度策�?

### 5.1 Feature Flag vs Version

- 对于**尚在验证价值或不够稳定**的能力，优先使用 Feature Flag 控制�?
  - 示例�?
    - `EnableDocSearch`（是否启用外部资料检�?Skill）；
    - `EnableLongRunningWorkflow`（是否启用长�?Workflow）�?

- Feature Flag 维度�?
  - 全局 / 租户 / 产品 / 用户级别�?
  - 通过配置表或专用 FeatureFlag 服务管理�?

- 当一个功能通过实验验证后，再考虑�?
  - 固化为正�?Workflow 模板 / Skill 版本�?
  - �?Flag 退役（仅保留为应�?Kill Switch）�?

### 5.2 A/B 测试与多版本对比

- 对于关键逻辑（如决策流程、评分算法），可以并行维护两套版本：
  - `Workflow v1` �?`Workflow v2`�?
  - �?`Skill.DocSearch 1.x` �?`Skill.DocSearch 2.x`�?

- Dispatcher/Commander 支持�?

  ```pascal
  type
    TExperimentConfig = record
      ExperimentId: string;
      Variants: TArray<string>; // �?['v1', 'v2']
      TrafficSplit: TArray<Double>; // �?[0.5, 0.5]
    end;
  ```

- 运行时：
  - 按用�?Session/租户做哈希，将请求路由到不同版本�?
  - 在审计与实验日志中记录：
    - 使用了哪�?Variant（版本）�?
    - 关键业务指标（时�?成功�?用户反馈）�?

### 5.3 Kill Switch（紧急开关）

- 对于可能引入高风险的版本（特别是�?
  - 涉及资金、外部系统写入、数据删除；
  - 安全策略与权限改动），需为其设计 Kill Switch�?

  - 一旦发现严重问题：
    - 通过配置迅速禁用新版本�?
    - 回退到旧版本或禁用相关功能�?

- Kill Switch 的配置项应：
  - 有明确的所有者（Owner）；
  - 有变更记录与审批流程�?

---

## 6. 审计、安全与版本

### 6.1 审计日志中必须包含的版本信息

�?`audit_logs` / `audit_logs_detailed` 等表中，建议增加或保证：

- `platform_version`：当�?UniFlow 平台版本�?
- `workflow_def_id` + `workflow_def_version`�?
- `skill_name` + `skill_version`�?
- 关键策略版本（如 `policy_id` / `retention_policy_id`）�?

这样在排查问题或做合规审计时，可以回答：

> “当时跑的是哪一版逻辑？当时适用的是哪一版策略？�?

### 6.2 风险敏感变更的版本要�?

对于涉及以下内容的变更，必须视为**高风险变�?*�?

- 权限模型 / 租户隔离�?
- 数据暴露范围（新增字段到外部接口/日志）；
- 安全策略（加密方式、合规规则）�?

要求�?

1. 必须增加版本（平�?/ Skill / Workflow / 策略之一或多者）�?
2. 必须有变更记录（Why / What / Impact / Rollback）；
3. 必须经过安全评审（仙儿角色）确认�?

---

## 7. 实施路线建议

### 7.1 第一阶段（当�?~ MVP 完成�?

- 明确当前代码基线�?**平台版本**（如 `0.1.0-MVP`）；
- �?`TSkillMeta` / WorkflowDef 等结构中补齐版本字段的使用；
- 在审计日志和错误日志中开始记�?Skill/Workflow 版本信息�?

### 7.2 第二阶段�?.0�?

- 正式采用语义化版本管�?Skill�?
  - 给现�?Official Skills 补充版本号；
  - �?Registry 中实现版本解�?默认版本逻辑�?

- 对关�?Workflow 模板（洞�?/ 元老院）确立版本编号与变更流程�?
- 实现基础�?Feature Flag 机制，支持部分功能灰度启用�? 

### 7.3 第三阶段（理想版本）

- 引入更完整的实验框架�?
  - 支持 Workflow/Skill 层面�?A/B 测试�?
  - 统一收集 Variant 间的对比指标�?

- �?Kill Switch 体系与运维告警联动：
  - 某版本错误率/延迟/成本异常升高时，自动建议或触发回退�?

- 在安全与合规层面，版本信息全面纳入审计与报告体系�?

---

## 8. 相关文档

- `03.01.Arch-UniFlow架构设计-v1.0.md`  
- `04.01.Data-UniFlow数据模型-v1.0.md`  
- `05.01.API-UniFlow接口规范-v1.0.md`  
- `05.02.API-UniFlow-MCP支持规划-v1.0.md`  
- `06.03.Dev-UniFlow-MVP开发规�?v1.0.md`  
- `06.05.Dev-UniFlow-1.0开发规�?v1.0.md`  
- `06.07.Dev-UniFlow-理想版本规范-v1.0.md`

---

*文档版本: 1.0*  
*更新日期: 2025-12-03*