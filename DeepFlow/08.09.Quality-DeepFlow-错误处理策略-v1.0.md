# UniFlow 错误处理策略

> 定义 UniFlow 的错误分类、错误码体系、错误传播链与降级策略�?
>
> 与安�?权限相关的前置约束以 `08.01.Quality-UniFlow-安全与数据权限策�?v1.0.md` 为准，测试验证方法可结合 `08.07.Quality-UniFlow-测试策略-v1.0.md` 使用�?

创建日期: 2025-12-03  版本: 1.0

---

## 1. 错误分类

### 1.1 按严重程度分�?

| 等级 | 名称 | 描述 | 处理方式 |
|------|------|------|----------|
| L1 | Fatal | 系统级故障，无法继续运行 | 立即停止，告警，人工介入 |
| L2 | Error | 请求级故障，当前请求失败 | 返回错误，记录日志，可重�?|
| L3 | Warning | 降级运行，功能受限但可继�?| 记录警告，触发告警，自动恢复 |
| L4 | Info | 预期内的业务拒绝（如权限不足�?| 返回业务错误码，正常记录 |

### 1.2 按来源分�?

| 来源 | 错误码前缀 | 示例 |
|------|-----------|------|
| 引擎/基础设施 | `ENGINE_` | `ENGINE_ROLE_NOT_FOUND` |
| 守卫/校验 | `GUARD_` | `GUARD_PERMISSION_DENIED` |
| 智囊/AI | `AI_` | `AI_TIMEOUT`, `AI_HALLUCINATION` |
| 执行�?Skill | `SKILL_` | `SKILL_EXECUTION_FAILED` |
| 通信�?外部 | `EXTERNAL_` | `EXTERNAL_SERVICE_UNAVAILABLE` |
| 调度�?Workflow | `WORKFLOW_` | `WORKFLOW_STEP_FAILED` |
| 业务逻辑 | `BIZ_` | `BIZ_INVALID_INPUT` |

---

## 2. 标准错误�?

### 2.1 引擎错误 (ENGINE_)

| 错误�?| 描述 | 可重�?| 建议处理 |
|--------|------|--------|----------|
| `ENGINE_ROLE_NOT_FOUND` | 目标角色未注�?| �?| 检查角色注�?|
| `ENGINE_ROLE_UNAVAILABLE` | 角色暂时不可�?| �?| 等待后重�?|
| `ENGINE_MESSAGE_TIMEOUT` | 消息处理超时 | �?| 增加超时或拆分任�?|
| `ENGINE_QUEUE_FULL` | 消息队列已满 | �?| 等待或降�?|
| `ENGINE_SIGNATURE_INVALID` | 消息签名无效 | �?| 检查发送方身份 |
| `ENGINE_STARTUP_FAILED` | 引擎启动失败 | �?| 检查配置和依赖 |

### 2.2 守卫错误 (GUARD_)

| 错误�?| 描述 | 可重�?| 建议处理 |
|--------|------|--------|----------|
| `GUARD_PERMISSION_DENIED` | 权限不足 | �?| 检查用户角�?|
| `GUARD_SCHEMA_VIOLATION` | 输入不符�?Schema | �?| 修正输入格式 |
| `GUARD_RATE_LIMITED` | 触发限流 | �?| 等待后重�?|
| `GUARD_BUDGET_EXCEEDED` | 超出资源预算 | �?| 申请更多配额 |
| `GUARD_OUTPUT_REJECTED` | 输出校验失败 | �?| 检查生成逻辑 |
| `GUARD_HALLUCINATION_DETECTED` | 检测到幻觉 | �?| 换模型或调整 Prompt |

### 2.3 AI 错误 (AI_)

| 错误�?| 描述 | 可重�?| 建议处理 |
|--------|------|--------|----------|
| `AI_TIMEOUT` | LLM 调用超时 | �?| 重试或降级到规则 |
| `AI_RATE_LIMITED` | LLM API 限流 | �?| 等待后重�?|
| `AI_CONTEXT_TOO_LONG` | 上下文超�?| �?| 截断或摘�?|
| `AI_PARSE_FAILED` | 输出解析失败 | �?| 重试或放宽格�?|
| `AI_MODEL_UNAVAILABLE` | 模型不可�?| �?| 切换备用模型 |
| `AI_SAFETY_BLOCKED` | 安全策略拦截 | �?| 调整 Prompt |

### 2.4 Skill 错误 (SKILL_)

| 错误�?| 描述 | 可重�?| 建议处理 |
|--------|------|--------|----------|
| `SKILL_NOT_FOUND` | Skill 未注�?| �?| 检�?Skill ID |
| `SKILL_EXECUTION_FAILED` | 执行失败 | 视情�?| 检查日�?|
| `SKILL_TIMEOUT` | 执行超时 | �?| 增加超时或优�?Skill |
| `SKILL_INVALID_PARAMS` | 参数错误 | �?| 检查参�?Schema |
| `SKILL_DEPENDENCY_FAILED` | 依赖服务失败 | �?| 检查依赖状�?|

### 2.5 外部服务错误 (EXTERNAL_)

| 错误�?| 描述 | 可重�?| 建议处理 |
|--------|------|--------|----------|
| `EXTERNAL_SERVICE_UNAVAILABLE` | 服务不可�?| �?| 熔断/降级 |
| `EXTERNAL_TIMEOUT` | 调用超时 | �?| 重试或降�?|
| `EXTERNAL_AUTH_FAILED` | 认证失败 | �?| 检查凭�?|
| `EXTERNAL_RATE_LIMITED` | 第三方限�?| �?| 等待后重�?|
| `EXTERNAL_RESPONSE_INVALID` | 响应格式异常 | �?| 检�?API 版本 |

### 2.6 Workflow 错误 (WORKFLOW_)

| 错误�?| 描述 | 可重�?| 建议处理 |
|--------|------|--------|----------|
| `WORKFLOW_NOT_FOUND` | Workflow 定义不存�?| �?| 检�?ID |
| `WORKFLOW_INSTANCE_NOT_FOUND` | 实例不存�?| �?| 检查实�?ID |
| `WORKFLOW_STEP_FAILED` | 步骤执行失败 | 视情�?| 检查步骤日�?|
| `WORKFLOW_MAX_RETRIES` | 超过最大重试次�?| �?| 人工介入 |
| `WORKFLOW_CANCELLED` | 已被取消 | �?| 确认取消原因 |
| `WORKFLOW_WAITING_HUMAN` | 等待人工输入 | �?| 提示用户操作 |

---

## 3. 错误传播�?

### 3.1 传播规则

```
错误产生 �?本地处理（重�?降级�?�?上报上游 �?最终兜�?

┌─────────────────────────────────────────────────────────────────�?
�?                       错误传播路径                              �?
├─────────────────────────────────────────────────────────────────�?
�?                                                                �?
�?  执行�?通信�?�?智囊 �?调度�?�?总指�?�?外部调用�?           �?
�?       �?          �?       �?        �?                       �?
�?    本地重试    降级处理  步骤重试  最终响�?                     �?
�?                                                                �?
�?  守卫可在任何环节拦截并直接返回错�?                            �?
�?                                                                �?
└─────────────────────────────────────────────────────────────────�?
```

### 3.2 错误包装

每层传播时应包装错误，保留原始信息：

```typescript
interface WrappedError {
  code: string              // 当前层错误码
  message: string           // 当前层描�?
  cause?: WrappedError      // 原始错误（递归�?
  role: RoleType            // 产生错误的角�?
  timestamp: Date
  correlationId: string
  context?: Record<string, unknown>
}

// 示例：智囊调�?LLM 超时
{
  code: "WORKFLOW_STEP_FAILED",
  message: "步骤 'generate_title' 执行失败",
  role: "dispatcher",
  cause: {
    code: "AI_TIMEOUT",
    message: "LLM 调用超时",
    role: "advisor",
    cause: {
      code: "EXTERNAL_TIMEOUT",
      message: "POST /v1/chat/completions 超时",
      role: "signalOfficer"
    }
  }
}
```

### 3.3 错误截断

为避免错误链过长，最多保�?5 �?`cause`；超过时保留首尾并标�?`truncated: true`�?

---

## 4. 重试策略

### 4.1 重试决策

```typescript
interface RetryPolicy {
  maxRetries: number        // 最大重试次�?
  backoff: 'fixed' | 'linear' | 'exponential'
  baseMs: number            // 基础等待时间
  maxBackoffMs: number      // 最大等待时�?
  retryableErrors: string[] // 可重试的错误�?
  nonRetryableErrors: string[] // 不可重试的错误码
}

// 默认策略
const defaultRetryPolicy: RetryPolicy = {
  maxRetries: 3,
  backoff: 'exponential',
  baseMs: 500,
  maxBackoffMs: 30000,
  retryableErrors: [
    'ENGINE_ROLE_UNAVAILABLE',
    'ENGINE_MESSAGE_TIMEOUT',
    'AI_TIMEOUT',
    'AI_RATE_LIMITED',
    'EXTERNAL_SERVICE_UNAVAILABLE',
    'EXTERNAL_TIMEOUT',
    'SKILL_TIMEOUT'
  ],
  nonRetryableErrors: [
    'GUARD_PERMISSION_DENIED',
    'GUARD_SCHEMA_VIOLATION',
    'AI_SAFETY_BLOCKED'
  ]
}
```

### 4.2 退避算�?

```typescript
function calculateBackoff(attempt: number, policy: RetryPolicy): number {
  let delay: number
  switch (policy.backoff) {
    case 'fixed':
      delay = policy.baseMs
      break
    case 'linear':
      delay = policy.baseMs * attempt
      break
    case 'exponential':
      delay = policy.baseMs * Math.pow(2, attempt - 1)
      break
  }
  // 添加抖动（�?0%�?
  const jitter = delay * 0.1 * (Math.random() * 2 - 1)
  return Math.min(delay + jitter, policy.maxBackoffMs)
}
```

---

## 5. 降级策略

### 5.1 降级类型

| 类型 | 触发条件 | 降级行为 |
|------|----------|----------|
| **AI 降级** | LLM 不可用或超时 | 使用规则/模板替代 |
| **Skill 降级** | Skill 执行失败 | 返回缓存结果或默认�?|
| **外部服务降级** | 服务不可�?| 熔断 + 本地缓存 |
| **Workflow 降级** | 步骤反复失败 | 跳过可选步�?|

### 5.2 降级配置

```typescript
interface DegradationConfig {
  role: RoleType
  trigger: {
    errorCodes: string[]
    consecutiveFailures: number
    timeWindowMs: number
  }
  action: {
    type: 'fallback' | 'skip' | 'cache' | 'circuit_break'
    fallbackValue?: unknown
    cacheTtlMs?: number
    circuitBreakDurationMs?: number
  }
  notification: {
    level: 'info' | 'warning' | 'critical'
    channels: ('log' | 'alert' | 'slack')[]
  }
}

// 示例：智囊降级配�?
const advisorDegradation: DegradationConfig = {
  role: 'advisor',
  trigger: {
    errorCodes: ['AI_TIMEOUT', 'AI_MODEL_UNAVAILABLE'],
    consecutiveFailures: 3,
    timeWindowMs: 60000
  },
  action: {
    type: 'fallback',
    fallbackValue: { intent: { type: 'unknown' }, confidence: 0 }
  },
  notification: {
    level: 'warning',
    channels: ['log', 'alert']
  }
}
```

### 5.3 熔断�?

```typescript
interface CircuitBreaker {
  state: 'closed' | 'open' | 'half_open'
  
  // 关闭状态：正常请求
  // 打开状态：直接拒绝，返回降级响�?
  // 半开状态：允许少量请求探测

  config: {
    failureThreshold: number      // 失败次数阈�?
    successThreshold: number      // 半开状态成功次数阈�?
    timeoutMs: number             // 打开状态持续时�?
    monitorWindowMs: number       // 监控窗口
  }
}

// 示例配置
const externalServiceCircuitBreaker = {
  failureThreshold: 5,
  successThreshold: 3,
  timeoutMs: 30000,
  monitorWindowMs: 60000
}
```

---

## 6. 错误恢复

### 6.1 自动恢复

| 场景 | 恢复策略 |
|------|----------|
| 角色心跳超时 | 引擎自动重启角色（最�?3 次） |
| 消息队列阻塞 | 自动扩展队列或丢弃低优先级消�?|
| 外部服务恢复 | 熔断器自动进入半开状态探�?|
| Workflow 步骤失败 | 按重试策略自动重�?|

### 6.2 人工恢复

| 场景 | 触发条件 | 处理方式 |
|------|----------|----------|
| 角色启动失败 | 连续 3 次重启失�?| 告警 + 等待运维 |
| 数据不一�?| 校验检测到异常 | 告警 + 人工修复 |
| 安全事件 | 守卫检测到异常行为 | 隔离 + 人工调查 |
| Workflow 阻塞 | 超过 SLA 未完�?| 告警 + 人工介入 |

---

## 7. 错误监控与告�?

### 7.1 错误指标

```typescript
interface ErrorMetrics {
  // 计数�?
  errorCount: Counter              // 按错误码分组
  retryCount: Counter              // 重试次数
  degradationCount: Counter        // 降级次数
  
  // 比率
  errorRate: Gauge                 // 错误率（滑动窗口�?
  retrySuccessRate: Gauge          // 重试成功�?
  
  // 直方�?
  errorLatency: Histogram          // 错误响应延迟
  
  // 标签
  labels: ['role', 'errorCode', 'retryable']
}
```

### 7.2 告警规则

| 指标 | 阈�?| 告警级别 |
|------|------|----------|
| 错误�?| > 5% (5分钟) | Warning |
| 错误�?| > 10% (5分钟) | Critical |
| 连续失败 | > 10 �?| Critical |
| 熔断器打开 | 任意服务 | Warning |
| L1 Fatal 错误 | 任意 | Critical |

---

## 8. 错误日志格式

```json
{
  "timestamp": "2025-12-03T08:30:00Z",
  "level": "ERROR",
  "role": "advisor",
  "action": "understand",
  "error": {
    "code": "AI_TIMEOUT",
    "message": "LLM 调用超时",
    "retryable": true,
    "attempt": 2,
    "maxRetries": 3
  },
  "context": {
    "correlationId": "xxx",
    "traceId": "yyy",
    "workflowInstanceId": "wfi-001",
    "inputSize": 1024,
    "model": "gpt-4"
  },
  "cause": {
    "code": "EXTERNAL_TIMEOUT",
    "role": "signalOfficer"
  }
}
```

---

## 9. 与现有文档的关系

- 错误码在《UniFlow-消息协议规范.md》的响应中使�?
- 接口方法的返回类型引用《UniFlow-接口契约定义.md》的 `Result<T>`
- 告警渠道与《UniFlow-指标体系.md》配�?
- 降级行为影响《UniFlow-角色图谱.md》中的请求流
