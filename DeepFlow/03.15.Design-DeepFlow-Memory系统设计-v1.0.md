# UniFlow Memory 系统设计

> 详细定义 UniFlow 的三层记忆架构、向量检索方案、容量管理与淘汰策略�?

创建日期: 2025-12-04  版本: 1.0

---

## 1. 概述

Memory 系统�?AI Agent 的核心能力之一，负责管理上下文、存储知识、支持回忆。UniFlow 采用三层记忆架构，平衡性能与持久化需求�?

### 1.1 设计目标

- **上下文连�?*：跨轮对话保持上下文
- **知识积累**：用户偏好和历史经验可学�?
- **高效检�?*：语义相关内容快速召�?
- **容量可控**：防止无限增�?

### 1.2 Memory 在角色中的位�?

```
┌─────────────────────────────────────────────────────────────�?
�?                       Memory 系统                          �?
├─────────────────────────────────────────────────────────────�?
�?                                                             �?
�? ┌──────────────�? ┌──────────────�? ┌──────────────�?      �?
�? �?Working      �? �?Session      �? �?Long-term    �?      �?
�? �?Memory       �? �?Memory       �? �?Memory       �?      �?
�? �?(工作记忆)   �? �?(会话记忆)   �? �?(长期记忆)   �?      �?
�? �?             �? �?             �? �?             �?      �?
�? �?- 当前任务   �? �?- 对话历史   �? �?- 用户偏好   �?      �?
�? �?- 临时变量   �? �?- 上下文窗�?�? �?- 历史经验   �?      �?
�? �?- 执行状�?  �? �?- 意图追踪   �? �?- 知识图谱   �?      �?
�? └──────────────�? └──────────────�? └──────────────�?      �?
�?       �?                �?                �?               �?
�?       �?                �?                �?               �?
�? ┌─────┴─────────────────┴─────────────────┴─────�?         �?
�? �?             Memory Manager                    �?         �?
�? �? - 层级路由    - 容量管理    - 淘汰策略       �?         �?
�? └───────────────────────────────────────────────�?         �?
�?                         �?                                  �?
�?                         �?                                  �?
�? ┌───────────────────────┴───────────────────────�?         �?
�? �?             Vector Store (可�?               �?         �?
�? �? - 本地: SQLite FTS5 / Chroma                 �?         �?
�? �? - 云端: Pinecone / Weaviate                  �?         �?
�? └───────────────────────────────────────────────�?         �?
└─────────────────────────────────────────────────────────────�?
```

---

## 2. 三层记忆架构

### 2.1 Working Memory（工作记忆）

**定位**：当前任务执行期间的短期存储

**特点**�?
- 生命周期：单次请�?Workflow 执行期间
- 存储位置：内�?
- 容量：小�? 1MB�?
- 访问速度�? 1ms

**存储内容**�?
```pascal
TWorkingMemory = class
private
  FCurrentTaskId: string;           // 当前任务 ID
  FVariables: TDictionary<string, TJSONValue>;  // 临时变量
  FExecutionStack: TStack<TStepContext>;        // 执行�?
  FIntermediateResults: TList<TJSONObject>;     // 中间结果
  FStartTime: TDateTime;
public
  procedure Set(const AKey: string; AValue: TJSONValue);
  function Get(const AKey: string): TJSONValue;
  procedure Push(AContext: TStepContext);
  function Pop: TStepContext;
  procedure Clear;  // 任务结束时调�?
end;
```

**使用场景**�?
```pascal
// Workflow 执行中传递变�?
WorkingMemory.Set('user_intent', TJSONString.Create('shotlist'));
WorkingMemory.Set('extracted_entities', LEntitiesJSON);

// 后续步骤读取
LIntent := WorkingMemory.Get('user_intent').Value;
```

### 2.2 Session Memory（会话记忆）

**定位**：单次会话（对话）期间的上下�?

**特点**�?
- 生命周期：会话期间（分钟到小时）
- 存储位置：内�?+ 可选持久化
- 容量：中等（< 100KB/会话�?
- 访问速度�? 5ms

**存储内容**�?
```pascal
TSessionMemory = class
private
  FSessionId: string;
  FUserId: string;
  FMessages: TList<TChatMessage>;         // 对话历史
  FContextWindow: TArray<TChatMessage>;   // 当前上下文窗�?
  FIntentHiDeepDeepDeepDeepDeepStory: TList<TIntentRecord>;   // 意图追踪
  FCreatedAt: TDateTime;
  FLastActiveAt: TDateTime;
  FMaxMessages: Integer;                  // 最大消息数
  FMaxTokens: Integer;                    // 上下文窗�?token 限制
public
  procedure AddMessage(ARole: TChatRole; const AContent: string);
  function GetContextWindow(AMaxTokens: Integer): TArray<TChatMessage>;
  function GetRecentMessages(ACount: Integer): TArray<TChatMessage>;
  procedure TrackIntent(AIntent: TIntentRecord);
  function GetIntentHiDeepDeepDeepDeepDeepStory: TArray<TIntentRecord>;
  procedure Trim;  // 超出限制时裁�?
end;

TChatMessage = record
  Role: TChatRole;      // user | assistant | system
  Content: string;
  Timestamp: TDateTime;
  TokenCount: Integer;
  Metadata: TJSONObject;  // 附加信息（如置信度）
end;
```

**上下文窗口管�?*�?
```pascal
function TSessionMemory.GetContextWindow(AMaxTokens: Integer): TArray<TChatMessage>;
var
  LTotalTokens: Integer;
  I: Integer;
begin
  // 从最新消息开始，向前累加直到达到 token 限制
  LTotalTokens := 0;
  SetLength(Result, 0);
  
  for I := FMessages.Count - 1 downto 0 do
  begin
    if LTotalTokens + FMessages[I].TokenCount > AMaxTokens then
      Break;
    
    // 插入到结果开�?
    SetLength(Result, Length(Result) + 1);
    Move(Result[0], Result[1], (Length(Result) - 1) * SizeOf(TChatMessage));
    Result[0] := FMessages[I];
    
    LTotalTokens := LTotalTokens + FMessages[I].TokenCount;
  end;
end;
```

**意图追踪**�?
```pascal
TIntentRecord = record
  Timestamp: TDateTime;
  UserInput: string;
  RecognizedIntent: string;
  Confidence: Double;
  Resolved: Boolean;      // 是否已完�?
  Resolution: string;     // 完成方式
end;

// 使用示例：检测用户是否在追问同一话题
function TSessionMemory.IsFollowUpQuestion(const ACurrentInput: string): Boolean;
var
  LLastIntent: TIntentRecord;
begin
  if FIntentHiDeepDeepDeepDeepDeepStory.Count = 0 then
    Exit(False);
    
  LLastIntent := FIntentHiDeepDeepDeepDeepDeepStory.Last;
  
  // 如果上一个意图未完成，且时间间隔 < 5 分钟
  Result := (not LLastIntent.Resolved) and 
            (MinutesBetween(Now, LLastIntent.Timestamp) < 5);
end;
```

### 2.3 Long-term Memory（长期记忆）

**定位**：跨会话持久化的用户知识

**特点**�?
- 生命周期：永久（除非显式删除�?
- 存储位置：数据库 + 向量存储
- 容量：大（按用户配额�?
- 访问速度�?0-100ms

**存储内容**�?
```pascal
TLongTermMemory = class
private
  FUserId: string;
  FVectorStore: IVectorStore;
  FDatabase: IDatabase;
public
  // 存储
  procedure Store(AEntry: TMemoryEntry);
  procedure StorePreference(AKey: string; AValue: TJSONValue);
  procedure StoreExperience(AExperience: TExperience);
  
  // 检�?
  function Search(const AQuery: string; ATopK: Integer = 5): TArray<TMemoryEntry>;
  function GetPreference(const AKey: string): TJSONValue;
  function GetSimilarExperiences(const ASituation: string): TArray<TExperience>;
  
  // 管理
  function GetUsage: TMemoryUsage;
  procedure Cleanup(AOlderThan: TDateTime);
  procedure Forget(const AEntryId: string);
end;

TMemoryEntry = record
  Id: string;
  UserId: string;
  Content: string;
  Embedding: TArray<Single>;   // 向量表示
  Tags: TArray<string>;        // 分类标签
  Source: string;              // 来源（对�?导入/学习�?
  Importance: Double;          // 重要性分�?0-1
  AccessCount: Integer;        // 访问次数
  LastAccessedAt: TDateTime;
  CreatedAt: TDateTime;
  Metadata: TJSONObject;
end;

TExperience = record
  Id: string;
  Situation: string;           // 情境描述
  Action: string;              // 采取的行�?
  Outcome: string;             // 结果
  UserFeedback: TFeedbackType; // positive | negative | neutral
  LearnedAt: TDateTime;
end;

TUserPreference = record
  Key: string;                 // �?'communication_style'
  Value: TJSONValue;           // �?'"concise"'
  Confidence: Double;          // 推断置信�?
  Source: string;              // explicit(用户明说) | inferred(推断)
  UpdatedAt: TDateTime;
end;
```

---

## 3. 向量检索方�?

### 3.1 Embedding 生成

```pascal
IEmbeddingProvider = interface
  function Embed(const AText: string): TArray<Single>;
  function EmbedBatch(const ATexts: TArray<string>): TArray<TArray<Single>>;
  function GetDimension: Integer;
end;

// MVP：使�?OpenAI Embedding API
TOpenAIEmbedding = class(TInterfacedObject, IEmbeddingProvider)
private
  FClient: THttpClient;
  FModel: string;  // text-embedding-3-small
public
  function Embed(const AText: string): TArray<Single>;
  function GetDimension: Integer;  // 1536
end;

// 未来：支持本地模�?
TLocalEmbedding = class(TInterfacedObject, IEmbeddingProvider)
  // 使用 ONNX Runtime 运行本地模型
end;
```

### 3.2 向量存储选型

| 方案 | 适用场景 | 优点 | 缺点 | MVP 支持 |
|------|----------|------|------|----------|
| **SQLite + FTS5** | 小规模本�?| 零依赖、简�?| 非真向量搜索 | �?|
| **Chroma** | 中规模本�?| 开源、易�?| 需 Python | �?|
| **FAISS** | 大规模本�?| 高性能 | 复杂度高 | V1.1 |
| **Pinecone** | 云端 | 托管、可扩展 | 需联网、付�?| V2.0 |

### 3.3 MVP 向量存储实现

```pascal
// 使用 SQLite 存储 + 简单余弦相似度
TSQLiteVectorStore = class(TInterfacedObject, IVectorStore)
private
  FConnection: TSQLiteConnection;
  FEmbeddingProvider: IEmbeddingProvider;
  
  function CosineSimilarity(const A, B: TArray<Single>): Double;
public
  constructor Create(const ADbPath: string; AEmbedding: IEmbeddingProvider);
  
  procedure Add(const AId: string; const AContent: string; 
    const ATags: TArray<string>);
  
  function Search(const AQuery: string; ATopK: Integer): TArray<TSearchResult>;
  
  procedure Delete(const AId: string);
end;

// 数据库表结构
const
  CREATE_MEMORY_TABLE = '''
    CREATE TABLE IF NOT EXISTS memory_entries (
      id TEXT PRIMARY KEY,
      user_id TEXT NOT NULL,
      content TEXT NOT NULL,
      embedding BLOB NOT NULL,          -- 序列化的 float 数组
      tags TEXT,                        -- JSON 数组
      importance REAL DEFAULT 0.5,
      access_count INTEGER DEFAULT 0,
      last_accessed_at TEXT,
      created_at TEXT NOT NULL,
      metadata TEXT                     -- JSON
    );
    
    CREATE INDEX IF NOT EXISTS idx_memory_user ON memory_entries(user_id);
    CREATE INDEX IF NOT EXISTS idx_memory_importance ON memory_entries(importance DESC);
  ''';

function TSQLiteVectorStore.Search(const AQuery: string; 
  ATopK: Integer): TArray<TSearchResult>;
var
  LQueryEmbedding: TArray<Single>;
  LAllEntries: TArray<TMemoryEntry>;
  LScores: TArray<TPair<string, Double>>;
  I: Integer;
begin
  // 1. 生成查询向量
  LQueryEmbedding := FEmbeddingProvider.Embed(AQuery);
  
  // 2. 加载所有条目（MVP 简单实现，后续优化�?
  LAllEntries := LoadAllEntries;
  
  // 3. 计算相似�?
  SetLength(LScores, Length(LAllEntries));
  for I := 0 to High(LAllEntries) do
  begin
    LScores[I].Key := LAllEntries[I].Id;
    LScores[I].Value := CosineSimilarity(LQueryEmbedding, LAllEntries[I].Embedding);
  end;
  
  // 4. 排序�?TopK
  TArray.Sort<TPair<string, Double>>(LScores, 
    TComparer<TPair<string, Double>>.Construct(
      function(const A, B: TPair<string, Double>): Integer
      begin
        Result := Sign(B.Value - A.Value);  // 降序
      end
    )
  );
  
  SetLength(Result, Min(ATopK, Length(LScores)));
  for I := 0 to High(Result) do
  begin
    Result[I].Id := LScores[I].Key;
    Result[I].Score := LScores[I].Value;
    Result[I].Entry := GetEntryById(LScores[I].Key);
  end;
end;
```

### 3.4 混合检索策�?

```pascal
// 结合关键词搜索和向量搜索
function TLongTermMemory.HybridSearch(const AQuery: string; 
  ATopK: Integer): TArray<TMemoryEntry>;
var
  LVectorResults: TArray<TSearchResult>;
  LKeywordResults: TArray<TSearchResult>;
  LMerged: TDictionary<string, Double>;
  LEntry: TSearchResult;
begin
  // 1. 向量搜索
  LVectorResults := FVectorStore.Search(AQuery, ATopK * 2);
  
  // 2. 关键词搜索（FTS5�?
  LKeywordResults := FDatabase.FullTextSearch(AQuery, ATopK * 2);
  
  // 3. 融合分数（RRF - Reciprocal Rank Fusion�?
  LMerged := TDictionary<string, Double>.Create;
  try
    for LEntry in LVectorResults do
      LMerged.AddOrSetValue(LEntry.Id, 
        LMerged.GetValueOrDefault(LEntry.Id, 0) + 1 / (60 + LEntry.Rank));
    
    for LEntry in LKeywordResults do
      LMerged.AddOrSetValue(LEntry.Id, 
        LMerged.GetValueOrDefault(LEntry.Id, 0) + 1 / (60 + LEntry.Rank));
    
    // 4. 按融合分数排序返�?TopK
    Result := GetTopKByScore(LMerged, ATopK);
  finally
    LMerged.Free;
  end;
end;
```

---

## 4. 容量管理与淘汰策�?

### 4.1 容量配额

```pascal
TMemoryQuota = record
  // Working Memory
  MaxWorkingMemorySize: Integer;     // 默认 1MB
  
  // Session Memory
  MaxSessionsPerUser: Integer;       // 默认 10
  MaxMessagesPerSession: Integer;    // 默认 100
  MaxTokensPerSession: Integer;      // 默认 8000
  SessionTimeout: Integer;           // 默认 30 分钟
  
  // Long-term Memory
  MaxEntriesPerUser: Integer;        // 默认 10000
  MaxStorageMBPerUser: Integer;      // 默认 100MB
end;

TMemoryUsage = record
  WorkingMemoryBytes: Int64;
  SessionCount: Integer;
  SessionMessagesTotal: Integer;
  LongTermEntryCount: Integer;
  LongTermStorageBytes: Int64;
  QuotaUsagePercent: Double;
end;
```

### 4.2 淘汰策略

```pascal
TEvictionPolicy = (
  epLRU,           // 最近最少使�?
  epLFU,           // 最少使用频�?
  epFIFO,          // 先进先出
  epImportance,    // 按重要�?
  epHybrid         // 综合策略
);

// 综合淘汰策略
function TMemoryManager.CalculateEvictionScore(AEntry: TMemoryEntry): Double;
var
  LRecencyScore: Double;
  LFrequencyScore: Double;
  LImportanceScore: Double;
begin
  // 1. 时间衰减（越久分数越低）
  LRecencyScore := Exp(-DaysBetween(Now, AEntry.LastAccessedAt) / 30);
  
  // 2. 访问频率（归一化）
  LFrequencyScore := Min(AEntry.AccessCount / 100, 1.0);
  
  // 3. 重要性（用户标记或推断）
  LImportanceScore := AEntry.Importance;
  
  // 加权求和
  Result := 0.3 * LRecencyScore + 0.3 * LFrequencyScore + 0.4 * LImportanceScore;
end;

procedure TMemoryManager.Evict(ATargetCount: Integer);
var
  LEntries: TArray<TMemoryEntry>;
  LScores: TArray<TPair<string, Double>>;
  I: Integer;
begin
  LEntries := FLongTermMemory.GetAllEntries;
  SetLength(LScores, Length(LEntries));
  
  // 计算所有条目的淘汰分数
  for I := 0 to High(LEntries) do
  begin
    LScores[I].Key := LEntries[I].Id;
    LScores[I].Value := CalculateEvictionScore(LEntries[I]);
  end;
  
  // 按分数升序排序（低分先淘汰）
  TArray.Sort<TPair<string, Double>>(LScores, ...);
  
  // 删除分数最低的 ATargetCount 个条�?
  for I := 0 to ATargetCount - 1 do
    FLongTermMemory.Delete(LScores[I].Key);
end;
```

### 4.3 Session 清理

```pascal
procedure TSessionManager.CleanupExpiredSessions;
var
  LSession: TSessionMemory;
begin
  for LSession in FSessions.Values do
  begin
    // 超过超时时间未活�?
    if MinutesBetween(Now, LSession.LastActiveAt) > FQuota.SessionTimeout then
    begin
      // 可选：将重要内容转存到长期记忆
      if LSession.HasImportantContent then
        TransferToLongTerm(LSession);
      
      FSessions.Remove(LSession.SessionId);
      LSession.Free;
    end;
  end;
end;

function TSessionMemory.HasImportantContent: Boolean;
begin
  // 判断标准�?
  // 1. 用户明确表达的偏�?
  // 2. 成功完成的任�?
  // 3. 用户正面反馈的内�?
  Result := (FIntentHiDeepDeepDeepDeepDeepStory.Count > 0) and 
            (FIntentHiDeepDeepDeepDeepDeepStory.Any(function(I: TIntentRecord): Boolean
              begin Result := I.Resolved and (I.UserFeedback = ftPositive); end));
end;
```

---

## 5. 记忆学习机制

### 5.1 用户偏好推断

```pascal
TPreferenceLearner = class
private
  FLongTermMemory: TLongTermMemory;
public
  // 从对话历史推断偏�?
  procedure LearnFromSession(ASession: TSessionMemory);
  
  // 从用户反馈学�?
  procedure LearnFromFeedback(const AContext: string; AFeedback: TFeedbackType);
end;

procedure TPreferenceLearner.LearnFromSession(ASession: TSessionMemory);
var
  LMessages: TArray<TChatMessage>;
  LPatterns: TArray<TPattern>;
begin
  LMessages := ASession.GetAllMessages;
  
  // 1. 分析沟通风格偏�?
  if DetectPrefersConcise(LMessages) then
    FLongTermMemory.StorePreference('communication_style', 
      TJSONString.Create('concise'), 0.7, 'inferred');
  
  // 2. 分析时间偏好
  LPatterns := ExtractTimePatterns(LMessages);
  if Length(LPatterns) > 0 then
    FLongTermMemory.StorePreference('active_hours', 
      TJSONArray.Create(LPatterns), 0.6, 'inferred');
  
  // 3. 分析主题兴趣
  // ...
end;
```

### 5.2 经验积累

```pascal
procedure TMemoryManager.RecordExperience(
  const ASituation: string;
  const AAction: string;
  const AOutcome: string;
  AFeedback: TFeedbackType);
var
  LExperience: TExperience;
begin
  LExperience.Id := GenerateId;
  LExperience.Situation := ASituation;
  LExperience.Action := AAction;
  LExperience.Outcome := AOutcome;
  LExperience.UserFeedback := AFeedback;
  LExperience.LearnedAt := Now;
  
  FLongTermMemory.StoreExperience(LExperience);
end;

// Advisor 使用历史经验
function TAdvisor.GetRelevantExperiences(const ASituation: string): string;
var
  LExperiences: TArray<TExperience>;
  LPromptPart: string;
begin
  LExperiences := FMemory.GetSimilarExperiences(ASituation);
  
  if Length(LExperiences) = 0 then
    Exit('');
  
  LPromptPart := '基于历史经验�? + sLineBreak;
  for var Exp in LExperiences do
  begin
    if Exp.UserFeedback = ftPositive then
      LPromptPart := LPromptPart + Format('- 在类似情�?%s"下，采取"%s"效果良好', 
        [Exp.Situation, Exp.Action]) + sLineBreak
    else if Exp.UserFeedback = ftNegative then
      LPromptPart := LPromptPart + Format('- 避免：在"%s"情况下采�?%s"（效果不佳）', 
        [Exp.Situation, Exp.Action]) + sLineBreak;
  end;
  
  Result := LPromptPart;
end;
```

---

## 6. 接口定义

### 6.1 Memory Manager 接口

```pascal
IMemoryManager = interface
  ['{GUID}']
  // Working Memory
  function GetWorkingMemory: IWorkingMemory;
  
  // Session Memory
  function GetOrCreateSession(const ASessionId, AUserId: string): ISessionMemory;
  function GetSession(const ASessionId: string): ISessionMemory;
  procedure EndSession(const ASessionId: string);
  
  // Long-term Memory
  function GetLongTermMemory(const AUserId: string): ILongTermMemory;
  
  // 管理
  function GetUsage(const AUserId: string): TMemoryUsage;
  procedure Cleanup;
  procedure SetQuota(AQuota: TMemoryQuota);
end;
```

### 6.2 Chronicler 集成

Memory 的变更通过 Chronicler 记录审计日志�?

```pascal
// Memory 存储�?
Chronicler.Log(TLogEntry.Create(
  'MEMORY_STORE',
  Format('User %s stored memory entry %s', [AUserId, AEntryId]),
  TLogLevel.Info,
  TJSONObject.Create
    .AddPair('entryId', AEntryId)
    .AddPair('contentLength', Length(AContent))
    .AddPair('tags', TJSONArray.Create(ATags))
));

// Memory 检索时
Chronicler.Log(TLogEntry.Create(
  'MEMORY_SEARCH',
  Format('User %s searched: %s, found %d results', [AUserId, AQuery, Length(LResults)]),
  TLogLevel.Debug
));
```

---

## 7. 配置参数

```json
{
  "memory": {
    "working": {
      "maxSizeBytes": 1048576
    },
    "session": {
      "maxPerUser": 10,
      "maxMessages": 100,
      "maxTokens": 8000,
      "timeoutMinutes": 30
    },
    "longTerm": {
      "maxEntriesPerUser": 10000,
      "maxStorageMB": 100,
      "evictionPolicy": "hybrid",
      "vectorStore": {
        "type": "sqlite",
        "path": "data/vectors.db"
      },
      "embedding": {
        "provider": "openai",
        "model": "text-embedding-3-small",
        "dimension": 1536
      }
    },
    "learning": {
      "enabled": true,
      "minConfidence": 0.7,
      "preferenceDecayDays": 90
    }
  }
}
```

---

## 8. 与现有文档的关系

- 记录员（Chronicler）的审计日志参�?`03.01.Arch-UniFlow架构设计`
- 用户数据隔离参�?`08.01.Quality-UniFlow-安全与数据权限策略`
- Memory 相关 API 参�?`05.05.API-UniFlow-接口契约定义`
- 测试用例参�?`08.07.Quality-UniFlow-测试策略`

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作�?|
|------|------|----------|------|
| 1.0 | 2025-12-04 | 初始版本 | - |
