# Dev-UniFlow FlowEngine 实现任务清单

> **编号**: 06.13  
> **版本**: 1.0  
> **更新时间**: 2025-12-04  
> **适用范围**: 负责 UniFlow Core / Flow 引擎相关实现的开发与评审人员�?

---

## 1. 概述

本清单将 "UniFlow Core / Flow 引擎�? 拆解为四个模块的可执行任务：

1. `UniFlow.Types` �?基础类型与核心模�?
2. `UniFlow.FlowStore` �?Flow / Event / Snapshot 的持久化（SQLite�?
3. `UniFlow.FlowEngine` �?Flow 状态机与对�?API
4. `UniFlow.FlowService` �?面向 SoIs / SayDone / DevDirector 的高层服�?

以及�?

- 一个最小端到端用例（Build + Maintain�?
- 一组基础测试与验收标�?

> 注：架构与模型定义以 `03.01.Design-uniFlow-CoreModelAndPrinciples-1.0.md` �?`04.01.Data-UniFlow数据模型-v1.0.md` 为准，本清单仅给出工程实现视角的任务拆解�?

---

## 2. 参考文�?

实现 Flow 引擎前，建议至少通读�?

- 模型与原则：`03.01.Design-uniFlow-CoreModelAndPrinciples-1.0.md`
- 数据模型：`04.01.Data-UniFlow数据模型-v1.0.md`
- 架构设计：`03.01.Arch-UniFlow架构设计-v1.0.md`
- MVP 开发规范：`06.03.Dev-UniFlow-MVP开发规�?v1.0.md`
- MVP 定义指南：`06.11.Dev-UniFlow-MVP定义与实现指�?v1.0.md`
- 安全策略（Flow 相关部分）：`08.01.Quality-UniFlow-安全与数据权限策�?v1.0.md`

---

## 3. 模块一：UniFlow.Types（核心类型）

> 目标：将 Flow / Event / Snapshot / Node 等核心模型抽象为统一�?Delphi 类型，供全系统复用�?

### 3.1 设计要点

- Flow 类型与状态：`TUniFlowType` / `TUniFlowStatus`
- 事件状态：`TUniFlowEventStatus`
- 字符串别名：`TUniFlowId` / `TUniNodeId` / `TUniNodePath`
- 事件结构：`TUniFlowEvent`
- 快照结构：`TUniFlowSnapshot`
- Node 结构：`TUniFlowNode`

### 3.2 开发任�?

- 定义 `UniFlow.Types.pas` 单元�?
  - [ ] 定义 `TUniFlowType = (uftBuild, uftMaintain, uftNlConvert, uftSceneChange, uftCodeChange)`�?
  - [ ] 定义 `TUniFlowStatus = (ufsCreated, ufsRunning, ufsWaitingUser, ufsSucceeded, ufsFailed, ufsCancelled)`�?
  - [ ] 定义 `TUniFlowEventStatus = (ufeStarted, ufeSucceeded, ufeFailed)`�?
  - [ ] 定义 `TUniFlowId`/`TUniNodeId`/`TUniNodePath` �?`type string`�?
  - [ ] 定义 `TUniFlowEvent` 结构，包�?Id/FlowId/FlowType/Step/Source/Status/Timestamp/PayloadJson/ErrorCode�?
  - [ ] 定义 `TUniFlowSnapshot` 结构，包�?FlowId/Version/StateJson/CreatedAt�?
  - [ ] 定义 `TUniFlowNode` 结构，包�?NodeId/NodeType/ParentId/AttributesJson�?

- 约定与样例：
  - [ ] 在注释中明确约定�?
    - Failed 事件必须携带非空 `ErrorCode`�?
    - `PayloadJson` 一律使�?UTF-8 JSON 字符串，不在类型层解析；
    - `NodeType` 建议使用固定枚举字符串，�?`Project`/`Scene`/`Step`/`Feature`/`CodeBlock`�?

---

## 4. 模块二：UniFlow.FlowStore（SQLite 存储�?

> 目标：提�?IUniFlowStore 接口，屏�?SQLite 细节，负�?Flow / Event / Snapshot 的可靠持久化与查询�?

### 4.1 表结构（参�?04.01 & 06.03�?

最�?MVP 需要的表（建议）：

- `flow_instances` �?Flow 实例�?
  - `id TEXT PRIMARY KEY`
  - `type TEXT NOT NULL`    // 映射 TUniFlowType
  - `status TEXT NOT NULL`  // 映射 TUniFlowStatus
  - `created_at TIMESTAMP`
  - `updated_at TIMESTAMP`

- `flow_events` �?事件�?
  - `id TEXT PRIMARY KEY`
  - `flow_id TEXT NOT NULL`
  - `type TEXT NOT NULL`
  - `step TEXT NOT NULL`
  - `source TEXT NOT NULL`
  - `status TEXT NOT NULL`
  - `timestamp TIMESTAMP NOT NULL`
  - `payload_json TEXT`
  - `error_code TEXT`

- `flow_snapshots` �?快照�?
  - `flow_id TEXT NOT NULL`
  - `version INTEGER NOT NULL`
  - `state_json TEXT NOT NULL`
  - `created_at TIMESTAMP NOT NULL`
  - `PRIMARY KEY (flow_id, version)`

### 4.2 接口定义

�?`UniFlow.FlowStore.pas` 中：

- [ ] 定义 `TUniFlowInstance`（Id/FlowType/Status/CreatedAt/UpdatedAt）；
- [ ] 定义 `IUniFlowStore` 接口，至少包含：
  - `CreateFlowInstance(AFlowType, AInitialSnapshotJson): TUniFlowId`�?
  - `UpdateFlowStatus(AFlowId, AStatus)`�?
  - `GetFlowInstance(AFlowId, out AInstance)`�?
  - `AppendEvent(AEvent)`�?
  - `GetEvents(AFlowId): TArray<TUniFlowEvent>`�?
  - `SaveSnapshot(ASnapshot)`�?
  - `GetLatestSnapshot(AFlowId, out ASnapshot)`�?

### 4.3 实现任务

- [ ] 实现 `TUniFlowStoreSqlite = class(TInterfacedObject, IUniFlowStore)`�?
  - 构造函数接�?SQLite 连接（或连接工厂）；
  - 使用事务保证 `AppendEvent` + `UpdateFlowStatus` + `SaveSnapshot` 的原子性（视调用点需求）�?

- [ ] �?`TUniFlowType` / `TUniFlowStatus` 与数据库中的 TEXT 映射写成统一的转换函数，避免魔法字符串散落代码：
  - `function FlowTypeToStr(AType: TUniFlowType): string;`
  - `function StrToFlowType(const S: string): TUniFlowType;`
  - `function FlowStatusToStr(AStatus: TUniFlowStatus): string;` 等�?

- [ ] 编写最小单元测试：
  - CreateFlowInstance �?GetFlowInstance�?
  - AppendEvent �?GetEvents�?
  - SaveSnapshot �?GetLatestSnapshot�?

---

## 5. 模块三：UniFlow.FlowEngine（状态机�?API�?

> 目标：在 IUniFlowStore 之上实现 Flow 状态机与基础 API，统一管理 Flow 的创建、事件追加与状态变更�?

### 5.1 接口定义

�?`UniFlow.FlowEngine.pas` 中：

- [ ] 定义 `TUniFlowTransitionResult`：FlowId/NewStatus/SnapshotJson�?
- [ ] 定义 `IUniFlowEngine` 接口�?
  - `CreateFlow(AFlowType, AInitialSnapshotJson): TUniFlowId`�?
  - `AppendEventAndAdvance(AEvent, AUpdateSnapshotJson): TUniFlowTransitionResult`�?
  - `GetFlowStatus(AFlowId): TUniFlowStatus`�?
  - `GetFlowEvents(AFlowId): TArray<TUniFlowEvent>`�?
  - `RebuildSnapshot(AFlowId): string`（通过事件重放构�?StateJson）�?

- [ ] 提供一个默认实�?`TUniFlowEngine = class(TInterfacedObject, IUniFlowEngine)`�?
  - 持有 `FStore: IUniFlowStore`�?
  - 内部函数 `NextStatus(ACurrentStatus, AEvent): TUniFlowStatus` 实现状态机�?

### 5.2 状态机 MVP 规则

建议先实现一套简单规则（后续可配置化）：

- 初始状态：
  - 新建 Flow �?`ufsCreated`�?
- 事件驱动迁移（示意）�?
  - `ufsCreated + (任何事件)` �?`ufsRunning`�?
  - `ufsRunning + Failed 事件` �?`ufsFailed`�?
  - `ufsRunning + Succeeded 事件 & Step in {终结步骤集合}` �?`ufsSucceeded`�?
  - （可选）当业务显式写�?Cancel 事件 �?`ufsCancelled`�?
  - `ufsWaitingUser` 用于需要用户确认的步骤，可由上层决定何时进�?退出�?

实现任务�?

- [ ] �?Store 获取当前 Flow 状态（若不存在则视�?`ufsCreated`）；
- [ ] �?`AppendEventAndAdvance` 中：
  - 调用 `FStore.AppendEvent(AEvent)`�?
  - 计算新状�?`NewStatus := NextStatus(CurrentStatus, AEvent)`�?
  - 调用 `FStore.UpdateFlowStatus(FlowId, NewStatus)`�?
  - �?`AUpdateSnapshotJson` 非空，写�?Snapshot�?
  - 返回 `TUniFlowTransitionResult`�?
- [ ] 对非法迁移（例如�?`ufsSucceeded` 再写事件）抛出统一错误�?
  - 使用 `EUniFlowError` 或等价异常类型；
  - `ErrorCode` 推荐统一�?`WORKFLOW_INVALID_TRANSITION`�?

### 5.3 快照重建

- [ ] �?`RebuildSnapshot(AFlowId)` 中：
  - 获取所有事件；
  - 从初�?Snapshot 开始（可为�?`{}`）；
  - 按时间顺序应用业务侧提供的“状态变更函数”（MVP 阶段可以先不实现复杂合并，只做占位或复用现有逻辑）�?

> 说明：MVP 阶段可以先只依赖最�?Snapshot，不从事件完全重放；但接口签名建议按最终目标设计好�?

---

## 6. 模块四：UniFlow.FlowService（业务集成层�?

> 目标：为 SoIs / SayDone / DevDirector 提供高层友好的服务接口，避免它们直接操作 Store/Engine�?

### 6.1 接口定义

�?`UniFlow.FlowService.pas` 中：

- [ ] 定义 `IUniFlowService` 接口�?
  - `StartBuildFlow(AInitialStateJson): TUniFlowId`�?
  - `StartMaintainFlow(ASourceFlowId, ATargetNodePath, AInitialStateJson): TUniFlowId`�?
  - `AddBusinessEvent(AFlowId, AFlowType, AStep, ASource, AStatus, APayloadJson, AErrorCode): TUniFlowTransitionResult`�?

- [ ] 实现 `TUniFlowService = class(TInterfacedObject, IUniFlowService)`，内部组�?`IUniFlowEngine`�?
  - `StartBuildFlow`：`CreateFlow(uftBuild, AInitialStateJson)`�?
  - `StartMaintainFlow`：可�?Snapshot �?Payload 中记�?`source_flow_id`/`target_node_path`�?
  - `AddBusinessEvent`：组�?`TUniFlowEvent` 并调�?`AppendEventAndAdvance`�?

### 6.2 SoIs / SayDone 接入改造任�?

- SoIs�?
  - [ ] 在构�?Loop A/B 时，不再自建 HiDeepDeepDeepDeepDeepStoryEvent / 自己�?sqlite�?
  - [ ] 替换为：在合适步骤调�?`IUniFlowService.AddBusinessEvent` 写入 `UniFlowEvent`�?
  - [ ] 为每个用户会话维护当�?`BuildFlowId` / `MaintainFlowId`�?

- SayDone�?
  - [ ] �?NL→结构化转换的关键节点（如模板选择、Patch 计算）写入对应的 Event�?
  - [ ] Payload 中可携带领域结构�?JSON，方便后续回放与调试�?

- DevDirector�?
  - [ ] 在导入场景树、应�?Patch、运�?回滚时，写对应的 Flow 事件�?
  - [ ] 在界面层提供查看 Flow 历史的入口（可延后）�?

---

## 7. 最小端到端用例（验收用�?

> 目标：通过一条简单的 Build Flow + Maintain Flow，验�?Types / Store / Engine / Service 整链路是否正常工作�?

### 7.1 Build Flow 用例

场景（简化）�?

1. SoIs：用户输入需�?�?`ClarifyRequirement` 事件�?
2. SoIs：生成草�?�?`GenerateSketch` 事件�?
3. 用户确认草图 �?`ConfirmSketch` 事件�?
4. SayDone：NL→结构化 �?`NlConvertToStructure` 事件�?
5. DevDirector：导入场景树 �?`ImportToSceneTree` 事件�?
6. Flow 最终状�?= `ufsSucceeded`，有一个最�?Snapshot�?

任务�?

- [ ] 使用 `IUniFlowService.StartBuildFlow` 创建 Flow�?
- [ ] 依次调用 `AddBusinessEvent` 模拟上述步骤（可以先在测试代码中硬编码）�?
- [ ] 验证�?
  - `GetFlowStatus` 返回 `ufsSucceeded`�?
  - `GetFlowEvents` 中按顺序包含 5 个步骤；
  - `GetLatestSnapshot` 非空�?

### 7.2 Maintain Flow 用例

场景（简化）�?

1. DevDirector：用户在界面上选中�?Node，点击“修改”；
2. FlowService：`StartMaintainFlow(源FlowId, NodePath)`�?
3. SoIs：澄清修�?�?`ClarifyChange` 事件�?
4. SayDone：计�?Patch �?`ComputePatch` 事件�?
5. DevDirector：应�?Patch �?`ApplyPatch` 事件�?
6. Maintain Flow 状�?= `ufsSucceeded`，Snapshot 中记录新结构/新版本引用�?

任务�?

- [ ] 在测试或 Demo 程序中模拟这条链路；
- [ ] 确认 Flow 与原�?Build Flow 之间的关联（可在 Payload �?Snapshot 中记录）�?
- [ ] 为后�?UI 提供可视化基础�?

---

## 8. 测试与验收标准（Flow 引擎线）

### 8.1 单元测试建议

- `UniFlow.Types`
  - [ ] 测试枚举与字符串转换（如有实�?`ToStr/FromStr`）；
- `UniFlow.FlowStore`
  - [ ] 创建 Flow �?查询 Flow�?
  - [ ] 写入多条 Event �?按时间顺序读取；
  - [ ] 多版�?Snapshot 写入 �?读取最新版本；
- `UniFlow.FlowEngine`
  - [ ] `CreateFlow` 后状态为 `ufsCreated`�?
  - [ ] 第一条事件追加后状态为 `ufsRunning`�?
  - [ ] Failed 事件使状态变�?`ufsFailed`�?
  - [ ] 非法迁移抛出异常（如�?`ufsSucceeded` 再次写入运行事件）�?

### 8.2 集成测试建议

- [ ] 建一个最小子系统：`FlowStore + FlowEngine + FlowService`�?
- [ ] 按第 7 章的 Build/Maintain 用例执行，检查状态与事件数量�?
- [ ] �?SoIs / SayDone 中各增加一条代码路径，真正调用 `IUniFlowService` �?Flow 事件，跑一遍真�?UI/交互场景�?

### 8.3 验收标准

当满足以下条件，可认�?"UniFlow Core / Flow 引擎线（MVP�? 基本完成�?

1. `UniFlow.Types` / `UniFlow.FlowStore` / `UniFlow.FlowEngine` / `UniFlow.FlowService` 四个模块均存在且编译通过�?
2. SQLite �?`flow_instances` / `flow_events` / `flow_snapshots` 表结构与 04.01 文档一致；
3. 能通过测试代码跑通一�?Build Flow 与一�?Maintain Flow，状态、事件顺序与 Snapshot 符合预期�?
4. 至少有一�?SoIs �?SayDone 的实际用户操作，会在 Flow 历史中留下完整可追溯�?`UniFlowEvent` 序列�?
5. 相关单元/集成测试可稳定通过，且后续改动 Flow 引擎时，可用这些测试辅助回归�?

---

> 建议：后续新增或重命名与 Flow 相关的模�?类型时，优先更新 `03.01.Design-uniFlow-CoreModelAndPrinciples-1.0.md` 与本任务清单，以保持文档与实现的一致性