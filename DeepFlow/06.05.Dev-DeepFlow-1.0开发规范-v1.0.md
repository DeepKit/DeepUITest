# DeepFlow 1.0 版本规范文档

> **版本**: 1.0 | **编制日期**: 2025-12-03
> 1.0 主要变更：作为独立发布的产品，见证 + 元宇宙验证
>
MVP 详细章节可参考 `06.03.Dev-DeepFlow-MVP版本规范-v1.0.md` 和 `06.11.Dev-DeepFlow-MVP功能实现指南-v1.0.md`，本篇在此基础上，1.0 版本的主要迭代内容已在 MVP 章节中详细介绍

---

## 1. 1.0 目标

### 1.1 核心目标

基于 MVP 版本，DeepFlow 定位为**作为独立第三方产品发布**
- 动态加载 Skill 功能
- 支持 LLM 多语言
- AI Persona ϵͳ
- 可选的云端同步功能
- 功能完善的 SDK

### 1.2 验证目标

基于 MVP 的卡牌类游戏毕业设计，包含角色卡牌系统和元素引擎

**官方技能系统功能**：
1. 输入卡牌数据，包括卡牌名称、属性、效果等
2. 处理卡牌数据版本号
3. 用户自定义信息
4. 内容可搜索

### 1.3 验收标准

1. ? 须同时支持「玩家」与「官方技能」
2. ? Official Skills 达 20 级
3. ? 支持对 LLM 进行对话
4. ? AI 需构建团队
5. ? 完成项目相关文档
6. Skill SDK 依赖

---

## 2. 技能生态清单

### 2.1 Skill 生态相关

#### 2.1.1 Official Extensions 官方扩展

| Skill | 说明 | 语言 |
|-------|------|------|
| skill_dingtalk | 钉钉消息 | Python |
| skill_wecom | 企业微信消息 | Python |
| skill_feishu | 飞书消息 | Python |
| skill_email | 邮件通知 | Python |
| skill_sms | 短信发送 | Python |
| skill_ocr | 图片文字识别 | Python |
| skill_tts | 文本转语音 | Python |
| skill_asr | 语音转文字 | Python |
| skill_translate | 多语言翻译 | Python |
| skill_summarize | 文本摘要 | Python |
| skill_sentiment | 情感分析 | Python |
| skill_ner | 实体识别 | Python |
| skill_image_gen | 图片生成 | Python |
| skill_pdf_parse | PDF 解析 | Python |
| skill_excel | Excel 读写 | Python |
| skill_mysql | MySQL 管理 | Python |
| skill_postgres | PostgreSQL 管理 | Python |
| skill_redis | Redis 管理 | Python |
| skill_webhook | Webhook 管理 | Python |
| skill_cron | 高级定时任务管理 | Delphi |

#### 2.1.2 Skill SDK

**Delphi SDK**概
```pascal
unit UniFlow.Skill.SDK;

interface

type
  /// Skill 功能框架
  TSkillBase = class(TInterfacedObject, ISkill)
  protected
    FMeta: TSkillMeta;
    function DoExecute(const AInput: TSkillInput): TSkillOutput; virtual; abstract;
  public
    constructor Create(const AName, AVersion, ADescription: string);
    function GetMeta: TSkillMeta;
    function Validate(const AInput: TSkillInput): TValidationResult; virtual;
    function Execute(const AInput: TSkillInput): TSkillOutput;
  end;
  
  /// Skill 注册函数
  TSkillSDK = class
    class procedure Register(const ASkill: ISkill);
    class procedure RegisterFromAssembly;
  end;
```

**Python SDK**概
```python
# uniflow_sdk/skill.py
from abc import ABC, abstractmethod
from pydantic import BaseModel
from typing import AsyncIterator

class UniFlowSkill(ABC):
    """UniFlow Skill 基础接口"""
    
    @property
    @abstractmethod
    def name(self) -> str:
        pass
    
    @property
    @abstractmethod
    def version(self) -> str:
        pass
    
    @property
    def description(self) -> str:
        return ""
    
    @abstractmethod
    async def execute(self, input: dict, context: dict) -> dict:
        pass
    
    async def stream(self, input: dict, context: dict) -> AsyncIterator[str]:
        """可选流式输出"""
        result = await self.execute(input, context)
        yield str(result)
    
    def validate(self, input: dict) -> bool:
        """可选参数校验"""
        return True

# 装饰器注册
def skill(name: str, version: str = "1.0.0"):
    def decorator(cls):
        cls._skill_name = name
        cls._skill_version = version
        return cls
    return decorator
```

#### 2.1.3 Skill 市场内部参数

| 参数 | 说明 |
|------|------|
| 基本信息 | 名称、标签、版本等 |
| 核心能力 | 通过：AI模型、数据、接口... |
| 版本管理 | 稳定版本发布，定向修复版本 |
| 稳定性 | 稳定性指标、性能、文档 |
| 扩展框架 | Skill 扩展框架插件 |

### 2.2 能力增强

#### 2.2.1 针对 LLM 模型支持

```pascal
type
  /// LLM 智能路由
  TLLMRouteStrategy = (
    lrsRoundRobin,    // 负载均衡
    lrsCostOptimal,   // 成本最优
    lrsQualityFirst,  // 质量优先
    lrsLatencyFirst,  // 延迟最低
    lrsCustom         // 自定义
  );

/// LLM 网关配置
  TLLMGatewayConfig = record
    Backends: TArray<TLLMBackend>;
    RouteStrategy: TLLMRouteStrategy;
    FallbackEnabled: Boolean;
    FallbackOrder: TArray<string>;
    CostBudget: Double;  // 每日成本预算
  end;
```

**支持的模型库**: 
| 提供商 | 模型 | 特点; |
|------|------|------|
| OpenAI | GPT-4, GPT-3.5 | 通用接口 |
| Anthropic | Claude 3 | 安全可靠伦理 |
| Azure OpenAI | GPT-4 | 企业级场景 |
| 通义千问 | Qwen | 智能优化 |
| 百度文心 | ERNIE | 智能优化 |
| 开源 Ollama | Llama, Mistral | 本地部署 |

#### 2.2.2 AI Persona ϵͳ

```pascal
type
  /// AI 人设核心配置（必填）
  TAIPersonaCore = record
    Honesty: Boolean;      // 诚实
    Respect: Boolean;      // 尊重
    Reliability: Boolean;  // 可靠
  end;

  /// AI 人格特征集合
  TAIPersonaStyle = record
    Name: string;           // 人格名称
    Tone: string;           // 语气类型 formal, casual, friendly
    Verbosity: Integer;     // 详细程度 1-5
     Emoji: Boolean;         // 是否使用表情
     Language: string;       // 语言偏好
     DomainExpertise: TArray<string>;  // 领域专家
  end;

   /// AI 人格
  TAIPersona = record
     Core: TAIPersonaCore;    // 核心：统一模型
     Style: TAIPersonaStyle;  // 风格：表现模式
  end;
```

**预订部门信息表**
| 姓名 | 所属部门 |
|------|----------|
| 高级工程部 | 研发中心总部 |
| 高级软件工程师 | 信息技术管理部 |
| 错误 | 注册 |
| 高级教师 | 数据分析 |
| 错误信号 | 拒绝模式 |

#### 2.2.3 信任信号栈

```pascal
type
  /// 信任信号
  TTrustSignal = record
    SessionId: string;
    ActionId: string;
    SignalType: TTrustSignalType;  // Accept, Reject, Edit, Ignore
    UserFeedback: string;
    Timestamp: TDateTime;
  end;

  /// 信任分析结果
  TTrustAnalysis = record
    AcceptRate: Double;
    CommonRejections: TArray<string>;
    SuggestedAdjustments: TArray<string>;
  end;
```

### 2.3 信任分析体系

#### 2.3.1 信任模型设计

```pascal
type
/// 上下文桥接记录定义
  TContextBridge = record
    SourceProduct: string;   // 源产品信息
    TargetProduct: string;   // 目标产品
    ContextKeys: TArray<string>;  // 各场景的配置项
    SecurityGateway: Boolean;     // 是否经过安全网关
    AuditEnabled: Boolean;        // 是否启用审计
  end;
```

**功能示例如下**：
- 视频平台 与 元数据管理 系统联动
- 影院档案 与 影视文化数据库 同步
- 推荐产品 与 DeepBase 用户偏好同步

#### 2.3.2 合规保障措施

| 类型 | 说明 |
|------|------|
| 模型服务 | 统一LLM推理服务 |
| 安全网关 | 统一认证/授权 |
| 消息系统 | 统一的事件日志 |
| 用户系统 | 统一的用户认证和1.0+ |

### 2.4 逻辑架构设计

#### 2.4.1 定时检查 Workflow

```pascal
type
  /// 检查点
  TWorkflowCheckpoint = record
    WorkflowId: string;
    NodeId: string;
    State: TJSONObject;
    CreatedAt: TDateTime;
  end;

  /// 长时间 Workflow 配置
  TLongRunningConfig = record
    CheckpointInterval: Integer;  // 检查间隔（秒）
    MaxDuration: Integer;         // 最大运行时间（秒）
    TimeoutAction: TTimeoutAction;  // Pause, Fail, Notify
    ExternalEventEnabled: Boolean;  // 是否支持外部事件
  end;
```

**支持的类型**：
| 类型 | 说明 |
|------|------|
| 空闲 | 正在运行的状态 |
| 超时处理 | 默认超时时间 |
| 等待 | 等待外部信号 |
| 外部事件 | 等待外部信号 |
| 人工干预 | 停止等待人工 |

#### 2.4.2 模板选择

```pascal
type
  /// 工作流模板
  TWorkflowTemplate = record
    Id: string;
    Name: string;
    Description: string;
    Category: string;
    Parameters: TArray<TTemplateParam>;  // 模板参数
    Definition: TWorkflowDef;
    IsPublic: Boolean;
    Author: string;
    Rating: Double;
    UsageCount: Integer;
  end;
```

**预设模板**：
| 模板 | 调用场景 |
|------|----------|
| 基础红色模板 | 用于创建用户账户 |
| 企业级分布式缓存服务 | 存储层 |
| MongoDB模板 | 通用 |
| 对象存储方案 | 通用 |

### 2.5 PostgreSQL迁移方案

#### 2.5.1 同步架构

```
├─────────────────────────────────────────────────────────────────────┤
││                    应用                              ││
││  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐   ││
  与              SQLite数据库相比                  与   与
□  支持所有主流数据库引擎，包括SQLite、PostgreSQL、MySQL等数据库，满足各类应用场景需求
  ✗ 不同选项  
│  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐  │
①  ②            동일상품                            판매   고객
用户  客户  ? 客户端                                      用户   客户
用户  客户  ? 用户合同                                      用户   客户
用户  客户  ? 用户协议                                      用户   客户
这是一个包含大量重复文本内容的段落，用于测试文档修复功能是否正确处理损坏的字符并恢复为正确的汉字。
SQLite数据库与PostgreSQL数据库在功能和性能方面各有优势，需根据具体需求进行选择对比分析评估
                        ││ HTTPS
├─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
✓                    移动方案                                ✓
││  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐   ││
│  │              PostgreSQL                          │   │
``  ``  ? 用户账号                                      ``   ``
登录  授权  ? 同步方案
登录  授权  ? Skill 服务资源配置
登录  授权  ? 账号配置  ? 同步方式  ? 冲突解决策略  ? 数据格式  ? 映射规则
登录  授权  ? 账号配置  ? 同步方式  ? 冲突解决策略  ? 数据格式  ? 映射规则
```

#### 2.5.2 同步方案

| 业务场景 | 同步方案 |
|----------|----------|
| 用户偏好 | 自动双向同步 |
| Workflow 模式 | 手动上传/下载 |
| 操作记录 | 用户选择同步 |
| Skill 配置 | 自动同步 |
| 系统日志 | 仅同步异常变更日志 |

---

## 3. 外部接口

### 3.1 LLM 网关接口

```pascal
type
  ILLMGateway = interface
    function Chat(const ARequest: TLLMRequest): TLLMResponse;
    function ChatAsync(const ARequest: TLLMRequest; 
      const ACallback: TLLMCallback): IFuture;
    function ChatStream(const ARequest: TLLMRequest;
      const AChunkCallback: TLLMChunkCallback): IFuture;
    procedure SetRouteStrategy(const AStrategy: TLLMRouteStrategy);
    function GetUsage: TLLMUsageStats;
  end;
```

### 3.2 Persona 接口

```pascal
type
  IPersonaManager = interface
    function GetPersona(const AName: string): TAIPersona;
    procedure SetActivePersona(const AName: string);
    function GetActivePersona: TAIPersona;
    procedure RegisterPersona(const APersona: TAIPersona);
    function ListPersonas: TArray<string>;
  end;
```

### 3.3 同步接口

```pascal
type
  ISyncEngine = interface
    procedure StartSync;
    procedure StopSync;
    procedure SyncNow;
    function GetSyncStatus: TSyncStatus;
    procedure SetSyncConfig(const AConfig: TSyncConfig);
    procedure ResolveConflict(const AConflict: TSyncConflict; 
      const AResolution: TConflictResolution);
  end;
```

---

## 4. 模块化设计

### 4.1 Skill 市场表

```sql
-- Skill 市场表
CREATE TABLE skill_market (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    version TEXT NOT NULL,
    description TEXT,
    category TEXT NOT NULL,
    author TEXT,
    input_schema TEXT,
    output_schema TEXT,
    trust_level TEXT NOT NULL,
    language TEXT NOT NULL,
    package_url TEXT,
    readme TEXT,
    rating REAL DEFAULT 0,
    download_count INTEGER DEFAULT 0,
    is_official INTEGER DEFAULT 0,
    is_active INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Skill 评价表
CREATE TABLE skill_reviews (
    id TEXT PRIMARY KEY,
    skill_id TEXT NOT NULL,
    user_id TEXT NOT NULL,
    rating INTEGER NOT NULL,
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (skill_id) REFERENCES skill_market(id)
);
```

### 4.2 Persona 表

```sql
-- AI 人格表
CREATE TABLE ai_personas (
    name TEXT PRIMARY KEY,
    display_name TEXT NOT NULL,
    description TEXT,
    tone TEXT DEFAULT 'professional',
    verbosity INTEGER DEFAULT 3,
    use_emoji INTEGER DEFAULT 0,
    language TEXT DEFAULT 'zh-CN',
    domain_expertise TEXT,  -- JSON array
    system_prompt TEXT,
    is_builtin INTEGER DEFAULT 0,
    is_active INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.3 同步队列表

```sql
-- 同步队列表
CREATE TABLE sync_queue (
    id TEXT PRIMARY KEY,
    table_name TEXT NOT NULL,
    record_id TEXT NOT NULL,
    operation TEXT NOT NULL,  -- INSERT, UPDATE, DELETE
    data_json TEXT,
    status TEXT DEFAULT 'pending',
    retry_count INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    synced_at TIMESTAMP
);

-- 同步冲突表
CREATE TABLE sync_conflicts (
    id TEXT PRIMARY KEY,
    table_name TEXT NOT NULL,
    record_id TEXT NOT NULL,
    local_data TEXT,
    remote_data TEXT,
    resolution TEXT,  -- local, remote, merge, pending
    resolved_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 5. 实现阶段

### 5.1 第一阶段：Skill实现（3周）

1. 实现 Skill SDK（Delphi + Python）
2. 实现 20 个 Official Skills
3. 实现 Skill 平台管理（内部）
4. 撰写 Skill 文档

### 5.2 第二阶段：模型增强（3周）

1. 实现 LLM 训练
2. 生成对 LLM 数据
3. 实现 AI Persona 系统
4. 实现 LLM 评测

### 5.3 第三阶段：逻辑推理（2周）

1. 实现超时处理 Workflow
2. 实现数据模块系统
3. 实现外部事件排队

### 5.4 迁移阶段：预计2周：

1. 搭建 PostgreSQL 实例
2. 实现同步CDC
3. 实现客户迁移

### 5.5 测试阶段：预计2周：

1. 编写单元测试
2. 接口测试
3. 性能优化
4. 文档整理

---

## 6. 安装注册清单

### 6.1 系统配置

- [ ] Official Skills 包括 20 个
- [ ] Skill SDK 使用文档
- [ ] 开源 LLM 微调流程
- [ ] AI Persona 系统配置管理
- [ ] 临时 Workflow 支持创建/修改
- [ ] 移动同步清单

### 6.2 文档管理

- [ ] 用户登录状态验证
- [ ] 元数据后台管理配置
- [ ] 数据导入成功记录管理

### 6.3 验收标准

- [ ] 单元测试覆盖率 ≥ 70%
- [ ] 架构设计文档评审
- [ ] API 文档评审
- [ ] 产品需求规格说明书

---

## 7. 交付文档

- **MVP 版本号**`${}06.03.Dev-DeepFlow-MVP版本号-v1.0.md`
- **产品版本**：`06.07.Dev-DeepFlow-产品版本-v1.0.md`
- **元宇宙产品**：`D:\_Progs\02Business\TheSenate\docs\01-00A.项目-产品清单.md`
- **用户操作流程**：`07.01.Case-DeepFlow用户操作流程-v1.0.md`

---

*文档版本: 1.0*
*编写日期: 2025-12-03*
