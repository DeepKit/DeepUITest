# Case-UniFlow-DeepDeepDeepDeepDeepInsight 决策思维显影金路径（结论可选）

> **文档编号**: 07.02  
> **版本**: 1.0  
> **更新日期**: 2025-12-04  
> **适用版本**: MVP / 1.0 为主，理想版在此基础上增�? 
> **关联文档**:  
> - `07.01.Case-UniFlow用户旅程案例-v1.0.md`（本篇基于其中“洞见·多角色决策推演”案例深入）  
> - `01.01.Product-UniFlow三层演进规划-v1.0.md`  
> - `03.01.Arch-UniFlow架构设计-v1.0.md`  
> - `04.01.Data-UniFlow数据模型-v1.0.md`  
> - `05.01.API-UniFlow接口规范-v1.0.md`  
> - `06.03/06.05/06.07 Dev-UniFlow-*开发规�?v1.0.md`  
> - 《洞见·思维显影室宪法与反宪法》（产品哲学文档，用于约束本金路径的语气与边界）

---

## 1. 场景与目�?

### 1.1 场景简�?

**产品**：洞�?· 思维显影室（DeepDeepDeepDeepDeepInsight�? 
**典型问题**：例如「是否应该离开现有工作创业？」、重大职�?产品/战略决策�? 

用户在洞�?UI 中输入决策问题及少量背景，系统调�?UniFlow 的多角色推演能力�?*优先帮助用户把想法与情绪显影出来，而不是直接给出结�?*�?

- �?**总指挥（Commander�?* 接受请求并确认走“决策思维显影金路径”；
- **调度员（Dispatcher�?* 创建包含多角色并行推演的 Workflow�?
- **智囊（Advisor�?+ 执行者（Executor�?* 通过 Python Skills 完成不同视角的推演；
- **守卫（Guard�?* 校验各个视角产出与最终胶片；
- **记录员（Chronicler�?* �?**后勤（Logistics�?* 将结果写入记忆与日志�?
- 在前端优先展�?*决策胶片 + 关键视角列表 + 自我提问清单**，如用户明确选择需要行动灵感，再附�?*小步行动建议**（而非“最终答案”）�?

### 1.2 本文目标

- 提供一�?*以“显�?+ 觉察”为主、行动建议为可选副产物的黄�?Workflow 定义**，用于指�?MVP / 1.0 实现�?
- 列出该路径涉及的 **Skill 清单、角色协作、关键安全点与指�?*�?
- 为后续的 A/B 实验、版本演进提供一条稳定的基线流程，但**不以“是否给出结论”或“是否跑完整条流程”作为唯一成功标准**�?

配套可视化流程图见：`docs/uniFlow/ui/UniFlow-洞见-决策推演金路�?svg`�?

---

## 2. Workflow 视图

### 2.1 高层步骤拆解

黄金路径可抽象为以下阶段�?

1. **输入与建�?*�?
   - 接收问题与基础背景�?
   - 补齐必要的上下文（例如用户画像、过往决策记录）�?
2. **多视角推�?*�?
   - 并行调用多个“角�?Skill”（教练、批评者、镜像等）完成不同视角的分析�?
3. **聚合与综�?*�?
   - 汇总多视角结果，生成结构化“视角列�?+ 结论草案”；
4. **胶片生成与校�?*�?
   - 将决策过�?视角以“胶�?+ X 光片”等形式可视化；
   - 守卫做格式、安全与业务合理性校验；
5. **呈现与记忆写�?*�?
   - 将结果返回前端并写入长短期记忆，以便后续复盘和再推演�?

### 2.2 Workflow 节点示意（伪 YAML�?

```yaml
id: wf_DeepDeepDeepDeepDeepInsight_decision_gold
version: 1.0.0
name: 洞见 · 决策思维显影金路�?
run_as: user   # 交互式调用，默认以用户会话主体执�?

steps:
  - id: receive_input
    type: SystemTask
    action: ReceiveDecisionProblem

  - id: enrich_context
    type: SystemTask
    action: EnrichContext
    input_mapping:
      problem: "{{ steps.receive_input.output.problem }}"
    # 从会�?/ 项目记忆中拉取相关背�?

  - id: fork_roles
    type: ParallelFork
    branches: [coach, critic, mirror, observer]

  - id: coach
    type: SkillTask
    skill: decision_coach
    input_mapping:
      problem: "{{ steps.receive_input.output.problem }}"
      context: "{{ steps.enrich_context.output.context }}"

  - id: critic
    type: SkillTask
    skill: decision_critic
    input_mapping:
      problem: "{{ steps.receive_input.output.problem }}"
      context: "{{ steps.enrich_context.output.context }}"

  - id: mirror
    type: SkillTask
    skill: decision_mirror
    input_mapping:
      problem: "{{ steps.receive_input.output.problem }}"
      context: "{{ steps.enrich_context.output.context }}"

  - id: observer
    type: SkillTask
    skill: decision_observer
    input_mapping:
      problem: "{{ steps.receive_input.output.problem }}"
      context: "{{ steps.enrich_context.output.context }}"

  - id: join_results
    type: ParallelJoin
    branches: [coach, critic, mirror, observer]

  - id: synthesize
    type: AgentTask
    agent: advisor_synthesizer
    input_mapping:
      perspectives: "{{ steps.join_results.output.perspectives }}"

  - id: generate_film
    type: SkillTask
    skill: film_generator
    input_mapping:
      synthesis: "{{ steps.synthesize.output.synthesis }}"
      perspectives: "{{ steps.join_results.output.perspectives }}"

  - id: guard_review
    type: SystemTask
    action: GuardReview
    input_mapping:
      film: "{{ steps.generate_film.output.film }}"
      problem: "{{ steps.receive_input.output.problem }}"

  - id: write_memory
    type: SystemTask
    action: WriteDecisionMemory
    input_mapping:
      film: "{{ steps.generate_film.output.film }}"
      metadata: "{{ steps.synthesize.output.metadata }}"

  - id: reply_user
    type: SystemTask
    action: ReplyDeepDeepDeepDeepDeepInsightResult
    input_mapping:
      film: "{{ steps.generate_film.output.film }}"
      summary: "{{ steps.synthesize.output.summary }}"

transitions:
  - from: receive_input
    to: enrich_context
  - from: enrich_context
    to: fork_roles
  - from: fork_roles
    to: [coach, critic, mirror, observer]
  - from: [coach, critic, mirror, observer]
    to: join_results
  - from: join_results
    to: synthesize
  - from: synthesize
    to: generate_film
  - from: generate_film
    to: guard_review
  - from: guard_review
    to: write_memory
  - from: write_memory
    to: reply_user
```

> 说明：实际实现时可拆分为多个�?Workflow 或模板；此处以单一主流程示意�?

---

## 3. Skill �?Agent 清单

### 3.1 决策推演相关 Skills（MVP/1.0�?

| 名称 | 类型 | 版本示例 | 作用 | 典型数据等级 |
|------|------|----------|------|--------------|
| `decision_coach` | Python Skill | 0.1.0 / 1.0.0 | 从“教练”视角提出结构化思考框架与建议 | 输入/输出多为 L2（个人与业务情境�?|
| `decision_critic` | Python Skill | 0.1.0 / 1.0.0 | 从“批评者”视角列出风险与反例 | L2 |
| `decision_mirror` | Python Skill | 0.1.0 / 1.0.0 | 镜像用户动机与情绪，帮助自我觉察 | L2（含情绪画像�?|
| `decision_observer` | Python Skill | 0.1.0 / 1.0.0 | 以中立观察者视角梳理事实与选项 | L1–L2 |
| `film_generator` | Python Skill | 0.1.0 / 1.0.0 | 将多视角结果转为“决策胶�?X光片”结�?| L2 输出（结构化胶片�?|

> 建议：上�?Skill 的实现全部走 Python Skill HTTP API，Skill 元数据在 `skills` 表中注册，`SourceType = sstPython`，`TrustLevel = limited`，输出必须经守卫校验�?

### 3.2 智囊 Agent（Synthesizer�?

`advisor_synthesizer` 可以视作基于 LLM 的高级聚�?Agent�?

- 输入：多角色视角列表（coach/critic/mirror/observer 的输出）�?
- 输出�?
-  - `summary`：以陪伴语气撰写的中文自然语言总结与自我觉察提示，可包含用户可选的小步行动灵感，但不直接替用户做决定；
-  - `metadata`：结构化标签（风险点列表、机会点列表、情绪评估等）；
- 运行约束�?
  - 不直接写入记忆或触发副作用，所有输出只作为“提案”，交由后续节点（film_generator / write_memory）处理；
  - 调用 LLM 时遵守安全策略中�?L2 数据出站的限制�?

---

## 4. 安全与运行时配置

### 4.1 run_as 与运行主�?

- 本金路径�?*交互式调�?*，入口由用户在洞�?UI 触发�?
  - Workflow `run_as: user`�?
  - 所�?SkillTask 默认继承用户会话主体，除非某些后台补�?重试步骤显式配置�?`project_runtime`（此场景下可以先不启用）�?
- 若后续引入定时“批量重�?复盘”任务，则应通过独立 Workflow（`run_as: project_runtime`）实现，不与交互式金路径混合�?

### 4.2 数据等级与敏感点

- 输入�?
  - 决策问题 + 背景描述�?*L2**（个�?业务敏感）；
  - 用户画像（若使用）：**L2**�?
- 中间过程�?
  - 多角色视角输出：**L2**�?
  - 情绪/动机等心理画像：**L2**，但在某些合规语境中可视作偏高敏，需要谨慎出站�?
- 输出�?
  - 决策胶片/X光片本身�?*L2**�?

**约束**�?

- Guard �?`guard_review` 节点需至少执行�?
  - Schema 校验：输出结构符合预期；
  - 敏感内容校验：防止出现不当建议（如直接法�?医疗诊断）或泄露他方机密信息�?
  - 业务规则校验：例如不直接给出“绝对结论”，而是以风�?条件式表述�?
- 所有相关访�?写入行为应在 `audit_logs` 中记录版本信息（Workflow/Skill/LLM Config）�?

### 4.3 LLM / MCP 出站策略

- 多角色推演与 synthesize 阶段若调用外�?LLM/MCP�?
  - 不得出站 L3 数据（如密钥、账号信息）�?
  - 尽量避免直接出站完整用户画像，可�?ID 或摘要代替；
  - 必须满足用户/租户在设置中对“数据可出站到云�?LLM”开关的配置�?

---

## 5. 指标与观�?

为评估与优化洞见决策推演金路径，建议至少埋点以下指标�?

1. **使用频次与转�?*
   - `DeepDeepDeepDeepDeepInsight_decision_runs_total`：洞见决策推演启动次数；
   - `DeepDeepDeepDeepDeepInsight_decision_completed_total`：完整跑�?Workflow 的次数；
   - `DeepDeepDeepDeepDeepInsight_decision_abort_rate`：用户中途放弃或失败率�?

2. **性能与成�?*
   - `DeepDeepDeepDeepDeepInsight_decision_latency_ms`：从用户提交到结果返回的总耗时�?
   - 各关键节点耗时：enrich_context / fork_roles / synthesize / generate_film�?
   - LLM/MCP 调用次数�?Token/成本统计�?

3. **质量与满意度（可选）**
   - 用户�?UI 中对本次结果的主观评分（1�? 星）�?
   - 是否进行二次提问/追问（反映结果是否足够清晰）�?

4. **安全与异�?*
   - Guard 拦截次数（按原因分类：Schema / 安全 / 业务规则）；
   - 死信队列中与洞见 Workflow 相关的任务数量与类型�?

> 说明：以上指标仅用于内部评估稳定性与体验质量，不用于对用户施压或作为“必须跑完流程”的考核；`DeepDeepDeepDeepDeepInsight_decision_abort_rate` 也不等同于产品或用户的失败，应结合洞见宪法中“主权在使用者”的原则解读�?

---

## 6. MVP / 1.0 / 理想版差异建�?

### 6.1 MVP 建议范围

- 必须实现�?
  - 单问题单轮的多角色并行推演（至少 3 个角色：coach / critic / mirror）；
  - 基本的胶片结构（视角列表 + 简要总结）；
  - 基础�?Guard 校验（Schema + 简单敏感词过滤）；
  - 最基础的审计日志（记录一次推演的主键 ID 与时间）�?
- 可以暂缓�?
  - 复杂情绪建模与画像；
  - 完整的指标体系，只需先有调用次数与耗时的粗粒度统计�?

### 6.2 1.0 增强�?

- 增强�?
  - 视角种类与可配置性（支持自定义角色模板）�?
  - 胶片结构更丰富（时间线、决策树、可点击展开细节）；
  - 指标体系�?A/B 实验（不同角色组合、不同聚合策略）�?
  - 与项目记�?知识库的深度整合（自动拉取过往类似决策）�?

### 6.3 理想版想�?

- 支持多轮推演与对话式优化�?
  - 用户可以对胶片中的某个视角进行“深入追问”并局部再推演�?
- 支持协同决策�?
  - 多个真实用户在同一决策会话中协作，系统记录每个人的观点与共识过程；
- 支持“决策复盘实验”：
  - 将过往决策结果（成�?失败）与当时胶片挂钩，训练更好的推演策略�?

---

## 7. 实施建议

- 开发顺序建议：
  1. 按本文件�?Workflow 定义，在 UniFlow 中实现最小可运行版本（MVP）；
  2. 使用`07.01`中的洞见案例作为真实测试用例跑通整个链路；
  3. �?`03.02` 版本与实验策略指导下，为不同角色组合/胶片样式设计 A/B 实验�?
  4. 根据实际使用反馈，逐步�?1.0 与理想版增强�?

- 文档与代码联动：
  - 若实际实现中的节�?ID / Skill �?/ Agent 名称与本文不一致，应在本文中更新为代码中的最终命名；
  - 建议�?`workflow_definitions` 表中为此黄金路径设定固定 `id` 或稳�?`name`，便于日志与指标查询�?
