unit UniFlow.Constants;
(*
  UniFlow 全局常量定义
  
  ENTROPY-012: 硬编码常量消�?  - 集中管理超时、重试、URL 等可配置常量
  - 生产环境应通过 Config 覆盖这些默认�?*)

interface

const
  // ============================================================================
  // 超时配置（毫秒）
  // ============================================================================
  
  DEFAULT_STEP_TIMEOUT_MS = 30000;        // 单步执行超时
  DEFAULT_HTTP_TIMEOUT_MS = 30000;        // HTTP 请求超时
  DEFAULT_LLM_TIMEOUT_MS = 60000;         // LLM 调用超时
  DEFAULT_WORKFLOW_TIMEOUT_MS = 3600000;  // 工作流整体超时（1小时�?  DEFAULT_SESSION_TIMEOUT_SEC = 3600;     // 会话超时（秒�?  
  // ============================================================================
  // 重试配置
  // ============================================================================
  
  DEFAULT_MAX_RETRIES = 3;                // 默认最大重试次�?  DEFAULT_RETRY_BASE_DELAY_MS = 1000;     // 重试基础延迟
  DEFAULT_RETRY_MAX_DELAY_MS = 30000;     // 重试最大延�?  DEFAULT_COOLDOWN_MS = 30000;            // 冷却时间
  
  // ============================================================================
  // 限制配置
  // ============================================================================
  
  DEFAULT_MAX_ITERATIONS = 1000;          // 循环最大迭代次�?  DEFAULT_MAX_TOKENS = 1000;              // LLM 默认最�?Token
  DEFAULT_MAX_PARALLEL_TASKS = 10;        // 并行任务最大数
  DEFAULT_BATCH_SIZE = 100;               // 批处理大�?  
  // ============================================================================
  // 服务端点（开发环境默认值）
  // ============================================================================
  
  DEFAULT_SKILL_SERVICE_URL = 'http://localhost:8000';
  DEFAULT_OLLAMA_URL = 'http://localhost:11434';
  DEFAULT_OTLP_ENDPOINT = 'http://localhost:4317';
  DEFAULT_POSTGRES_PORT = 5432;
  
  // ============================================================================
  // 缓存配置
  // ============================================================================
  
  DEFAULT_CACHE_TTL_SEC = 300;            // 缓存 TTL（秒�?  DEFAULT_CACHE_MAX_SIZE = 1000;          // 缓存最大条目数
  
  // ============================================================================
  // 性能阈�?  // ============================================================================
  
  PERF_MIN_THROUGHPUT_PER_SEC = 1000;     // 最小吞吐量要求
  PERF_TARGET_LATENCY_MS = 100;           // 目标延迟

implementation

end.
