# ODD Hub 产品愿景

> 版本: 0.3
> 日期: 2026-03-10

---

## 一、定�?
一句话�?*�?ODD 的理论、工具、源头实践与商业化路径收成同一套生态叙事�?*

ODD Hub 不是单一软件，而是 ODD（产出物驱动开发）方法论的生态入口。它�?odd-demo、Progee Lite、ProgramEasy、odd-core �?Assayer 产品线组织成一条清晰的增长与变现路径�?
---

## 二、五产品生�?
### odd-demo（已完成�?
Python 概念验证脚本。跑通了"需求→契约→AI生成→验证→封存"的完整链路，配合三套面向不同受众的说明文档（开发者版、管理者版、决策者版）和社交媒体文案，用�?Hacker News / Product Hunt / 掘金的冷启动传播�?
它的使命：让�?*相信** ODD 可行�?
已验证的核心假设�?- 契约可从自然语言需求推导（关键词匹配→产出物标准库�?- 隐式需求可通过标准库传递给 AI�?条安全需求自动注入）
- AI 生成的代码可被自动验证（6项静态检查，5/6通过�?- 完整责任链可通过哈希封存建立（SHA-256 哈希链）

### Progee Lite（开发中�?
Delphi FMX 桌面应用。拖入文件、一键验证、生成朋友圈卡片和验证报告。零配置�?分钟体验完整闭环。附带在线徽章服务（FastAPI + PostgreSQL）�?
它的使命：让�?*觉得** ODD 好用�?
核心能力：AI推导契约 + 用户确认 + 三轮赛马修复 + 封存 + 朋友圈卡�?+ 在线徽章。对�?ODD L1.5 增强层�?
### ProgramEasy（开发中�?
Delphi 主产品，也是 ODD 理论的源头实践样板。它不是“再做一个验证器”，而是�?ODD 当成真正的软件工厂方法来落地�?
它的使命：让�?*看到** ODD 不只是验�?AI 代码的补丁，而是一整套可持续的软件生产方式�?
核心能力：软件工厂流程、工程级组织方式、可扩展�?ODD 原生工作流�?
### odd-core（待建，本文档的主角�?
Python CLI 工具，MIT 开源。从 odd-demo 的种子代码重构而来，可插拔架构，社区可通过插件和模板扩展�?
它的使命：让人在真实项目�?*持续使用** ODD�?
### Assayer 产品线（规划中）

Assayer 不是单一 SKU，而是一条分层产品线�?
- `Assayer Free`：个人与小团队的免费入口
- `Assayer Runtime`：企�?AI 安全与合规运行时
- `Assayer Cloud`：基�?Runtime 的托管交付形�?
它的使命：把 ODD 从“开发阶段的验证”延伸到“正式业务中的运行时治理”，并承担主要商业化收口�?
### 五产品的关系

Progee Lite 是体验鱼饵，ProgramEasy 是源头实践样板，odd-core 是开源鱼钩，Assayer 产品线是商业化收口。odd-demo 是让人上桌的那张邀请函�?
---

## 三、用户旅�?
### 阶段一：好奇（Progee Lite + odd-demo 负责�?
小李是个独立开发者，�?Cursor 写代码，经常担心 AI 生成的代码有没有问题。他在社交媒体上看到一张朋友圈卡片，上面写着"契约→验证→封存，今天又走了一�?，配了一个深绿色的验证通过徽章�?
他觉得挺酷，扫了二维码，下载�?Progee Lite�?
打开 Progee Lite，拖入一�?Python 文件�?0 秒后看到 AI 推导出的契约——三个验证场景、两个边界条件、一个异常输入。他点了"确认契约，开始验�?，测试全部通过，生成了一张朋友圈卡片�?
他发了朋友圈。这是他第一次接�?ODD，但他不知道这叫 ODD，他只觉�?这个工具能帮我验�?AI 写的代码"�?
另一条路径：技术型用户�?Hacker News 上看�?odd-demo 的帖子，clone 下来跑了一�?`python main.py generate`，看到完整的契约→生成→验证→封存链路，被说服了�?
Progee Lite �?odd-demo 的任务到此完成：让人看到 ODD 的价值�?
### 阶段二：上手（odd-core 负责�?
小李用了几次 Progee Lite，觉得不错，但有两个痛点：一是每次都要拖文件进去，不够顺手；二是只能验证单文件，他的项目有十几个文件�?
他在 Progee Lite 的“关于”页面看到了 odd-core 的链接：“想在命令行里用？试�?odd-core。�?
他装�?odd-core，配了自己的 Claude API Key，在项目根目录跑�?`odd init`，然�?`odd verify src/auth.py`。熟悉的流程——契约推导、测试生成、本地执行、封存——但这次是在终端里完成的，可以集成到他的开发流程中�?
他又试了 `odd verify --all`，一次性验证了整个项目。每个文件的契约和封存记录都保存�?`.odd/` 目录下，可以 git 提交�?
他开始在每次提交前跑一�?`odd verify`，就像跑 `pytest` 一样自然�?
### 阶段三：深入（odd-core 社区负责�?
小李发现自己每次�?auth 模块的契约都差不多，于是搜了一下社区模板库，找到了 `auth/login-basic`，拉下来稍微改改就能用�?
后来他写了一个支付模块的契约模板，觉得写得不错，推到了社区。模板通过了自动质量检查，上架了。一个月后有 200 多人用了他的模板，他�?GitHub 上多了几十个 star�?
他也开始给 odd-core �?PR——加了一�?Go 语言的验证插件，因为他的新项目用 Go�?
### 阶段四：团队（Assayer Cloud 开始收费）

小李加入了一�?5 人的创业团队。团队也想用 ODD，但遇到了几个问题：每个人都要自己配 API Key，模型不统一，验证结果不一致；有人�?Key 额度用完了，验证跑不通，阻塞了提交；老板想看团队的验证通过率，没有统一的数据�?
小李想起 odd-core 里有�?`Assayer-cloud` 选项。他注册了一个团队账号，充了 50 美元，把团队�?odd-core 配置统一指向 Assayer Cloud�?
现在团队不需要管 Key 了——Assayer Cloud 在背后自动路由到最合适的 Provider，某�?Provider 限流了自动降级，Token 太长了自动压缩。老板�?Dashboard 上能看到团队的验证统计�?
50 美元用了两个月，比每个人单独�?Key 还便宜，因为 Assayer 的批量折扣和智能路由帮他们省了钱�?
### 阶段五：企业（Assayer Runtime / Cloud 企业版）

小李的公司拿到了融资，团队扩�?30 人，开始有合规要求。审计部门问�?你们�?AI 生成的代码，有没有验证记录？能不能追溯？"

小李最终选择部署 Assayer Runtime，或者采�?Assayer Cloud 企业版。无论哪种形态，证据链都可以导出审计报告；每个产出物都有 SHA-256 哈希、验证时间、使用的模型、契约版本。FREEZE 状态的审批工作流让技术负责人可以�?Dashboard 上裁决机器无法判定的情况�?
---

## 四、技术架�?
### 全局技术分�?
```
┌─────────────────────────────────────────────────────────────�?�? 理论层：EthoShield                                         �?�? QP 投影、属集演算、伦理流�?—�?提供数学可证明的约束保证     �?├─────────────────────────────────────────────────────────────�?�? 规则层：Nomos / 宪法引擎                                    �?�? hard / soft / untouchable 三层约束体系                      �?�? 规则生成�?+ 解释�?+ 优化器，规则的生命周期管�?            �?├─────────────────────────────────────────────────────────────�?�? 执行层：Assayer Runtime（Delphi 实现�?                     �?�? 三重审计（绝对门�?+ 质疑引擎 + 响应门禁�?                 �?�? 审计日志、合规报告、W3C Trace Context                       �?├─────────────────────────────────────────────────────────────�?�? 入口�?                                                     �?�? odd-core（开发阶�?CLI�? Assayer Free（个�?团队入口代理�?�?└─────────────────────────────────────────────────────────────�?```

- 理论层为规则层提供数学后端（当正则匹配不够时，QP 投影提供确定性裁决）
- 规则层为执行层提供统一的约束语言（constitution.yaml�?- 执行层为入口层提供治理能力（odd-core、Assayer Free �?Assayer Cloud 共享同一套规则引擎）
- odd-core 负责开发阶段的事前预防，Assayer Runtime 负责运行阶段的事中控�?+ 事后追溯

### odd-core 内部结构

odd-core 采用可插拔架构，核心引擎小而稳，社区通过插件和模板扩展：

- odd.contract �?契约解析、验证、版本管�?- odd.gate �?门禁引擎（PASS / FAIL / FREEZE�?- odd.seal �?封存（哈�?+ 证据链）
- odd.runner �?测试执行器（两层：静态检�?+ AI 语义验证�?- odd.cli �?命令行入�?- odd.plugins �?插件机制

### �?odd-demo 继承的种子代�?
odd-demo 已验证的核心逻辑可直接复用：
- 产出物标准库�?YAML 格式定义
- 契约生成器的关键词匹配逻辑
- 静态验证器�?must_contain_any / must_not_contain 规则引擎
- SHA-256 哈希链封存逻辑
- LLM 调用的代码生成逻辑

odd-core 需要新建的�?- CLI 入口（odd init / odd verify / odd seal / odd template�?- 项目级配置文件（odd.yaml�?- .odd/ 目录管理（契约、封存记录的持久化）
- 插件机制骨架（AI provider 可替换、语言支持可扩展）
- FREEZE 状态（odd-demo 里没有，只有 PASS/FAIL�?- AI 语义验证层（解决 odd-demo 中的误报问题�?
### AI 调用三种模式

模式 A（直连）：用户自�?API Key，odd-core 直接调用 Provider。零额外依赖，永远免费�?
模式 B（本�?Assayer Free）：Assayer Free 作为本地后台服务运行，odd-core 通过 localhost:8080 调用。获得多账号、降级、压缩等高级能力�?
模式 C（Assayer Cloud）：odd-core 调用云端 API。用户不需要管 Key、不需要管 Provider 选择。最省心，按量付费�?
### 验证器的两层设计

第一层（快速静态检查）：odd-demo 现有�?must_contain / must_not_contain 模式匹配。速度快，�?AI 消耗，但会有误报�?
第二层（AI 语义验证）：�?AI 判断"这个 password = 是赋值还是存�?。更准确，但消�?token。可选开启�?
用户可以选择只跑第一层（免费、快），或者两层都跑（消�?AI 调用、准）。这自然地把用户推向 Assayer Cloud�?
---

## 五、商业模�?
### 原则

ODD 理论免费开放。odd-core 工具 MIT 开源。维护者的报酬来自云端服务，不来自锁住代码�?
### 免费层（永远免费�?
- odd-core 全功能（自带 Key 使用�?- Assayer Free 本地使用
- 社区契约模板（自动上架、机器审核、社区排序）
- 基础徽章

### 付费层（Assayer Cloud�?
- AI 调用额度（按 token 计费，赚差价 + 批量折扣�?- 云端封存（证据链永久存储�?- 高级徽章（自定义样式、验证详情页�?- 用量统计 Dashboard

### 企业�?
- 无限云端封存
- 审计报告导出
- FREEZE 审批工作�?- 优先支持 + SLA
- 私有部署选项
- SSO / LDAP

### 契约模板市场

不做人工审核。机器审�?+ 社区自治�?
- 自动上架：任何人都能发布，无需审核
- 质量信号：下载量、验证通过率、星标数自动排序
- 官方认证：维护者偶尔给好模板打 �?official 标签
- 社区举报：有问题的模板由社区标记，自动下�?- 元审计：模板必须通过 odd-core 的自动质量门�?
### 护城�?
1. 契约模板库的网络效应 �?先发优势，谁的模板多质量高用户就去谁那里
2. 社区品牌 �?"ODD 官方工具"的认知，fork 不走社区
3. Assayer 的数据积�?�?哪个 Provider 在什么场景下效果最好，只有我们�?4. 垂直整合 �?别人要么只做代理，要么只做验证，我们把两者打�?
---

## 六、开发路�?
### 第一步（2周）：odd-core MVP

- odd verify <file> �?单文件验证（�?odd-demo 种子代码重构�?- odd init �?项目初始�?- odd seal �?本地封存
- AI 调用走用户自�?Key

### 第二步（2周）：社区基础

- odd verify --all �?项目级验�?- odd template pull/push �?契约模板共享
- 插件机制骨架
- FREEZE 状�?- AI 语义验证层（第二层）

### 第三步（2周）：Assayer 集成

- odd-core 内置 Assayer-cloud provider
- 注册 �?充�?�?odd verify 一条龙
- 云端封存 + 徽章

### 第四步（持续）：社区运营

- 契约模板库冷启动（自己先�?10-20 个常见场景模板）
- 语言插件扩展（Python 内置，JS/Go/Rust 社区贡献�?- 失效案例库（收集"�?ODD 做了但失败了"的案例）

---

## 七、传播策�?
> 详细执行方案�?`docs/推广/` 目录�?
### 核心原则

不推销方法论，切入具体痛点�?AI 生成的代码又�?bug 了怎么�?。推广中绝对不提 ASTO、NTE、属集变迁等术语�?
### 渠道优先�?
| 优先�?| 渠道 | 定位 |
|--------|------|------|
| ★★�?| 知乎 | 答主模式建立权威�?个月潜伏期后正式推出 ODD |
| ★★�?| B�?| 实验视频一鸣惊人（"同一个AI，缺陷率差了59%"�?|
| ★★�?| GitHub | 可运行工�?+ 真实案例，所有渠道的最终转化终�?|
| ★★�?| X / 即刻 | 短内容高频输出，持续输出"AI 编程的坑" |
| ★☆�?| 掘金 / SegmentFault | 深度实战文章，知�?B站的深度转化 |

### 核心证据

所有渠道共用同一套实证数据（来源�?72个代码任务对照实验，Claude Sonnet 4.5）：

- 缺陷率：Contract-First 4.1% vs AI-First 10.0%，降�?59.5%（p=0.029，Fisher精确检验）

### 冷启动（odd-demo 负责�?
odd-demo 配套三套受众文档�?- 开发者版：强�?从砌砖工人到建筑监理"的角色升�?- 管理者版：强调效率提�?+ 风险可控 + 审计追溯
- 决策者版：强调生产关系重�?+ 结构性红�?+ 合规价�?
### 持续传播（Progee Lite 负责�?
每个 Progee Lite 用户生成的朋友圈卡片都是一次免费传播，形成社交裂变�?
### 社区增长（odd-core 负责�?
GitHub star、契约模板贡献、语言插件 PR �?每一次社区互动都是传播。GitHub 是最终转化终点，所有渠道内容都指向同一个仓库�?
---

## 八、待决策事项

1. odd-core �?Python 包名：odd-core 还是 odd 还是 oddpy？需要检�?PyPI 是否可用
2. Assayer Cloud 的域名和品牌：是否统一�?Assayer.com�?3. 定价细节：团队版和企业版的具体价格需要市场调�?4. 第一批契约模板的场景选择：CRUD / Auth / Payment / 还是其他�?5. odd-demo 是否迁移�?odd-hub 下，还是保持独立仓库�?