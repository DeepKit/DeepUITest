# goodmem.cn 当前代码�?2026-04-01 定稿之间的差距清�?
> 日期�?026-04-01
> 作用：记录当前仓库代码和最新开发文档之间的主要差距，供后续开发按优先级逐条修正�?
---

## P0 阻断�?
### 1. `/break/result`、`/break/pay`、`/break/pay/basic` 还不是独立页面状�?
涉及文件�?
- `frontend/index.html`
- `backend/main.py`

现状�?
- 路由虽然存在，但�?alias 到同一�?`index.html`
- `index.html` 里也还没有真实的结果页结构、支付页结构、通用底包页结�?- 当前更多是“落地页 + 识局弹窗 + 登录弹窗�?
为什么是阻断项：

- 产品中段值感全部落不到真实页面里
- `/break/pay` 还没有�?99 买到什么、不买到什么、适合谁、不适合谁”这些关键承�?
修正目标�?
- 保留单壳路由可以
- 但不同路径必须有真实页面状态和真实内容结构

---

### 2. 用户可见语言仍大量停留在旧口�?
涉及文件�?
- `frontend/rfi-core.js`
- `frontend/index.html`
- `frontend/reader.html`
- `frontend/reader.js`

现状�?
- 仍出�?`识局·快速版`
- 仍出�?`识局·精校版`
- 仍出�?`主局已显`
- 仍出�?`双局拉扯`
- 仍出�?`分岔带`

为什么是阻断项：

- 这批词已经被新版对外口径明确废弃
- 第一次进来的用户会看不懂

修正目标�?
- 改为 `快速看一眼（9题）`
- 改为 `看得更准�?2题）`
- 改为 `已经看清 / 两边都像 / 还要再看`

---

### 3. `risk_level` 仍是旧词，不�?`将陷 / 已陷 / 深陷`

涉及文件�?
- `frontend/rfi-core.js`
- `frontend/index.html`
- `frontend/reader.js`

现状�?
- 当前代码里还在生成：
  - `重度高危`
  - `中度摩擦`
  - `低感量耗散`
- 页面上还在显示：
  - `结构风险`

为什么是阻断项：

- 这和定稿契约完全不一�?- 结果页、Reader、联调都会出现口径冲�?
修正目标�?
- 系统值改为：`将陷 / 已陷 / 深陷`
- 页面标签改为：`这个问题现在有多深`

---

### 4. `UNKNOWN -> 再补1分钟` 还没有真实实�?
涉及文件�?
- `frontend/rfi-core.js`
- `frontend/index.html`
- `frontend/reader.js`

现状�?
- 当前只有 `UNKNOWN`
- 没有 `mini-3` 定向裁决�?- 也没有“只对邻局冲突型未定开放”的分流逻辑

为什么是阻断项：

- 这会�?`UNKNOWN` 直接塌成无解
- 产品中段的关键承接缺口仍然存�?
修正目标�?
- 仅对符合条件�?`UNKNOWN` 进入 `再补1分钟，把它看清`
- 结构固定�?`2�?+ 1张情境卡`
- 只允许补一�?
---

## P1 高优先级�?
### 5. Reader 首屏仍偏“目录工具”，不够像购后承接页

涉及文件�?
- `frontend/reader.html`
- `frontend/reader.js`

现状�?
- 首屏虽然已有说明，但仍偏“先看证明件�?- 没有完整实现�?  - `今天先拿走这3样`
  - `先记住这一句`
  - `先做这一步`
  - `先别这样做`
  - `先看�?篇为什么排在这里`

为什么重要：

- 这决定用户付�?499 后会不会立刻觉得�?
修正目标�?
- Reader 首屏从“阅读工具”改成“起始导读页�?
---

### 6. 结果页字段展示顺序与新契约不一�?
涉及文件�?
- `frontend/index.html`
- `frontend/reader.js`

现状�?
- 当前仍是�?  - 状�?  - 标题
  - 判断
  - 风险
  - 把握
  - 先做
- 还没有新结果页要求的 10 段顺�?
修正目标�?
按新顺序显示�?
1. 你现在更像卡�?2. 现在看到哪一�?3. 这个问题现在有多�?4. 一句说中你的判�?5. 为什么总会这样
6. 你现在先做这一�?7. 你现在先别这样做
8. 有没有这种感�?9. 先看�?篇样�?10. 主按�?
---

### 7. 支付入口仍以“Passcode 登录弹窗”为主，不是正式支付�?
涉及文件�?
- `frontend/index.html`

现状�?
- 现在的“支付”更像登录弹�?- 用户先看到二维码占位�?Passcode 输入
- 没有正式支付页文案结�?
修正目标�?
- `/break/pay` �?`/break/pay/basic` 先承接价值，再给支付动作
- Passcode 输入可以保留，但不要继续充当整个支付�?
---

### 8. 后端目录接口仍返�?`cop_type`

涉及文件�?
- `backend/main.py`

现状�?
- `/api/v1/articles` 仍返�?`cop_type`
- 前端目录项仍把它�?`data-cop-type`

为什么重要：

- 语义还停在旧体系
- 后续如果要按五行排序，字段会越来越绕

修正目标�?
- 迁到 `rfi_type`
- 若暂时不改库，至少在接口层做兼容映射

---

## P2 可后移项

### 9. 诊断结果目前主要保存在本�?
涉及文件�?
- `frontend/rfi-core.js`
- `frontend/reader.js`

现状�?
- `mindbreak_diag_v1` 仍主要保存在本地
- 同浏览器可延续，但跨设备、跨会话能力�?
建议�?
- 如果本轮只求最小上线，可以先保�?- 但后续最好把结果�?`token` 绑定

---

### 10. 样章区标题与当前文章标题未完全和最新对外文案一�?
涉及文件�?
- `frontend/index.html`

现状�?
- 当前样章区还保留部分旧标题表�?
建议�?
- �?`03` 文案统一时一起修

---

## 一句话执行结论

**当前仓库已经有可继续开发的骨架，但�?2026-04-01 的可上线版本还差一轮明确的前端重构；优先补 P0，再�?P1�?*
