# 02-技术架构与部署规范

> 日期�?026-04-06
> 用途：冻结 `goodmem.cn` 的技术基线、API 契约、数据模型与部署流程�?> 状态：**已冻�?*

---

## 1. 技术栈与环境基�?
### 1.1 核心组件
- **接入�?*：`Nginx` (负责 HTTPS �?静态转�?
- **前端**：单纯的 `HTML + CSS + Vanilla JS`，不引入复杂框架以保持极致加载速度�?- **后端**：`FastAPI` (Python 3.9+)
- **数据�?*：`PostgreSQL`
- **配置管理**：`backend/.env` (所有敏感信息必须离岸管�?

### 1.2 部署原则
1. **前后端解�?*：但支持同服部署�?2. **环境变量阻断**：若配置文件缺失核心字段（支付密钥、JWT 密钥等），系统应拒绝启动�?3. **安全底座**：心跳续签、设备互踢、动态水印机制必须全量保留�?
---

## 2. 页面路由�?API 契约

### 2.1 页面路由地图
V1 版本的核心路径如下（支持 Nginx �?后端 Alias 重定向）�?- `/break` : 入口�?- `/break/diag` : 识局采样�?- `/break/result` : 识局结果�?- `/break/pay` : 标准支付�?- `/break/pay/basic` : 通用底包支付�?- `/reader.html` : 最终阅读器

### 2.2 核心 API 概览
| 方法 | 路径 | 描述 |
|---|---|---|
| `POST` | `/api/v1/auth` | Passcode 换取短效 JWT |
| `POST` | `/api/v1/heartbeat` | 心跳续签与互踢检�?(30s 频率) |
| `GET` | `/api/v1/articles` | 获取权限内的文章目录 |
| `GET` | `/api/v1/articles/{id}` | 按需获取乱序加密的正文碎�?|
| `POST` | `/api/v1/diagnosis` | (New) 诊断结果�?Token 绑定存储 |

---

## 3. 数据模型 (Schema)

### 3.1 用户模型 (Users)
- `token` : 唯一访问凭证
- `payment_status` : 支付状�?- `rfi_type` : 主五行局类型
- `diagnosis_payload` : 完整的诊�?JSON 原始数据
- `last_active_at` : 最后活跃时�?- `kick_count` : 10 分钟内互踢次�?(风控)

### 3.2 内容模型 (Articles)
- `id` : 文章 ID
- `rfi_type` : 关联的五行局类型 (替代旧语�?`cop_type`)
- `content_fragments` : 碎片化存储的正文

---

## 4. 状态机与分流逻辑 (UNKNOWN 逻辑)

### 4.1 UNKNOWN 分流规则
并非所有无法判定的结果都进入“补看”。仅满足以下条件�?`UNKNOWN` 才触�?`/break/diag/disambiguation` (再补 1 分钟)�?1. 已完�?12 题识局�?2. 属于邻局冲突型（如：�?vs 木、火 vs 金）�?3. 分差小于等于 2 分�?
### 4.2 诊断状态映�?| 内部系统�?| 对外显示�?|
|---|---|
| `RESOLVE` | `已经看清` |
| `MIXED` | `两边都像` |
| `UNKNOWN` | `还要再看` |
| `risk_level` | `这个问题现在有多深：将陷 / 已陷 / 深陷` |

---

## 5. 防盗与鉴权基�?
1. **JWT 互踢**：新设备上线强制踢掉旧设备，10 分钟内超�?5 次则封禁�?2. **正文乱序**：后端下发碎片，前端根据密钥拼装，抗简单爬虫�?3. **动态水�?*：`goodmem.cn | token�?�?| HH:mm` 每分钟更新�?4. **DOM 投毒**：正文节点中混入不可见的干扰文字，防止剪切板直接拖拽�?
---

## 6. 一句话执行结论
**架构设计必须优先保证“支付链”与“鉴权链”的稳定性，技术实现上允许用前端本地存储作为后端落库前的过渡�?*
