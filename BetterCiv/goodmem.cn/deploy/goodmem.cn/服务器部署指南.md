# goodmem.cn 服务器部署指�?
> 适用范围：当�?`WSM / 思维越狱`、`WSH / 五行和悦论` H5 页面、FastAPI 后端、PostgreSQL、本�?mock 支付链路
> 生成日期�?026-04-21

## 1. 推荐服务器目�?
推荐�?Linux 服务器上使用这一套目录：

```text
/srv/goodmem.cn
├─ app
�? └─ current
�?    ├─ backend
�?    ├─ frontend
�?    └─ deploy
├─ env
�? └─ backend.env
├─ logs
├─ data
�? └─ proofs
└─ certs
   └─ wechatpay
```

说明�?
- `app/current`：当前生效代�?- `env/backend.env`：后端环境变量，不进代码仓库
- `logs`：Uvicorn / systemd / 应用日志
- `data/proofs`：返现或支付相关上传目录
- `certs/wechatpay`：微信支付平台证书、公钥、商户私钥等

## 2. 服务器建�?
- Ubuntu 22.04 �?24.04
- 2C2G 起步
- 已备案域�?- 已解析：
  - `goodmem.cn`
  - `www.goodmem.cn`

## 3. 当前包内包含什�?
本次打包只包含可部署的最小内容：

- `frontend/`
- `backend/main.py`
- `backend/requirements.txt`
- `backend/schema.sql`
- `backend/migrate_v1.sql`
- `backend/migrate_v2_508.sql`
- `backend/migrate_v3_payments.sql`
- `backend/migrate_v4_expand_schema.py`
- `backend/migrate_v5_product_tier.py`
- `backend/init_db.py`
- `backend/import_articles.py`
- `backend/article_mapping.json`
- `backend/test_h5_mock.py`
- `backend/test_tiers.py`
- `backend/.env.example`
- `deploy/nginx.goodmem.cn.conf`
- `deploy/goodmem-api.service`

其中前端当前包含�?
- `WSM` 页面与配�?- `WSH` 页面与配�?- 公共静态脚�?
不包含：

- 本地 `.env`
- `venv`
- 本地日志
- 本地缓存
- 旧临时文�?
## 4. 推荐部署步骤

1. 在服务器创建目录�?
```bash
sudo mkdir -p /srv/goodmem.cn/app/current
sudo mkdir -p /srv/goodmem.cn/env
sudo mkdir -p /srv/goodmem.cn/logs
sudo mkdir -p /srv/goodmem.cn/data/proofs
sudo mkdir -p /srv/goodmem.cn/certs/wechatpay
sudo chown -R $USER:$USER /srv/goodmem.cn
```

2. 将压缩包上传到服务器，例如：

```bash
scp goodmem.cn_deploy_20260422.zip user@your-server:/tmp/
```

3. 解压到目标目录：

```bash
cd /srv/goodmem.cn/app/current
unzip /tmp/goodmem.cn_deploy_20260422.zip
```

4. 创建 Python 虚拟环境并安装依赖：

```bash
cd /srv/goodmem.cn/app/current/goodmem.cn/backend
python3 -m venv venv
./venv/bin/pip install --upgrade pip
./venv/bin/pip install -r requirements.txt
```

5. 根据 `.env.example` 生成 `/srv/goodmem.cn/env/backend.env`

6. 配置 PostgreSQL，并执行 schema / migrate

7. 配置 systemd 服务

8. 配置 Nginx

9. 申请 HTTPS 证书

10. 启动并联�?
## 5. Nginx �?systemd

包内已附带模板：

- `deploy/nginx.goodmem.cn.conf`
- `deploy/goodmem-api.service`

建议�?
- Nginx 监听 `80/443`
- FastAPI 只监�?`127.0.0.1:8000`
- 通过 `systemd` 托管 `uvicorn`

## 6. 环境变量

至少需要：

- `DATABASE_URL`
- `JWT_SECRET`
- `HMAC_SECRET`

支付相关预留�?
- `WECHAT_PAY_API_V3_KEY`
- `WECHAT_PAY_PLATFORM_CERT_PATH`
- `WECHAT_PAY_PLATFORM_PUBLIC_KEY_PATH`
- `WECHAT_PAY_PLATFORM_SERIAL`

## 7. 数据�?
当前产品口径�?
- `299`�? �?- `399`�?08 �?- `999`�?36 �?
当前后端代码按这个规格计算权限，但前提是数据库里�?508 篇内容已经完整导入�?
## 8. 支付说明

当前代码状态分两层�?
### 8.1 已完�?
- `WSM` H5 商品页原�?- `WSH` H5 回正产品链原�?- 三档 SKU 展示
- `/api/v1/pay/prepay` mock 下单接口
- `/api/v1/wechat/webhook` 回调入口
- 支付申请截图链路

### 8.2 正式收款前必须补�?
- �?`/api/v1/pay/prepay` �?mock 改成真实微信支付 V3 H5 下单
- 准备商户号、商户私钥、APIv3 Key、平台证�?公钥
- 配置 HTTPS 正式域名
- 打通微信支付异步回调到 `/api/v1/wechat/webhook`
- 回调成功后，按订单为用户发放对应 `product_tier`
- 做重复回调、签名校验、异常订单与退款处�?
## 9. 上线前检�?
- `GET /break` 正常打开
- `GET /break?view=pay&sku=system` 正常打开
- `GET /break?view=post&sku=system` 正常打开
- `GET /return` 正常打开
- `GET /return?view=pay&sku=annual` 正常打开
- `GET /return?view=post&sku=monthly` 正常打开
- `POST /api/v1/pay/prepay` 返回成功
- Nginx 反代 `/api/` 正常
- HTTPS 正常
- Webhook 白名单与签名校验正常

## 10. 当前建议

如果当前目标只是支付申请与前端审查：

- 可以先部署当前包
- 可以先保�?mock 下单
- 先完成截图、H5 页面可访问、基础联调

如果目标是正式收款：

- 不要直接上线当前 mock 支付
- 必须先完成真实微信支�?V3 H5 接入
