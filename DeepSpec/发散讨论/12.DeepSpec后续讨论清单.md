# DeepSpec 后续讨论清单

## 已经定稿

1. MVP 产品定位与边界；
2. DeepShell 桌面标准壳；
3. DeepShell 核心类、接口、事件机制；
4. DeepSpec 接入 DeepShell 的 Provider 方案；
5. DeepSpec 工程目录与单元划分；
6. 第一批开发：壳 + 扫描 + 报告；
7. 第二批开发：Delphi/VCL 解析 + UI 树 + 模块树；
8. 第三批开发：候选功能树 + 基准确认 + AI 参考摘要；
9. 第四批开发：测试样例库与 MVP 验收标准；
10. 第五批开发：数据保存与 `.deepspec` 目录规范；
11. 第六批开发：Delphi/VCL 解析策略。

## 后续还需细化

### 1. DFM Parser 具体算法

- 逐行解析状态机；
- object / inherited / inline 正则；
- 属性行解析；
- 字符串解码；
- `#数字` 编码解码；
- object/end 不匹配处理；
- source line 记录。

### 2. VirtualStringTree 展示方案

- 第一版用 TTreeView 还是 VirtualStringTree；
- 树表列如何设计；
- 节点图标；
- 节点状态颜色；
- 候选 / 已确认 / 异常的视觉表达；
- 右键菜单；
- 搜索过滤。

### 3. HTML 模板样式

需要定：

- 统一 CSS；
- 卡片样式；
- 代码片段样式；
- 问题提示样式；
- 来源路径显示；
- 可复制块。

### 4. AI Reference 模板

需要定：

- `ai-reference.md` 固定结构；
- Delphi/VCL 注意事项；
- DeepShell 约束；
- 三棵树摘要压缩；
- 禁止偏离事项表达。

### 5. 复制给 AI 的文本模板

需要定不同节点类型模板：

- UI 节点；
- 模块节点；
- 功能节点；
- 文件节点；
- 问题节点；
- 基准节点。

### 6. 基准对比机制

MVP 只保存 baseline。后续可讨论：

- 重新扫描后比较当前树与 baseline；
- 新增节点；
- 删除节点；
- 移动节点；
- 属性变化；
- Caption 变化；
- 模块移动；
- UI 层次变化。

### 7. DeepUITest 联动

未来讨论：

- UI 层次树如何生成 UI 测试配置；
- 控件节点如何映射测试目标；
- DeepSpec 的 UI 基准如何传给 DeepUITest；
- DeepUITest 的测试结果如何回写 DeepSpec。

## 下一步建议

优先讨论：

# DFM Parser 具体算法与测试用例设计

因为 `.dfm` 解析是 DeepSpec MVP 的关键亮点，决定 UI 层次树能不能真正生成出来。
